# 更新记录

> **格式规范**
> - `日期`：统一 `YYYY-MM-DD HH:MM` 格式
> - `类型`：`feat` / `fix` / `chore` / `docs` / `refactor`
> - `任务ID`：关联 `5.2 任务分解表` 编号，无则 `—`
> - `涉及模块`：代码文件用 `` `path` ``，文档链接用 `[[文档名#章节]]`
> - `相关文档`：Obsidian `[[wikilink]]` 格式
> - 批量文档同步：在摘要表 `涉及模块`/`相关文档` 列填 `↓详见 §章节名`，下方以 `### §章节名` 展开

---

## 2026-06-18

|  时间   |  类型   | 任务ID |         任务名          |                                            完成内容摘要                                             |                                   涉及模块                                    |           相关文档            |
| :---: | :---: | :--: | :------------------: | :-------------------------------------------------------------------------------------------: | :-----------------------------------------------------------------------: | :-----------------------: |
| 08:30 | feat  | T6.5 |    Navbar 滚动背景透明度    |               `useTransform` 驱动 bg alpha scrollY[0,50]→[0,0.85]，初始透明→滚动后渐现背景+阴影               |                               `Navbar.tsx`                                | [[3.3 交互与动效说明#八、导航栏滚动效果]] |
| 08:35 | feat  | T6.7 | LocaleContent 语言切换过渡 |     创建 LocaleContent.tsx，AnimatePresence mode="wait"+key={locale}，exit 300ms→enter 500ms      |                 `LocaleContent.tsx`, `[lang]/layout.tsx`                  | [[3.3 交互与动效说明#五、语言切换过渡]]  |
| 08:38 | feat  | T6.8 |   页面过渡 scrollToTop   |                              LayoutShell 添加 onExitComplete 滚动到顶部                              |                             `LayoutShell.tsx`                             |  [[3.3 交互与动效说明#四、页面过渡]]   |
| 09:00 |  fix  |  —   |   proxy.ts 移入 src/   |                      根目录→src/proxy.ts（Next.js 16 要求），解决 landing page 404                      |                                `proxy.ts`                                 |    [[4.2 项目文件结构文档#六]]     |
| 09:02 | chore |  —   |     更新图片引用 + 上传      | C1-1.png→hero-character.png, fyp-iot.png→fyp-iot-cover.png, portfolio.png→portfolio-cover.png | `HeroSection.tsx`, `projects.json`, `case-studies.json`, `portfolio-*.md` |    [[4.3 视觉资产与使用规范#一]]    |
| 11:00 | docs  |  —   |   文档同步：更新 10 份设计文档   |                                        批量文档与代码 diff 对齐                                        |                                ↓详见 §11:00                                 |        ↓详见 §11:00         |
| 11:30 | feat  |  —   | ContactSection 内容左对齐 |             移除 `mx-auto max-w-md justify-center`，邮箱/GitHub/LinkedIn/简历按钮与标题统一左对齐              |                           `ContactSection.tsx`                            |     [[3.2 原型与线框图#六]]      |
| 11:35 |  fix  |  —   | HeroSection 手机端底部留白  |                   `min-h-screen`→`md:min-h-screen`；`pb-16`→`pb-8 md:pb-16`                    |                             `HeroSection.tsx`                             |     [[3.2 原型与线框图#二]]      |
| 11:38 | docs  |  —   | 文档同步：Contact/Hero 对齐 |                            ContactSection 布局 + Hero 手机端描述同步到 6 份文档                            |                                ↓详见 §11:38                                 |        ↓详见 §11:38         |
| 15:30 | docs  |  —   |       VN 游戏感改造       |                全站文档新增 VN 浏览体验方向：章节标题系统、蓝色对话框叙事、选项式交互、过渡台词、系统菜单 UI 框架。10 份文档变更                 |                                ↓详见 §15:30                                 |        ↓详见 §15:30         |
| 15:30 |  fix  |  —   |        蓝色素材修正        |                蓝色色值 `#8EB8D6`→`#2FA4D7`；对话框双层结构；系统菜单代码化；globals.css 新增 tokens                 |                                ↓详见 §15:30                                 |        ↓详见 §15:30         |
| 15:30 | docs  |  —   |       素材分辨率更新        |             dialog-box.png (1216×217) 和 option-button.png (900×60) 实际分辨率写入所有相关文档              |                                ↓详见 §15:30                                 |        ↓详见 §15:30         |

---

### §11:00 — 文档-代码同步更新

**触发**：`project-analysis` skill 执行后，对代码与设计文档做全面 diff 并更新文档。

**涉及文档**：完整技术规格书、3.1 风格指南、3.2 原型与线框图、3.3 交互与动效说明、4.1 技术栈与架构、4.2 文件结构、2.2 内容规划、5.2 任务分解、7.1 内容更新指南、7.2 SEO 说明、5.1 编码规范

| # | 文档 | 变更内容 |
|---|------|---------|
| 1 | **完整技术规格书** `§二` | 字体方案：从"三语都是 next/font/local 多字重"更正为「JP/EN 仅 Regular 一字重 + next/font；ZH 用 fonts.css 手动 @font-face (13 CJK 子集)」 |
| 2 | **完整技术规格书** `§五` | 目录树：移除 `api/`，添加 `_not-found/`、`LayoutShell.tsx`、`LocaleContent.tsx`、`ThemeAwareHtml.tsx` |
| 3 | **完整技术规格书** `§六 4.8` | ContactSection 描述从「纯 UI 表单（姓名/邮箱/留言/发送按钮）」改为「邮箱链接 + 社交链接图标 + 简历下载按钮（无表单）」 |
| 4 | **完整技术规格书** `§七` | CI 流水线章节添加 ⚠️ 提示：`.github/workflows/ci.yml` 尚未在仓库中创建 |
| 5 | **完整技术规格书** `§九` | messages 中 formX 键添加注释「ContactSection 当前未渲染表单」 |
| 6 | **4.1 技术栈与架构** `§三` | 组件 `"use client"` 表更新：CaseStudyCard、ProjectCard、Footer 从「否」改为「是」；[lang]/layout.tsx 从「是」改为 Server Component；添加 LocaleContent、LayoutShell 行 |
| 7 | **4.1 技术栈与架构** `§三 组件树` | ContactSection 子组件移除 ContactForm |
| 8 | **4.1 技术栈与架构** `§四` | 数据读取函数名 `readProfile/readProjects/readCaseStudies` → `getProfile/getProjects/getCaseStudies`；Zod schema socialLinks 从 `z.array(SocialLinkSchema)` 改为 `z.object({ github: z.string().url(), linkedin: z.string() })` |
| 9 | **4.1 技术栈与架构** `§五` | 目录树 font 文件改为仅列出实际存在的 Regular 字重；移除 `api/` 添加 `fonts.css` 及新组件 |
| 10 | **4.1 技术栈与架构** `§七` | next.config.ts 从含 images 配置改为「使用 Next.js 默认值（空对象）」 |
| 11 | **4.2 文件结构文档** `§二` | [lang]/layout.tsx 类型从「Client Component」改为「Server Component（内部使用 LocaleContent + LayoutShell）」 |
| 12 | **4.2 文件结构文档** `§三 3.2/3.8-3.10` | Footer 从「Client Component（仅版权年份）」改为「Client Component（framer-motion）」；CaseStudyCard/ProjectCard 从 Server Component 改为 Client Component；ContactSection 移除表单相关描述 |
| 13 | **4.2 文件结构文档** `§四 4.2` | read-data.ts 函数签名 `readProfile/readProjects/readCaseStudies` → `getProfile/getProjects/getCaseStudies` |
| 14 | **4.2 文件结构文档** `§五` | profile.json socialLinks 从 array-of-objects 改为 object；projects.json 和 case-studies.json 移除顶层包装对象 `{ projects: [...] }` → 直接数组 |
| 15 | **3.1 风格指南** `§二` | 字体字重：JP/EN 从 `400,500,700` 等多字重改为「仅 400 (Regular)」，ZH 加载方式从 `next/font/local` 改为 `fonts.css @font-face` |
| 16 | **3.2 原型与线框图** `§六` | Contact 区块从双列（左列表单 + 右列信息）改为单列（邮箱 + 社交链接 + 简历下载） |
| 17 | **3.3 交互与动效说明** `§二` | Hero 时序从独立 motion.div+delay 方案改为 staggerContainer 统一控制；代码示例同步更新；Mobile 加速逻辑标注「当前未实现」 |
| 18 | **3.3 交互与动效说明** `§三` | Contact Section 入场描述从「左列表单 → 右列信息」改为「邮箱 + 社交 + 简历」 |
| 19 | **2.2 内容规划表** | Contact 说明移除「联系表单」；socialLinks 字段类型从数组改为对象；示例 JSON 同步修正 |
| 20 | **7.1 内容更新指南** | socialLinks 示例从 array-of-objects 改为 object；formX 键添加「当前未使用」注释 |
| 21 | **7.2 SEO 说明** | `readProfile()` → `getProfile()`；sameAs 从 `profile.socialLinks.map((link) => link.url)` 改为 `Object.values(profile.socialLinks)` |
| 22 | **5.1 编码规范** | `readProfile()` 命名示例 → `getProfile()` |
| 23 | **5.2 任务分解** | T2.9 导出函数名 `readProfile/readProjects/readCaseStudies` → `getProfile/getProjects/getCaseStudies` |

---

### §11:38 — 文档同步：ContactSection 对齐 + HeroSection 手机留白

| # | 文档 | 变更内容 |
|---|------|---------|
| 1 | **完整技术规格书** `§三` | 联系区块描述「UI 表单」→「邮箱链接 + 社交链接 + 简历下载」 |
| 2 | **5.2 任务分解** `T4.7` | ContactSection 从「双列布局（左列表单 UI + 右列...）」改为「邮箱链接 + 社交图标 + 简历下载（无表单），内容与标题左对齐」 |
| 3 | **3.2 原型与线框图** `§六` | Contact 区块布局从「单列居中」→「单列左对齐」 |
| 4 | **3.2 原型与线框图** `§二 2.1` | Hero 高度从固定 `100vh` 改为「Desktop: 100vh; Mobile: 自适应内容高度」 |
| 5 | **3.2 原型与线框图** `§二 2.5` | Tablet Hero `min-h-screen` 标注限定「桌面端」 |
| 6 | **3.2 原型与线框图** `§二 2.6` | Mobile Hero 新增「不设 min-h-screen，高度由内容决定」「底部内边距 pb-8」 |

---

### §15:30 — VN 游戏感改造 & 蓝色素材修正 & 分辨率更新

**触发**：重新评估网站浏览体验，决定从"干净网页"转向"VN 式浏览"。

| # | 文档 | 变更内容 |
|---|------|---------|
| 1 | **1.1 品牌愿景** | 新增 VN 浏览体验目标；设计方向加入蓝色素材和章节式滚动；新增 VN 设计五原则 |
| 2 | **1.2 功能清单** | MVP 新增 5 项：VN 章节标题、蓝色对话框、选项按钮、过渡台词、菜单 UI 框架；开发顺序追加 VN UI 阶段 |
| 3 | **3.1 风格指南** | 新增 `--color-dialog-blue / --color-dialog-dark / --color-dialog-soft` 蓝色色系；新增 DialogBox / ChapterHeader / OptionButton 三种 VN 组件样式规范；新增 VN 专属动效表 |
| 4 | **3.1 风格指南** | `--color-dialog-*` 三色值从 `#8EB8D6/#5A7A96/#E8F0F8` 改为 `#2FA4D7/#1A8DB8/#E5F4FC`；对话框组件规范改为双层嵌套结构图解+说明；OptionButton 色值同步更新；使用规则同步 |
| 5 | **3.1 风格指南** | 对话框最大宽度 420→480px；内层增加 PNG 方案说明；增加高度约束 `min-h-[80px]`；OptionButton 增加 Dialog-PNG 变体（h-[60px]），原始分辨率 900×60 写入文档 |
| 6 | **3.2 原型与线框图** | 各 Section 顶部增加章节标题（CHAPTER N）；Case Study 卡片描述改用蓝色对话框；About 自我介绍改为对话框叙事；Contact 改为系统菜单框架；新增 §七 Section 过渡台词 |
| 7 | **3.2 原型与线框图** | Case Study 描述对话框、About 对话框统一使用双层结构示意；Contact 系统菜单改为纯 CSS 实现说明（无素材依赖）；CTA 按钮改为 OptionButton 风格可选 |
| 8 | **3.3 交互与动效说明** | 新增 §七 VN 式交互（章节标题入场/对话框入场/选项按钮交互/过渡台词/页面暗转）；新增 5 个动画 variants；更新响应式禁用策略表 |
| 9 | **3.3 交互与动效说明** | 对话框入场实现要点增加双层结构说明 |
| 10 | **4.1 技术架构** | 组件树新增 DialogBox / ChapterHeader / SectionTransition / OptionButton；use client 表更新 |
| 11 | **4.2 文件结构** | 新增 DialogBox / ChapterHeader / OptionButton / SectionTransition 的 Props 接口定义；文件创建顺序更新 |
| 12 | **4.2 文件结构** | DialogBox 样式描述改为双层结构 |
| 13 | **4.2 文件结构** | DialogBox 样式描述中 `max-w` 改为 `480px`，增加 PNG 方案提及 |
| 14 | **4.3 视觉资产** | 新增 §1.1 UI 素材资产表（对话框/选项按钮/菜单边框 PNG）及使用规范 |
| 15 | **4.3 视觉资产规范** | 蓝色色值更新；移除 `menu-frame.png` 资产条目（系统菜单无素材）；对话框素材说明改为"可选替换内层"；增加 CSS 实现为主的说明 |
| 16 | **4.3 视觉资产** | 1.1 表加入原始分辨率列（1216×217 / 900×60）；增加使用方式说明（PNG 适用于 bg-cover、显示宽度约束）；资产汇总表上限更新 |
| 17 | **5.2 任务分解** | 新增 Phase 6（VN UI 系统 7 任务）+ Phase 7（首页集成 VN + 动效 11 任务）；原阶段全部后移；总任务数 64→74 |
| 18 | **完整技术规格书** | 品牌调性增加 VN 浏览体验描述；新增蓝色色值 tokens；新增 §2.3 VN 浏览体验章节；目录树新增 VN 组件；messages 新增 transitions 键；SDLC 新增阶段 6-7 |
| 19 | **完整技术规格书** | 蓝色色值同步更新 |
| 20 | **完整技术规格书** | 资产上限条目改为实际分辨率 |
| 21 | **globals.css**（代码文件） | 新增 `--color-dialog-blue: #2fa4d7; --color-dialog-dark: #1a8db8; --color-dialog-soft: #e5f4fc;` 到 `@theme` 和 `:root` |

---

### §17:44 — 文档同步：VN 组件最终实现代码对账

**触发**：Phase 6+7 代码实现完成后，将设计文档与实际代码 diff 对齐。

| # | 文档 | 变更内容 |
|---|------|---------|
| 1 | **VN 游戏感改造执行手册** `§二` | 改动全景图同步：CaseStudyCard 从修改列表移除（未修改）；OptionButton 从 4 变体→单变体；SectionTransition 从 AnimatePresence→简化 `~ text ~`；所有任务标记 ✅ |
| 2 | **VN 游戏感改造执行手册** `§四` | Phase 6+7 任务表反映最终实现：DialogBox 三层圆角+flex 1:3；ChapterHeader 左滑入；OptionButton 纯 CSS 单变体；SectionTransition 过渡线 |
| 3 | **VN 游戏感改造执行手册** `§五` | 验收清单全部标记 ✅ 并更新描述 |
| 4 | **3.1 风格指南** `§5.6` | DialogBox 从双层嵌套改为三层圆角结构（rounded-lg / rounded-xl / rounded-md），`aspect-[1200/225]`，flex 1:3 固定比例 |
| 5 | **3.1 风格指南** `§5.7` | ChapterHeader 从分割线+◆ 装饰改为 CHAPTER 标签+标题+蓝色下划线，x:-30→0 入场 |
| 6 | **3.1 风格指南** `§5.8` | OptionButton 从 4 变体（primary/dialog/ghost/dialog-png）改为纯 CSS 单变体（蓝底白字+▶） |
| 7 | **3.1 风格指南** `§7.3` | VN 动效表与实际 variants 一致 |
| 8 | **3.2 原型与线框图** `§三` | Case Studies 区块：卡片保持原始结构，顶部加 ChapterHeader |
| 9 | **3.2 原型与线框图** `§五` | About 区块 DialogBox 结构改为三层圆角（名字 pill 在框内，flex 1:3） |
| 10 | **3.2 原型与线框图** `§六` | Contact 区块从双列表单→系统菜单框架→OptionButton 列表（无菜单容器） |
| 11 | **3.2 原型与线框图** `§七` | Section 过渡台词从 DialogBox+AnimatePresence→`~ text ~`+水平线 |
| 12 | **3.3 交互与动效说明** `§七` | 全部 7 节重写：ChapterHeader 左滑入、DialogBox dialogSlideUp、OptionButton whileHover/whileTap、SectionTransition transitionFade、variants 名称更正 |
| 13 | **3.3 交互与动效说明** `§九` | 响应式禁用策略更新：过渡台词保留，无逐字/暗转 option |
| 14 | **3.3 交互与动效说明** `附录` | animations.ts 完整代码追加 VN variants |
| 15 | **4.2 文件结构** `§3.16-3.19` | DialogBox/ChapterHeader/SectionTransition/OptionButton Props 接口与实际代码一致 |
| 16 | **4.3 视觉资产** `§1.1` | dialog-box.png 和 option-button.png 标注为"当前未使用"，保留在仓库 |
| 17 | **5.2 任务分解** `Phase 6-7` | 所有 T6.x/T7.x 任务描述更新为实际实现；标记 ✅ 完成；CaseStudyCard 从 T7.2 移除 |
