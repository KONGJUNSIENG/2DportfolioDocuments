# 5.1 编码规范文档

**项目：** KONG JUN SIENG — Software Developer Portfolio  
**文档目标：** 定义命名约定、TypeScript Strict 规则、组件组织规范与代码风格，约束 AI 生成的代码保持全仓库一致性。

---

## 一、命名规范

### 1.1 文件与目录命名

| 类别 | 风格 | 示例 | 来源/理由 |
|------|------|------|----------|
| React 组件 | PascalCase | `HeroSection.tsx`、`SkillTag.tsx` | [[4.2 项目文件结构文档.md#七]] |
| 工具/库文件 | kebab-case | `read-data.ts`、`animations.ts` | [[4.2 项目文件结构文档.md#七]] |
| JSON 数据 | kebab-case | `profile.json`、`case-studies.json` | [[4.2 项目文件结构文档.md#七]] |
| 消息文件 | `{lang}.json` | `zh.json`、`ja.json` | [[4.2 项目文件结构文档.md#七]] |
| 图片文件 | kebab-case | `hero-character.png`、`ccms-cover.png` | [[4.2 项目文件结构文档.md#七]]、[[4.3 视觉资产与使用规范.md#5.2]] |
| 字体文件 | kebab-case（Noto Sans SC）或 PascalCase（购买字体原始名称） | `RoundedMplus1c-Regular.woff2`、`noto-sans-sc-4-400-normal.woff2` | [[4.2 项目文件结构文档.md#七]] |
| 文档文件（简历/Case Study） | `{slug}-{lang}.{ext}` | `resume-zh.pdf`、`casestudy-en.md` | [[4.2 项目文件结构文档.md#七]] |
| 目录 | kebab-case | `components/`、`hooks/` | 行业标准 |
| Zod schema | kebab-case + `.schema` | `case-studies.schema.ts` | [[4.1 技术栈与架构文档.md#4.3]] 模式 |

### 1.2 代码符号命名

| 类别 | 风格 | 示例 | 说明 |
|------|------|------|------|
| 组件名 | PascalCase | `Navbar`、`CaseStudyCard` | React 组件 |
| 工具函数 | camelCase | `getVNScenario()`、`cn()` | 普通函数 |
| 动画 variants | camelCase | `staggerContainer`、`dialogSlideUp`、`transitionFade` | Framer Motion variants，统一在 `src/lib/animations.ts` 中定义 |
| 变量 | camelCase | `isMenuOpen`、`scrollY` | 所有普通变量 |
| 常量（非枚举） | camelCase 或 UPPER_SNAKE | `defaultLocale`、`MAX_ITEMS` | 魔法数字用 UPPER_SNAKE |
| 类型/接口 | PascalCase | `Profile`、`VNScene` | — |
| 枚举 | 禁用 `enum`，使用 `as const` + union type | `type Locale = "zh" \| "ja" \| "en"` | 行业标准推荐 union type |
| CSS 自定义属性 | kebab-case + `--` 前缀 | `--color-primary`、`--color-dialog-blue` | CSS 标准 |
| Tailwind class | 小写连字符 | `bg-surface`、`text-text-secondary` | Tailwind 约定 |
| boolean 变量 | 前缀 `is` / `has` / `should` | `isDesktop`、`hasError`、`shouldShow` | 行业标准 |

> **Props 接口命名**：当前仓库不使用单独的 `{ComponentName}Props` 接口，所有 Props 类型通过 `Readonly<{...}>` 内联定义（见 §3.3）。

### 1.3 路由与 URL 命名

| 类别 | 风格 | 示例 |
|------|------|------|
| 路由段 | kebab-case | `/[lang]/projects/[slug]` |
| 查询参数 | camelCase | `?sortBy=date` |
| 锚点 ID | kebab-case | `#case-studies`、`#contact` |

---

## 二、TypeScript 严格模式规则

### 2.1 `tsconfig.json` 配置基准

```jsonc
{
  "compilerOptions": {
    "target": "ES2017",
    "lib": ["dom", "dom.iterable", "esnext"],
    "allowJs": true,
    "skipLibCheck": true,
    "strict": true,
    "noUncheckedIndexedAccess": true,
    "noImplicitReturns": true,
    "noEmit": true,
    "esModuleInterop": true,
    "module": "esnext",
    "moduleResolution": "bundler",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "jsx": "react-jsx",
    "incremental": true,
    "plugins": [{ "name": "next" }],
    "paths": {
      "@/*": ["./src/*"]
    }
  },
  "include": [
    "next-env.d.ts",
    "proxy.ts",
    "**/*.ts",
    "**/*.tsx",
    ".next/types/**/*.ts",
    "**/*.mts",
    "**/*.cts"
  ],
  "exclude": ["node_modules"]
}
```

参见 [[4.1 技术栈与架构文档.md#7.1]] 完整配置。

### 2.2 类型定义规则

| 规则 | 要求 | 示例 |
|------|------|------|
| **显式返回值类型** | 所有函数必须标注返回类型 | `function getVNScenario(lang: string): VNScenario` |
| **Props 定义** | 统一使用 `Readonly<{...}>` 包装，内联在组件函数参数中 | `Readonly<{ slug: string; cover: string }>` |
| **数据模型** | 使用 `type` + Zod 推断 | `type VNScene = z.infer<typeof VNSceneSchema>` |
| **禁止 `any`** | 严格禁止，使用 `unknown` 替代后窄化 | `function parseJSON<T>(raw: string): T` |
| **空状态处理** | 数组使用 `T[]` 而非 `T[] \| undefined` | `skills: string[]`（空数组=无技能） |
| **可选字段** | 使用 `?` 而非 `\| undefined` 联合 | `cover?: string` |
| **枚举偏好** | 禁用 `enum`，使用 `as const` + union type | `type Locale = "zh" \| "ja" \| "en"` |

> **Props 接口 vs 内联**：当前仓库所有组件使用 `Readonly<{...}>` 内联定义 Props 类型，不使用单独的 `interface XXXProps`。此做法避免了重复命名和额外的类型文件开销。

### 2.3 类型文件组织

| 规模 | 位置 | 说明 |
|------|------|------|
| 组件 Props | 组件参数中 `Readonly<{...}>` 内联 | 每个组件自行定义 |
| 业务类型 | Zod schema 所在文件或 `src/lib/*.schema.ts` | schema 推断的类型直接 export |
| Zod schema | `src/lib/*.schema.ts` | 数据校验 schema + 推断类型（当前仅 `case-studies.schema.ts`） |
| 内联 schema | `src/lib/read-data.ts` | VN 场景类型（VNSceneSchema 等）定义在使用处，无独立 schema 文件 |
| i18n 路由类型 | `src/i18n/routing.ts` | `Locale` union type 由 routing 模块导出 |

> **注意**：当前仓库不设 `src/types/` 目录，所有类型分散在所属模块的文件中。

---

## 三、组件规范

### 3.1 组件声明风格

统一使用函数声明（function declaration）：

```tsx
// ✅ 正确 — function declaration + Readonly 包装
export function ChapterHeader({
  title,
  className,
}: Readonly<{
  title: string
  className?: string
}>) {
  return <header>...</header>
}

// ❌ 避免 — 箭头函数 + React.FC
export const ChapterHeader: React.FC<{ title: string }> = ({ title }) => { ... }
```

理由：stack trace 显示真实函数名、hoisting 友好、无 `React.FC` 的隐式 `children` 问题。

### 3.2 Server vs Client Component 边界

| 类型 | `"use client"` | 适用场景 | 组件 |
|------|---------------|---------|------|
| Server Component | 否 | 纯展示、无交互、无 hooks | `page.tsx`（数据读取层）、`SkillTag.tsx`、`FlowStepBar.tsx`、`CaseStudyContent.tsx`（纯渲染） |
| Client Component | 是 | 交互、动画、浏览器 API、hooks | `Navbar`、`HeroSection`、`CaseStudyCard`、`CaseStudySections`、`Footer`、各 Section（含 whileInView） |
| 布局组件 | 是（多语 layout） | `[lang]/layout.tsx` 需 Client 因 `NextIntlClientProvider` | 见 [[4.2 项目文件结构文档.md#二]] |

> **注意事项**：
> - 页面组件（`page.tsx`）本身不标记 `"use client"`，作为 Server Component 加载数据，再传入子 Client Components
> - 即使纯展示但使用 `useTranslations` 也需标记 `"use client"`（如 `Footer.tsx`、`CaseStudiesSection.tsx`）
> - 若组件被 Client Component 独占引用（非独自出现在 Server Component 树中），可省略 `"use client"`（如 `CaseStudyContent.tsx`），但推荐显式标记以保持可移植性

### 3.3 Props 模式

全部组件统一使用 **`Readonly<{...}>` 包装 + 函数参数解构**：

```tsx
// ✅ 正确 — Readonly 内联
export function CaseStudyCard({
  slug,
  cover,
  techStack,
  titleKey,
  descriptionKey,
}: Readonly<{
  slug: string
  cover: string
  techStack: string[]
  titleKey: string
  descriptionKey: string
}>) {
  return <div>...</div>
}

// ❌ 避免 — 单独的 interface + 无 Readonly 包装
interface CaseStudyCardProps { ... }
export function CaseStudyCard({ ... }: CaseStudyCardProps) { ... }
```

> **理由**：`Readonly<{...}>` 确保所有 Props 在组件内不可被意外修改；内联避免了额外命名和文件开销。当前仓库 34 个组件中 16 个有 Props，全部使用此模式。

### 3.4 组件文件结构

```
"use client"（如需）
（空行）
import 语句（分组排序，见 §四）
（空行）
export function XXX({ ... }: Readonly<{ ... }>) {
  // hooks（如果 Client Component）
  // 数据处理
  // JSX return
}
```

---

## 四、导入与导出规范

### 4.1 导入顺序

按以下松顺序组织 import 语句（组内无强制空行分隔，保持可读性即可）：

```
// 1. React / Next.js 优先
import { useCallback } from "react"
import Image from "next/image"

// 2. 第三方库
import { motion } from "framer-motion"
import { useTranslations } from "next-intl"
import { Download, Check } from "lucide-react"

// 3. 内部模块（`@/` 别名）
import { cn } from "@/lib/utils"
import { dialogSlideUp } from "@/lib/animations"

// 4. 数据文件
import profile from "@/data/profile.json"

// 5. 同级/相对引用
import { SkillTag } from "./SkillTag"
import { Link } from "@/i18n/routing" // 特殊：@/i18n/routing 属于内部模块但不含路径前缀限制
```

> **实际遵守度**：当前代码库未严格按组用空行分隔，但整体趋势遵循"React → 第三方 → 内部 → 数据 → 相对"的流向。建议新增文件遵循此顺序。

### 4.2 路径别名

- 使用 `@/` 映射到 `src/`（`tsconfig.json` paths 配置）
- 同级组件引用使用相对路径 `./ComponentName`
- 上层目录引用最多 1 层 `../`，禁止超过 2 层

### 4.3 导出规则

| 类型 | 导出方式 | 示例 |
|------|---------|------|
| 组件 | named export | `export function Navbar()` |
| 工具函数 | named export | `export function getVNScenario()` |
| 动画 variants | named export | `export const staggerContainer: Variants` |
| 类型 | named export | `export type VNScene = z.infer<typeof VNSceneSchema>` |
| Zod schema | named export | `export const CaseStudyItemSchema = z.object({...})` |
| 默认导出 | 仅用于 Next.js 路由文件 | `page.tsx`、`layout.tsx`、`not-found.tsx`、`sitemap.ts`、`robots.ts`、`proxy.ts`、`i18n/request.ts` |

---

## 五、代码格式与质量

### 5.1 格式化工具链

| 工具 | 用途 | 配置来源 |
|------|------|---------|
| **ESLint** | 代码质量与格式 | `eslint.config.mjs`（Flat Config） |

> **Prettier**：当前项目未配置 Prettier。代码格式由 ESLint（`eslint-config-next`）统一管理。如需格式化，手动遵循以下约定：
> - 缩进：2 空格
> - 引号：双引号
> - 分号：有（Next.js 默认）
> - 尾逗号：`all`
> - 打印宽度：不限（以可读性为准）

### 5.2 ESLint 配置

使用 ESLint Flat Config（`eslint.config.mjs`），基于 `eslint-config-next` 的两个预设：

```
eslint-config-next/core-web-vitals  → 核心质量规则
eslint-config-next/typescript       → TypeScript 规则
```

忽略目录：`.next/`、`out/`、`build/`、`next-env.d.ts`

无自定义规则覆写，使用 Next.js 官方推荐配置。

### 5.3 代码注释规范

| 场景 | 规范 | 示例 |
|------|------|------|
| 复杂逻辑 | 写 `// why`，不写 `// what` | `// offset 补偿导航栏高度` |
| TODO | `// TODO: 原因/期限` | `// TODO: v2 接入真实后端` |
| JSDoc | 仅用于公共库函数 | `/** 读取 JSON + Zod 校验 */` |
| 组件说明 | 不需要 | 组件名即文档 |

---

## 六、React 与 Hooks 规范

### 6.1 Hooks 规则

| 规则 | 说明 |
|------|------|
| 命名 | `useXxx` 前缀 |
| 自定义 Hooks | 放在 `src/hooks/` 目录 |
| `useEffect` 依赖 | 必须声明完整依赖数组（ESLint exhaustive-deps） |
| `useCallback` / `useMemo` | 仅在明确性能问题时使用，不滥用 |
| 条件调用 | 禁止（React hooks 调用顺序必须一致） |

### 6.2 自定义 Hooks 清单

| Hook | 位置 | 用途 |
|------|------|------|
| `useMediaQuery` | `src/hooks/useMediaQuery.ts` | 响应式断点检测（返回 `boolean`） |

> 当前仅此一个自定义 Hook。滚动锚点跳转由 `ScrollToAnchor` 组件直接处理（`window.scrollTo` + 导航栏偏移补偿），未抽取为独立 Hook。

### 6.3 Framer Motion 动画规范

| 规范 | 说明 |
|------|------|
| variants 集中管理 | 优先从 `src/lib/animations.ts` 引用命名 variants |
| 行内 variants | 允许用于简单过渡（如 `<motion.div initial={{ opacity: 0 }} animate={{ opacity: 1 }}>`），复杂动画应抽取至 `animations.ts` |
| CSS 动画 | 仅用于循环装饰动画（光晕脉冲 `glow-pulse`、角色浮动 `float`） |
| transition 参数 | 缓动函数统一使用 `[0.16, 1, 0.3, 1]`（自定义 cubic-bezier，模拟弹性缓出） |

#### 集中定义的动画 variants（`src/lib/animations.ts`）

| Export | 用途 | 关键属性 |
|--------|------|---------|
| `staggerContainer` | 子元素交错入场容器 | `staggerChildren: 0.15, delayChildren: 0.1` |
| `staggerItem` | 交错入场子元素 | `opacity: 0 → 1, y: 20 → 0` |
| `pageTransition` | 页面级过渡 | `opacity` 变化，入场 0.4s / 离场 0.15s |
| `chapterReveal` | 章节标题/内容揭示 | `x: -30 → 0` |
| `dialogSlideUp` | 对话框/卡片滑入 | `y: 24 → 0, scale: 0.98 → 1` |
| `optionStagger` | 选项按钮交错入场容器 | `staggerChildren: 0.12` |
| `optionItem` | 选项按钮入场 | `x: -20 → 0` |
| `transitionFade` | 简单缩放淡入（过渡分隔线） | `scale: 0.92 → 1` |

---

## 七、CSS 与样式规范

### 7.1 Tailwind 优先

| 优先级 | 方案 | 适用场景 |
|--------|------|---------|
| 最高 | Tailwind 原子类 | 所有样式优先用 Tailwind 类实现 |
| 次之 | CSS 自定义属性 | 颜色/间距/阴影等设计令牌（[[3.1 风格指南.md#九]]） |
| 最低 | 自定义 CSS | 仅 Tailwind 无法实现的动效（`@keyframes`） |

### 7.2 自定义 CSS 限制

- 仅允许写在 `src/styles/globals.css` 中
- 使用 `@keyframes` 时确保添加 `@media (prefers-reduced-motion: reduce)` 降级
- 禁止在组件文件中编写 `<style>` 标签或 CSS Modules

### 7.3 行内样式（`style` prop）

允许在以下场景使用 `style` prop（不在 Tailwind 能力范围内）：

| 场景 | 组件 | 示例 |
|------|------|------|
| 动态计算值 | `Navbar.tsx` | `style={{ backgroundColor: bgColor, boxShadow }}`（随滚动变化） |
| CSS 变量覆盖 | `HeroSection.tsx` | `style={{ objectPosition: "center 45%" }}` |
| `backgroundImage` | `OptionButton.tsx` | `style={{ backgroundImage: "url(...)" }}` |
| 位置偏移 | `VNScenario.tsx` | `style={{ transform: `translate(...)` }}` |

> **原则**：能用 Tailwind 原子类的不用 `style` prop；`style` prop 仅用于运行时动态值或 Tailwind 无法表达的场景。

### 7.4 `cn()` 工具函数

使用 `@/lib/utils.ts` 中的 `cn()` 合并 Tailwind 类：

```tsx
import { cn } from "@/lib/utils"

// 实际项目中的使用方式
<button className={cn("bg-primary text-white px-4 py-2", isActive && "ring-2")}>
```

---

## 八、数据层规范

### 8.1 数据读取模式

```typescript
// src/lib/read-data.ts — 当前实现
import "server-only"

import fs from "fs"
import path from "path"
import { z } from "zod"
import { CaseStudiesSchema } from "./case-studies.schema"

import caseStudiesData from "@/data/case-studies.json"

export function getVNScenario(lang: string): VNScenario {
  const filePath = path.join(process.cwd(), `src/data/vn-scenario.${lang}.json`)
  const raw = fs.readFileSync(filePath, "utf-8")
  const parsed = JSON.parse(raw)
  return VNScenarioSchema.parse(parsed)
}

export function getCaseStudies() {
  return CaseStudiesSchema.parse(caseStudiesData)
}
```

> 当前实现不包含 `try/catch`，数据验证失败时直接 `throw`，在 build 阶段暴露。`getCaseStudies()` 通过 ES module import 而非 `fs.readFileSync` 读取 JSON。

### 8.2 Zod Schema 规范

| 规则 | 说明 |
|------|------|
| 独立的 schema 文件 | `src/lib/case-studies.schema.ts` → `case-studies.json` |
| 内联 schema（在 `read-data.ts` 中定义） | `VNSceneSchema`、`VNChoiceSchema`、`VNScenarioSchema` → `vn-scenario.{lang}.json` |
| 类型从 schema 推断 | `type VNScene = z.infer<typeof VNSceneSchema>` |
| 错误处理 | `parse()` 失败时抛出异常，build 时暴露，不静默降级 |

> **注意**：`profile.json` 和 `projects.json` 无独立 schema 文件，通过 TypeScript 类型断言或 `as` 在组件中直接引用。

### 8.3 Case Study 内容来源

Case Study 详情页内容**不来自 MDX/MD 文件**，而是通过 `next-intl` 的 `t.raw()` 从 `messages/{lang}.json` 中读取多语言文本：

```
src/messages/en.json → t.raw("caseStudies.casestudy.executiveSummary")
src/messages/ja.json → t.raw("caseStudies.casestudy.executiveSummary")
src/messages/zh.json → t.raw("caseStudies.casestudy.executiveSummary")
```

Markdown 解析由 `CaseStudyContent.tsx` 实现（自定义 Block/Inline 解析器，非 `react-markdown`）。

### 8.4 数据不可变性

- JSON 数据文件视为只读，运行时永不修改
- 组件中不对数据做 `map`/`filter` 外的变换
- 数据排序/过滤在 JSON 源文件中预先完成

---

## 九、Git 规范

### 9.1 分支命名

```
feature/{简短描述}     → 新功能
fix/{简短描述}         → 修复
chore/{简短描述}       → 配置/依赖/文档
```

示例：`feature/hero-section`、`fix/navbar-scroll`

### 9.2 Commit 信息格式

使用 Conventional Commits：

```
<type>(<scope>): <简短描述>

[可选的正文]
```

| type | 场景 |
|------|------|
| `feat` | 新功能 |
| `fix` | 修复 |
| `chore` | 配置、依赖、文档更新 |
| `refactor` | 重构 |
| `style` | 仅格式变化 |

示例：
```
feat(hero): 实现 Hero 出场动画序列
fix(nav): 修正移动端汉堡菜单定位
chore(deps): 添加 framer-motion 依赖
```

### 9.3 Commit 前检查

```bash
# 运行以下命令确保通过
pnpm run lint
pnpm run typecheck
pnpm run build
```

---

## 十、错误处理与日志规范

### 10.1 错误处理策略

| 层级 | 策略 | 实现 |
|------|------|------|
| 数据读取（build 时） | Zod `parse()` 失败 → `throw`，无 `try/catch` | `getVNScenario()`、`getCaseStudies()` |
| 组件渲染 | 无自定义 Error Boundary，异常时 Next.js 兜底 | 无 `error.tsx`（可使用默认错误页） |
| 路由 | `notFound()` / `not-found.tsx`（不含 `"use client"`） | `[lang]/layout.tsx` 中 locale 不匹配时调用 |
| 图片加载 | `ZoomableImage` / `VNScenario` 使用 `img.onerror` 静默跳过 | VN 场景中图片加载失败仍继续 |

### 10.2 日志规范

| 环境 | 级别 | 用途 |
|------|------|------|
| 开发 | `console.log` / `console.error` | 调试 |
| 生产 | 不输出 `console.log` | ESLint 会提示保留的 `console.log` |
| Build 时 | 异常直接抛出，无 `console.error` | 数据验证失败时中断 build |

### 10.3 Error Boundary

当前 v1 未实现自定义 Error Boundary，使用 Next.js 默认错误处理。
如需自定义，在 `src/app/error.tsx` 中添加。

---

## 十一、环境变量规范

| 规则 | 说明 |
|------|------|
| 模板 | `.env.example` 中记录所有变量名（空值） |
| 前缀 | `NEXT_PUBLIC_` 仅用于客户端暴露变量 |
| 类型 | 使用 Zod 或 `process.env.X` 运行时读取 |
| 当前状态 | 无环境变量需求（纯静态站点） |

---

## 十二、汇总校验清单

开发完成/提交前，对照以下清单检查：

- [ ] TypeScript 编译通过（`pnpm run typecheck`）
- [ ] ESLint 无 error（`pnpm run lint`）
- [ ] Build 无报错（`pnpm run build`）
- [ ] 文件命名符合 `§一` 规范
- [ ] 组件声明使用 `export function`（非箭头函数）
- [ ] Props 使用 `Readonly<{...}>` 包装
- [ ] Server/Client 边界正确（需要交互/动画的组件标记 `"use client"`）
- [ ] 路径别名使用 `@/`（禁止 `../` 超过 2 层）
- [ ] 导出具名（除 Next.js 路由文件外不使用 `export default`）
- [ ] 无 `any` 类型
- [ ] 无遗留 `console.log`（生产环境）

