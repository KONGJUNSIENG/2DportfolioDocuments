# 5.1 编码规范文档

**项目：** KONG JUN SIENG — Software Developer Portfolio  
**文档目标：** 定义命名约定、TypeScript Strict 规则、组件组织规范与代码风格，约束 AI 生成的代码保持全仓库一致性。

---

## 一、命名规范

### 1.1 文件与目录命名

| 类别 | 风格 | 示例 | 来源/理由 |
|------|------|------|----------|
| React 组件 | PascalCase | `HeroSection.tsx`、`SkillTag.tsx` | [[4.2 项目文件结构文档.md#七]] |
| 工具/库文件 | kebab-case | `read-data.ts`、`animations.ts` | [[4.2 项目文件结构文档.md#七]] |
| JSON 数据 | kebab-case | `profile.json`、`case-studies.json` | [[4.2 项目文件结构文档.md#七]] |
| Markdown 数据 | `{slug}-{lang}.md` | `portfolio-zh.md` | [[4.2 项目文件结构文档.md#七]] |
| 消息文件 | `{lang}.json` | `zh.json`、`ja.json` | [[4.2 项目文件结构文档.md#七]] |
| 图片文件 | kebab-case | `hero-character.png`、`ccms-cover.png` | [[4.2 项目文件结构文档.md#七]]、[[4.3 视觉资产与使用规范.md#5.2]] |
| 字体文件 | PascalCase（原始名称） | `MPlusRounded1c-Regular.woff2` | [[4.2 项目文件结构文档.md#七]] |
| 简历文件 | `resume-{lang}.pdf` | `resume-zh.pdf` | [[4.2 项目文件结构文档.md#七]] |
| 目录 | kebab-case | `components/`、`case-studies/` | 行业标准 |
| Zod schema | kebab-case + `.schema` | `profile.schema.ts`、`projects.schema.ts` | [[4.1 技术栈与架构文档.md#4.3]] 模式 |

### 1.2 代码符号命名

| 类别 | 风格 | 示例 | 说明 |
|------|------|------|------|
| 组件名 | PascalCase | `Navbar`、`CaseStudyCard` | React 组件 |
| 工具函数 | camelCase | `getProfile()`、`cn()` | 普通函数 |
| 动画 variants | camelCase | `fadeInUp`、`staggerContainer` | Framer Motion variants |
| 变量 | camelCase | `isMenuOpen`、`scrollY` | 所有普通变量 |
| 常量（非枚举） | camelCase 或 UPPER_SNAKE | `defaultLocale`、`MAX_ITEMS` | 魔法数字用 UPPER_SNAKE |
| 类型/接口 | PascalCase | `Profile`、`CaseStudyCardProps` | — |
| Props 接口 | `{ComponentName}Props` | `NavbarProps`、`CaseStudyCardProps` | 行业标准 |
| 枚举 | PascalCase + UPPER 成员 | `enum Locale { Zh = "zh", Ja = "ja" }` | 行业标准 |
| CSS 自定义属性 | kebab-case + `--` 前缀 | `--color-primary`、`--radius-md` | CSS 标准 |
| Tailwind class | 小写连字符 | `bg-surface`、`text-text-secondary` | Tailwind 约定 |
| boolean 变量 | 前缀 `is` / `has` / `should` | `isDesktop`、`hasError`、`shouldShow` | 行业标准 |

### 1.3 路由与 URL 命名

| 类别 | 风格 | 示例 |
|------|------|------|
| 路由段 | kebab-case | `/[lang]/projects/[slug]` |
| 查询参数 | camelCase | `?sortBy=date` |
| 锚点 ID | kebab-case | `#case-studies`、`#contact` |

---

## 二、TypeScript 严格模式规则

### 2.1 `tsconfig.json` 配置基准

```jsonc
{
  "compilerOptions": {
    "strict": true,
    "noUncheckedIndexedAccess": true,
    "noImplicitReturns": true,
    "noFallthroughCasesInSwitch": true,
    "exactOptionalPropertyTypes": false,
    "forceConsistentCasingInFileNames": true,
    "moduleResolution": "bundler",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "paths": {
      "@/*": ["./src/*"]
    }
  }
}
```

参见 [[4.1 技术栈与架构文档.md#7.1]] 完整配置。

### 2.2 类型定义规则

| 规则 | 要求 | 示例 |
|------|------|------|
| **显式返回值类型** | 所有函数必须标注返回类型 | `function getProfile(): Profile` |
| **Props 接口** | 使用 `interface`，放在组件文件顶部或同目录 `.types.ts` | `interface NavbarProps { lang: string }` |
| **数据模型** | 使用 `type` + Zod 推断 | `type Profile = z.infer<typeof ProfileSchema>` |
| **禁止 `any`** | 严格禁止，使用 `unknown` 替代后窄化 | `function parseJSON<T>(raw: string): T` |
| **空状态处理** | 数组使用 `T[]` 而非 `T[] \| undefined` | `skills: string[]`（空数组=无技能） |
| **可选字段** | 使用 `?` 而非 `\| undefined` 联合 | `cover?: string` |
| **枚举偏好** | 优先使用 `as const` + union type | `type Locale = "zh" | "ja" | "en"` |

### 2.3 类型文件组织

| 规模 | 位置 | 说明 |
|------|------|------|
| 小型（≤5 行） | 组件文件内 `interface XXXProps` | 直接写在组件文件顶部 |
| 中型 | `src/types/*.ts` | 项目共享类型 |
| Zod schema | `src/lib/*.schema.ts` | 数据校验 schema + 推断类型 |

---

## 三、组件规范

### 3.1 组件声明风格

统一使用函数声明（function declaration）：

```tsx
// ✅ 正确
export function Navbar({ lang }: NavbarProps) {
  return <nav>...</nav>
}

// ❌ 避免 — 箭头函数 + React.FC
export const Navbar: React.FC<NavbarProps> = ({ lang }) => { ... }
```

理由：stack trace 显示真实函数名、hoisting 友好、无 `React.FC` 的隐式 `children` 问题。

### 3.2 Server vs Client Component 边界

| 类型 | `"use client"` | 适用场景 | 参考 [[4.1 技术栈与架构文档.md#3.2]] |
|------|---------------|---------|---------------|
| Server Component | 否 | 纯展示、数据读取、SEO metadata | `page.tsx`、`CaseStudyCard`、`Footer`、`SkillTag`、`SocialLink` |
| Client Component | 是 | 交互、动画、浏览器 API | `Navbar`、`HeroSection`、各 Section（含 whileInView） |
| 布局组件 | 否（根 layout）/ 是（多语 layout） | `[lang]/layout.tsx` 需 Client 因 `AnimatePresence` | 见 [[4.2 项目文件结构文档.md#二]] |

### 3.3 Props 接口模式

```tsx
// 1. 接口定义在文件顶部（小型组件）
interface CaseStudyCardProps {
  slug: string
  cover: string
  techStack: string[]
  titleKey: string
  descriptionKey: string
}

// 2. 使用解构 + 默认值
export function CaseStudyCard({
  slug,
  cover,
  techStack,
  titleKey,
  descriptionKey,
}: CaseStudyCardProps) {
  return <div>...</div>
}
```

### 3.4 组件文件结构

```
import 语句（分组排序，见 §四）

interface XXXProps {
  ...
}

export function XXX({ ... }: XXXProps) {
  // hooks（如果 Client Component）
  // 数据处理
  // return JSX
}
```

### 3.5 shadcn/ui 使用规范

| 规范 | 说明 |
|------|------|
| 骨架更新 | `pnpm dlx shadcn@latest add button` 仅取骨架 |
| 样式覆写 | 通过 `globals.css` 中 `@theme` 声明的 CSS 变量控制，不手动改 `components/ui/*.tsx` 中的样式类 |
| 组件注册 | `components/ui/` 目录下的 shadcn 组件不手动编辑 |

---

## 四、导入与导出规范

### 4.1 导入顺序（每组内部按字母序）

```
// 1. React / Next.js
import { useCallback } from "react"
import Link from "next/link"

// 2. 第三方库
import { motion } from "framer-motion"
import { useTranslations } from "next-intl"

// 3. 内部模块
import { cn } from "@/lib/utils"
import { SkillTag } from "./SkillTag"

// 4. 数据/消息
import profile from "@/data/profile.json"

// 5. 样式
import "@/styles/globals.css"  // 仅根 layout 中
```

### 4.2 路径别名

- 使用 `@/` 映射到 `src/`
- 同级组件引用使用相对路径 `./ComponentName`
- 禁止 `../` 向上超过 2 层

### 4.3 导出规则

| 类型 | 导出方式 | 示例 |
|------|---------|------|
| 组件 | named export | `export function Navbar()` |
| 工具函数 | named export | `export function getProfile()` |
| 动画 variants | named export | `export const fadeInUp: Variants` |
| 类型/接口 | named export | `export interface NavbarProps` |
| Zod schema | named export + 推断类型 | `export const ProfileSchema`, `export type Profile` |
| 默认导出 | 仅用于 `page.tsx` / `layout.tsx`（Next.js 要求） | `export default function Page()` |

---

## 五、代码格式与质量

### 5.1 格式化工具链

| 工具 | 用途 | 配置来源 |
|------|------|---------|
| **ESLint** | 代码质量（未使用变量、类型检查、React hooks 规则） | `eslint.config.mjs` |
| **Prettier** | 代码格式（缩进、引号、尾逗号、打印宽度） | `.prettierrc` |

### 5.2 Prettier 配置

```jsonc
// .prettierrc
{
  "semi": false,
  "singleQuote": false,
  "tabWidth": 2,
  "trailingComma": "all",
  "printWidth": 100
}
```

### 5.3 ESLint 核心规则

| 规则 | 值 | 说明 |
|------|-----|------|
| `@typescript-eslint/no-unused-vars` | `error` | 禁止未使用变量 |
| `@typescript-eslint/no-explicit-any` | `error` | 禁止 `any` |
| `react-hooks/rules-of-hooks` | `error` | Hooks 调用顺序规则 |
| `react-hooks/exhaustive-deps` | `warn` | 依赖数组完整性 |
| `import/order` | `warn` | 导入顺序（见 §四） |

### 5.4 代码注释规范

| 场景 | 规范 | 示例 |
|------|------|------|
| 复杂逻辑 | 写 `// why`，不写 `// what` | `// offset 补偿导航栏高度` |
| TODO | `// TODO: 原因/期限` | `// TODO: v2 接入真实后端` |
| JSDoc | 仅用于公共库函数 | `/** 读取 JSON + Zod 校验 */` |
| 组件说明 | 不需要 | 组件名即文档 |

---

## 六、React 与 Hooks 规范

### 6.1 Hooks 规则

| 规则 | 说明 |
|------|------|
| 命名 | `useXxx` 前缀 |
| 自定义 Hooks | 放在 `src/hooks/` 目录 |
| `useEffect` 依赖 | 必须声明完整依赖数组（ESLint exhaustive-deps） |
| `useCallback` / `useMemo` | 仅在明确性能问题时使用，不滥用 |
| 条件调用 | 禁止（React hooks 调用顺序必须一致） |

### 6.2 自定义 Hooks 清单

| Hook | 位置 | 已有定义 | 用途 |
|------|------|---------|------|
| `useMediaQuery` | `src/hooks/useMediaQuery.ts` | [[3.3 交互与动效说明.md#6.3]] | 响应式断点检测 |
| `useScrollTo` | `src/hooks/useScrollTo.ts` | (新建) | 平滑滚动 + 偏移补偿 |

### 6.3 Framer Motion 动画规范

| 规范 | 说明 |
|------|------|
| variants 集中管理 | `src/lib/animations.ts` 中定义，组件中只引用名称 |
| 行内 variants | 禁止，必须使用集中定义的 variants |
| CSS 动画 | 仅用于循环装饰动画（光晕脉冲、角色浮动） |
| transition 参数 | 使用 [[3.1 风格指南.md#七]] 定义的缓动函数和时长 |

---

## 七、CSS 与样式规范

### 7.1 Tailwind 优先

| 优先级 | 方案 | 适用场景 |
|--------|------|---------|
| 最高 | Tailwind 原子类 | 所有样式优先用 Tailwind 类实现 |
| 次之 | CSS 自定义属性 | 颜色/间距/阴影等设计令牌（[[3.1 风格指南.md#九]]） |
| 最低 | 自定义 CSS | 仅 Tailwind 无法实现的动效（`@keyframes`） |

### 7.2 自定义 CSS 限制

- 仅允许写在 `src/styles/globals.css` 中
- 使用 `@keyframes` 时确保添加 `@media (prefers-reduced-motion: reduce)` 降级
- 禁止在组件文件中编写 `<style>` 标签或 CSS Modules

### 7.3 `cn()` 工具函数

使用 `@/lib/utils.ts` 中的 `cn()` 合并 Tailwind 类：

```tsx
import { cn } from "@/lib/utils"

<button className={cn("btn-primary", isActive && "ring-2")}>
```

---

## 八、数据层规范

### 8.1 数据读取模式

```typescript
// src/lib/read-data.ts — 所有 JSON 读取遵循此模式
function readJSON<T>(path: string, schema: z.ZodSchema<T>): T {
  try {
    const raw = fs.readFileSync(path, "utf-8")
    const parsed = JSON.parse(raw)
    return schema.parse(parsed)
  } catch (error) {
    console.error(`数据验证失败: ${path}`, error)
    throw error
  }
}
```

### 8.2 Zod Schema 规范

| 规则 | 说明 |
|------|------|
| 每个 JSON 数据文件对应一个 schema | `profile.schema.ts` ←→ `profile.json` |
| 类型从 schema 推断 | `type Profile = z.infer<typeof ProfileSchema>` |
| 错误处理 | `parse()` 失败时 build 阶段抛出，不静默降级 |

### 8.3 数据不可变性

- JSON 数据文件视为只读，运行时永不修改
- 组件中不对数据做 `map`/`filter` 外的变换
- 数据排序/过滤由 AI 在构建阶段完成

---

## 九、Git 规范

### 9.1 分支命名

```
feature/{简短描述}     → 新功能
fix/{简短描述}         → 修复
chore/{简短描述}       → 配置/依赖/文档
```

示例：`feature/hero-section`、`fix/navbar-scroll`

### 9.2 Commit 信息格式

使用 Conventional Commits：

```
<type>(<scope>): <简短描述>

[可选的正文]
```

| type | 场景 |
|------|------|
| `feat` | 新功能 |
| `fix` | 修复 |
| `chore` | 配置、依赖、文档更新 |
| `refactor` | 重构 |
| `style` | 仅格式变化 |

示例：
```
feat(hero): 实现 Hero 出场动画序列
fix(nav): 修正移动端汉堡菜单定位
chore(deps): 添加 framer-motion 依赖
```

### 9.3 Commit 前检查

```bash
# 运行以下命令确保通过
pnpm run lint
pnpm run typecheck
pnpm run build
```

---

## 十、错误处理与日志规范

### 10.1 错误处理策略

| 层级 | 策略 | 示例 |
|------|------|------|
| 数据读取 | Zod parse 失败 → `throw`（build 时暴露） | `readJSON()` |
| 组件渲染 | 不 catch，让 Error Boundary 处理 | 首页区块渲染 |
| 表单 UI | 纯展示，无错误处理逻辑 | Contact 表单 |
| 路由 | `notFound()` / `not-found.tsx` | 404 处理 |

### 10.2 日志规范

| 环境 | 级别 | 用途 |
|------|------|------|
| 开发 | `console.log` / `console.error` | 调试 |
| 生产 | 不输出 `console.log` | — |
| Build 时 | `console.error` | 数据验证失败提示 |

### 10.3 Error Boundary

当前 v1 不实现自定义 Error Boundary，使用 Next.js 默认 error 处理。
如需自定义，在 `src/app/error.tsx` 中添加。

---

## 十一、环境变量规范

| 规则 | 说明 |
|------|------|
| 模板 | `.env.example` 中记录所有变量名（空值） |
| 前缀 | `NEXT_PUBLIC_` 仅用于客户端暴露变量 |
| 类型 | 使用 Zod 或 `process.env.X` 运行时读取 |
| 当前状态 | 无环境变量需求（纯静态站点） |

---

## 十二、汇总校验清单

开发完成/提交前，对照以下清单检查：

- [ ] TypeScript 编译通过（`pnpm run typecheck`）
- [ ] ESLint 无 error（`pnpm run lint`）
- [ ] Prettier 格式一致（`pnpm run format`）
- [ ] Build 无报错（`pnpm run build`）
- [ ] 文件命名符合 `§一` 规范
- [ ] 组件声明使用 function declaration
- [ ] Props 使用 `{ComponentName}Props` 接口
- [ ] 导入按 `§四` 顺序分组
- [ ] 无 `any` 类型
- [ ] 无遗留 `console.log`（生产环境）

