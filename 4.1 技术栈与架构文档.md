# 4.1 技术栈与架构文档

**项目：** KONG JUN SIENG — Software Developer Portfolio  
**文档目标：** 定义技术选型、架构决策与完整依赖配置，供 AI 代理按此初始化项目骨架和安装依赖。

---

## 一、技术选型矩阵

### 1.1 核心框架与语言

| 层面 | 选择 | 版本 | 选用理由 |
|------|------|------|---------|
| **框架** | Next.js | 16.2.9 | App Router 原生支持 i18n 路由、Server Components 提升性能、Vercel 生态原生集成 |
| **语言** | TypeScript | ^5 | `strict: true` + `noUncheckedIndexedAccess` + `noImplicitReturns`，全量类型覆盖 |
| **包管理** | pnpm | >=11.7.0 (corepack) | 磁盘效率高、依赖隔离严格 |
| **React** | React | 19.2.4 | Next.js 16 内置，支持 Server Components |

### 1.2 CSS 与样式

| 方案 | 版本 | 选用理由 |
|------|------|---------|
| **Tailwind CSS** | v4 | 零运行时、设计令牌驱动、与自定义令牌系统完美配合 |
| **自定义令牌** | — | 通过 `src/styles/globals.css` 中的 `@theme inline` 指令声明自定义色彩令牌（primary / secondary / accent / surface / dialog 等），其余维度使用 Tailwind v4 预设默认值 |
| **全局 CSS** | — | 单文件 `src/styles/globals.css`，含 `@import "tailwindcss"` + `@theme` 设计令牌 + 自定义属性 |

### 1.3 动画

| 方案 | 选用理由 |
|------|---------|
| **Framer Motion 12.x** | 唯一动画库（不用 GSAP / anime.js），统一控制入场/页面过渡/视差/VN 对话框/章节过渡/选项交互；声明式 variants（`staggerContainer` / `chapterReveal` / `dialogSlideUp` / `pageTransition` / `transitionFade` 等）集中管理于 `src/lib/animations.ts` |
| **CSS Animations** | 仅用于循环装饰动画（光晕脉冲、角色浮动），减少 JS 开销；`@keyframes` 定义于 `globals.css` |

### 1.4 国际化

| 方案 | 版本 | 选用理由 |
|------|------|---------|
| **next-intl** | 最新 | App Router 原生支持、path-based 路由策略、类型安全消息键 |
| **策略** | path-based | `/[lang]/...`，proxy.ts 处理 `/` → `/en/intro` 重定向（VN 序幕入口） |
| **默认语言** | en | `i18n/routing.ts` 中 `defaultLocale: "en"` |
| **语言** | zh / ja / en | 对应三语 JSON 文件 `src/messages/{lang}.json` |

### 1.5 数据层

| 方案 | 选用理由 |
|------|---------|
| **本地 JSON** | 项目数据（profile / projects / case-studies / vn-scenario）无需 CMS，Zod 运行时校验保障类型 |
| **messages JSON** | Case Study 详情正文结构化存储在 `messages/{lang}.json` 中，通过 next-intl `getTranslations` 直接读取（含 Markdown 正文渲染） |
| **Zod** | `case-studies.schema.ts` + `read-data.ts` 内联 VN Scenario 校验；`profile` 与 `projects` 因结构简单无需独立 schema，直接从 JSON 导入 |

### 1.6 UI 组件

| 方案 | 选用理由 |
|------|---------|
| **自定义组件** | 全部 UI 为自定义组件（无第三方 UI 库依赖），遵循 VN 游戏风格设计体系 |
| **Lucide React** | 图标库，线性风格 `strokeWidth={1.5}`（当前仅 Navbar 汉堡菜单使用） |

### 1.7 开发工具与流水线

| 工具 | 用途 |
|------|------|
| **ESLint 9.x** | 代码规范（Flat Config `eslint.config.mjs`，使用 `eslint-config-next` core-web-vitals + typescript） |
| **TypeScript** | 类型检查（`tsconfig.json` strict 模式） |
| **GitHub Actions** | CI 流水线：lint + typecheck + build，push/PR 到 main 触发 |
| **Vercel** | 部署平台，Production 从 main 自动部署，PR 生成 Preview |

### 1.8 排除项（有意识不做）

| 技术 | 排除原因 |
|------|---------|
| 测试框架 | 不引入，依赖 TS + ESLint + Build 保障质量 |
| 数据库 / CMS | 内容量少且固定，本地文件 + 手动更新更轻量 |
| 后端 API | 联系表单纯 UI，无后端逻辑 |
| GSAP / Lottie | Framer Motion + CSS Animations 已覆盖全部动效需求 |
| 状态管理 | 无全局共享状态需求，prop drilling + Server Components 即可 |
| PWA / SWR / TanStack Query | v1 无数据变更/离线需求 |

---

## 二、架构设计

### 2.1 架构分层

```
┌─────────────────────────────────────────────────────────────┐
│                     Presentation Layer                       │
│  Navbar / HeroSection / CaseStudiesSection / ProjectsSection │
│  AboutSection / ContactSection / CaseStudyDetailPage         │
│  Footer / LanguageSwitcher / ScrollToAnchor                  │
├─────────────────────────────────────────────────────────────┤
│                     Data Access Layer                         │
│  src/lib/read-data.ts   — JSON 读取 + Zod 校验               │
├─────────────────────────────────────────────────────────────┤
│                        Data Layer                             │
│  src/data/profile.json                                       │
│  src/data/projects.json / case-studies.json                   │
│  src/data/vn-scenario.{lang}.json                             │
│  src/messages/{lang}.json                                     │
├─────────────────────────────────────────────────────────────┤
│                        i18n Layer                             │
│  next-intl + proxy.ts — 语言检测 / 重定向 / 消息分发          │
├─────────────────────────────────────────────────────────────┤
│                     Infrastructure Layer                      │
│  Vercel — Deployment                                         │
│  GitHub Actions — CI                                         │
└─────────────────────────────────────────────────────────────┘
```

### 2.2 架构决策记录

| 决策 ID | 决策 | 备选方案 | 理由 |
|---------|------|---------|------|
| ADR-001 | App Router (Server Components first) | Pages Router | App Router 原生支持布局嵌套、Server Components 减少客户端 JS、i18n 路由更简洁 |
| ADR-002 | 本地文件 + Zod 校验 | CMS / 数据库 | 内容量 ≤20 条，变更频率低；Zod 在 build 时保证数据完整性 |
| ADR-003 | 全自定义 UI 组件（VN 游戏风格） | shadcn/ui / 完整组件库 | 项目采用 VN 游戏叙事设计体系，无标准 UI 组件适用；所有组件手写，样式由设计令牌控制 |
| ADR-004 | next-intl path-based 路由 | cookie-based / domain-based | 最简单方案、SEO 友好（每个语言页面独立 URL）、proxy 统一处理；默认语言 en |
| ADR-005 | Framer Motion 唯一动画库 | GSAP + Framer 并存 | 减少依赖体积、统一动画范式、声明式 variants 更契合 React 组件模型 |
| ADR-006 | 无测试框架 | Jest / Vitest + Testing Library | v1 规模极小（1 首页 + 1 详情页 + 10 组件），过度测试增加维护成本；TS + Build 已覆盖类型和构建错误 |

### 2.3 渲染策略

| 页面 | 渲染方式 | 说明 |
|------|---------|------|
| 首页 `/[lang]` | Server Component (RSC) + Client Islands | 根布局/文本为 RSC，Navbar/各 Section 中动效部分标记 `"use client"` |
| Case Study 详情页 `/[lang]/projects/[slug]` | Server Component | 从 `messages/{lang}.json` 读取 9 节结构化字段（executiveSummary / background / methodology / developmentProcess / challenges / results / discussion / reflections / conclusion / appendix + sub-fields），由 `CaseStudySections`（Client）渲染含 Markdown 的正文；含 `CaseStudyNav`（Client 侧栏导航）、`CaseStudyDownloadButton`（Client）与 `BackToTopButton`（Client） |
| 数据读取层 | Server-only | `read-data.ts` 仅在 Server Component / `generateStaticParams` 中使用 |
| 游戏风序幕 `/[lang]/intro` | Client Component | 全部为客户端组件（IntroShell + TitleScreen + VNScenario + LoadingOverlay），Framer Motion + CSS 动画驱动 |

### 2.4 路由映射

```
/                                   → proxy.ts i18n 检测 + redirect → /en/intro
/en/intro (default)                 → 游戏风序幕（TitleScreen → 暗転 → Scenario → LoadingOverlay → 首页）
/zh/intro                           → 游戏风序幕
/ja/intro                           → 游戏风序幕
/en (default)                       → 首页单页滚动
/zh                                 → 首页单页滚动
/ja                                 → 首页单页滚动
/[lang]                             → 首页单页滚动
  ├── #hero                         → Hero Banner
  ├── #case-studies                 → Case Studies 卡片列表
  ├── #projects                     → 项目展示卡片
  ├── #about                        → 关于我
  └── #contact                      → 联系区块
/[lang]/projects/[slug]             → Case Study 详情页
/not-found                          → 404 页面（跨语言）
```

### 2.5 重定向规则

| 来源 | 目标 | 条件 | 实现 |
|------|------|------|------|
| `/` | `/en/intro` | 始终 | proxy.ts（VN 序幕入口） |
| `/[lang]` | — | 正常匹配 | — |
| 未知 `[lang]` 值 | `/not-found` | 未在 `locales` 数组中 | proxy.ts |
| 未知路由 | `/not-found` | 未匹配 | `src/app/not-found.tsx`（VN 游戏风风格：ERROR 404 标签 + 粉色大字 + 蓝线 + ▶ Go home；英文文案固定） |

---

## 三、组件架构

### 3.1 组件依赖树

```
RootLayout (根 layout.tsx — html + body + fonts)
└── LocaleLayout ([lang]/layout.tsx — NextIntlClientProvider)
    ├── Navbar
    │   ├── LogoLink (→ /[lang])
    │   ├── NavLink[] (案例 / 作品 / 关于 / 联系)
    │   └── LanguageSwitcher (中 / 日 / EN)
    ├── HomePage (page.tsx)
    │   ├── HeroSection
    │   │   ├── HeroCharacterImage (next/image)
    │   │   ├── Slogan | Name | Title
    │   │   └── CTAButton (OptionButton 风格)
    │   ├── ChapterHeader (★ 游戏风章节标题 — 复用 4 次)
    │   │   ├── 装饰分割线 (左右各一条)
    │   │   ├── 「CHAPTER N」编号
    │   │   └── 章节名称
    │   ├── SectionTransition (★ 游戏风过渡台词 — 章节之间各一次)
    │   │   └── (无子组件，独立渲染)
    │   ├── CaseStudiesSection
    │   │   ├── ChapterHeader
    │   │   └── CaseStudyCard[]
    │   │       ├── CoverImage (next/image)
    │   │       ├── TechStackTag[]
    │   │       ├── Title
    │   │       ├── Description
    │   │       └── ViewButton (→ /[lang]/projects/[slug])
    │   ├── ProjectsSection
    │   │   ├── ChapterHeader
    │   │   └── ProjectCard[]
    │   │       ├── CoverImage (next/image)
    │   │       ├── TechStackTag[]
    │   │       └── Title
    │   ├── AboutSection
    │   │   ├── ChapterHeader
    │   │   ├── DialogBox (★ 蓝色对话框承载自我介绍)
    │   │   │   ├── SpeakerName
    │   │   │   └── BioText (可选逐字显示)
    │   │   └── SkillTag[]
    │   └── ContactSection
    │       ├── ChapterHeader
    │       ├── EmailLink (OptionButton 风格)
    │       ├── SocialLink[] (GitHub / LinkedIn)
    │       └── ResumeDownloadButton (OptionButton 风格)
    ├── CaseStudyPage (projects/[slug]/page.tsx — Server)
    │   ├── CaseStudyDownloadButton (Client — VN 系统菜单风格，点击 blur→绿色完成态 1.5s 重置)
    │   ├── BackToTopButton (Client — VN Skip 风格纯文字 ▲ 按钮，scrollY>250 浮现)
    │   ├── CaseStudyNav (Client — 右侧固定侧栏导航，滚动高亮当前章节)
    │   ├── CoverImage (next/image)
    │   ├── Title
    │   ├── TechStackTag[] (SkillTag 复用)
    │   └── CaseStudySections (Client — whileInView 入场动画)
    │       └── ChapterHeader (复用) × N
    │       └── CaseStudyContent (正文渲染器)
    │           ├── FlowStepBar (流程步骤可视化)
    │           └── ZoomableImage (点击放大 modal)
    ├── JaLocaleToast (Client — ja 语言首次加载时显示翻译不完全提示)
    └── Footer
        └── CopyrightText
    ├── IntroPage (intro/page.tsx)
    │   └── IntroShell
    │       ├── TitleScreen
    │       │   └── HeroCharacterImage (next/image)
    │       ├── PixelsCurtain (暗転遮罩)
    │       ├── VNScenario
    │       │   ├── ScenarioCharacterImage (next/image, 表情切换)
    │       │   ├── ChapterHeader (章节标题)
    │       │   └── DialogBox (蓝色对话框 + 打字机效果)
    │       ├── LoadingOverlay
    │       │   ├── LoadingRipple (波纹扩散)
    │       │   └── ScrambleText (文字乱码效果)
    │       └── IntroAnimation (动画协调器)
```

### 3.2 组件职责边界

| 层级 | 组件 | `"use client"` | 说明 |
|------|------|---------------|------|
| **页面级** | `page.tsx` (首页) | 否 | Server Component，组装各 Section |
| **页面级** | `projects/[slug]/page.tsx` | 否 | Server Component，读取 messages JSON + 渲染 |
| **布局级** | `[lang]/layout.tsx` | 否（Server Component，内部使用 LocaleContent / LayoutShell 客户端组件） | NextIntlClientProvider + 组合 LocaleContent + LayoutShell |
| **客户端** | Navbar | 是 | 滚动阴影 + 汉堡菜单交互 |
| **客户端** | LanguageSwitcher | 是 | 语言切换交互 |
| **客户端** | HeroSection | 是 | Framer Motion 入场序列 + 视差 |
| **客户端** | CaseStudiesSection | 是 | whileInView 入场动画 |
| **客户端** | ProjectsSection | 是 | whileInView 入场动画 |
| **客户端** | AboutSection | 是 | whileInView 入场动画 |
| **客户端** | ContactSection | 是 | whileInView 入场动画 |
| **客户端** | CaseStudyCard | 是 | Framer Motion 空闲微摆 + hover 放大；`useReducedMotion` 关闭 |
| **客户端** | ProjectCard | 是 | Framer Motion 空闲弹跳 + hover 放大 + 点击 clip-path 圆形展开详情；`useReducedMotion` 关闭 |
| **客户端** | CaseStudySections | 是 | whileInView 入场动画，组合 ChapterHeader + CaseStudyContent |
| **客户端** | CaseStudyContent | 否（纯渲染组件，无交互状态） | Markdown 正文解析渲染器 |
| **客户端** | CaseStudyNav | 是 | scroll 事件监听 + 高亮当前章节 + 平滑滚动 |
| **客户端** | ZoomableImage | 是 | 点击放大 modal + Escape 关闭 |
| **客户端** | FlowStepBar | 否（纯展示组件） | 流程步骤可视化（箭头分割 + 圆点连接线） |
| **客户端** | CaseStudyDownloadButton | 是 | Motion 交互按钮：点击 blur → 绿色完成态 1.5s 重置 |
| **客户端** | BackToTopButton | 是 | VN Skip 风格纯文字，scrollY>250 浮现，click 平滑回顶 |
| **客户端** | JaLocaleToast | 是 | ja 语言首次加载时短暂提示 |
| **客户端** | Footer | 是 | 使用 `useTranslations` 获取版权文案 |
| **客户端** | DialogBox | 是 | Framer Motion 入场动画 + 可选逐字效果 |
| **客户端** | ChapterHeader | 是 | whileInView 分割线展开 + 文字淡入 |
| **客户端** | SectionTransition | 是 | AnimatePresence 过渡台词弹出/消失 |
| **客户端** | OptionButton | 是 | whileTap scale + hover 背景过渡 |
| **客户端** | LocaleContent | 是 | AnimatePresence 语言切换过渡 |
| **客户端** | LayoutShell | 是 | AnimatePresence 页面过渡 + Nav + Footer |
| **工具** | ScrollToAnchor | 是 | 平滑滚动辅助 |
| **工具** | useMediaQuery | 是 | 响应式断点检测 hook |

### 3.3 复用组件清单

| 组件 | 复用场景 |
|------|---------|
| ChapterHeader | 首页各 Section（×5）+ Case Study 详情页各节（×9） |
| CaseStudyCard | Case Studies 区块（首页） |
| ProjectCard | Projects 区块（首页），初始显示封面/标题/标签，点击 clip-path 展开详情 |
| SkillTag | About 区块 + Case Study 详情页 tech stack 标签 |
| LanguageSwitcher | Navbar |
| CaseStudyContent | Case Study 详情页各节内部（正文渲染） |
| FlowStepBar | Case Study 详情页 Methodology 正文内部 |
| ZoomableImage | Case Study 详情页各节正文中图片 |
| CaseStudyDownloadButton | Case Study 详情页（PDF 下载） |
| BackToTopButton | Case Study 详情页 |

---

## 四、数据流架构

### 4.1 数据依赖关系

```
┌─────────────────────────────────────────────────────────┐
│                    src/messages/{lang}.json               │
│  nav | hero | caseStudies | projects | about | contact   │
│  footer                                                   │
│          ▲                                                │
│          │ key: titleKey / descriptionKey                  │
│          │                                                │
│  ┌───────────────┐  ┌──────────────────┐  ┌──────────────┐  │
│  │ profile.json   │  │ projects.json    │  │ case-studies │  │
│  │ name           │  │ slug             │  │ .json        │  │
│  │ slogan         │  │ cover            │  │ slug         │  │
│  │ skills         │  │ techStack        │  │ cover        │  │
│  │ socialLinks    │  │ titleKey → msg   │  │ techStack    │  │
│  │ email          │  │ backgroundKey    │  │ titleKey     │  │
│  └───────────────┘  │ contributeKey    │  │ descKey      │  │
│                      │ outcomeKey       │  └──────┬───────┘  │
│                      └──────────────────┘         │          │
│                                            ┌──────▼───────┐  │
│                                            │ vn-scenario  │  │
│                                            │ .{lang}.json │  │
│                                            │ scenes[]     │  │
│                                            └──────────────┘  │
│                                                  │        │
│                                                  ▼        │
│                               ┌────────────────────────┐  │
│                               │ public/resume-{lang}.pdf│  │
│                               │ (直接下载，不经数据层)  │  │
│                               └────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
```

### 4.2 数据读取策略

| 函数 | 文件 | 返回值 | 缓存策略 |
|------|------|--------|---------|
| `getCaseStudies()` | `src/lib/read-data.ts` | `CaseStudyItem[]`（Zod: `CaseStudiesSchema`） | 无（静态 JSON，build 时加载） |
| `getVNScenario(lang)` | `src/lib/read-data.ts` | `{ scenes: VNScene[] }`（内联 Zod 校验） | 无 |
| Case Study 正文 | `messages/{lang}.json`（通过 `getTranslations().raw()`） | 字符串字段（Markdown） | next-intl 内部缓存 |
| 语言消息 | next-intl `getRequestConfig` 自动加载 | `messages/{locale}.json` | next-intl 内部缓存 |

### 4.3 数据校验

```typescript
// Zod Schema — case-studies.schema.ts（项目中唯一独立的 schema 文件）
import { z } from "zod"

export const CaseStudyItemSchema = z.object({
  slug: z.string().min(1),
  cover: z.string().min(1),
  techStack: z.array(z.string()).min(1),
  titleKey: z.string().min(1),
  descriptionKey: z.string().min(1),
})

export const CaseStudiesSchema = z.array(CaseStudyItemSchema).min(0)
export type CaseStudyItem = z.infer<typeof CaseStudyItemSchema>
```

`read-data.ts` 读取模式：

```typescript
// "server-only" — 仅在 Server Component 中使用
import "server-only"
import fs from "fs"
import path from "path"
import { z } from "zod"
import { CaseStudiesSchema } from "./case-studies.schema"

// Case Studies — import JSON + Zod 校验
import caseStudiesData from "@/data/case-studies.json"

export function getCaseStudies() {
  return CaseStudiesSchema.parse(caseStudiesData)
}

// VN Scenario — 内联 Zod + fs.readFileSync（动态语言参数）
const VNScenarioSchema = z.object({
  scenes: z.array(VNSceneSchema),
})

export function getVNScenario(lang: string): VNScenario {
  const filePath = path.join(process.cwd(), `src/data/vn-scenario.${lang}.json`)
  const raw = fs.readFileSync(filePath, "utf-8")
  const parsed = JSON.parse(raw)
  return VNScenarioSchema.parse(parsed)
}
```

### 4.4 数据类型一览

| 类型 | 定义位置 | 说明 |
|------|---------|------|
| `Profile` | 从 `profile.json` 隐式推断 | 个人资料（直接 import JSON，无需 Zod schema） |
| `Project` | 从 `projects.json` 隐式推断 | { slug, cover, techStack, titleKey, backgroundKey, contributeKey, outcomeKey } |
| `CaseStudyItem` | `case-studies.schema.ts` | { slug, cover, techStack, titleKey, descriptionKey } |
| `VNScene` / `VNScenario` | `read-data.ts`（内联 Zod） | VN 序幕场景数据 |
| `Messages` | next-intl 类型 | 由 `messages/{lang}.json` 结构决定 |

---

## 五、目录结构

```
```
project-root/
├── .editorconfig
├── .env.example                    # 环境变量模板（v1 无需配置）
├── .github/
│   └── workflows/
│       └── ci.yml                  # lint → typecheck → build（Node 22 + pnpm --frozen-lockfile）
│
├── .gitignore
├── .npmrc
├── AGENTS.md                       # opencode 项目上下文（框架 quirk / 架构 / 约定）
├── eslint.config.mjs               # ESLint 9 Flat Config（eslint-config-next core-web-vitals + ts）
├── next.config.ts                  # Next.js 配置（next-intl plugin + images formats）
├── next-env.d.ts
├── package.json
├── pnpm-lock.yaml
├── pnpm-workspace.yaml             # pnpm allowBuilds（sharp / unrs-resolver / @parcel/watcher / @swc/core）
├── postcss.config.mjs              # PostCSS 配置（@tailwindcss/postcss）
├── tsconfig.json                   # TypeScript strict 模式
│
├── public/
│   ├── images/
│   │   ├── hero-character.png      # 大立绘（透明背景 PNG）
│   │   ├── portfolio-cover.png     # Portfolio 项目封面图
│   │   ├── ccms-cover.png          # CCMS 项目封面图
│   │   ├── case-study-cover.png    # Case Study 封面图
│   │   ├── option-button.png       # VN 选项按钮背景图
│   │   ├── homepage.png            # 首页截图
│   │   ├── {char-emotion}.png      # VN 角色表情（normal / smile / bigsmile / emm / shock / mad / sad / shy）
│   │   ├── obsidian_repo.png       # Case Study 正文用图
│   │   ├── opencode_*.jpg          # Case Study 正文用图
│   │   ├── commits.png             # Case Study 正文用图
│   │   ├── working_record.png      # Case Study 正文用图
│   │   ├── obsidian_graph.png      # Case Study 正文用图
│   │   └── vnscene*.png            # Case Study 正文用图
│   ├── fonts/
│   │   ├── RoundedMplus1c-Regular.woff2      # JP 字体（仅此一字重）
│   │   ├── Nunito-Regular.woff2              # EN 字体（仅此一字重）
│   │   └── noto-sans-sc-{4-30}-400-normal.woff2  # ZH 字体：13 个 CJK 子集 woff2（编号 4-6, 21-30）
│   ├── resume-zh.pdf
│   ├── resume-zh.md                          # 简历 Markdown 源文件
│   ├── resume-ja.pdf
│   ├── resume-ja.md
│   ├── resume-en.pdf
│   └── resume-en.md
│
├── src/
│   ├── proxy.ts                    # i18n 语言检测 + 重定向（Next.js 16 约定替代 middleware.ts）
│   │
│   ├── app/
│   │   ├── layout.tsx              # 根布局（html + body + next/font {fontJp, fontEn} + globals.css + fonts.css）
│   │   ├── not-found.tsx           # 404 页面（VN 游戏风风格）
│   │   ├── sitemap.ts              # 动态 sitemap.xml（3 languages × (home + case study) = 6 URLs）
│   │   ├── robots.ts               # robots.txt
│   │   ├── favicon.ico
│   │   └── [lang]/
│   │       ├── layout.tsx          # 多语布局（NextIntlClientProvider + JaLocaleToast + LocaleContent + LayoutShell）
│   │       ├── page.tsx            # 首页（组合 HeroSection + SectionTransition ×4 + 各 Section）
│   │       ├── intro/
│   │       │   └── page.tsx        # VN 序幕页面（Server → IntroShell → Client 状态机）
│   │       └── projects/
│   │           └── [slug]/
│   │               └── page.tsx    # Case Study 详情页（Server + Client islands）
│   │
│   ├── components/
│   │   ├── AboutSection.tsx
│   │   ├── BackToTopButton.tsx
│   │   ├── CaseStudiesSection.tsx
│   │   ├── CaseStudyCard.tsx
│   │   ├── CaseStudyContent.tsx    # Markdown 正文解析渲染器
│   │   ├── CaseStudyDownloadButton.tsx
│   │   ├── CaseStudyNav.tsx        # 右侧固定侧栏导航
│   │   ├── CaseStudySections.tsx   # 详情页章节容器（whileInView 入场）
│   │   ├── ChapterHeader.tsx
│   │   ├── ContactSection.tsx
│   │   ├── DialogBox.tsx
│   │   ├── FlowStepBar.tsx         # 流程步骤可视化
│   │   ├── Footer.tsx
│   │   ├── HeroSection.tsx
│   │   ├── IntroAnimation.tsx      # 序幕动画协调器
│   │   ├── IntroShell.tsx          # 序幕外壳（状态机）
│   │   ├── JaLocaleToast.tsx
│   │   ├── LanguageSwitcher.tsx
│   │   ├── LayoutShell.tsx
│   │   ├── LoadingOverlay.tsx
│   │   ├── LoadingRipple.tsx
│   │   ├── LocaleContent.tsx
│   │   ├── Navbar.tsx
│   │   ├── OptionButton.tsx        # VN 风格选项按钮
│   │   ├── PixelsCurtain.tsx       # 暗転遮罩
│   │   ├── ProjectCard.tsx
│   │   ├── ProjectsSection.tsx
│   │   ├── ScrambleText.tsx        # 文字乱码效果
│   │   ├── ScrollToAnchor.tsx
│   │   ├── SectionTransition.tsx
│   │   ├── SkillTag.tsx
│   │   ├── TitleScreen.tsx
│   │   ├── VNScenario.tsx          # VN 情景播放
│   │   └── ZoomableImage.tsx       # 点击放大图片
│   │
│   ├── hooks/
│   │   └── useMediaQuery.ts
│   │
│   ├── i18n/
│   │   ├── request.ts              # next-intl 请求配置（动态 import messages/{locale}.json）
│   │   └── routing.ts              # next-intl 路由配置（locales: [en, ja, zh]; defaultLocale: en）
│   │
│   ├── lib/
│   │   ├── animations.ts           # Framer Motion variants 集中管理
│   │   ├── fonts.ts                # next/font/local（fontJp / fontEn）
│   │   ├── case-studies.schema.ts  # Zod schema: Case Study 列表数据（唯一独立 schema）
│   │   ├── read-data.ts            # JSON 读取 + Zod 校验（getCaseStudies / getVNScenario）
│   │   └── utils.ts                # cn() = clsx + tailwind-merge
│   │
│   ├── data/
│   │   ├── profile.json
│   │   ├── projects.json
│   │   ├── case-studies.json
│   │   ├── vn-scenario.zh.json
│   │   ├── vn-scenario.ja.json
│   │   └── vn-scenario.en.json
│   │
│   ├── messages/
│   │   ├── zh.json
│   │   ├── ja.json
│   │   └── en.json
│   │
│   └── styles/
│       ├── globals.css             # @import "tailwindcss" + @theme inline（color tokens）+ @keyframes + utility classes
│       └── fonts.css               # Noto Sans SC @font-face（13 子集 woff2）
│
└── README.md
```
```

---

## 六、依赖清单

### 6.1 生产依赖

```jsonc
{
  "dependencies": {
    "next": "16.2.9",
    "react": "19.2.4",
    "react-dom": "19.2.4",
    "next-intl": "^4.13.0",
    "framer-motion": "^12.40.0",
    "zod": "^4.4.3",
    "lucide-react": "^1.18.0",
    "clsx": "^2.1.1",
    "tailwind-merge": "^3.6.0"
  }
}
```

### 6.2 开发依赖

```jsonc
{
  "devDependencies": {
    "typescript": "^5",
    "tailwindcss": "^4",
    "@tailwindcss/postcss": "^4",
    "eslint": "^9",
    "eslint-config-next": "16.2.9",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19"
  }
}
```

### 6.3 依赖角色说明

| 依赖 | 类型 | 角色 |
|------|------|------|
| `next`, `react`, `react-dom` | 生产 | 框架核心 |
| `next-intl` | 生产 | 多语言路由 + 消息分发 |
| `framer-motion` | 生产 | 所有 JS 驱动动效（入场/过渡/视差/VN variants） |
| `zod` | 生产 | JSON 数据运行时校验 |
| `lucide-react` | 生产 | 图标库（仅 Navbar 汉堡菜单使用） |
| `clsx` + `tailwind-merge` | 生产 | Tailwind class 合并（cn() 工具函数） |
| `tailwindcss` + `@tailwindcss/postcss` | 开发 | CSS 框架（v4 CSS-first 配置，无 tailwind.config） |
| `typescript` | 开发 | 类型检查（strict + noUncheckedIndexedAccess + noImplicitReturns） |
| `eslint` + `eslint-config-next` | 开发 | 代码规范（Flat Config） |

---

## 七、配置清单

### 7.1 TypeScript (`tsconfig.json`)

```jsonc
{
  "compilerOptions": {
    "target": "ES2017",
    "lib": ["dom", "dom.iterable", "esnext"],
    "allowJs": true,
    "skipLibCheck": true,
    "strict": true,
    "noUncheckedIndexedAccess": true,
    "noImplicitReturns": true,
    "noEmit": true,
    "esModuleInterop": true,
    "module": "esnext",
    "moduleResolution": "bundler",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "jsx": "react-jsx",
    "incremental": true,
    "plugins": [{ "name": "next" }],
    "paths": {
      "@/*": ["./src/*"]
    }
  },
  "include": [
    "next-env.d.ts",
    "proxy.ts",
    "**/*.ts",
    "**/*.tsx",
    ".next/types/**/*.ts",
    "**/*.mts",
    "**/*.cts",
    ".next/dev/types/**/*.ts"
  ],
  "exclude": ["node_modules"]
}
```

### 7.2 Next.js (`next.config.ts`)

```typescript
import type { NextConfig } from "next"
import createNextIntlPlugin from "next-intl/plugin"

const withNextIntl = createNextIntlPlugin()

const nextConfig: NextConfig = {
  images: {
    formats: ["image/avif", "image/webp"],
  },
}

export default withNextIntl(nextConfig)
```

### 7.3 Tailwind v4 (`src/styles/globals.css`)

Tailwind v4 采用 CSS-first 配置，所有令牌在 `globals.css` 中通过 `@theme inline` 声明。

```css
@import "tailwindcss";

@theme inline {
  --color-primary: #ffb7c5;
  --color-primary-soft: #ffe4ec;
  --color-secondary: #d5c6e0;
  --color-accent: #b5ead7;
  --color-background: #fffafd;
  --color-surface: #ffffff;
  --color-text-primary: #4a3f42;
  --color-text-secondary: #8e8386;
  --color-border: #f0e6eb;
  --color-dialog-blue: #2fa4d7;
  --color-dialog-dark: #1a8db8;
  --color-dialog-soft: #e5f4fc;
}
```

当前只自定义色彩令牌，其余设计维度（间距/圆角/阴影/字体等）使用 Tailwind v4 预设默认值。

含自定义 CSS 动画：
- `glow-pulse` — 光晕脉冲（4s 循环）
- `float` — 角色浮动（4s 循环）

以及实用 class：`hero-glow` / `character-float` / `nav-link` / `tag-hover` / `scrollbar-micro`。

### 7.4 next-intl 配置

| 文件 | 说明 |
|------|------|
| `src/proxy.ts` | 语言检测、`/` → `/en/intro` 重定向（Next.js 16 用 proxy.ts 替代 middleware.ts） |
| `src/i18n/request.ts` | next-intl 请求配置（语言 → messages 映射） |
| `src/i18n/routing.ts` | next-intl 路由配置（locales 列表、defaultLocale、导出 Link/usePathname/useRouter 等工具） |
| `src/messages/{lang}.json` | 各语言消息文件 |

`proxy.ts` 核心逻辑：

```typescript
import createMiddleware from "next-intl/middleware";
import { routing } from "./i18n/routing";
import { NextRequest, NextResponse } from "next/server";

const intlMiddleware = createMiddleware(routing);

export default function middleware(request: NextRequest) {
  const { pathname } = request.nextUrl;

  if (pathname === "/") {
    request.nextUrl.pathname = "/en/intro";
    return NextResponse.redirect(request.nextUrl);
  }

  return intlMiddleware(request);
}

export const config = {
  matcher: ["/((?!api|_next|_vercel|.*\\..*).*)"],
};
```

---

## 八、环境与部署架构

```
┌──────────────────┐     Push/PR      ┌───────────────────┐
│   Developer      │ ──────────────→   │   GitHub          │
│   (pnpm dev)     │                   │   Actions (CI)    │
└──────────────────┘                   │   lint + tsc      │
                                       │   + build         │
                                       └────────┬──────────┘
                                                │ pass
                                                ▼
                                       ┌───────────────────┐
                                       │   Vercel           │
                                       │   ┌─────────────┐  │
                                       │   │ main → Prod │  │
                                       │   │ PR → Preview│  │
                                       │   └─────────────┘  │
                                       └───────────────────┘
```

| 环境 | URL | 触发方式 | 说明 |
|------|-----|---------|------|
| 本地开发 | `http://localhost:3000` | `pnpm dev` | 热更新开发 |
| CI | GitHub Actions | push/PR to main | 执行 `.github/workflows/ci.yml`：`pnpm install --frozen-lockfile` → `lint` → `typecheck` → `build` |
| Production | Vercel 分配域名 | push to main（通过 Vercel Git Integration） | 最终上线环境 |
| Preview | Vercel 自动生成 URL | PR create/update | 预览/测试环境 |

---

## 九、性能与约束

| 约束项 | 规格 | 说明 |
|--------|------|------|
| Lighthouse 目标 | Performance ≥ 90, Accessibility ≥ 95, SEO ≥ 95 | 桌面端基准 |
| 图片格式 | 源 PNG → Next/Image 自动输出 AVIF + WebP | 无需手动转换 |
| 字体 | 全部自托管：JP/EN 用 `next/font/local`（零 CLS），ZH 用 `@font-face`（13 子集 woff2） | 无 Google Fonts 网络请求 |
| 总页面数 | 3（游戏风序幕 + 首页 + Case Study 详情页）| × 三语 = 9 个静态路径 |
| 项目/案例上限 | ≤20 | 超限需考虑性能优化 |
| 动效 JS 上限 | Framer Motion 唯一库，CSS 循环动画 ≤3 个 | 避免过度动画导致重排 |

---

## 附录：开发环境要求

| 工具 | 版本要求 | 验证命令 |
|------|---------|---------|
| Node.js | ≥22.0.0 | `node -v` |
| pnpm | ≥9.0.0（推荐 11.x） | `pnpm -v` |
| OS | 不限 | — |

初始化命令：

```bash
pnpm create next-app@latest . --typescript --eslint --app --src-dir --import-alias "@/*"
pnpm add next-intl framer-motion zod lucide-react clsx tailwind-merge
pnpm add -D @tailwindcss/postcss
```

注意：`postcss` 无需单独安装，`@tailwindcss/postcss` 已包含其作为内部依赖。
