# 4.1 技术栈与架构文档

**项目：** KONG JUN SIENG — Software Developer Portfolio  
**文档目标：** 定义技术选型、架构决策与完整依赖配置，供 AI 代理按此初始化项目骨架和安装依赖。

---

## 一、技术选型矩阵

### 1.1 核心框架与语言

| 层面 | 选择 | 版本 | 选用理由 |
|------|------|------|---------|
| **框架** | Next.js | 15.x | App Router 原生支持 i18n 路由、Server Components 提升性能、Vercel 生态原生集成 |
| **语言** | TypeScript | 5.x | `strict: true`，全量类型覆盖，杜绝隐式 any |
| **包管理** | pnpm | >=9.x | 磁盘效率高、依赖隔离严格、monorepo 友好（虽当前为单 repo） |
| **React** | React | 19.x | Next.js 15 内置，支持 Server Components / Actions |

### 1.2 CSS 与样式

| 方案 | 版本 | 选用理由 |
|------|------|---------|
| **Tailwind CSS** | v4 | 零运行时、设计令牌驱动、与自定义令牌系统完美配合 |
| **自定义令牌** | — | 通过 `src/styles/globals.css` 中的 `@theme` 指令声明颜色/字体/间距/阴影，CSS-first 配置覆盖 Tailwind 默认主题 |
| **全局 CSS** | — | 单文件 `src/styles/globals.css`，含 `@import "tailwindcss"` + `@theme` 设计令牌 + 自定义属性 |

### 1.3 动画

| 方案 | 选用理由 |
|------|---------|
| **Framer Motion** | 唯一动画库（不用 GSAP / anime.js），统一控制入场/页面过渡/视差；声明式 variants 与 Tailwind 共存 |
| **CSS Animations** | 仅用于循环装饰动画（光晕脉冲、角色浮动），减少 JS 开销 |

### 1.4 国际化

| 方案 | 版本 | 选用理由 |
|------|------|---------|
| **next-intl** | 最新 | App Router 原生支持、path-based 路由策略、类型安全消息键 |
| **策略** | path-based | `/[lang]/...`，middleware 处理 `/` → `/zh` 重定向 |
| **语言** | zh / ja / en | 对应三语 JSON 文件 `src/messages/{lang}.json` |

### 1.5 数据层

| 方案 | 选用理由 |
|------|---------|
| **本地 JSON** | 项目数据（profile / projects / case-studies）无需 CMS，Zod 运行时校验保障类型 |
| **Markdown** | Case Study 详情页，gray-matter 解析 frontmatter，react-markdown 渲染 body |
| **Zod** | JSON 数据文件运行时校验，与 TypeScript 类型推断联动 |

### 1.6 Markdown 渲染

| 依赖 | 用途 |
|------|------|
| `gray-matter` | 解析 Markdown frontmatter（title / slug / cover / techStack / role / date） |
| `react-markdown` | 渲染 Markdown body 为 React 组件 |
| `remark-gfm` | GFM 扩展（表格、任务列表等） |

### 1.7 UI 组件

| 方案 | 选用理由 |
|------|---------|
| **shadcn/ui** | 仅取基础骨架（Button / Card / Input / Textarea），覆写全部样式以匹配设计令牌；不自带色彩主题 |
| **Lucide React** | 全站唯一图标库，线性风格 `strokeWidth={1.5}`，大小统一 |

### 1.8 开发工具与流水线

| 工具 | 用途 |
|------|------|
| **ESLint** | 代码规范（Next.js 内置配置 + 自定义规则） |
| **TypeScript** | 类型检查（`tsconfig.json` strict 模式） |
| **GitHub Actions** | CI 流水线：lint + typecheck + build，push/PR 到 main 触发 |
| **Vercel** | 部署平台，Production 从 main 自动部署，PR 生成 Preview |

### 1.9 排除项（有意识不做）

| 技术 | 排除原因 |
|------|---------|
| 测试框架 | v1 不引入，依赖 TS + ESLint + Build 保障质量 |
| 数据库 / CMS | 内容量少且固定，本地文件 + 手动更新更轻量 |
| 后端 API | 联系表单纯 UI，无后端逻辑 |
| GSAP / Lottie | Framer Motion + CSS Animations 已覆盖全部动效需求 |
| 状态管理 | 无全局共享状态需求，prop drilling + Server Components 即可 |
| PWA / SWR / TanStack Query | v1 无数据变更/离线需求 |

---

## 二、架构设计

### 2.1 架构分层

```
┌─────────────────────────────────────────────────────────────┐
│                     Presentation Layer                       │
│  Navbar / HeroSection / CaseStudiesSection / ProjectsSection │
│  AboutSection / ContactSection / CaseStudyDetailPage         │
│  Footer / LanguageSwitcher / ScrollToAnchor                  │
├─────────────────────────────────────────────────────────────┤
│                     Data Access Layer                         │
│  src/lib/read-data.ts   — JSON 读取 + Zod 校验               │
│  src/lib/read-mdx.ts    — Markdown 读取 + frontmatter 解析    │
├─────────────────────────────────────────────────────────────┤
│                        Data Layer                             │
│  src/data/profile.json                                       │
│  src/data/projects.json / case-studies.json                   │
│  src/data/portfolio-{lang}.md                                 │
│  src/messages/{lang}.json                                     │
├─────────────────────────────────────────────────────────────┤
│                        i18n Layer                             │
│  next-intl + middleware.ts — 语言检测 / 重定向 / 消息分发     │
├─────────────────────────────────────────────────────────────┤
│                     Infrastructure Layer                      │
│  Vercel — Deployment                                         │
│  GitHub Actions — CI                                         │
└─────────────────────────────────────────────────────────────┘
```

### 2.2 架构决策记录

| 决策 ID | 决策 | 备选方案 | 理由 |
|---------|------|---------|------|
| ADR-001 | App Router (Server Components first) | Pages Router | App Router 原生支持布局嵌套、Server Components 减少客户端 JS、i18n 路由更简洁 |
| ADR-002 | 本地文件 + Zod 校验 | CMS / 数据库 | 内容量 ≤20 条，变更频率低；Zod 在 build 时保证数据完整性 |
| ADR-003 | shadcn/ui 骨架 + 全覆写 | 全手写 / 完整组件库 | 按钮/卡片/表单基础 HTML 骨架正确性由 shadcn 保证，样式全由自定义令牌控制 |
| ADR-004 | next-intl path-based 路由 | cookie-based / domain-based | 最简单方案、SEO 友好（每个语言页面独立 URL）、middleware 统一处理 |
| ADR-005 | Framer Motion 唯一动画库 | GSAP + Framer 并存 | 减少依赖体积、统一动画范式、声明式 variants 更契合 React 组件模型 |
| ADR-006 | 无测试框架 | Jest / Vitest + Testing Library | v1 规模极小（1 首页 + 1 详情页 + 10 组件），过度测试增加维护成本；TS + Build 已覆盖类型和构建错误 |

### 2.3 渲染策略

| 页面 | 渲染方式 | 说明 |
|------|---------|------|
| 首页 `/[lang]` | Server Component (RSC) + Client Islands | 根布局/文本为 RSC，Navbar/各 Section 中动效部分标记 `"use client"` |
| Case Study 详情页 `/[lang]/projects/[slug]` | Server Component | 读取 Markdown + frontmatter 在服务端完成，react-markdown 渲染到客户端 |
| 数据读取层 | Server-only | `read-data.ts` / `read-mdx.ts` 仅在 Server Component / `generateStaticParams` 中使用 |

### 2.4 路由映射

```
/                                   → middleware.ts i18n 检测
/zh (default)                       → 首页单页滚动
/en                                 → 首页单页滚动
/ja                                 → 首页单页滚动
/[lang]                             → 首页单页滚动
  ├── #hero                         → Hero Banner
  ├── #case-studies                 → Case Studies 卡片列表
  ├── #projects                     → 项目展示卡片
  ├── #about                        → 关于我
  └── #contact                      → 联系区块
/[lang]/projects/[slug]             → Case Study 详情页
/not-found                          → 404 页面（跨语言）
```

### 2.5 重定向规则

| 来源 | 目标 | 条件 | 实现 |
|------|------|------|------|
| `/` | `/zh` | 始终 | middleware.ts |
| `/[lang]` | — | 正常匹配 | — |
| 未知 `[lang]` 值 | `/not-found` | 未在 `locales` 数组中 | middleware.ts |
| 未知路由 | `/not-found` | 未匹配 | `not-found.tsx` |

---

## 三、组件架构

### 3.1 组件依赖树

```
RootLayout (根 layout.tsx — html + body + fonts)
└── LocaleLayout ([lang]/layout.tsx — NextIntlClientProvider)
    ├── Navbar
    │   ├── LogoLink (→ /[lang])
    │   ├── NavLink[] (案例 / 作品 / 关于 / 联系)
    │   └── LanguageSwitcher (中 / 日 / EN)
    ├── HomePage (page.tsx)
    │   ├── HeroSection
    │   │   ├── HeroCharacterImage (next/image)
    │   │   ├── Slogan | Name | Title
    │   │   └── CTAButton
    │   ├── CaseStudiesSection
    │   │   └── CaseStudyCard[]
    │   │       ├── CoverImage (next/image)
    │   │       ├── TechStackTag[]
    │   │       ├── Title | Description
    │   │       └── ViewButton (→ /[lang]/projects/[slug])
    │   ├── ProjectsSection
    │   │   └── ProjectCard[]
    │   │       ├── CoverImage (next/image)
    │   │       ├── TechStackTag[]
    │   │       └── Title
    │   ├── AboutSection
    │   │   ├── BioText
    │   │   └── SkillTag[]
    │   └── ContactSection
    │       ├── ContactForm (UI only)
    │       ├── EmailLink
    │       ├── SocialLink[] (GitHub / LinkedIn)
    │       └── ResumeDownloadButton (→ public/resume-{lang}.pdf)
    ├── CaseStudyPage (projects/[slug]/page.tsx)
    │   ├── BackButton (→ /[lang])
    │   ├── CoverImage (next/image)
    │   ├── Title | Role | Date
    │   ├── TechStackTag[]
    │   ├── MarkdownBody (react-markdown)
    │   └── BackButton
    └── Footer
        ├── CopyrightText
        └── SocialLink[]
```

### 3.2 组件职责边界

| 层级 | 组件 | `"use client"` | 说明 |
|------|------|---------------|------|
| **页面级** | `page.tsx` (首页) | 否 | Server Component，组装各 Section |
| **页面级** | `projects/[slug]/page.tsx` | 否 | Server Component，读取 Markdown + 渲染 |
| **布局级** | `[lang]/layout.tsx` | 是 | 包裹 Navbar + Footer + 内容区域；含 AnimatePresence 页面过渡 + usePathname |
| **客户端** | SkipToContent | 是 | 无障碍跳转到内容链接 |
| **客户端** | Navbar | 是 | 滚动阴影 + 汉堡菜单交互 |
| **客户端** | LanguageSwitcher | 是 | 语言切换交互 |
| **客户端** | HeroSection | 是 | Framer Motion 入场序列 + 视差 |
| **客户端** | CaseStudiesSection | 是 | whileInView 入场动画 |
| **客户端** | ProjectsSection | 是 | whileInView 入场动画 |
| **客户端** | AboutSection | 是 | whileInView 入场动画 |
| **客户端** | ContactSection | 是 | whileInView 入场动画 |
| **客户端** | CaseStudyCard | 否 | 纯展示，无交互逻辑 |
| **客户端** | ProjectCard | 否 | 纯展示，无交互逻辑 |
| **客户端** | Footer | 否 | 静态内容 |
| **工具** | ScrollToAnchor | 是 | 平滑滚动辅助 |
| **工具** | useMediaQuery | 是 | 响应式断点检测 hook |

### 3.3 复用组件清单

| 组件 | 复用场景 |
|------|---------|
| CaseStudyCard | Case Studies 区块（首页）+ 潜在同类卡片 |
| ProjectCard | Projects 区块（首页），与 CaseStudyCard 结构相似但无按钮 |
| SkillTag | About 区块 + Case Study 详情页 tech stack 标签 |
| SocialLink | Contact 区块 + Footer |
| LanguageSwitcher | Navbar |

---

## 四、数据流架构

### 4.1 数据依赖关系

```
┌─────────────────────────────────────────────────────────┐
│                    src/messages/{lang}.json               │
│  nav | hero | caseStudies | projects | about | contact   │
│  footer                                                   │
│          ▲                                                │
│          │ key: titleKey / descriptionKey                  │
│          │                                                │
│  ┌───────────────┐  ┌──────────────────┐  ┌───────────┐  │
│  │ profile.json   │  │ projects.json    │  │ case-     │  │
│  │ name           │  │ slug             │  │ studies   │  │
│  │ slogan         │  │ cover            │  │ .json     │  │
│  │ skills         │  │ techStack        │  │ slug      │  │
│  │ socialLinks    │  │ titleKey → msg   │  │ cover     │  │
│  │ email          │  └──────────────────┘  │ techStack  │  │
│  └───────────────┘                        │ titleKey   │  │
│                                            │ descKey    │  │
│                                            └─────┬─────┘  │
│                                                  │        │
│                                                  ▼        │
│                               ┌────────────────────────┐  │
│                               │ data/portfolio-{lang}.md│  │
│                               │ frontmatter: title,     │  │
│                               │   slug, cover, tech,    │  │
│                               │   role, date            │  │
│                               │ body: Markdown 正文     │  │
│                               └────────────────────────┘  │
│                                                  │        │
│                                                  ▼        │
│                               ┌────────────────────────┐  │
│                               │ public/resume-{lang}.pdf│  │
│                               │ (直接下载，不经数据层)  │  │
│                               └────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
```

### 4.2 数据读取策略

| 函数 | 文件 | 返回值 | 缓存策略 |
|------|------|--------|---------|
| `readProfile()` | `src/lib/read-data.ts` | `Profile` (Zod 推断类型) | 无（静态 JSON，build 时加载） |
| `readProjects()` | `src/lib/read-data.ts` | `Project[]` | 无 |
| `readCaseStudies()` | `src/lib/read-data.ts` | `CaseStudy[]` | 无 |
| `readCaseStudyDetail(slug, lang)` | `src/lib/read-mdx.ts` | `{ frontmatter, content }` | 无 |
| `getMessages(lang)` | next-intl 自动 | `Messages` | next-intl 内部缓存 |

### 4.3 数据校验

```typescript
// Zod Schema — profile.schema.ts (示意)
import { z } from "zod"

export const SocialLinkSchema = z.object({
  platform: z.string(),
  url: z.string().url(),
})

export const ProfileSchema = z.object({
  name: z.string(),
  slogan: z.string(),
  skills: z.array(z.string()),
  socialLinks: z.array(SocialLinkSchema),
  email: z.string().email(),
})

export type Profile = z.infer<typeof ProfileSchema>
```

`read-data.ts` 通用读取模式：

```typescript
// 所有 JSON 读取遵循此模式
function readJSON<T>(path: string, schema: z.ZodSchema<T>): T {
  const raw = fs.readFileSync(path, "utf-8")
  const parsed = JSON.parse(raw)
  return schema.parse(parsed)
}
```

### 4.4 数据类型一览

| 类型 | 定义位置 | 说明 |
|------|---------|------|
| `Profile` | `profile.schema.ts` | 个人资料 |
| `Project` | `projects.schema.ts` | { slug, cover, techStack, titleKey } |
| `CaseStudy` | `case-studies.schema.ts` | { slug, cover, techStack, titleKey, descriptionKey } |
| `CaseStudyDetail` | 运行时推断 | { frontmatter: Frontmatter, content: string } |
| `Messages` | next-intl 类型 | 由 `messages/{lang}.json` 结构决定 |

---

## 五、目录结构

```
project-root/
├── .github/
│   └── workflows/
│       └── ci.yml                  # lint + typecheck + build
│
├── public/
│   ├── images/
│   │   ├── hero-character.png      # 大立绘（透明背景 PNG）
│   │   ├── portfolio-cover.png     # Portfolio 项目封面图
│   │   ├── fyp-iot-cover.png       # FYP IoT 项目封面图
│   │   └── ...                     # 其他项目/案例封面图
│   ├── fonts/
│   │   ├── MPlusRounded1c-Regular.woff2
│   │   ├── MPlusRounded1c-Medium.woff2
│   │   ├── MPlusRounded1c-Bold.woff2
│   │   ├── Nunito-Regular.woff2
│   │   ├── Nunito-Medium.woff2
│   │   ├── Nunito-SemiBold.woff2
│   │   ├── Nunito-Bold.woff2
│   │   ├── NotoSansSC-Regular.woff2 # Noto Sans SC Regular
│   │   ├── NotoSansSC-Medium.woff2  # Noto Sans SC Medium
│   │   └── NotoSansSC-Bold.woff2   # Noto Sans SC Bold
│   ├── resume-zh.pdf
│   ├── resume-ja.pdf
│   └── resume-en.pdf
│
├── src/
│   ├── app/
│   │   ├── [lang]/
│   │   │   ├── layout.tsx          # 多语布局（NextIntlClientProvider + Nav + Footer）
│   │   │   ├── page.tsx            # 首页（组合所有 Section）
│   │   │   └── projects/
│   │   │       └── [slug]/
│   │   │           └── page.tsx    # Case Study 详情页
│   │   ├── api/                    # (预留) 未来联系表单后端
│   │   ├── layout.tsx              # 根布局（html + body + next/font 注入）
│   │   ├── not-found.tsx           # 404 页面
│   │   ├── sitemap.ts              # 动态 sitemap.xml
│   │   └── robots.ts               # robots.txt
│   │
│   ├── components/
│   │   ├── Navbar.tsx
│   │   ├── Footer.tsx
│   │   ├── LanguageSwitcher.tsx
│   │   ├── HeroSection.tsx
│   │   ├── CaseStudiesSection.tsx
│   │   ├── ProjectsSection.tsx
│   │   ├── AboutSection.tsx
│   │   ├── ContactSection.tsx
│   │   ├── CaseStudyCard.tsx
│   │   ├── ProjectCard.tsx
│   │   ├── SkillTag.tsx
│   │   ├── SocialLink.tsx
│   │   └── ScrollToAnchor.tsx
│   │
│   ├── hooks/
│   │   └── useMediaQuery.ts        # 响应式断点检测 hook
│   │
│   ├── i18n/
│   │   └── request.ts              # next-intl 请求配置
│   │
│   ├── lib/
│   │   ├── animations.ts           # Framer Motion variants 集中管理
│   │   ├── fonts.ts                # next/font 声明（字体文件配置）
│   │   ├── profile.schema.ts       # Zod schema: 个人资料
│   │   ├── projects.schema.ts      # Zod schema: 项目卡片数据
│   │   ├── case-studies.schema.ts  # Zod schema: Case Study 列表数据
│   │   ├── read-data.ts            # JSON 读取 + Zod 校验
│   │   ├── read-mdx.ts             # Markdown 读取 + frontmatter 解析
│   │   └── utils.ts                # Tailwind cn() 等工具函数
│   │
│   ├── data/
│   │   ├── profile.json
│   │   ├── projects.json
│   │   ├── case-studies.json
│   │   ├── portfolio-zh.md
│   │   ├── portfolio-ja.md
│   │   └── portfolio-en.md
│   │
│   ├── messages/
│   │   ├── zh.json
│   │   ├── ja.json
│   │   └── en.json
│   │
│   ├── styles/
│   │   └── globals.css             # Tailwind 指令 + CSS 自定义属性
│   │
│   └── middleware.ts               # i18n 语言检测 + 重定向
│
├── .env.example                    # 环境变量模板（当前为空）
├── .gitignore
├── .prettierrc                     # Prettier 格式化配置
├── next.config.ts                  # Next.js 配置
├── postcss.config.mjs              # PostCSS 配置（@tailwindcss/postcss）
├── tsconfig.json                   # TypeScript strict 配置
├── package.json
└── README.md
```

---

## 六、依赖清单

### 6.1 生产依赖

```jsonc
{
  "dependencies": {
    "next": "^15.0.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "next-intl": "^4.0.0",
    "framer-motion": "^11.0.0",
    "zod": "^3.23.0",
    "gray-matter": "^4.0.0",
    "react-markdown": "^9.0.0",
    "remark-gfm": "^4.0.0",
    "lucide-react": "^0.400.0",
    "class-variance-authority": "^0.7.0",
    "clsx": "^2.1.0",
    "tailwind-merge": "^2.5.0"
  }
}
```

### 6.2 开发依赖

```jsonc
{
  "devDependencies": {
    "typescript": "^5.6.0",
    "tailwindcss": "^4.0.0",
    "@tailwindcss/postcss": "^4.0.0",
    "postcss": "^8.4.0",
    "eslint": "^9.0.0",
    "@eslint/eslintrc": "^3.0.0",
    "@types/node": "^22.0.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "eslint-config-next": "^15.0.0"
  }
}
```

### 6.3 依赖角色说明

| 依赖 | 类型 | 角色 |
|------|------|------|
| `next`, `react`, `react-dom` | 生产 | 框架核心 |
| `next-intl` | 生产 | 多语言路由 + 消息分发 |
| `framer-motion` | 生产 | 所有 JS 驱动动效（入场/过渡/视差） |
| `zod` | 生产 | JSON 数据运行时校验 |
| `gray-matter` | 生产 | Markdown frontmatter 解析 |
| `react-markdown` + `remark-gfm` | 生产 | Markdown 正文渲染 |
| `lucide-react` | 生产 | 图标库 |
| `cva` | 生产 | 组件变体管理（Button 变体等） |
| `clsx` + `tailwind-merge` | 生产 | Tailwind class 合并（cn() 工具函数） |
| `tailwindcss` | 开发 | CSS 框架 |
| `typescript` | 开发 | 类型检查 |
| `eslint` + `eslint-config-next` | 开发 | 代码规范 |

---

## 七、配置清单

### 7.1 TypeScript (`tsconfig.json`)

```jsonc
{
  "compilerOptions": {
    "target": "ES2017",
    "lib": ["dom", "dom.iterable", "esnext"],
    "allowJs": true,
    "skipLibCheck": true,
    "strict": true,
    "noEmit": true,
    "esModuleInterop": true,
    "module": "esnext",
    "moduleResolution": "bundler",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "jsx": "preserve",
    "incremental": true,
    "plugins": [{ "name": "next" }],
    "paths": {
      "@/*": ["./src/*"]
    }
  },
  "include": ["next-env.d.ts", "**/*.ts", "**/*.tsx", ".next/types/**/*.ts"],
  "exclude": ["node_modules"]
}
```

### 7.2 Next.js (`next.config.ts`)

```typescript
import type { NextConfig } from "next"
import createNextIntlPlugin from "next-intl/plugin"

const withNextIntl = createNextIntlPlugin()

const nextConfig: NextConfig = {
  images: {
    formats: ["image/avif", "image/webp"],
  },
}

export default withNextIntl(nextConfig)
```

### 7.3 Tailwind v4 (`src/styles/globals.css`)

Tailwind v4 采用 CSS-first 配置，所有令牌在 `globals.css` 中通过 `@theme` 声明。

令牌定义详见 [[3.1 风格指南.md#九 设计令牌映射表]]，包含：
- `colors` — primary / secondary / accent / background / surface / text / border
- `fontFamily` — ja / en / zh
- `fontSize` — xs → 5xl（自定义比例尺）
- `spacing` — 1 → 20（4px 基准）
- `borderRadius` — sm → xl
- `boxShadow` — xs → xl（暖调偏移）
- `width` — content (1152px) / narrow (768px)
- `transitionDuration` — fast / normal / slow / ambient
- `transitionTimingFunction` — in-soft

### 7.4 next-intl 配置

| 文件 | 说明 |
|------|------|
| `src/middleware.ts` | 语言检测、`/` → `/zh` 重定向 |
| `src/i18n/request.ts` | next-intl 请求配置（语言 → messages 映射） |
| `src/messages/{lang}.json` | 各语言消息文件 |

`middleware.ts` 核心逻辑：

```typescript
import createMiddleware from "next-intl/middleware"

export default createMiddleware({
  locales: ["zh", "ja", "en"],
  defaultLocale: "zh",
  localePrefix: "always",
})

export const config = {
  matcher: ["/((?!api|_next|_vercel|images|fonts|resume|.*\\..*).*)"],
}
```

---

## 八、环境与部署架构

```
┌──────────────────┐     Push/PR      ┌───────────────────┐
│   Developer      │ ──────────────→   │   GitHub          │
│   (pnpm dev)     │                   │   Actions (CI)    │
└──────────────────┘                   │   lint + tsc      │
                                       │   + build         │
                                       └────────┬──────────┘
                                                │ pass
                                                ▼
                                       ┌───────────────────┐
                                       │   Vercel           │
                                       │   ┌─────────────┐  │
                                       │   │ main → Prod │  │
                                       │   │ PR → Preview│  │
                                       │   └─────────────┘  │
                                       └───────────────────┘
```

| 环境 | URL | 触发方式 | 说明 |
|------|-----|---------|------|
| 本地开发 | `http://localhost:3000` | `pnpm dev` | 热更新开发 |
| Production | Vercel 分配域名 | push to main | 最终上线环境 |
| Preview | Vercel 自动生成 URL | PR create/update | 预览/测试环境 |

---

## 九、性能与约束

| 约束项 | 规格 | 说明 |
|--------|------|------|
| Lighthouse 目标 | Performance ≥ 90, Accessibility ≥ 95, SEO ≥ 95 | 桌面端基准 |
| 图片格式 | 源 PNG → Next/Image 自动输出 AVIF + WebP | 无需手动转换 |
| 字体 | 全部自托管，`next/font` 零 CLS | 无 Google Fonts 网络请求 |
| 总页面数 | 2（首页 + Case Study 详情页）| × 三语 = 6 个静态路径 |
| 项目/案例上限 | ≤20 | 超限需考虑性能优化 |
| 动效 JS 上限 | Framer Motion 唯一库，CSS 循环动画 ≤3 个 | 避免过度动画导致重排 |

---

## 附录：开发环境要求

| 工具 | 版本要求 | 验证命令 |
|------|---------|---------|
| Node.js | ≥22.0.0 | `node -v` |
| pnpm | ≥9.0.0 | `pnpm -v` |
| OS | 不限 | — |

初始化命令：

```bash
pnpm create next-app@latest . --typescript --eslint --app --src-dir --import-alias "@/*"
pnpm add next-intl framer-motion zod gray-matter react-markdown remark-gfm lucide-react class-variance-authority clsx tailwind-merge
pnpm add -D @tailwindcss/postcss
```
