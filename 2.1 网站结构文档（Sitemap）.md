# 2.1 网站结构文档（Sitemap）

**项目：** KONG JUN SIENG — Software Developer Portfolio  
**文档目标：** 定义页面层级树、URL 映射与导航关系，供 AI 生成 App Router 目录结构和导航组件。

---

## 一、网站层级树（含 i18n）

```
/ (根域)
├── public/
│   ├── images/
│   │   ├── hero-character.png         → 首页角色立绘
│   │   ├── normal.png ~ bigsmile.png  → VN 角色表情（8 种）
│   │   ├── vnscene1.png               → VN 场景图
│   │   ├── vnscene2-option.png        → VN 选择分支场景图
│   │   ├── option-button.png          → 选项按钮平铺素材
│   │   ├── portfolio-cover.png        → Portfolio 项目封面
│   │   ├── ccms-cover.png             → CCMS 项目封面
│   │   ├── case-study-cover.png       → Case Study 封面
│   │   └── *.png                      → Case Study 详情页截图（obsidian_graph 等）
│   ├── fonts/                         → WOFF2 字体文件（JP/EN/ZH 13 子集）
│   ├── resume-zh.pdf                  → 中文简历
│   ├── resume-ja.pdf                  → 日本語履歴書
│   ├── resume-en.pdf                  → English Resume
│   ├── casestudy-zh.pdf               → 中文 Case Study PDF
│   ├── casestudy-ja.pdf               → 日本語 Case Study PDF
│   ├── casestudy-en.pdf               → English Case Study PDF
│   └── casestudy-{lang}.md            → Case Study Markdown 源文件（未用于渲染）
│
└── src/
    ├── proxy.ts                        → 根路由重定向 + next-intl 中间件
    ├── i18n/
    │   ├── routing.ts                  → 语言配置（en/ja/zh，默认 en）
    │   └── request.ts                  → 消息文件动态加载
    ├── messages/
    │   ├── zh.json                     → 中文翻译
    │   ├── ja.json                     → 日本語翻訳
    │   └── en.json                     → English translations
    ├── data/
    │   ├── profile.json                → 个人资料
    │   ├── projects.json               → 项目卡片数据（ccms / portfolio）
    │   ├── case-studies.json           → Case Study 列表数据（slug: casestudy）
    │   ├── vn-scenario.zh.json         → 序幕场景（中文，43 场景）
    │   ├── vn-scenario.ja.json         → 序幕场景（日本語）
    │   └── vn-scenario.en.json         → 序幕场景（English）
    │
    └── app/
        ├── [lang]/                     → 三语路由层
        │   ├── intro/
        │   │   └── page               → 游戏风序幕（开幕动画 → TitleScreen → Scenario → LoadingOverlay → 首页）
        │   ├── page                    → 首页（单页滚动）
        │   │   ├── #hero               → Hero Banner
        │   │   ├── #case-studies       → Case Studies 卡片列表
        │   │   ├── #projects           → 项目展示卡片
        │   │   ├── #about              → 关于我
        │   │   └── #contact            → 联系区块（选项按钮式）
        │   │
        │   ├── layout.tsx              → 多语布局（NextIntlClientProvider + JaLocaleToast + LocaleContent + LayoutShell）
        │   └── projects/
        │       └── [slug]/
        │           └── page            → Case Study 详情页（动态路由 + 右侧目录导航 + PDF 下载）
        │
        ├── layout.tsx                  → 全局根布局（仅 html + body + 字体变量 + globals.css + fonts.css）
        ├── not-found.tsx               → 404 页面（无导航栏/页脚）
        ├── sitemap.ts                  → 动态 sitemap.xml
        └── robots.ts                   → robots.txt
```

---

## 二、URL 映射表

| # | 页面 | 路由 | 导航栏可见 | 数据依赖 | 说明 |
|---|------|------|-----------|---------|------|
| 1 | 游戏风序幕 | `/[lang]/intro` | 否（`/` 自动重定向至 `/en/intro`） | `src/data/vn-scenario.{lang}.json`, `public/images/*.png` | 4 阶段状态机：IntroAnimation → TitleScreen → VNScenario → LoadingOverlay。无导航栏/页脚 |
| 2 | 首页（单页滚动） | `/[lang]` | 是（默认） | `src/data/profile.json`, `src/data/projects.json`, `src/data/case-studies.json`, `src/messages/{lang}.json` | 5 个 section + 4 条 SectionTransition 分隔。Hero CTA 跳转至 `#case-studies` |
| 3 | Case Study 详情页 | `/[lang]/projects/[slug]` | 否（Case Studies 区块跳转入） | `src/data/case-studies.json`, `src/messages/{lang}.json`（全部章节内容通过 `t.raw()`） | 含 CaseStudyNav（右侧固定目录导航，滚动追踪章节位置）+ CaseStudyDownloadButton（PDF 勾选动画）+ BackToTopButton + CaseStudyContent（Markdown 风格渲染器） |

---

## 三、App Router 目录映射（AI 直接生成）

```
src/app/
├── [lang]/
│   ├── intro/
│   │   └── page.tsx                    → 游戏风序幕
│   ├── page.tsx                        → 首页（Hero + CaseStudies + Projects + About + Contact）
│   ├── layout.tsx                      → 多语布局（Provider + Toast + 过渡动画 + LayoutShell）
│   └── projects/
│       └── [slug]/
│           └── page.tsx                → Case Study 详情页
├── layout.tsx                          → 根布局（字体 + CSS）
├── not-found.tsx                       → 404 页面
├── sitemap.ts                          → 动态 sitemap.xml
└── robots.ts                           → robots.txt
```

> 注意：`src/app/api/` 目录当前不存在，为预留位。未来 P1 接入联系表单后端时创建。

---

## 四、导航结构

### 4.1 Navbar 链接映射

| 导航项 | 动作 | 说明 |
|--------|------|------|
| Logo / "KONG JUN SIENG" | 首页: `scrollToTop` / 其他页: → `/[lang]` | 回到首页顶部或跳转至首页 |
| 案例研究 | → `#[lang]#case-studies` | 锚点跳转至 Case Studies 区块 |
| 作品 | → `#[lang]#projects` | 锚点跳转至 Projects 区块 |
| 关于 | → `#[lang]#about` | 锚点跳转至 About 区块 |
| 联系 | → `#[lang]#contact` | 锚点跳转至 Contact 区块 |
| 语言切换 | 切换 `[lang]` 前缀 | 中 / 日 / EN，当前语言高亮，切换时淡入淡出过渡 |

导航文本来自 `messages/{lang}.json`（`nav` 命名空间）。

### 4.2 其他导航入口

- **Hero CTA 按钮**：页面 Hero 区块内「查看案例研究」按钮 → `#case-studies`
- **Case Studies 卡片**：卡片「查看详情」按钮 → `/[lang]/projects/[slug]`（独立详情页）
- **Footer**：仅版权信息（无导航链接）
- **404 页面**：提供「Go home」链接 → `/`（proxy 重定向至 `/en/intro`）

---

## 五、重定向策略

| 来源 | 目标 | 方式 |
|------|------|------|
| `/` | `/en/intro` | proxy.ts 硬编码重定向（默认英文，进入游戏风序幕） |
| `/[lang]`（已含语言前缀） | `/[lang]` | 正常匹配路由 |
| `/[lang]/projects/[slug]` | `/[lang]/projects/[slug]` | 正常匹配 |
| 无效语言前缀（如 `/fr/...`） | `/_not-found` → 404 页面 | `[lang]/layout.tsx` 中 `hasLocale()` 检查失败 → `notFound()` |
| 有效语言下不存在的 slug | `/_not-found` → 404 页面 | `[slug]/page.tsx` 中未找到匹配 → `notFound()` |
| 未匹配路由 | `/_not-found` | 404 页面（无导航栏/页脚） |

简历 / Case Study PDF 不经过路由，Contact 区块内按钮直接下载 `public/resume-{lang}.pdf` / `public/casestudy-{lang}.pdf`。

---

## 六、技术说明

- **多语言方案**：next-intl，URL 策略为 path-based (`/[lang]/...`)，默认语言英文（`en`）
- **proxy.ts**：先检查 `/` 根路径 → 硬编码重定向至 `/en/intro`，其余请求交由 next-intl `createMiddleware` 统一处理语言检测/补全
- **语言配置**：定义在 `src/i18n/routing.ts`，`localePrefix: "always"`，所有路由强制带语言前缀
- **Intro 路由隔离**：`LayoutShell` 检测 `pathname === "/intro"` → 跳过 Navbar/Footer/页面过渡，仅渲染裸内容
- **页面过渡动画**：非 Intro 页面间使用 Framer Motion `AnimatePresence mode="wait"` + `pageTransition` 变体，换页时自动 `scrollTo({ top: 0 })`
- **锚点导航**：首页各 Section 使用 `id` 属性（`#hero` / `#case-studies` / `#projects` / `#about` / `#contact`），Navbar 通过 `ScrollToAnchor` 组件实现平滑滚动 + 64px 导航栏高度补偿
- **Case Study 详情页**：内容数据源为 `messages/{lang}.json`（通过 `t.raw()` 读取含 Markdown 风格标记的文本），非独立 `.md` 文件。渲染器支持 heading / list / code / table / imageGrid / 流程图行（3+ 箭头 → `FlowStepBar`）
- **Sitemap**：动态生成，覆盖 3 语言 × 2 路由（首页 + `/projects/casestudy`），共 6 条
- **Robots**：允许所有爬虫，指向 `https://junsieng-portfolio.vercel.app/sitemap.xml`