# 2.1 网站结构文档（Sitemap）

**项目：** KONG JUN SIENG — Software Developer Portfolio  
**文档目标：** 定义页面层级树、URL 映射与导航关系，供 AI 生成 App Router 目录结构和导航组件。

---

## 一、网站层级树（含 i18n）

```
/ (根域)
├── middleware.ts                  → i18n 检测 + 重定向 (zh/ja/en)
│
├── messages/                      → i18n JSON 文件（zh.json / ja.json / en.json）
│
├── [lang]/ (zh/ja/en 三语路由)
│   ├── page                      → 首页（单页滚动）
│   │   ├── #hero                 ─ Hero Banner
│   │   ├── #case-studies         ─ Case Studies 卡片列表
│   │   ├── #projects             ─ 项目展示卡片
│   │   ├── #about                ─ 关于我
│   │   └── #contact              ─ 联系区块
│   │
│   └── projects/
│       └── [slug]/page           → Case Study 详情页（动态路由）
│
├── public/
│   ├── resume-zh.pdf              → 中文简历
│   ├── resume-ja.pdf              → 日本語履歴書
│   ├── resume-en.pdf              → English Resume
│   └── images/                    → 封面图、项目截图等
│
├── data/
│   ├── profile.json               → 个人资料
│   ├── projects.json              → 项目卡片数据
│   ├── case-studies.json          → Case Study 列表数据
│   ├── portfolio-zh.md             → Case Study Markdown 文件
│   ├── portfolio-ja.md
│   └── portfolio-en.md
│
└── src/
    └── app/
        ├── [lang]/                → 三语路由层
        ├── api/                   → (预留，未来联系表单后端)
        └── layout.tsx             → 全局根布局 (仅 html + body)
```

---

## 二、URL 映射表

| # | 页面 | 路由 | 导航栏可见 | 数据依赖 |
|---|------|------|-----------|---------|
| 1 | 首页（单页滚动） | `/[lang]` | 是（默认） | `profile.json`, `projects.json`, `case-studies.json`, `messages/{lang}.json` |
| 2 | Case Study 详情页 | `/[lang]/projects/[slug]` | 否（Case Studies 区块跳转进入） | `data/*-{lang}.md`, `messages/{lang}.json` |

---

## 三、App Router 目录映射（AI 直接生成）

```
src/app/
├── [lang]/
│   ├── page.tsx                        → 首页
│   ├── layout.tsx                      → 多语布局（导航栏 + 页脚）
│   └── projects/
│       └── [slug]/
│           └── page.tsx                → Case Study 详情页
├── api/
│   └── contact/
│       └── route.ts                    → (预留，联系表单 API)
├── layout.tsx                          → 根布局（仅 html + body）
└── not-found.tsx                       → 404 页面
```

---

## 四、导航结构

| 导航项 | 动作 | 说明 |
|--------|------|------|
| Logo / 站点名 | → `/[lang]` | 回到首页顶部 |
| 案例 | → `/[lang]#case-studies` | 锚点跳转至 Case Studies 区块 |
| 作品 | → `/[lang]#projects` | 锚点跳转至 Projects 区块 |
| 关于 | → `/[lang]#about` | 锚点跳转至 About 区块 |
| 联系 | → `/[lang]#contact` | 锚点跳转至 Contact 区块 |
| 语言切换 | 切换 `[lang]` 前缀 | 中 / 日 / EN，当前语言高亮 |

导航文本来自 `messages/{lang}.json`。

---

## 五、重定向策略

| 来源 | 目标 | 方式 |
|------|------|------|
| `/` | `/zh` | middleware 重定向（默认中文） |
| `/[lang]` | `/[lang]` | middleware 重写，正常匹配路由 |
| `/[lang]/projects/[slug]` | `/[lang]/projects/[slug]` | 正常匹配 |
| 未匹配路由 | `/not-found` | 404 页面 |

简历下载不经过路由，Contact 区块内按钮直接下载 `public/resume-{lang}.pdf`。

---

## 六、技术说明

- **多语言方案**：next-intl，URL 策略为 path-based (`/[lang]/...`)
- **middleware**：统一处理语言检测、缺失语言前缀补全重定向（含 `/` → `/zh`）
- **锚点导航**：首页各区块使用 `id` 属性，导航栏通过 `<Link href="/[lang]#projects">` 跳转
- **动态路由**：`/projects/[slug]` 根据 slug 匹配 `data/` 下对应语言的 Markdown 文件
- **简历下载**：Contact 区块按钮直接链接到 `public/resume-{lang}.pdf`，无独立页面
