# 6.2 部署环境配置表

**项目：** KONG JUN SIENG — Software Developer Portfolio  
**文档目标：** 环境变量清单与 Vercel 项目配置参数汇总表，AI 按此填充部署配置。

---

## 一、环境总览

| 环境 | 域名 | 触发方式 | Node.js | pnpm |
|------|------|---------|---------|------|
| **Production** | `junsieng-portfolio.vercel.app` | push to `main` | 22.x | 9.x |
| **Preview** | `junsieng-portfolio-git-{branch}-{hash}.vercel.app` | PR 创建/更新 | 22.x | 9.x |
| **Development** | `http://localhost:3000` | `pnpm dev` | ≥22.0.0 | ≥9.0.0 |

---

## 二、环境变量表

### 2.1 当前状态（v1 — 纯静态站点）

**零环境变量需求。** 所有配置通过源代码和框架配置文件管理。

| 变量名 | 值 | 来源 | 生效环境 | 用途 |
|--------|-----|------|---------|------|
| — | — | — | — | — |

### 2.2 预留变量（未来扩展）

| 变量名 | 预期用途 | 计划接入阶段 | 备注 |
|--------|---------|-------------|------|
| `RESEND_API_KEY` | 联系表单真实发送（Resend） | P2 (T10.4) | 仅 Production 需要 |
| `NEXT_PUBLIC_GA_ID` | Google Analytics / Vercel Analytics | P2 (T10.3) | NEXT_PUBLIC_ 前缀暴露给客户端 |
| `CONTACT_EMAIL` | 联系表单收件地址 | P2 (T10.4) | 备用 js.kong@outlook.com |

---

## 三、Vercel 项目配置

### 3.1 项目基本信息

| 配置项 | 值 |
|--------|-----|
| 项目名称 | `junsieng-portfolio` |
| 框架预设 | Next.js |
| 根目录 | `./` |
| Node.js 版本 | 22.x |
| 构建命令 | `next build` |
| 输出目录 | `.next` |
| 安装命令 | `pnpm install` |
| 开发命令 | `next dev` |
| 部署计划 | Hobby（免费） |
| 生产域名 | `junsieng-portfolio.vercel.app` |
| 自定义域名 | 无（使用默认域名） |

### 3.2 构建与环境设置

| 配置项 | 值 | 备注 |
|--------|-----|------|
| Framework Detection | Automatic | Vercel 自动识别 Next.js |
| Build & Development Settings | 使用默认 | 无需覆写 |
| Root Directory Override | 空（项目根目录） | — |
| Output Directory Override | 空（默认 `.next`） | — |
| Install Command Override | 空（默认 `pnpm install`） | — |
| Build Command Override | 空（默认 `next build`） | — |
| Development Command | 空（默认 `next dev`） | — |
| Node.js Version | 22.x | 与 CI 一致 |
| Enable Automatically expose System Variables | 开启 | 暴露 `VERCEL_URL`、`VERCEL_ENV` 等 |

### 3.3 Git 集成

| 配置项 | 值 |
|--------|-----|
| Git Provider | GitHub |
| 关联仓库 | `KONGJUNSIENG/2D-portfolio` |
| Production Branch | `main` |
| Auto-deploy on Git Push | 开启 |
| Auto-deploy on PR Creation | 开启 |
| Auto-cancel Preview Deployments | 开启（新推送自动取消旧部署） |
| Lock Deployment for CI | 关闭 |

### 3.4 域名与 SSL

| 配置项 | 值 |
|--------|-----|
| 默认域名 | `junsieng-portfolio.vercel.app` |
| 自定义域名 | 无 |
| SSL/TLS | 自动管理（Vercel 签发 + 自动续期） |
| HSTS | 默认开启 |
| CDN | Vercel Edge Network (全球) |

### 3.5 部署保护

| 配置项 | 值 | 说明 |
|--------|-----|------|
| Deployment Protection | 启用 | Hobby 计划默认行为 |
| Protection Bypass for Production | 关闭 | — |
| Password Protection | Vercel 自动生成 | 可在 Dashboard 中查看/修改 |
| IP Bypass | 未配置 | — |

### 3.6 日志与监控

| 配置项 | 状态 | 说明 |
|--------|------|------|
| Build Logs | 自动记录 | 每个部署在 Dashboard → Deployments 中可查看 |
| Runtime Logs | 自动记录 | Dashboard → Functions → Logs |
| Vercel Analytics | 未启用（P2） | 可选接入 |
| Vercel Speed Insights | 未启用（P2） | 可选接入 |
| Error Tracking | 未启用 | 无 Sentry |

---

## 四、配置文件清单

### 4.1 项目配置文件索引

| 文件 | 用途 | 参考文档 |
|------|------|---------|
| `next.config.ts` | Next.js 配置（i18n 插件 + Image 格式） | [[4.1 技术栈与架构文档.md#7.2]] |
| `tsconfig.json` | TypeScript strict 配置 | [[4.1 技术栈与架构文档.md#7.1]] |
| `src/styles/globals.css` | Tailwind v4 设计令牌（`@theme`） | [[3.1 风格指南.md#九]] |
| `postcss.config.mjs` | PostCSS 配置 | — |
| `.prettierrc` | Prettier 格式化配置 | [[5.1 编码规范文档.md#5.2]] |
| `src/proxy.ts` | i18n 语言检测 + 重定向（Next.js 16 已弃用 middleware.ts） | [[4.1 技术栈与架构文档.md#7.4]] |
| `src/i18n/request.ts` | next-intl 请求配置 | [[4.1 技术栈与架构文档.md#7.4]] |
| `.github/workflows/ci.yml` | CI 流水线定义 | [[6.1 部署说明文档.md#4.1]] |
| `.env.example` | 环境变量模板（当前为空） | — |
| `.gitignore` | Git 忽略规则 | — |
| `package.json` | 依赖 + npm scripts | [[4.1 技术栈与架构文档.md#六]] |

### 4.2 关键配置参数速查

#### `next.config.ts`

```typescript
import type { NextConfig } from "next"
import createNextIntlPlugin from "next-intl/plugin"

const withNextIntl = createNextIntlPlugin()

const nextConfig: NextConfig = {
  images: {
    formats: ["image/avif", "image/webp"],
  },
}

export default withNextIntl(nextConfig)
```

#### `tsconfig.json` 关键项

| 参数 | 值 |
|------|-----|
| `strict` | `true` |
| `moduleResolution` | `bundler` |
| `paths["@/*"]` | `["./src/*"]` |
| `target` | `ES2017` |

#### `src/proxy.ts`

| 参数 | 值 |
|------|-----|
| `locales` | `["zh", "ja", "en"]` |
| `defaultLocale` | `"zh"` |
| `localePrefix` | `"always"` |
| `matcher` | `"/((?!api|_next|_vercel|images|fonts|resume|.*\\..*).*)"` |

---

## 五、CI 配置

| 配置项 | 值 |
|--------|-----|
| CI 平台 | GitHub Actions |
| 触发条件 | push 到 `main` + PR 到 `main` |
| 运行环境 | `ubuntu-latest` |
| Node.js 版本 | 22 |
| 包管理器 | pnpm（latest 版本） |
| 缓存 | `pnpm` 缓存 |
| 安装模式 | `--frozen-lockfile` |
| Steps | `lint` → `typecheck` → `build` |

---

## 六、Git 配置

### 6.1 `.gitignore` 关键规则

```
node_modules/
.next/
*.local
.env*.local
```

### 6.2 分支配置

| 配置项 | 值 |
|--------|-----|
| 默认分支 | `main` |
| 分支命名规则 | `feature/*`, `fix/*`, `chore/*` |
| PR 目标分支 | `main` |

---

## 七、运行时依赖版本锁

| 依赖 | 版本范围 | 锁定方式 | 说明 |
|------|---------|---------|------|
| Node.js | ≥22.0.0 | `package.json` engines 字段 + Vercel 设定 + CI 设定 | 三处统一 |
| pnpm | ≥9.0.0 | `package.json` packageManager 字段 + CI | — |
| Next.js | ^16.0.0 | `pnpm-lock.yaml` | lockfile 锁定 |
| React | ^19.0.0 | `pnpm-lock.yaml` | lockfile 锁定 |

```jsonc
// package.json — engines 声明
{
  "engines": {
    "node": ">=22.0.0",
    "pnpm": ">=9.0.0"
  },
  "packageManager": "pnpm@9.15.0"
}
```

---

## 八、部署前后配置检查清单

### 8.1 部署前置检查

- [ ] `package.json` engines 字段已声明 Node.js ≥22.0.0
- [ ] `package.json` packageManager 已指定 pnpm 版本
- [ ] `.env.example` 已包含所有环境变量名（当前为空）
- [ ] `.gitignore` 包含 `*.local` 和 `.env*.local`
- [ ] `next.config.ts` 已配置 `next-intl` 插件
- [ ] `tsconfig.json` strict 模式已启用
- [ ] `.github/workflows/ci.yml` 配置完整

### 8.2 Vercel 部署前检查

- [ ] GitHub 仓库已授权 Vercel
- [ ] Vercel 项目已创建，Framework 自动检测为 Next.js
- [ ] Node.js 版本设为 22.x
- [ ] Production Branch 设为 `main`
- [ ] 项目名称为 `junsieng-portfolio`
- [ ] 环境变量为空（v1 无需）

### 8.3 部署后验证

- [ ] 生产域名 `junsieng-portfolio.vercel.app` 可访问
- [ ] `/` 自动重定向到 `/zh`
- [ ] `/zh` `/ja` `/en` 三语页面正常
- [ ] `/zh/projects/portfolio` Case Study 详情页正常
- [ ] 所有图片正常加载
- [ ] 字体渲染正确
- [ ] 动效按预期触发
- [ ] `pnpm run build` 本地构建通过
- [ ] CI 流水线通过


