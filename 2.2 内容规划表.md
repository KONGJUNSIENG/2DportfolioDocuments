# 2.2 内容规划表

**项目：** KONG JUN SIENG — Software Developer Portfolio  
**文档目标：** 定义每个页面/区块的内容模板与字段映射，供 AI 自动填充数据到页面组件。

---

## 一、内容模板总览

| 页面/区块 | 内容来源 | 多语 | 格式 |
|-----------|---------|------|------|
| 首页 Hero （section） | `profile.json` + `messages/{lang}.json` | 部分 | JSON + 文本 |
| 首页 Case Studies （section） | `case-studies.json` + `messages/{lang}.json` | 部分 | JSON |
| 首页 Projects （section） | `projects.json` + `messages/{lang}.json` | 部分 | JSON |
| 首页 About （section） | `messages/{lang}.json` + `profile.json` | 部分 | JSON + 文本 |
| 首页 Contact （section） | 硬编码 + `messages/{lang}.json` + `profile.json` | 部分 | 文本 |
| Case Study 详情页 | `messages/{lang}.json`（executiveSummary/problemStatement/targetUser/designProcess/techArchitecture/keyFeatures/implementation/outcome/appendix 共 9 节结构化字段） | 是 | JSON 结构化文本 |
| 游戏风序幕（intro） | `vn-scenario.{lang}.json` | 是 | JSON（43 场景剧本） |

---

## 二、各页面/区块内容模板明细

### 2.1 首页 Hero（section）

| 字段 | 类型 | 数据源 | 多语 | 示例值 |
|------|------|--------|------|--------|
| `name` | 文本 | `profile.json` | 否 | KONG JUN SIENG |
| `title` | 文本 | `messages/{lang}.json` | 是 | `zh`: Software Developer / 全栈探索者 |
| `slogan` | 文本 | `profile.json` | 否 | やればできる！ |
| `ctaText` | 文本 | `messages/{lang}.json` | 是 | `zh`: 查看作品 |

`profile.json` 提取字段：

```json
{
  "name": "KONG JUN SIENG",
  "slogan": "やればできる！"
}
```

`messages/{lang}.json` 对应路径：

```jsonc
{
  "hero": {
    "title": "Software Developer / 全栈探索者",
    "ctaText": "查看作品"
  }
}
```

---

### 2.2 首页 Case Studies（section）

卡片式布局，与 Projects 区块一致：封面图 + 标题 + 技术标签 + "查看详情"按钮，点击跳转至 `/[lang]/projects/[slug]`。初始仅 Portfolio 项目的 Case Study。

| 字段 | 类型 | 数据源 | 多语 |
|------|------|--------|------|
| `sectionTitle` | 文本 | `messages/{lang}.json` | 是 |
| `caseStudies[]` | 数组 | `case-studies.json` | 标题/描述通过 key 映射到 locales |

`case-studies.json`：

```json
{
  "caseStudies": [
    {
      "slug": "portfolio",
      "cover": "/images/portfolio-cover.png",
      "techStack": ["Next.js", "Framer Motion"],
      "titleKey": "caseStudies.portfolio.title",
      "descriptionKey": "caseStudies.portfolio.description"
    }
  ]
}
```

`messages/{lang}.json` 对应路径：

```jsonc
{
  "caseStudies": {
    "sectionTitle": "案例研究",
    "portfolio": {
      "title": "Portfolio+AI半自动化开发案例",
      "description": "从设计到开发,全程与AI协作完成的Portfolio网站。融合日系萌系视觉风格与游戏风浏览体验。"
    },
    "viewButton": "查看详情"
  }
}
```

每条 Case Study 渲染为一个卡片，包含封面图 + 技术标签 + 标题 + 描述 + "查看详情"按钮，点击跳转至 `/[lang]/projects/[slug]`。

---

### 2.3 首页 Projects（section）

纯展示卡片，无跳转。初始 2 个项目卡片：FYP IoT、Portfolio。

| 字段 | 类型 | 数据源 | 多语 |
|------|------|--------|------|
| `sectionTitle` | 文本 | `messages/{lang}.json` | 是 |
| `projects[]` | 数组 | `projects.json` | 标题通过 key 映射到 locales |

`projects.json`：

```json
{
  "projects": [
    {
      "slug": "ccms",
      "cover": "/images/ccms-cover.png",
      "techStack": ["IoT", "Python", "Raspberry Pi", "Fast API", "RESTful APIs", "Next.js", "Node.js"],
      "titleKey": "projects.ccms.title",
      "backgroundKey": "projects.ccms.background",
      "contributeKey": "projects.ccms.contribute",
      "outcomeKey": "projects.ccms.outcome"
    },
    {
      "slug": "portfolio",
      "cover": "/images/portfolio-cover.png",
      "techStack": ["Next.js 16", "Node.js", "TypeScript", "Tailwind CSS", "Framer Motion"],
      "titleKey": "projects.portfolio.title",
      "backgroundKey": "projects.portfolio.background",
      "contributeKey": "projects.portfolio.contribute",
      "outcomeKey": "projects.portfolio.outcome"
    }
  ]
}
```

`messages/{lang}.json` 对应路径：

```jsonc
{
  "projects": {
    "sectionTitle": "作品",
    "backgroundLabel": "背景",
    "contributeLabel": "贡献",
    "outcomeLabel": "成果",
    "ccms": {
      "title": "Chicken Coop Monitoring System (CCMS)",
      "background": "受 Satoyama Farm 委托开发的 CCMS…",
      "contribute": ["硬体整合", "边缘端开发", "后端 API 开发"],
      "outcome": "边缘端系统完成部署并通过功能验证…"
    },
    "portfolio": {
      "title": "Portfolio",
      "background": "一个以展示和验证 AI 辅助开发模式…",
      "contribute": ["架构设计", "设计系统", "全栈开发", "游戏风体验", "CI/CD"],
      "outcome": "部署于 Vercel 的响应式多语 Portfolio 站…"
    }
  }
}
```

---

### 2.4 首页 About（section）

| 字段 | 类型 | 数据源 | 多语 | 说明 |
|------|------|--------|------|------|
| `bio` | 文本（长文） | `messages/{lang}.json` | 是 | 个人故事风格，约 200+ 字 |
| `skills` | 字符串数组 | `profile.json` | 否 | `["Python", "TypeScript", "React", "Next.js", "Git", "RESTful APIs", "Regular Expressions", "Internationalization (i18n)", "IoT", "Framer Motion"]` |

`profile.json` 提取字段：

```json
{
  "skills": ["Python", "TypeScript", "React", "Next.js", "Node.js", "Git", "RESTful APIs", "Regular Expressions", "Internationalization (i18n)", "IoT", "Framer Motion"]
}
```

`messages/{lang}.json` 对应路径：

```jsonc
{
  "about": {
    "bio": "毕业设计别人写论文,我写了套让设备自己学会汇报工作的IoT边缘端系统...（完整中文约 350 字个人故事，见 zh.json）"
  }
}
```

---

### 2.5 首页 Contact（section）

包含邮箱链接、社交链接、简历下载按钮（按语言下载对应 PDF，无表单）。

| 字段 | 类型 | 数据源 | 多语 |
|------|------|--------|------|
| `email` | 文本 | 硬编码 | 否 |
| `socialLinks` | 对象（key-value） | `profile.json` | 否 |
| `sectionTitle` | 文本 | `messages/{lang}.json` | 是 |
| `formLabels` | 对象 | `messages/{lang}.json`（含 formName/formEmail/formMessage/formSubmit 键，当前未使用） | 是 |
| `resumeBtnText` | 文本 | `messages/{lang}.json` | 是 |

硬编码字段：

```
email: js.kong@outlook.com
```

`profile.json` 提取字段：

```json
{
  "socialLinks": {
    "github": "https://github.com/KONGJUNSIENG",
    "linkedin": "https://www.linkedin.com/in/jun-sieng-kong-b1b3213b3"
  }
}
```

简历下载文件（按语言分离）：

| 语言 | 文件路径 |
|------|---------|
| zh | `public/resume-zh.pdf` |
| ja | `public/resume-ja.pdf` |
| en | `public/resume-en.pdf` |

下载逻辑：根据当前 `lang` 拼接 `resume-{lang}.pdf`。

`messages/{lang}.json` 对应路径：

```jsonc
{
  "contact": {
    "sectionTitle": "联系我",
    "resumeBtnText": "下载简历",
    "formName": "姓名",
    "formEmail": "邮箱",
    "formMessage": "留言",
    "formSubmit": "发送"
  }
}
```

---

### 2.6 Case Study 详情页 `/[lang]/projects/[slug]`

通过 URL 直接访问，导航栏无入口。正文采用结构化字段存储在 `messages/{lang}.json` 中：

**messages/{lang}.json 模板（9 节模板大纲）：**

```jsonc
{
  "caseStudies": {
    "executiveSummaryLabel": "执行摘要",          // 章节标题
    "problemStatementLabel": "问题陈述",
    "targetUserLabel": "目标用户",
    "designProcessLabel": "设计过程",
    "techArchitectureLabel": "技术架构",
    "keyFeaturesLabel": "核心功能",
    "implementationLabel": "实现细节",
    "outcomeLabel": "成果与反思",
    "appendixLabel": "附录与参考",
    "portfolio": {
      "title": "Portfolio+AI半自动化开发案例",
      "description": "从设计到开发…",
      "executiveSummary": "执行摘要正文…",
      "problemStatement": "问题陈述正文…",
      "targetUser": "目标用户正文…",
      "designProcess": "设计过程正文…",
      "techArchitecture": "技术架构正文…",
      "keyFeatures": "核心功能正文…",
      "implementation": "实现细节正文…",
      "outcome": "成果与反思正文…",
      "appendix": "附录与参考正文…"
    },
    "viewButton": "查看详情"
  }
}
```

字段说明：

| 字段 | 多语 | 说明 |
|------|------|------|
| `title` | 是 | 当前语言的标题 |
| `description` | 是 | 首页卡片显示的简述 |
| `executiveSummary` | 是 | 执行摘要 |
| `problemStatement` | 是 | 问题陈述 |
| `targetUser` | 是 | 目标用户 |
| `designProcess` | 是 | 设计过程 |
| `techArchitecture` | 是 | 技术架构 |
| `keyFeatures` | 是 | 核心功能 |
| `implementation` | 是 | 实现细节 |
| `outcome` | 是 | 成果与反思 |
| `appendix` | 是 | 附录与参考 |
| `executiveSummaryLabel` | 是 | 章节标题（跨 Case Study 复用） |
| `problemStatementLabel` | 是 | 章节标题 |
| `targetUserLabel` | 是 | 章节标题 |
| `designProcessLabel` | 是 | 章节标题 |
| `techArchitectureLabel` | 是 | 章节标题 |
| `keyFeaturesLabel` | 是 | 章节标题 |
| `implementationLabel` | 是 | 章节标题 |
| `outcomeLabel` | 是 | 章节标题 |
| `appendixLabel` | 是 | 章节标题 |

---

### 2.7 游戏风序幕（`/[lang]/intro`）

游戏风序幕由四阶段组成：TitleScreen → 暗転 → Scenario → LoadingOverlay → 首页自动跳转。

| 字段 | 类型 | 数据源 | 多语 |
|------|------|--------|------|
| `scenes[]` | 数组 | `vn-scenario.{lang}.json` | 是 |

`vn-scenario.{lang}.json` 结构：
```json
{
  "scenes": [
    {
      "id": 1,
      "chapterTitle": "プロローグ",
      "speaker": "???",
      "expression": "normal",
      "text": "这是一段序幕台词……",
      "nextScene": 2
    }
  ]
}
```

| 字段 | 说明 |
|------|------|
| `id` | 场景序号（1-43） |
| `chapterTitle` | 可选，章节标题（如「プロローグ」），仅章节首场景显示 |
| `speaker` | 说话者名称 |
| `expression` | 角色表情（normal/smile/sad/mad/shock/shy/bigsmile/emm） |
| `text` | 台词正文 |
| `nextScene` | 下一场景 ID，null 表示结束 |

---

## 三、全局共享内容

### 导航栏

| 导航项 | 动作 | 说明 |
|--------|------|------|
| Logo / 站点名 | → `/[lang]` | 回到首页顶部 |
| 案例 | → `/[lang]#case-studies` | 锚点跳转至首页 Case Studies 区块 |
| 作品 | → `/[lang]#projects` | 锚点跳转至首页 Projects 区块 |
| 关于 | → `/[lang]#about` | 锚点跳转至首页 About 区块 |
| 联系 | → `/[lang]#contact` | 锚点跳转至首页 Contact 区块 |
| 语言切换 | 切换 `[lang]` 前缀 | 中 / 日 / EN |

导航文本来自 `messages/{lang}.json`：

```jsonc
{
  "nav": {
    "caseStudies": "案例",
    "projects": "作品",
    "about": "关于",
    "contact": "联系"
  }
}
```

### 页脚

| 字段 | 类型 | 数据源 | 多语 |
|------|------|--------|------|
| `copyright` | 文本 | `messages/{lang}.json` | 是 |
| `socialLinks` | 对象 | `profile.json` | 否 |

```jsonc
{
  "footer": {
    "copyright": "© 2026 KONG JUN SIENG"
  }
}
```

---

## 四、i18n 字段索引表（AI 生成 locales JSON 用）

```jsonc
// src/messages/zh.json — 完整结构参考
// 序幕数据独立存储于 src/data/vn-scenario.{lang}.json，不在此 JSON 中
{
  "nav": {
    "caseStudies": "案例",
    "projects": "作品",
    "about": "关于",
    "contact": "联系"
  },
  "hero": {
    "title": "Software Developer / 全栈探索者",
    "ctaText": "查看作品"
  },
  "caseStudies": {
    "sectionTitle": "案例研究",
    "executiveSummaryLabel": "执行摘要",
    "problemStatementLabel": "问题陈述",
    "targetUserLabel": "目标用户",
    "designProcessLabel": "设计过程",
    "techArchitectureLabel": "技术架构",
    "keyFeaturesLabel": "核心功能",
    "implementationLabel": "实现细节",
    "outcomeLabel": "成果与反思",
    "appendixLabel": "附录与参考",
    "backToHome": "返回首页",
    "downloadPdf": "下载 PDF",
    "portfolio": {
      "title": "Portfolio+AI半自动化开发案例",
      "description": "从设计到开发,全程与AI协作完成的Portfolio网站。融合日系萌系视觉风格与游戏风浏览体验。",
      "executiveSummary": "…",
      "problemStatement": "…",
      "targetUser": "…",
      "designProcess": "…",
      "techArchitecture": "…",
      "keyFeatures": "…",
      "implementation": "…",
      "outcome": "…",
      "appendix": "…"
    },
    "viewButton": "查看详情"
  },
  "projects": {
    "sectionTitle": "作品",
    "backgroundLabel": "背景",
    "contributeLabel": "贡献",
    "outcomeLabel": "成果",
    "ccms": {
      "title": "Chicken Coop Monitoring System (CCMS)",
      "background": "受 Satoyama Farm 委托开发的 CCMS…",
      "contribute": ["硬体整合", "边缘端开发", "后端 API 开发"],
      "outcome": "边缘端系统完成部署并通过功能验证…"
    },
    "portfolio": {
      "title": "Portfolio",
      "background": "一个以展示和验证 AI 辅助开发模式…",
      "contribute": ["架构设计", "设计系统", "全栈开发", "游戏风体验", "CI/CD"],
      "outcome": "部署于 Vercel 的响应式多语 Portfolio 站…"
    }
  },
  "about": {
    "bio": "毕业设计别人写论文,我写了套让设备自己学会汇报工作的IoT边缘端系统...（完整中文约 350 字个人故事，见 zh.json）"
  },
  "contact": {
    "sectionTitle": "联系我",
    "resumeBtnText": "下载简历"
  },
  "transitions": {
    "heroToCaseStudies": "(∠・ω< )⌒★",
    "caseStudiesToProjects": "ヾ(ﾟ△ﾟ)ﾉ",
    "projectsToAbout": "((((；ﾟДﾟ))))",
    "aboutToContact": "(ﾟﾛﾟ;)"
  },
  "footer": {
    "copyright": "© 2026 KONG JUN SIENG"
  }
}
```

---

## 五、数据文件关系图（AI 依赖指引）

```
src/messages/{lang}.json      ←  所有文本（导航 / Hero / Projects / Case Studies / About / Contact）
     │
src/data/profile.json         ←  name, slogan, skills, socialLinks, email (固定, 不翻译)
     │
src/data/projects.json        ←  slug, cover, techStack (固定, 不翻译)
     │                         └── titleKey → src/messages/{lang}.json
     │
src/data/case-studies.json    ←  slug, cover, techStack (固定, 不翻译)
     │                         ├── titleKey, descriptionKey → src/messages/{lang}.json
     │                         └── executiveSummary/problemStatement/targetUser/…/appendix → src/messages/{lang}.json
     │
public/
├── resume-zh.pdf            ←  简历 PDF 按语言命名
├── resume-ja.pdf
└── resume-en.pdf
```
