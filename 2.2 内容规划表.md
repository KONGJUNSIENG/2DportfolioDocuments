# 2.2 内容规划表

**项目：** KONG JUN SIENG — Software Developer Portfolio  
**文档目标：** 定义每个页面/区块的内容模板与字段映射，供 AI 自动填充数据到页面组件。

---

## 一、内容模板总览

| 页面/区块 | 内容来源 | 多语 | 格式 |
|-----------|---------|------|------|
| 首页 Hero （section） | `profile.json` + `messages/{lang}.json` | 部分 | JSON + 文本 |
| 首页 Case Studies （section） | `case-studies.json` + `messages/{lang}.json` | 部分 | JSON |
| 首页 Projects （section） | `projects.json` + `messages/{lang}.json` | 部分 | JSON |
| 首页 About （section） | `messages/{lang}.json` + `profile.json` | 部分 | JSON + 文本 |
| 首页 Contact （section） | 硬编码 + `messages/{lang}.json` + `profile.json` | 部分 | 文本 |
| Case Study 详情页 | `src/data/*-{lang}.md` | 是 | Markdown |

---

## 二、各页面/区块内容模板明细

### 2.1 首页 Hero（section）

| 字段 | 类型 | 数据源 | 多语 | 示例值 |
|------|------|--------|------|--------|
| `name` | 文本 | `profile.json` | 否 | KONG JUN SIENG |
| `title` | 文本 | `messages/{lang}.json` | 是 | `zh`: Software Developer / 全栈探索者 |
| `slogan` | 文本 | `profile.json` | 否 | やればできる！ |
| `ctaText` | 文本 | `messages/{lang}.json` | 是 | `zh`: 查看作品 |

`profile.json` 提取字段：

```json
{
  "name": "KONG JUN SIENG",
  "slogan": "やればできる！"
}
```

`messages/{lang}.json` 对应路径：

```jsonc
{
  "hero": {
    "title": "Software Developer / 全栈探索者",
    "ctaText": "查看作品"
  }
}
```

---

### 2.2 首页 Case Studies（section）

卡片式布局，与 Projects 区块一致：封面图 + 标题 + 技术标签 + "查看详情"按钮，点击跳转至 `/[lang]/projects/[slug]`。初始仅 Portfolio 项目的 Case Study。

| 字段 | 类型 | 数据源 | 多语 |
|------|------|--------|------|
| `sectionTitle` | 文本 | `messages/{lang}.json` | 是 |
| `caseStudies[]` | 数组 | `case-studies.json` | 标题/描述通过 key 映射到 locales |

`case-studies.json`：

```json
{
  "caseStudies": [
    {
      "slug": "portfolio",
      "cover": "/images/portfolio-cover.png",
      "techStack": ["Next.js", "Framer Motion"],
      "titleKey": "caseStudies.portfolio.title",
      "descriptionKey": "caseStudies.portfolio.description"
    }
  ]
}
```

`messages/{lang}.json` 对应路径：

```jsonc
{
  "caseStudies": {
    "sectionTitle": "案例研究",
    "portfolio": {
      "title": "2D Portfolio 开发案例",
      "description": "一段简短的 Case Study 描述，概括该网站的设计与 AI 协作开发过程"
    },
    "viewButton": "查看详情"
  }
}
```

每条 Case Study 渲染为一个卡片，包含封面图 + 技术标签 + 标题 + 描述 + "查看详情"按钮，点击跳转至 `/[lang]/projects/[slug]`。

---

### 2.3 首页 Projects（section）

纯展示卡片，无跳转。初始 2 个项目卡片：FYP IoT、Portfolio。

| 字段 | 类型 | 数据源 | 多语 |
|------|------|--------|------|
| `sectionTitle` | 文本 | `messages/{lang}.json` | 是 |
| `projects[]` | 数组 | `projects.json` | 标题通过 key 映射到 locales |

`projects.json`：

```json
{
  "projects": [
    {
      "slug": "fyp-iot",
      "cover": "/images/fyp-iot-cover.png",
      "techStack": ["ESP32", "MQTT", "React"],
      "titleKey": "projects.fypIot.title"
    },
    {
      "slug": "portfolio",
      "cover": "/images/portfolio-cover.png",
      "techStack": ["Next.js", "Framer Motion"],
      "titleKey": "projects.portfolio.title"
    }
  ]
}
```

`messages/{lang}.json` 对应路径：

```jsonc
{
  "projects": {
    "sectionTitle": "作品",
    "fypIot": {
      "title": "FYP IoT 项目"
    },
    "portfolio": {
      "title": "2D Portfolio"
    }
  }
}
```

---

### 2.4 首页 About（section）

| 字段 | 类型 | 数据源 | 多语 | 说明 |
|------|------|--------|------|------|
| `bio` | 文本（一段话） | `messages/{lang}.json` | 是 | 约 20-50 字 |
| `skills` | 字符串数组 | `profile.json` | 否 | `["Figma", "React", "IoT", "TypeScript"]` |

`profile.json` 提取字段：

```json
{
  "skills": ["Figma", "React", "IoT", "TypeScript", "Python", "日语 N4"]
}
```

`messages/{lang}.json` 对应路径：

```jsonc
{
  "about": {
    "bio": "一段话的个人介绍，描述经历、技术栈、工作风格..."
  }
}
```

---

### 2.5 首页 Contact（section）

包含联系表单（UI 展示）、社交链接、简历下载按钮（按语言下载对应 PDF）。

| 字段 | 类型 | 数据源 | 多语 |
|------|------|--------|------|
| `email` | 文本 | 硬编码 | 否 |
| `socialLinks[]` | 数组 | `profile.json` | 否 |
| `sectionTitle` | 文本 | `messages/{lang}.json` | 是 |
| `formLabels` | 对象 | `messages/{lang}.json` | 是 |
| `resumeBtnText` | 文本 | `messages/{lang}.json` | 是 |

硬编码字段：

```
email: js.kong@outlook.com
```

`profile.json` 提取字段：

```json
{
  "socialLinks": [
    { "platform": "GitHub", "url": "https://github.com/KONGJUNSIENG" },
    { "platform": "LinkedIn", "url": "https://linkedin.com/in/..." }
  ]
}
```

简历下载文件（按语言分离）：

| 语言 | 文件路径 |
|------|---------|
| zh | `public/resume-zh.pdf` |
| ja | `public/resume-ja.pdf` |
| en | `public/resume-en.pdf` |

下载逻辑：根据当前 `lang` 拼接 `resume-{lang}.pdf`。

`messages/{lang}.json` 对应路径：

```jsonc
{
  "contact": {
    "sectionTitle": "联系我",
    "resumeBtnText": "下载简历",
    "formName": "姓名",
    "formEmail": "邮箱",
    "formMessage": "留言",
    "formSubmit": "发送"
  }
}
```

---

### 2.6 Case Study 详情页 `/[lang]/projects/[slug]`

通过 URL 直接访问，导航栏无入口。每个 Case Study 3 个 Markdown 文件：

```
src/data/
├── portfolio-zh.md
├── portfolio-ja.md
└── portfolio-en.md
```

**Markdown 模板：**

```markdown
---
title: "2D Portfolio 开发案例"
slug: "portfolio"
cover: "/images/portfolio-cover.png"
techStack: ["Next.js", "Framer Motion"]
role: "全栈开发者 / 设计师"
date: "2026"
---

## 背景

一段话描述项目背景。

## 目标

一段话描述项目目标。

## 我的角色

一段话描述承担的角色与职责。

## 过程

一段话描述开发/设计过程。

## 关键决策

一段话描述关键决策与原因。

## 结果

一段话描述项目成果、数据与反馈。
```

Frontmatter 说明：

| 字段 | 多语 | 说明 |
|------|------|------|
| `title` | 是 | 当前语言的标题 |
| `slug` | 否 | 与 URL 匹配 |
| `cover` | 否 | 封面图片路径（详情页头部展示） |
| `techStack` | 否 | 技术栈数组 |
| `role` | 是 | 当前语言的角色描述 |
| `date` | 否 | 项目日期 |

---

## 三、全局共享内容

### 导航栏

| 导航项 | 动作 | 说明 |
|--------|------|------|
| Logo / 站点名 | → `/[lang]` | 回到首页顶部 |
| 案例 | → `/[lang]#case-studies` | 锚点跳转至首页 Case Studies 区块 |
| 作品 | → `/[lang]#projects` | 锚点跳转至首页 Projects 区块 |
| 关于 | → `/[lang]#about` | 锚点跳转至首页 About 区块 |
| 联系 | → `/[lang]#contact` | 锚点跳转至首页 Contact 区块 |
| 语言切换 | 切换 `[lang]` 前缀 | 中 / 日 / EN |

导航文本来自 `messages/{lang}.json`：

```jsonc
{
  "nav": {
    "caseStudies": "案例",
    "projects": "作品",
    "about": "关于",
    "contact": "联系"
  }
}
```

### 页脚

| 字段 | 类型 | 数据源 | 多语 |
|------|------|--------|------|
| `copyright` | 文本 | `messages/{lang}.json` | 是 |
| `socialLinks` | 数组 | `profile.json` | 否 |

```jsonc
{
  "footer": {
    "copyright": "© 2026 KONG JUN SIENG"
  }
}
```

---

## 四、i18n 字段索引表（AI 生成 locales JSON 用）

```jsonc
// src/messages/zh.json — 完整结构参考
{
  "nav": {
    "caseStudies": "案例",
    "projects": "作品",
    "about": "关于",
    "contact": "联系"
  },
  "hero": {
    "title": "Software Developer / 全栈探索者",
    "ctaText": "查看作品"
  },
  "caseStudies": {
    "sectionTitle": "案例研究",
    "portfolio": {
      "title": "2D Portfolio 开发案例",
      "description": "一段简短的 Case Study 描述"
    },
    "viewButton": "查看详情"
  },
  "projects": {
    "sectionTitle": "作品",
    "fypIot": {
      "title": "FYP IoT 项目"
    },
    "portfolio": {
      "title": "2D Portfolio"
    }
  },
  "about": {
    "bio": "一段话的个人介绍..."
  },
  "contact": {
    "sectionTitle": "联系我",
    "resumeBtnText": "下载简历",
    "formName": "姓名",
    "formEmail": "邮箱",
    "formMessage": "留言",
    "formSubmit": "发送"
  },
  "footer": {
    "copyright": "© 2026 KONG JUN SIENG"
  }
}
```

---

## 五、数据文件关系图（AI 依赖指引）

```
src/messages/{lang}.json      ←  所有文本（导航 / Hero / Projects / Case Studies / About / Contact）
     │
src/data/profile.json         ←  name, slogan, skills, socialLinks, email (固定, 不翻译)
     │
src/data/projects.json        ←  slug, cover, techStack (固定, 不翻译)
     │                         └── titleKey → src/messages/{lang}.json
     │
src/data/case-studies.json    ←  slug, cover, techStack (固定, 不翻译)
     │                         └── titleKey, descriptionKey → src/messages/{lang}.json
     │
src/data/
├── portfolio-zh.md          ←  Case Study 按语言拆分
├── portfolio-ja.md
└── portfolio-en.md
     │
public/
├── resume-zh.pdf            ←  简历 PDF 按语言命名
├── resume-ja.pdf
└── resume-en.pdf
```
