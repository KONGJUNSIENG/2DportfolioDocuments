# 7.2 SEO 与访问性说明

**项目：** KONG JUN SIENG — Software Developer Portfolio  
**文档目标：** 定义 SEO meta/OG/sitemap/alt 与可访问性规则，AI 按此生成 SEO 配置和检查代码合规。  
**Lighthouse 目标：** Performance ≥ 90, Accessibility ≥ 95, SEO ≥ 95  
**无障碍标准：** WCAG 2.1 Level A

---

## 一、SEO 规范

### 1.1 页面 SEO 配置总表

| 页面 | 路由 | `title` | `description` | OG Image | `robots` |
|------|------|---------|---------------|----------|----------|
| 首页 | `/[lang]` | `{name} — {title}` | `{lang}` 品牌介绍 | OG 封面（可复用 `portfolio-cover.png`） | `index, follow` |
| 游戏风序幕 | `/[lang]/intro` | `{name} — Portfolio` | 游戏风风格的简短描述 | 使用 `hero-character.png` | `index, follow` |
| Case Study 详情页 | `/[lang]/projects/[slug]` | `{frontmatter.title} — {name}` | `{lang}` Case Study 摘要 | 对应封面图 | `index, follow` |
| 404 页面 | `/not-found` | `404 — {name}` | 页面未找到 | — | `noindex, nofollow` |

### 1.2 实现方式

所有页面使用 Next.js `generateMetadata` 动态生成 meta 标签：

```typescript
// src/app/[lang]/page.tsx — 首页 SEO 示例
export async function generateMetadata({
  params,
}: {
  params: { lang: string }
}): Promise<Metadata> {
  const profile = getProfile()
  const t = await getMessages(params.lang)

  return {
    title: `${profile.name} — ${t.hero.title}`,
    description: t.about.bio,
    openGraph: {
      title: `${profile.name} — ${t.hero.title}`,
      description: t.about.bio,
      siteName: profile.name,
      locale: params.lang,
      type: "website",
      images: [{ url: "/images/portfolio-cover.png", width: 1200, height: 675 }],
    },
    twitter: {
      card: "summary_large_image",
      title: `${profile.name} — ${t.hero.title}`,
      description: t.about.bio,
    },
    alternates: {
      languages: {
        zh: "/zh",
        ja: "/ja",
        en: "/en",
      },
    },
  }
}
```

```typescript
// src/app/[lang]/projects/[slug]/page.tsx — 详情页 SEO 示例
export async function generateMetadata({
  params,
}: {
  params: { lang: string; slug: string }
}): Promise<Metadata> {
  const { frontmatter } = readCaseStudyDetail(params.slug, params.lang)
  const profile = getProfile()
  const t = await getMessages(params.lang)

  return {
    title: `${frontmatter.title} — ${profile.name}`,
    description: `${frontmatter.role} | ${frontmatter.date}`,
    openGraph: {
      title: `${frontmatter.title} — ${profile.name}`,
      images: [{ url: frontmatter.cover, width: 1200, height: 675 }],
    },
  }
}
```

### 1.3 Meta 字段规则

| 字段 | 规则 | 来源 |
|------|------|------|
| `<title>` | `{页面特定标题} — {姓名}`，≤60 字符 | `messages/{lang}.json` + `profile.json` |
| `<meta name="description">` | 每页独立描述，≤160 字符 | `messages/{lang}.json` / frontmatter |
| `<meta name="robots">` | 首页+详情页 `index,follow`；404 `noindex,nofollow` | 按页面设定 |
| `<meta charset="utf-8">` | Next.js 自动注入 | — |
| `<meta name="viewport">` | `width=device-width, initial-scale=1` | Next.js 自动注入 |
| `<link rel="canonical">` | 当前页面的标准 URL（含语言前缀） | 由 `next-intl` 自动处理 |

### 1.4 Open Graph 规则

| 属性 | 首页值 | 详情页值 |
|------|--------|---------|
| `og:title` | `{name} — {title}` | `{frontmatter.title} — {name}` |
| `og:description` | `about.bio` | `frontmatter.role \| date` |
| `og:image` | `/images/portfolio-cover.png` (1200×630) | `frontmatter.cover` (1200×675) |
| `og:url` | `/{lang}` | `/{lang}/projects/{slug}` |
| `og:type` | `website` | `article` |
| `og:locale` | `zh_CN` / `ja_JP` / `en_US` | 同左 |
| `og:site_name` | `{name}` | `{name}` |

### 1.5 Twitter Card 规则

| 属性 | 值 |
|------|-----|
| `twitter:card` | `summary_large_image` |
| `twitter:title` | 同 `og:title` |
| `twitter:description` | 同 `og:description` |
| `twitter:image` | 同 `og:image` |

### 1.6 多语言 SEO（Hreflang）

每个页面需输出 `alternate` 链接，告诉搜索引擎不同语言版本的对应关系：

```html
<link rel="alternate" hreflang="zh" href="https://junsieng-portfolio.vercel.app/zh" />
<link rel="alternate" hreflang="ja" href="https://junsieng-portfolio.vercel.app/ja" />
<link rel="alternate" hreflang="en" href="https://junsieng-portfolio.vercel.app/en" />
<link rel="alternate" hreflang="x-default" href="https://junsieng-portfolio.vercel.app/zh" />
```

由 `generateMetadata` 中的 `alternates.languages` 实现。

### 1.7 结构化数据（JSON-LD）

首页添加 Person + WebSite Schema，帮助搜索引擎理解页面内容并展示丰富摘要：

#### Person Schema

```typescript
// src/lib/json-ld.ts
import type { Profile } from "./profile.schema"

export function generatePersonJsonLd(profile: Profile) {
  return {
    "@context": "https://schema.org",
    "@type": "Person",
    name: profile.name,
    url: "https://junsieng-portfolio.vercel.app",
    image: "https://junsieng-portfolio.vercel.app/images/hero-character.png",
    jobTitle: "Software Developer",
    knowsAbout: profile.skills,
    sameAs: Object.values(profile.socialLinks),
    email: profile.email,
  }
}
```

#### WebSite Schema

```typescript
export function generateWebSiteJsonLd() {
  return {
    "@context": "https://schema.org",
    "@type": "WebSite",
    name: "KONG JUN SIENG — Software Developer Portfolio",
    url: "https://junsieng-portfolio.vercel.app",
    inLanguage: ["zh", "ja", "en"],
  }
}
```

#### 注入方式

在根 `layout.tsx` 或 `[lang]/layout.tsx` 中通过 `<script type="application/ld+json">` 注入：

```tsx
// app/[lang]/layout.tsx
import { generatePersonJsonLd, generateWebSiteJsonLd } from "@/lib/json-ld"

export default function LocaleLayout({ children, params: { lang } }) {
  const profile = getProfile()

  const jsonLd = {
    ...generatePersonJsonLd(profile),
    ...generateWebSiteJsonLd(),
  }

  return (
    <>
      <script
        type="application/ld+json"
        dangerouslySetInnerHTML={{ __html: JSON.stringify(jsonLd) }}
      />
      {children}
    </>
  )
}
```

---

## 二、语义 HTML 与标题层级

### 2.1 各页面标题层级结构

#### 首页 `/[lang]`

```
<h1> KONG JUN SIENG                    ← Hero Name（唯一 h1）
  <p> やればできる！                     ← Slogan（不占标题层级）
  <p> Software Developer / 全栈探索者     ← Title（不占标题层级）

<section id="case-studies">
  <h2> 案例研究                          ← Section 标题

<section id="projects">
  <h2> 作品                             ← Section 标题

<section id="about">
  <h2> 关于                             ← Section 标题

<section id="contact">
  <h2> 联系我                            ← Section 标题
```

| 规则 | 说明 |
|------|------|
| `h1` | 每页仅一个，内容为 `{name}` |
| `h2` | 每个 Section 标题使用 `h2` |
| `h3` | 卡片标题（Case Study / Project 卡片）使用 `h3` |
| 层级不可跳跃 | `h1` → `h2` → `h3`，不可 `h1` → `h3` |

#### Case Study 详情页 `/[lang]/projects/[slug]`

```
<h1> 2D Portfolio 开发案例                ← 详情页标题（唯一 h1）
  <p> 全栈开发者 / 设计师 | 2026        ← 角色 + 日期
  <div> [Next.js] [Framer Motion]       ← 标签

<h2> 背景                                ← Markdown 章节标题（h2）
<h2> 目标                                ← h2
<h2> 我的角色                            ← h2
<h2> 过程                                ← h2
<h2> 关键决策                            ← h2
<h2> 结果                                ← h2
```

#### 404 页面

```
<h1> 404 — 页面未找到                    ← 唯一 h1
<p> 返回首页                              ← 链接
```

### 2.2 语义标签使用规则

| 元素 | 用途 | 禁用场景 |
|------|------|---------|
| `<nav>` | 导航栏 | 页面中仅导航栏使用 |
| `<main>` | 主内容区域 | 每个页面仅一个 |
| `<section>` | 首页各区块 | 每个内容区块 |
| `<article>` | 独立内容单元 | Case Study 详情页正文 |
| `<footer>` | 页脚 | 页面底部 |
| `<aside>` | 侧边栏装饰 | 本项目不使用 |
| `<button>` | 可点击交互元素 | 非链接的点击操作 |
| `<a>` | 页面跳转 | 按钮跳转不要用 `<div onClick>` |

---

## 三、Sitemap 与 Robots

### 3.1 sitemap.xml

```typescript
// src/app/sitemap.ts
import { MetadataRoute } from "next"

const locales = ["zh", "ja", "en"] as const
const defaultLocale = "zh"

export default function sitemap(): MetadataRoute.Sitemap {
  const baseUrl = "https://junsieng-portfolio.vercel.app"

  const entries: MetadataRoute.Sitemap = []

  for (const locale of locales) {
    // 首页
    entries.push({
      url: `${baseUrl}/${locale}`,
      lastModified: new Date(),
      changeFrequency: "monthly",
      priority: locale === defaultLocale ? 1.0 : 0.9,
      alternates: {
        languages: {
          zh: `${baseUrl}/zh`,
          ja: `${baseUrl}/ja`,
          en: `${baseUrl}/en`,
        },
      },
    })

    // 游戏风序幕
    entries.push({
      url: `${baseUrl}/${locale}/intro`,
      lastModified: new Date(),
      changeFrequency: "monthly",
      priority: 0.9,
    })

    // 详情页（当前仅 portfolio）
    entries.push({
      url: `${baseUrl}/${locale}/projects/portfolio`,
      lastModified: new Date(),
      changeFrequency: "monthly",
      priority: 0.8,
    })
  }

  return entries
}
```

**输出（9 条 URL）：**

| URL | Priority |
|-----|----------|
| `/zh` | 1.0 |
| `/ja` | 0.9 |
| `/en` | 0.9 |
| `/zh/intro` | 0.9 |
| `/ja/intro` | 0.9 |
| `/en/intro` | 0.9 |
| `/zh/projects/portfolio` | 0.8 |
| `/ja/projects/portfolio` | 0.8 |
| `/en/projects/portfolio` | 0.8 |

### 3.2 robots.txt

```typescript
// src/app/robots.ts
import { MetadataRoute } from "next"

export default function robots(): MetadataRoute.Robots {
  return {
    rules: {
      userAgent: "*",
      allow: "/",
      disallow: ["/api/"],
    },
    sitemap: "https://junsieng-portfolio.vercel.app/sitemap.xml",
  }
}
```

---

## 四、图片优化与 Alt 文本

### 4.1 Alt 文本规则

| 图片 | Alt 文本 | 规则来源 |
|------|---------|---------|
| Hero 立绘 | `"KONG JUN SIENG"`（品牌名） | [[4.3 视觉资产与使用规范.md#4.2]] |
| Case Study 封面 | `{caseTitle}` | 从 `messages/{lang}.json` 读取 |
| Project 封面 | `{projectTitle}` | 从 `messages/{lang}.json` 读取 |
| 详情页封面 | `{frontmatter.title}` | 从 Markdown frontmatter 读取 |
| 社交图标 | `"{platform}"`（如 `"GitHub"`） | 命名即可 |
| 装饰性元素 | `alt=""`（空） | 告知屏幕阅读器跳过 |

### 4.2 图片加载规则

| 图片 | `priority` | `loading` | `sizes` |
|------|-----------|-----------|---------|
| Hero 立绘 | `true` | — | `"(max-width: 768px) 70vw, 60vw"` |
| 卡片封面 | `false` | `lazy` | `"(max-width: 768px) 100vw, 1152px"` |
| 详情页封面 | `false` | `lazy` | `"(max-width: 768px) 100vw, 768px"` |

### 4.3 实现示例

```tsx
// CaseStudyCard.tsx — 正确的 alt 实现
<Image
  src={item.cover}
  alt={title}               // ← 从 messages 读取的翻译标题
  width={1200}
  height={675}
  loading="lazy"
  sizes="(max-width: 768px) 100vw, 1152px"
  className="w-full rounded-lg object-cover"
/>

// SocialLink.tsx — 图标 alt 实现
<Github className="w-5 h-5" aria-hidden="true" />
<span className="sr-only">{platform}</span>
```

---

## 五、URL 规范

### 5.1 路由结构

| 规则 | 示例 |
|------|------|
| 语言前缀 | `/[lang]/...` |
| 首页 | `/[lang]`（无尾部 `/`） |
| 游戏风序幕 | `/[lang]/intro` |
| 详情页 | `/[lang]/projects/[slug]` |
| 锚点 | `#case-studies`、`#projects` |
| 简历 | 直接文件路径 `/resume-{lang}.pdf` |

### 5.2 规则

- 所有路由小写
- slug 使用 kebab-case
- 无 URL 参数（纯静态）
- 无尾部斜杠（Next.js 默认行为）
- 404 页面通过 `not-found.tsx` 统一处理

---

## 六、无障碍（Accessibility）规范

### 6.1 WCAG 2.1 Level A 合规清单

#### 感知（Perceivable）

| 标准 | 要求 | 本项目实现 |
|------|------|-----------|
| 1.1.1 非文本内容 | 所有图片必须有 `alt` 文本（装饰性元素 `alt=""`） | §4.1 |
| 1.3.1 信息与关系 | 标题层级结构正确，Section 使用语义标签 | §二 |
| 1.4.1 颜色的使用 | 信息不单独依赖颜色传递 | 设计令牌确保文字/背景对比度 |
| 1.4.2 音频控制 | 无自动播放音频 | N/A |
| 1.4.3 对比度（最低） | 文字与背景对比度 ≥ 4.5:1 | 见 §6.2 对比度矩阵 |

#### 可操作（Operable）

| 标准 | 要求 | 本项目实现 |
|------|------|-----------|
| 2.1.1 键盘 | 所有交互元素可通过键盘操作 | §6.3 |
| 2.1.2 无键盘陷阱 | 焦点不可被困在元素内 | 自然 DOM 顺序 |
| 2.2.1 可调时间限制 | 无时间限制内容 | N/A |
| 2.2.2 暂停/停止/隐藏 | 动画持续时间 ≤5 秒或提供暂停 | prefers-reduced-motion §6.6 |
| 2.4.1 跳过块 | 提供"跳转到内容"链接 | §6.4 |
| 2.4.2 页面标题 | 每页有描述性 `<title>` | §1.2 |
| 2.4.3 焦点顺序 | 焦点顺序与 DOM 顺序一致 | 自然 DOM 顺序 |
| 2.4.4 链接目的（上下文内） | 链接文本描述目标 | 所有链接有描述性文本 |

#### 可理解（Understandable）

| 标准 | 要求 | 本项目实现 |
|------|------|-----------|
| 3.1.1 页面语言 | `<html lang="zh">` 等 | next-intl 自动设置 |
| 3.2.1 聚焦时 | 聚焦不触发上下文变化 | — |
| 3.2.2 输入时 | 输入不触发上下文变化 | — |
| 3.3.1 错误识别 | 表单错误描述 | 表单纯 UI，无验证逻辑 |
| 3.3.2 标签或说明 | 表单字段有 `<label>` | §6.5 |

#### 健壮（Robust）

| 标准 | 要求 | 本项目实现 |
|------|------|-----------|
| 4.1.1 解析 | HTML 语法正确 | JSX + Next.js 保证 |
| 4.1.2 名称、角色、值 | 交互元素有正确角色和名称 | 语义 HTML + ARIA 补充 |

### 6.2 色彩对比度矩阵

| 文本类型 | 前景色 | 背景色 | 对比度比 | WCAG AA 通过 |
|---------|--------|--------|---------|-------------|
| 主文字（正文/标题） | `#4A3F42` | `#FFFAFD`（页面背景） | **8.1:1** | ✅ |
| 主文字（正文/标题） | `#4A3F42` | `#FFFFFF`（卡片表面） | **7.5:1** | ✅ |
| 次要文字 | `#8E8386` | `#FFFAFD`（页面背景） | **2.8:1** | ❌（仅用于辅助文字） |
| 次要文字 | `#8E8386` | `#FFFFFF`（卡片表面） | **2.5:1** | ❌ |
| CTA 按钮文字 | `#FFFFFF` | `#FFB7C5`（主色） | **1.8:1** | ❌ |

> ⚠️ 当前设计令牌在 **CTA 按钮文字（白字粉色底）** 和 **次要文字（灰色）** 上不满足 WCAG AA 4.5:1 的对比度要求。Level A 不要求 1.4.3 对比度（那是 AA 级别的要求），所以 Level A 下无需修改。如果未来升级到 AA，需要：
> - CTA 按钮：使用更深的主色 `#E8919F` 作为背景，或使用深色文字
> - 次要文字：加深至 `#6B6063`

### 6.3 键盘导航规范

| 元素 | 键盘操作 | 焦点指示 |
|------|---------|---------|
| 导航链接 | Tab 顺序聚焦，Enter 触发跳转 | `outline: 2px solid --color-primary` |
| CTA 按钮 | Tab 聚焦，Enter/Space 触发 | `outline: 2px solid --color-primary` |
| 语言切换按钮 | Tab 聚焦，Enter/Space 切换 | `outline: 2px solid --color-primary` |
| 卡片链接 | Tab 聚焦，Enter 跳转 | `outline: 2px solid --color-primary` |
| 表单输入 | Tab 顺序聚焦 | `border-color: --color-primary` |
| 汉堡菜单 | Tab 聚焦 ☰ 按钮，Enter 展开，ESC 关闭 | `outline: 2px solid --color-primary` |

**焦点样式实现：**

```css
/* globals.css */
:focus-visible {
  outline: 2px solid var(--color-primary);
  outline-offset: 2px;
  border-radius: var(--radius-md);
}

/* 鼠标点击时不显示 focus ring */
:focus:not(:focus-visible) {
  outline: none;
}
```

### 6.4 跳转到内容（Skip to Content）

在所有页面的第一个可聚焦元素位置添加"跳转到内容"链接：

```tsx
// components/SkipToContent.tsx
"use client"
import { useTranslations } from "next-intl"

export function SkipToContent() {
  const t = useTranslations("accessibility")

  return (
    <a
      href="#main-content"
      className="sr-only focus:not-sr-only focus:fixed focus:top-4 focus:left-4
                 focus:z-[100] focus:px-4 focus:py-2 focus:bg-surface
                 focus:text-text-primary focus:shadow-lg focus:rounded-md"
    >
      {t("skipToContent")}
    </a>
  )
}
```

在 `[lang]/layout.tsx` 中放在 `<Navbar>` 之前：

```tsx
<body>
  <SkipToContent />
  <Navbar />
  <main id="main-content">{children}</main>
  <Footer />
</body>
```

在 `messages/{lang}.json` 中添加：

```jsonc
{
  "accessibility": {
    "skipToContent": "跳转到主要内容"
  }
}
```

### 6.5 表单无障碍

```tsx
// ContactSection.tsx — 表单无障碍实现
<form>
  <div>
    <label htmlFor="contact-name">{t("contact.formName")}</label>
    <input
      id="contact-name"
      name="name"
      type="text"
      autoComplete="name"
      placeholder={t("contact.formName")}
    />
  </div>

  <div>
    <label htmlFor="contact-email">{t("contact.formEmail")}</label>
    <input
      id="contact-email"
      name="email"
      type="email"
      autoComplete="email"
      placeholder={t("contact.formEmail")}
    />
  </div>

  <div>
    <label htmlFor="contact-message">{t("contact.formMessage")}</label>
    <textarea
      id="contact-message"
      name="message"
      rows={4}
      placeholder={t("contact.formMessage")}
    />
  </div>

  <button type="submit">{t("contact.formSubmit")}</button>
</form>
```

> 当前表单纯 UI 展示，无需 `required` 或错误验证逻辑。

### 6.6 动效无障碍（prefers-reduced-motion）

用户在系统层面开启"减少动效"时，所有动画应跳过：

```css
/* globals.css — 已在 [[3.3 交互与动效说明.md#10.1]] 定义 */
@media (prefers-reduced-motion: reduce) {
  *, *::before, *::after {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
  }
}
```

| 动效 | 正常 | prefers-reduced-motion |
|------|------|----------------------|
| Hero 出场序列 | 500ms stagger | opacity 1（最终态） |
| Section 入场 | 500ms whileInView | 直接显示 |
| 卡片 hover | 300ms transform | 无动效 |
| 背景光晕脉冲 | 4s CSS animation | 静止 |
| 页面过渡 | 200ms fade | 直接切换 |
| 视差效果 | 0.8x 速度滚动 | 无视差 |

### 6.7 ARIA 使用规则

| 场景 | 使用方式 |
|------|---------|
| 导航栏 | `<nav aria-label="Main navigation">` |
| 汉堡菜单按钮 | `aria-expanded={isOpen}` + `aria-controls="mobile-menu"` |
| 移动端菜单 | `id="mobile-menu"` 与按钮 `aria-controls` 关联 |
| 语言切换器 | `<nav aria-label="Language selector">` |
| 图标按钮 | `<span className="sr-only">{说明文字}</span>` |
| 装饰性图标 | `aria-hidden="true"` |
| 卡片链接 | `<Link>` 本身具备语义，无需额外 ARIA |
| 表单字段 | 使用原生 `<label>`，不依赖 `aria-label` |
| 当前导航项 | `aria-current="page"` |

**避免的 ARIA 模式：**

| 禁止 | 理由 |
|------|------|
| `<div role="button">` | 使用原生 `<button>` |
| `<div role="link">` | 使用原生 `<a>` / `<Link>` |
| 过度使用 `aria-label` | 优先使用可见文本标签 |
| `role="tablist"` 等复杂模式 | 本项目无复杂交互组件 |

### 6.8 汉堡菜单无障碍实现

```tsx
// Navbar.tsx — 移动端菜单无障碍
const [isMenuOpen, setIsMenuOpen] = useState(false)

return (
  <>
    <button
      onClick={() => setIsMenuOpen(!isMenuOpen)}
      aria-expanded={isMenuOpen}
      aria-controls="mobile-menu"
      aria-label={isMenuOpen ? "关闭菜单" : "打开菜单"}
      className="lg:hidden"
    >
      {isMenuOpen ? <X size={24} /> : <Menu size={24} />}
    </button>

    <nav
      id="mobile-menu"
      className={`${isMenuOpen ? "block" : "hidden"} lg:hidden`}
      role="navigation"
      aria-label="Mobile navigation"
    >
      {/* 导航链接 */}
    </nav>
  </>
)
```

---

## 七、性能 SEO（Core Web Vitals）

### 7.1 目标指标

| 指标 | 目标值 | 实现方式 |
|------|--------|---------|
| LCP (Largest Contentful Paint) | ≤ 2.5s | Hero 立绘 `priority` 加载 + CDN |
| FID (First Input Delay) | ≤ 100ms | 最小化客户端 JS |
| CLS (Cumulative Layout Shift) | ≤ 0.1 | `next/image` 固定宽高 + 字体 `next/font` 零 CLS |

### 7.2 具体措施

| 措施 | 影响指标 | 参考文档 |
|------|---------|---------|
| Hero 图片 `priority={true}` | LCP | [[4.3 视觉资产与使用规范.md#4.1]] |
| 图片使用 `next/image` 自动优化 | LCP | [[4.3 视觉资产与使用规范.md#四]] |
| 字体自托管 `next/font` | CLS | [[4.1 技术栈与架构文档.md#1.2]] |
| 无 Google Fonts 网络请求 | LCP | [[4.1 技术栈与架构文档.md#九]] |
| Server Components 减少客户端 JS | FID | [[4.1 技术栈与架构文档.md#3.2]] |
| 静态 SSG 预渲染 | LCP | [[4.1 技术栈与架构文档.md#2.3]] |
| 暖色调阴影无重排 | CLS | [[3.1 风格指南.md#4.2]] |

---

## 八、Lighthouse 检查清单

部署/修改后运行 Lighthouse（Chrome DevTools → Lighthouse）验证：

### Performance（目标 ≥ 90）

- [ ] Hero 立绘使用 `priority={true}`
- [ ] 所有图片使用 `next/image` 组件
- [ ] 字体使用 `next/font` 自托管
- [ ] Server Components 最小化客户端 JS
- [ ] 无第三方脚本/字体请求
- [ ] 图片格式 AVIF + WebP 自动优化
- [ ] 预渲染 SSG 减少服务端负载

### Accessibility（目标 ≥ 95）

- [ ] 所有图片有 `alt` 文本
- [ ] 标题层级正确（h1 → h2 → h3，不跳跃）
- [ ] `prefers-reduced-motion` 已实现
- [ ] 表单字段有 `<label>`
- [ ] 导航栏包含 `aria-label`
- [ ] 汉堡菜单使用 `aria-expanded` + `aria-controls`
- [ ] 焦点样式 `:focus-visible` 已定义
- [ ] 跳转到内容链接已添加
- [ ] 语义 HTML 标签正确使用（`<nav>` `<main>` `<section>`）
- [ ] 装饰性图标使用 `aria-hidden="true"`
- [ ] 颜色不单独传递信息

### SEO（目标 ≥ 95）

- [ ] 首页 `<title>` 包含品牌名
- [ ] 详情页 `<title>` 包含案例标题 + 品牌名
- [ ] 每页 `<meta name="description">` 独立
- [ ] Open Graph 标签完整（title/description/image/url/type）
- [ ] Twitter Card 标签完整
- [ ] Hreflang 标签正确输出
- [ ] `sitemap.xml` 可访问（含所有 6 页）
- [ ] `robots.txt` 正确配置
- [ ] JSON-LD 结构化数据已注入
- [ ] `<html lang="...">` 正确设置
- [ ] `<link rel="canonical">` 正确指向当前页面
- [ ] 游戏风序幕页面 `/intro` 有对应的 title/description/OG
- [ ] sitemap.xml 含 intro 页面（共 9 条 URL）


