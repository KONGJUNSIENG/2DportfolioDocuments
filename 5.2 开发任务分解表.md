# 5.2 开发任务分解表

**项目：** KONG JUN SIENG — Software Developer Portfolio  
**文档目标：** SDLC 分阶段任务列表、依赖关系与执行顺序，AI 代理按任务单元逐步推进开发。  
**总任务数：** 88 个（P0: 81 / P1: 2 / P2: 5）  
**估算精度：** T-shirt Sizes（XS ≤30min / S ≤2h / M ≤4h / L ≤8h / XL ≥2d）

---

## 一、任务表说明

| 列 | 说明 |
|----|------|
| ID | `T{阶段}.{序号}`，如 `T1.1` |
| 任务 | 具体操作描述 |
| 优先级 | P0=MVP 必须 / P1=上线后添加 / P2=未来扩展 |
| 依赖 | 前置任务 ID，完成后方可开始 |
| 工作量 | XS / S / M / L / XL |
| 验收标准 | 完成该任务的可验证条件 |
| 产出文件 | 任务创建或修改的主要文件 |

---

## 二、任务分解表

### Phase 1：项目初始化与环境搭建

| ID | 任务 | 优先级 | 依赖 | 工作量 | 验收标准 | 产出文件 |
|----|------|--------|------|--------|---------|---------|
| T1.1 | `pnpm create next-app@latest` 创建项目骨架（App Router + TS + Tailwind + ESLint） | P0 | — | S | `pnpm run dev` 成功启动空白页面 | `package.json`, `tsconfig.json`, `next.config.ts` |
| T1.2 | 配置 `tsconfig.json` strict 模式 + path alias `@/*` | P0 | T1.1 | XS | `tsc --noEmit` 无报错 | `tsconfig.json` |
| T1.3 | 安装所有生产依赖：`next-intl` `framer-motion` `zod` `gray-matter` `react-markdown` `remark-gfm` `lucide-react` `class-variance-authority` `clsx` `tailwind-merge` | P0 | T1.1 | S | `pnpm ls` 列出所有依赖 | `package.json` |
| T1.4 | 安装所有开发依赖：`typescript` `tailwindcss` `@tailwindcss/postcss` `postcss` `eslint` `@types/node` `@types/react` `@types/react-dom` `eslint-config-next` `prettier` | P0 | T1.1 | S | 开发依赖正确写入 `package.json` | `package.json` |
| T1.5 | 配置 Tailwind v4 自定义设计令牌：`globals.css` 中添加 `@import "tailwindcss"` + `@theme` 声明（颜色/字体/间距/圆角/阴影/容器宽度/动效时长/缓动函数） | P0 | T1.1 | M | 令牌值与 [[3.1 风格指南.md#九]] 完全一致 | `src/styles/globals.css` |
| T1.6 | 配置 Prettier（semi:false, singleQuote:false, tabWidth:2, trailingComma:all, printWidth:100） | P0 | T1.1 | XS | `pnpm run format` 按配置格式化代码 | `.prettierrc` |
| T1.7 | 创建 `src/lib/fonts.ts`：声明 jaFont / enFont / zhFont 三个 next/font/local 实例 | P0 | T1.1 | S | 三个字体实例导出 CSS 变量 | `src/lib/fonts.ts` |
| T1.8 | 根 `layout.tsx`：html + body + 字体 CSS 变量注入 | P0 | T1.7 | S | 页面加载后 `:root` 含 `--font-ja` / `--font-en` / `--font-zh` | `src/app/layout.tsx` |
| T1.9 | 配置 next-intl：`proxy.ts`（locales: [zh,ja,en], defaultLocale: zh）+ `i18n/request.ts` + 占位 messages JSON（Next.js 16 已弃用 middleware.ts，改用 proxy.ts） | P0 | T1.3 | M | 访问 `/` 重定向到 `/zh`，访问 `/ja` 正常渲染 | `src/proxy.ts`, `src/i18n/request.ts`, `src/messages/{zh,ja,en}.json` |
| T1.10 | 搭建目录结构：`src/components/` `src/hooks/` `src/i18n/` `src/lib/` `src/data/` `src/messages/` `src/styles/` `public/images/` `public/fonts/` | P0 | T1.1 | XS | 目录树与 [[4.2 项目文件结构文档.md#一]] 匹配 | 目录结构 |
| T1.11 | 配置 `.env.example` + `.github/workflows/ci.yml`（lint + typecheck + build） | P0 | T1.4 | M | `pnpm run lint` / `typecheck` / `build` 均有对应 npm script | `.env.example`, `.github/workflows/ci.yml` |

### Phase 2：数据层与类型系统

| ID | 任务 | 优先级 | 依赖 | 工作量 | 验收标准 | 产出文件 |
|----|------|--------|------|--------|---------|---------|
| T2.1 | Zod schema：`ProfileSchema`（name, slogan, skills, socialLinks, email）+ 推断类型 | P0 | T1.3 | S | `Profile` 类型与 [[4.1 技术栈与架构文档.md#4.3]] 一致 | `src/lib/profile.schema.ts` |
| T2.2 | Zod schema：`ProjectSchema` + `ProjectListSchema`（slug, cover, techStack, titleKey） | P0 | T1.3 | S | `Project` 类型匹配 [[2.2 内容规划表.md#2.3]] | `src/lib/projects.schema.ts` |
| T2.3 | Zod schema：`CaseStudySchema` + `CaseStudyListSchema`（slug, cover, techStack, titleKey, descriptionKey） | P0 | T1.3 | S | `CaseStudy` 类型匹配 [[2.2 内容规划表.md#2.2]] | `src/lib/case-studies.schema.ts` |
| T2.4 | 创建 `src/data/profile.json`（name, slogan, skills, socialLinks, email） | P0 | T2.1 | XS | 字段完整，Zod parse 通过 | `src/data/profile.json` |
| T2.5 | 创建 `src/data/projects.json`（fyp-iot + portfolio 两条） | P0 | T2.2 | XS | 字段完整，Zod parse 通过 | `src/data/projects.json` |
| T2.6 | 创建 `src/data/case-studies.json`（portfolio 一条） | P0 | T2.3 | XS | 字段完整，Zod parse 通过 | `src/data/case-studies.json` |
| T2.7 | 创建三语 messages JSON：`zh.json` / `ja.json` / `en.json`（nav, hero, caseStudies, projects, about, contact, footer 全字段） | P0 | T1.9 | M | 结构与 [[2.2 内容规划表.md#四]] 完全一致，三语 key 完整 | `src/messages/zh.json`, `src/messages/ja.json`, `src/messages/en.json` |
| T2.8 | 创建三语 Case Study Markdown：`portfolio-zh.md` / `portfolio-ja.md` / `portfolio-en.md`（frontmatter + 6 章节正文） | P0 | — | M | 6.1 实际内容需用户撰写，先填入占位符 | `src/data/portfolio-zh.md`, `src/data/portfolio-ja.md`, `src/data/portfolio-en.md` |
| T2.9 | 实现 `src/lib/read-data.ts`：通用 JSON 读取 + Zod parse + 错误处理，导出 `getProfile()` / `getProjects()` / `getCaseStudies()` | P0 | T2.1-T2.3 | M | 调用三个函数均返回类型安全的解析结果 | `src/lib/read-data.ts` |
| T2.10 | 实现 `src/lib/read-mdx.ts`：Markdown 读取 + gray-matter 解析 frontmatter + 导出 `readCaseStudyDetail(slug, lang)` | P0 | T1.3 | M | 解析 portfolio-zh.md 返回 `{ frontmatter, content }` | `src/lib/read-mdx.ts` |
| T2.11 | 创建 `src/lib/utils.ts`：`cn()` 工具函数（clsx + tailwind-merge） | P0 | T1.3 | XS | `cn("a", false && "b", "c")` 返回 `"a c"` | `src/lib/utils.ts` |
| T2.12 | 创建 `src/lib/animations.ts`：全局 Framer Motion variants（`fadeInUp` / `staggerContainer` / `staggerItem` / `pageTransition` / `sectionTitle`） | P0 | T1.3 | S | variants 参数与 [[3.3 交互与动效说明.md#附录]] 完全一致 | `src/lib/animations.ts` |
| T2.13 | 创建 `src/styles/globals.css`：Tailwind 指令 + CSS 自定义属性声明（色板/字体/间距/圆角/动效时长）+ prefers-reduced-motion + @keyframes 模板 | P0 | T1.5 | S | CSS 变量值与 [[3.1 风格指南.md#九]] 完全一致 | `src/styles/globals.css` |

### Phase 3：全局布局与导航

| ID | 任务 | 优先级 | 依赖 | 工作量 | 验收标准 | 产出文件 |
|----|------|--------|------|--------|---------|---------|
| T3.1 | `SocialLink.tsx`：渲染社交链接图标 + 平台名，props: `{ platform, url }` | P0 | T2.11 | XS | 点击跳转对应 URL，Lucide 图标 `strokeWidth={1.5}` | `src/components/SocialLink.tsx` |
| T3.2 | `SkillTag.tsx`：pill 形技能标签，`rounded-full`, `bg-primary-soft`, `text-xs` | P0 | T2.11 | XS | 标签样式与 [[3.1 风格指南.md#5.3]] 一致 | `src/components/SkillTag.tsx` |
| T3.3 | `LanguageSwitcher.tsx`：三语按钮（中/日/EN），当前语言 `--color-primary` 高亮，`useLocale` + `usePathname` + `useRouter` 驱动 | P0 | T3.1 | S | 点击切换语言后 URL 前缀变化，页面内容更新 | `src/components/LanguageSwitcher.tsx` |
| T3.4 | `Navbar.tsx`：固定顶部（h-16, z-50），背景 `rgba(255,250,253,0.85)` + `backdrop-blur-md`，滚动 >50px 渐入阴影，Desktop 完整链接 + Mobile 汉堡菜单（Framer Motion AnimatePresence） | P0 | T2.7, T3.3 | L | 4 个锚点链接正确跳转；滚动后阴影渐入；Mobile 点击汉堡展开/关闭 | `src/components/Navbar.tsx` |
| T3.5 | `Footer.tsx`：版权信息（`messages/{lang}.json`）+ `SocialLink` ×2（GitHub / LinkedIn），居中布局 | P0 | T2.4, T2.7, T3.1 | S | Footer 正确显示版权和社交图标 | `src/components/Footer.tsx` |
| T3.6 | `ScrollToAnchor.tsx`：平滑滚动 + 导航栏高度偏移补偿（`scroll-mt-16`） | P0 | — | XS | 锚点点击后滚动到正确位置（不被 Navbar 遮挡） | `src/components/ScrollToAnchor.tsx` |
| T3.7 | `[lang]/layout.tsx`：NextIntlClientProvider + Navbar + Footer + 页面过渡（AnimatePresence mode="wait", key=pathname） | P0 | T3.4, T3.5 | M | 所有页面由 Navbar + Footer 包裹；语言切换时内容淡入淡出 | `src/app/[lang]/layout.tsx` |

### Phase 4：首页各区块

| ID | 任务 | 优先级 | 依赖 | 工作量 | 验收标准 | 产出文件 |
|----|------|--------|------|--------|---------|---------|
| T4.1 | `HeroSection.tsx`：桌面端 flex-row 左立绘右文字，背景光晕脉冲 + 立绘浮动 + 视差效果。手机端 absolute 叠加布局（立绘 absolute inset-0 填满全屏，文字 absolute bottom-0 浮在底部渐变上）。出场序列（立绘→Slogan→Name→Title→CTA 依次延迟 0/0.7/1.2/1.7/2.2s） | P0 | T2.7, T2.12, T2.13 | XL | 桌面端 5 元素按时序入场；Mobile 立绘填满全屏，文字浮在底部渐层，option button 无纵向拉伸 | `src/components/HeroSection.tsx` |
| T4.2 | `CaseStudyCard.tsx`：可复用卡片（封面 16:9 + 标签 + 标题 + 描述 + "查看详情" Ghost Button），hover `translateY(-4px) scale(1.02)` + 阴影加深，右侧 `Link` 跳转 `/[lang]/projects/[slug]` | P0 | T2.7, T2.11, T3.1 | S | 卡片样式与 [[3.2 原型与线框图.md#3.2]] 一致；hover 动画 300ms；点击跳转详情页 | `src/components/CaseStudyCard.tsx` |
| T4.3 | `CaseStudiesSection.tsx`：Section 标题 + T4.2 CaseStudyCard 列表（初始 1 张），`staggerContainer` + `staggerItem`，`whileInView once` | P0 | T2.6, T2.7, T4.2 | M | Section 标题先入场（y:15→0, 400ms）→ 卡片 stagger 入场；数据绑定正确 | `src/components/CaseStudiesSection.tsx` |
| T4.4 | `ProjectCard.tsx`：可复用卡片（封面 4:3 + 标签 + 标题，无按钮纯展示），hover 动画同 CaseStudyCard | P0 | T2.11 | S | 卡片样式与 [[3.2 原型与线框图.md#4.2]] 一致 | `src/components/ProjectCard.tsx` |
| T4.5 | `ProjectsSection.tsx`：Section 标题 + 双列网格（`grid-cols-2`, gap-6）+ T4.4 ProjectCard（fyp-iot + portfolio），`staggerContainer` + `staggerItem` 逐个入场 0.15s，Mobile 改单列 | P0 | T2.5, T2.7, T4.4 | M | 桌面双列 Mobile 单列；卡片逐个入场；数据绑定正确 | `src/components/ProjectsSection.tsx` |
| T4.6 | `AboutSection.tsx`：Section 标题 + bio 段落（max-w-prose 65ch）+ SkillTag 网格（flex-wrap, gap-2），标题→文字→标签 stagger | P0 | T2.4, T2.7, T3.2 | S | 布局与 [[3.2 原型与线框图.md#五]] 一致；技能标签正确渲染 | `src/components/AboutSection.tsx` |
| T4.7 | `ContactSection.tsx`：邮箱链接 + GitHub/LinkedIn 社交图标 + 简历下载按钮（无表单），内容与标题左对齐。简历下载 `<a href="/resume-{lang}.pdf" download>` | P0 | T2.4, T2.7, T3.1 | M | 布局与 [[3.2 原型与线框图.md#六]] 一致；简历按钮按当前语言拼接路径 | `src/components/ContactSection.tsx` |
| T4.8 | 首页 `[lang]/page.tsx`：Server Component，组合 T4.1 + T4.3 + T4.5 + T4.6 + T4.7，导出 `generateMetadata`（SEO title/description/OG） | P0 | T4.1, T4.3, T4.5, T4.6, T4.7 | M | 首页所有 Section 按顺序渲染；meta 标签正确 | `src/app/[lang]/page.tsx` |

### Phase 5：Case Study 详情页

| ID | 任务 | 优先级 | 依赖 | 工作量 | 验收标准 | 产出文件 |
|----|------|--------|------|--------|---------|---------|
| T5.1 | `projects/[slug]/page.tsx`：动态路由，`generateStaticParams` 返回所有 `(lang, slug)` 组合，`generateMetadata` 从 frontmatter 生成 SEO 信息 | P0 | T2.8, T2.10 | M | 访问 `/zh/projects/portfolio` 正常渲染；SSG 预渲染 3 个语言页面 | `src/app/[lang]/projects/[slug]/page.tsx` |
| T5.2 | 详情页布局实现：封面图全宽 16:9 + 标题（text-2xl）+ role + date + SkillTag + 分割线 + Markdown 正文（react-markdown）+ 返回首页按钮 | P0 | T5.1 | M | 布局与 [[3.2 原型与线框图.md#七]] 一致；Markdown 正确渲染 6 章节 | 同上 |
| T5.3 | 返回按钮：页面顶部和底部各一个 Ghost Button，"← 返回首页"，`Link href="/[lang]"` | P0 | T5.2 | XS | 点击返回首页，语言保持一致 | 同上 |

### Phase 6：动效与交互动画系统

| ID | 任务 | 优先级 | 依赖 | 工作量 | 验收标准 | 产出文件 |
|----|------|--------|------|--------|---------|---------|
| T6.1 | Hero 背景 CSS 动画：渐变光晕脉冲（`@keyframes hero-glow-pulse` 4s）+ 立绘轻微浮动（`@keyframes character-float` 4s），Mobile 禁用 | P0 | T2.13, T4.1 | S | 光晕 opacity 0.3→0.6 循环；立绘 -3px→3px 循环；Mobile 无此动画 | `src/styles/globals.css` |
| T6.2 | Section 入场 Framer Motion `whileInView` 注入：T4.3/T4.5/T4.6/T4.7 全部使用 `staggerContainer` + `staggerItem` + `sectionTitle`，`viewport: { once: true, amount: 0.2 }` | P0 | T2.12, T4.3, T4.5, T4.6, T4.7 | M | 滚动到每个 Section 时，标题先入场 → 内容 stagger 入场；只触发一次 | （修改已有 Section 组件） |
| T6.3 | Hero 视差效果：Desktop 启用，立绘 `useTransform(scrollYProgress, [0, 0.3], [0, -60])`，光晕 `opacity: [0.6, 0]`，`useSpring` 阻尼平滑。Tablet/Mobile 禁用 | P0 | T4.1 | M | 滚动时立绘慢速上移（0.8x）；光晕淡出；禁用条件正确 | `src/components/HeroSection.tsx` |
| T6.4 | 卡片 hover CSS 动画：`transition: transform 300ms ease-out, box-shadow 300ms ease-out;` :hover `translateY(-4px) scale(1.02)` + 阴影 md→lg | P0 | T2.11, T4.2, T4.4 | S | 鼠标悬停卡片时上浮 + 阴影加深；Mobile 无 hover 效果 | `src/components/CaseStudyCard.tsx`, `src/components/ProjectCard.tsx` |
| T6.5 | 导航栏滚动效果：`useScroll` + `useTransform(scrollY, [0, 50], ["transparent", shadow-sm])`，背景模糊常驻 | P0 | T3.4 | S | 初始无阴影，滚动 50px 后 shadow-sm 渐入 | `src/components/Navbar.tsx` |
| T6.6 | 创建 `src/hooks/useMediaQuery.ts`：响应式断点检测 hook，`useMediaQuery("(min-width: 1024px)")` 返回 boolean | P0 | — | XS | T6.3 视差条件依赖此 hook | `src/hooks/useMediaQuery.ts` |
| T6.7 | 语言切换过渡动画：`LocaleContent` 组件包裹内容，`AnimatePresence mode="wait"` + `key={locale}`，exit opacity 0→新内容 opacity 1 | P0 | T2.12, T3.7 | S | 切换语言时整体内容淡出/淡入；Hero 不重新播放出场序列 | 修改 `src/app/[lang]/layout.tsx` |
| T6.8 | 页面过渡动画：首页 ↔ 详情页之间 `opacity: 0→1`（200ms），exit `opacity: 1→0`（150ms），`mode="wait"` | P0 | T2.12, T3.7 | S | 导航到详情页时旧页面淡出 → 新页面淡入；无布局抖动 | 修改 `src/app/[lang]/layout.tsx` |

### Phase 7：VN 游戏感 UI 系统与首页集成

| ID | 任务 | 优先级 | 依赖 | 工作量 | 验收标准 | 产出文件 | 状态 |
|----|------|--------|------|--------|---------|---------|:---:|
| T7.1 | **新建 `DialogBox.tsx`**：VN 对话框。三层圆角：白框 `rounded-lg` → 名字框 `rounded-xl` + 文字框 `rounded-md`。底色 `#2FA4D7`。`md:min-h-[100px]`（桌面自适应高度），`min-h-[120px]`（手机）。内层 flex 1:3。Props: `{ characterName?, children, className? }` | P0 | T2.12 | M | 进入视口淡入上浮；白框随内容自动撑高；名字/文字框高度比例 1:3；文字白色 | `src/components/DialogBox.tsx` | ✅ |
| T7.2 | **新建 `ChapterHeader.tsx`**：VN 章节标题。Props: `{ chapterNumber?, title, subtitle?, className? }`。渲染：`CHAPTER 01` 标签（蓝色）+ 标题 + 蓝色下划线 | P0 | T2.12 | M | 进入视口左侧滑入（x:-30→0, 500ms） | `src/components/ChapterHeader.tsx` | ✅ |
| T7.3 | **新建 `OptionButton.tsx`**：VN 选项按钮。纯 CSS 单变体：蓝底白字 + ▶ 指示符。Props: `{ children, onClick?, className? }`。`whileHover:{scale:1.02,x:4}`，`whileTap:{scale:0.98}` | P0 | T2.11 | S | 蓝底白字按钮；hover 放大右移；tap 缩小 | `src/components/OptionButton.tsx` | ✅ |
| T7.4 | **新建 `SectionTransition.tsx`**：过渡台词组件。`~ text ~` + 水平贯穿线。Props: `{ text, className? }`。`transitionFade` variants：`scale:0.92→1, 400ms` | P0 | T2.7 | M | 滚动到视口时文字从中心淡入放大 | `src/components/SectionTransition.tsx` | ✅ |
| T7.5 | **蓝色色值注入 `globals.css`**：`--color-dialog-blue` / `--color-dialog-dark` / `--color-dialog-soft` 入 `@theme` + `:root` | P0 | — | XS | Tailwind 中 `bg-dialog-blue` 可用 | `src/styles/globals.css` | ✅ |
| T7.6 | **animations.ts 追加 VN variants**：`chapterReveal` / `dialogSlideUp` / `optionStagger` / `optionItem` / `transitionFade` | P0 | T2.12 | S | variants 在对应组件中使用 | `src/lib/animations.ts` | ✅ |
| T7.7 | **messages 新增 transitions 键**：三语四段过渡台词 | P0 | T2.7 | XS | 正确 | `src/messages/{zh,ja,en}.json` | ✅ |
| T7.8 | Hero CTA 改为 OptionButton；按钮加高 `py-4 md:py-6`（手机端/桌面端区分）；间距 `mt-8`→`mt-6`，Scroll ↓ `mt-6`→`mt-4` | P0 | T7.3 | S | CTA 蓝底白字；间距合理 | `HeroSection.tsx` | ✅ |
| T7.9 | CaseStudies 顶部加 ChapterHeader(01)。CaseStudyCard 保持原始结构（无 DialogBox） | P0 | T7.2 | S | 章节标题显示 | `CaseStudiesSection.tsx` | ✅ |
| T7.10 | Projects 顶部加 ChapterHeader(02) | P0 | T7.2 | S | 章节标题显示 | `ProjectsSection.tsx` | ✅ |
| T7.11 | About 顶部加 ChapterHeader(03)；bio 替换为 `<DialogBox characterName="KONG JUN SIENG">` | P0 | T7.1, T7.2 | M | 章节标题；对话框内自我介绍 | `AboutSection.tsx` | ✅ |
| T7.12 | Contact 顶部加 ChapterHeader(04, subtitle="SELECT AN OPTION")；联系方式改为 OptionButton 列表 | P0 | T7.2, T7.3 | S | 章节标题；四个选项按钮 | `ContactSection.tsx` | ✅ |
| T7.13 | page.tsx Section 之间插入 4 段 SectionTransition | P0 | T7.4 | M | 页面可见过渡线条 | `page.tsx` | ✅ |

### Phase 8：SEO 与部署

| ID | 任务 | 优先级 | 依赖 | 工作量 | 验收标准 | 产出文件 |
|----|------|--------|------|--------|---------|---------|
| T8.1 | 首页 `generateMetadata`：从 `profile.json` + `messages/{lang}.json` 动态生成 title/description/OG | P0 | T7.13 | S | `/zh` 的 `<title>` 包含中文品牌名；OG meta 完整 | `src/app/[lang]/page.tsx` |
| T8.2 | 详情页 `generateMetadata`：从 frontmatter 动态生成 title/description/OG | P0 | T5.1 | S | `/zh/projects/portfolio` 的 OG title 为 Case Study 标题 | `src/app/[lang]/projects/[slug]/page.tsx` |
| T8.3 | `sitemap.ts`：动态生成 sitemap.xml，包含所有 `(lang, slug)` 组合（3 语言 × 2 页面 = 6 URL） | P0 | T2.8 | S | 访问 `/sitemap.xml` 返回 6 个 `<url>` 条目 | `src/app/sitemap.ts` |
| T8.4 | `robots.ts`：允许所有爬虫，指向 sitemap | P0 | T8.3 | XS | 访问 `/robots.txt` 返回正确配置 | `src/app/robots.ts` |
| T8.5 | `not-found.tsx`：404 页面，"页面未找到" + 返回首页链接，与全站风格一致 | P0 | — | S | 访问未匹配路由时显示 404 页面 | `src/app/not-found.tsx` |
| T8.6 | 放置公共资源：字体文件（10 个 woff2）到 `public/fonts/`，占位图片（3 张 + 可选 UI 素材）到 `public/images/`，简历 PDF（3 个）到 `public/` | P0 | — | M | `public/` 下文件列表与 [[4.3 视觉资产与使用规范.md#一]] 一致 | `public/fonts/*`, `public/images/*`, `public/resume-*.pdf` |
| T8.7 | 最终 CI 验证：`pnpm run lint` + `pnpm run typecheck` + `pnpm run build` 全部通过 | P0 | T1.11, T8.1-T8.6 | XS | 三条命令均返回退出码 0 | — |

### Phase 9：VN 序幕（Intro Scenario）

| ID | 任务 | 优先级 | 依赖 | 工作量 | 验收标准 | 产出文件 |
|----|------|--------|------|--------|---------|---------|
| T9.1 | 创建 VN 序幕数据 `vn-scenario.{lang}.json`（43 场景），三语同步 | P0 | T2.7 | M | `pnpm run build` 通过 | `src/data/vn-scenario.{zh,ja,en}.json` |
| T9.2 | 创建 `TitleScreen.tsx`：标题画面（角色立绘 + Slogan + 名称 + 开始按钮），立绘/Slogan/名称/按钮依次 fadeInUp stagger 0.4s | P0 | T2.12, T4.1 | M | 按钮点击前画面停留，点击触发开始回调 | `src/components/TitleScreen.tsx` |
| T9.3 | 创建 `PixelsCurtain.tsx`：暗転遮罩（全屏黑色 + 随机光效粒子向外扩散消散，CSS animation），点击开始按钮后触发 | P0 | — | M | 遮罩 opacity 0→1(400ms)→停留(700ms)→消散(400ms)，完成后回调 | `src/components/PixelsCurtain.tsx` |
| T9.4 | 创建 `VNScenario.tsx`：场景演绎（43 场景，立绘表情切换 + 蓝色对话框 + 打字机逐字效果 + 章节标题），点击/按键推进 | P0 | T9.1, T7.1, T7.2 | L | 逐场景推进；打字机每字约 30ms；场景计数显示；最终场景触发完成回调 | `src/components/VNScenario.tsx` |
| T9.5 | 创建 `LoadingOverlay.tsx`：加载遮罩（粒子光效 + 角色剪影浮现 + 波纹扩散），Scenario 结束后触发 | P0 | — | M | 剪影 opacity 0→1(2000ms)；波纹 3 层交错；完成后回调 | `src/components/LoadingOverlay.tsx` |
| T9.6 | 创建 `LoadingRipple.tsx`：波纹扩散组件，3 层同心圆环向外扩散 | P0 | — | S | CSS @keyframes 驱动，周期性扩散 | `src/components/LoadingRipple.tsx` |
| T9.7 | 创建 `ScrambleText.tsx`：文字乱码闪烁效果，LoadingOverlay 中提示文字 | P0 | — | S | 文字随机字符闪烁后归位 | `src/components/ScrambleText.tsx` |
| T9.8 | 创建 `IntroShell.tsx`：VN 序幕流程编排器，管理四阶段状态切换（TitleScreen → 暗転 → Scenario → LoadingOverlay → 首页跳转） | P0 | T9.2-T9.7 | M | 四阶段顺序执行；首页跳转使用 `router.push('/[lang]')` | `src/components/IntroShell.tsx` |
| T9.9 | 创建 `src/app/[lang]/intro/page.tsx`：Intro 路由页面，渲染 IntroShell | P0 | T9.8 | S | 访问 `/zh/intro` 显示 VN 序幕；`/` 重定向至此 | `src/app/[lang]/intro/page.tsx` |
| T9.10 | 更新 `proxy.ts`：`/` → `/zh/intro` 重定向 | P0 | T9.9 | XS | 访问 `/` 自动跳转到 `/zh/intro` | `src/proxy.ts` |

### Phase 10：ProjectCard 点击 clip-path 圆形展开交互

| ID | 任务 | 优先级 | 依赖 | 工作量 | 验收标准 | 产出文件 |
|----|------|--------|------|--------|---------|---------|
| T10.1 | Zod schema 扩展：`projects.schema.ts` 追加 `backgroundKey` / `contributeKey` / `outcomeKey` 三个必填 `z.string().min(1)` 字段 | P0 | T2.2 | XS | `ProjectItem` 类型新增 3 字段，Zod parse 通过 | `src/lib/projects.schema.ts` |
| T10.2 | 数据扩展：`projects.json` 为 fyp-iot + portfolio 填写 backgroundKey/contributeKey/outcomeKey 引用值；`messages/{lang}.json` 追加三语对应的 background/contribute/outcome 文本 | P0 | T10.1, T2.7 | S | `pnpm run build` 通过 | `src/data/projects.json`, `src/messages/{lang}.json` |
| T10.3 | `ProjectCard.tsx` 重写：双层级 DOM 结构（初始内容=正常文档流决定卡片高度，详情内容=`absolute inset-0` 填满同一空间），点击切换 `expanded` 状态。Framer Motion `animate={{ clipPath }}` 驱动 `circle(100%) ↔ circle(0% at 50% 50%)` 圆形扩散/收缩动画，`overflow-hidden` 裁切。Desktop 0.6s / Mobile 0.3s（useMediaQuery 缩短 duration） | P0 | T10.2, T6.4, T6.6 | M | 点击卡片封面以圆形 clip-path 扩散消失；显示 slug/background/contribute/outcome 详情；再次点击收拢回初始 | `src/components/ProjectCard.tsx` |

### Phase 11：扩展功能（P1 — 上线后添加）

| ID | 任务 | 优先级 | 依赖 | 工作量 | 验收标准 | 产出文件 |
|----|------|--------|------|--------|---------|---------|
| T11.1 | 项目分类筛选：按 IoT / Web / 游戏等类型筛选，项目 ≥ 4 时启用 | P1 | T4.5 | M | 筛选按钮出现，点击后过滤卡片 | `src/components/ProjectsSection.tsx` |
| T11.2 | 返回顶部按钮：滚动超过视口高度后 Framer Motion 淡入淡出，点击平滑回到顶部 | P1 | — | S | 向下滚动后按钮出现，点击回到顶部 | `src/components/ScrollToTop.tsx` |

### Phase 12：未来扩展（P2 — 按需）

| ID | 任务 | 优先级 | 依赖 | 工作量 | 验收标准 | 产出文件 |
|----|------|--------|------|--------|---------|---------|
| T12.1 | 深色模式：CSS 变量 + 切换按钮，所有颜色 token 增加 `dark:` 变体 | P2 | T2.13 | L | 按钮切换亮/暗模式，颜色正确 | `src/styles/globals.css` |
| T12.2 | 页面过渡动画增强：Framer Motion AnimatePresence 路由过渡 | P2 | T6.8 | M | 页面切换有滑动或缩放过渡 | （修改 layout） |
| T12.3 | 访问统计：接入 Vercel Analytics 或 Plausible | P2 | — | S | 控制台显示分析脚本加载 | `.env.local` |
| T12.4 | 联系表单后端：接入 Formspree 或 Resend 实现真实发送 | P2 | T4.7 | M | 填写表单后可收到邮件 | `src/app/api/contact/route.ts` |
| T12.5 | 博客/笔记：`/[lang]/blog` 路由 + Markdown 渲染，初期完全不包含 | P2 | T2.10 | XL | 访问 `/zh/blog` 显示文章列表 | `src/app/[lang]/blog/` |

---

## 三、依赖关系图

```
Phase 1 ─── T1.1 → T1.2 → T1.3 → T1.4 → T1.5
                ↘                           ↓
                  T1.7 → T1.8           T1.6
                    ↓
Phase 2 ─── T1.3 → T2.1 → T2.4 ──→ T2.9 ──→ T2.11
                ↘    T2.2 → T2.5 ──→   ↑        ↓
                  ↘  T2.3 → T2.6 ──→   │      T2.12
                    ↘                  │        ↓
                  T1.9 → T2.7 ──────→ T2.13
                    ↓
                  T2.8 (Markdown 正文)
                    ↓
Phase 3 ─── T2.11 → T3.1 → T3.3 → T3.4 ──→ T3.7
                  ↘    T2.4 ─→ T3.5 ──→  ↑
                    ↘  T2.7 ─→   ↑        ↑
                            T3.2 ──→ T3.6
                                       ↓
Phase 4 ─── T4.1  T4.3  T4.5  T4.6  T4.7 → T4.8
                ↓     ↓     ↓     ↓     ↓
Phase 5 ─── T2.10 + T2.8 → T5.1 → T5.2 → T5.3
                ↓
Phase 6 ─── T2.12+T2.13+T3.4+T4.1→T4.7 → T6.1~T6.8
                 ↓
Phase 7 ─── T2.7+T2.11+T2.12 → T7.1~T7.7
                 ↓
            T7.1~T7.7 → T7.8~T7.13
                 ↓
Phase 8 ─── T7.13 → T8.1/T8.2/T8.3/T8.4/T8.5/T8.6 → T8.7
                 ↓
Phase 9 ─── T9.1 → T9.2~T9.7 → T9.8 → T9.9 → T9.10
      ↓
Phase 10 ─── T10.1 → T10.2 → T10.3
```

### 关键路径（Critical Path）

```
T1.1 → T1.3 → T2.1/T2.2/T2.3 → T2.9 → T2.11/T2.12
  → T3.1 → T3.4 → T3.7
  → T4.1/T4.3/T4.5/T4.6/T4.7 → T4.8
  → T5.1 → T5.2 → T5.3
  → T6.1/T6.2/T6.3/T6.4/T6.5/T6.6/T6.7/T6.8
  → T7.1/T7.2/T7.3/T7.4/T7.5/T7.6/T7.7
  → T7.8/T7.9/T7.10/T7.11/T7.12/T7.13
  → T8.1/T8.2/T8.3/T8.4/T8.5/T8.6 → T8.7
  → T9.1 → T9.2 → T9.3 → T9.4 → T9.5/T9.6/T9.7 → T9.8 → T9.9 → T9.10
  → T10.1 → T10.2 → T10.3
```

---

## 四、阶段依赖矩阵

| 阶段 | 前置阶段 | 包含任务 | 里程碑 | 验证方式 |
|------|---------|---------|--------|---------|
| Phase 1 初始化 | — | 11 | `pnpm run dev` 启动空白页面 + CI 流水线通过 | 本地启动 |
| Phase 2 数据层 | Phase 1 | 13 | 所有 JSON/MD 数据可被 `read-data.ts` 正确读取 | `tsc --noEmit` |
| Phase 3 布局 | Phase 1+2 | 7 | 导航栏+页脚包裹所有页面，i18n 切换正常 | 浏览器手动测试 |
| Phase 4 首页 | Phase 1+2+3 | 8 | 首页 5 个 Section 全部渲染，布局匹配线框图 | 浏览器比对 3.2 |
| Phase 5 详情页 | Phase 1+2+4 | 3 | 详情页正确渲染 Markdown 内容 | 访问 `/zh/projects/portfolio` |
| Phase 6 动效与交互动画 | Phase 2+5 | 8 | 背景光晕/浮动、Section stagger、视差、卡片 hover、导航栏滚动、语言/页面过渡全部按 3.3 参数执行 | 浏览器逐项验收 |
| Phase 7 VN UI 系统与首页集成 | Phase 3+4+5+6 | 13 | 所有 Section 使用 VN 组件（ChapterHeader/DialogBox/OptionButton/SectionTransition） | `pnpm run build` + 浏览器测试 |
| Phase 8 SEO+部署 | Phase 1-7 | 7 | sitemap/404/CI 全部通过 → Vercel 部署成功 | `pnpm run build` + Vercel |
| Phase 9 VN 序幕 | Phase 2+7+8 | 10 | VN 序幕可访问，`/` → `/zh/intro` 重定向 | 访问 `/zh/intro` |
| Phase 10 ProjectCard clip-path 展开 | Phase 2+7+9 | 3 | 点击 ProjectCard 圆形 clip-path 扩散展示详情 | `pnpm run build` + 浏览器测试 |
| Phase 11 P1 | Phase 4+7 | 2 | 筛选/返回顶部功能正常工作 | 浏览器测试 |
| Phase 12 P2 | 全部 | 5 | 按需启用 | — |

---

## 五、任务执行策略

### 5.1 执行顺序

| 策略 | 说明 |
|------|------|
| 阶段内串行 | 每个阶段按 ID 顺序执行（尊重依赖关系） |
| 阶段间串行 | Phase 1 → 2 → 3 → 4 → 5 → 6 → 7 → 8 → 9 |
| 并行条件 | 无依赖的任务可并行（如 T2.4/T2.5/T2.6 可由同一 Agent 批量创建） |

### 5.2 验收方式

| 类型 | 方法 | 示例 |
|------|------|------|
| 编译验证 | `tsc --noEmit` 无报错 | T2.9 Zod parse |
| 视觉验证 | 浏览器比对线框图 3.2 | T4.1 Hero 布局 |
| 功能验证 | 手动交互测试 | T3.4 Navbar 锚点跳转 |
| 动效验证 | 浏览器观察动画参数 | T6.2 whileInView 触发 |

### 5.3 失败回退

若某任务构建/类型检查不通过：
1. 停止当前任务
2. 检查依赖前置任务是否完整
3. 修正后重新执行该任务
4. 确认通过后继续后续任务

---

## 六、工作量汇总

| 阶段 | XS | S | M | L | XL | 合计 |
|------|----|----|----|----|----|------|
| Phase 1 初始化 | 3 | 5 | 3 | 0 | 0 | 11 |
| Phase 2 数据层 | 4 | 5 | 4 | 0 | 0 | 13 |
| Phase 3 布局 | 3 | 2 | 1 | 1 | 0 | 7 |
| Phase 4 首页 | 0 | 3 | 4 | 0 | 1 | 8 |
| Phase 5 详情页 | 1 | 0 | 2 | 0 | 0 | 3 |
| Phase 6 动效与交互动画 | 1 | 5 | 2 | 0 | 0 | 8 |
| Phase 7 VN UI 系统与首页集成 | 2 | 6 | 5 | 0 | 0 | 13 |
| Phase 8 SEO | 2 | 4 | 1 | 0 | 0 | 7 |
| Phase 9 VN 序幕 | 1 | 3 | 4 | 2 | 0 | 10 |
| Phase 10 ProjectCard clip-path 展开 | 1 | 1 | 1 | 0 | 0 | 3 |
| Phase 11 P1 | 0 | 1 | 1 | 0 | 0 | 2 |
| Phase 12 P2 | 0 | 1 | 2 | 1 | 1 | 5 |
| **总计** | **18** | **37** | **30** | **4** | **2** | **91** |


