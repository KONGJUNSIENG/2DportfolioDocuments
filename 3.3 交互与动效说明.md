# 3.3 交互与动效说明

**项目：** KONG JUN SIENG — Software Developer Portfolio  
**品牌调性：** 日系萌系 × 浅色科技感，Gal 社风格柔和动效  
**动画引擎：** Framer Motion（JS 驱动）+ CSS Animations（装饰性/循环动画）  
**文档目标：** 定义每个交互的动效参数、时序、实现路径与禁用策略，供 AI Agent 直接生成动效代码。

---

## 一、动画系统架构

### 1.1 全局动画 Variants（集中管理）

所有 Framer Motion variants 集中定义在 `src/lib/animations.ts`，组件只引用 variant 名称，不重复写动画参数。

```typescript
// src/lib/animations.ts — 全局动画 variants
export const staggerContainer = {
  hidden: {},
  visible: {
    transition: {
      staggerChildren: 0.15,
      delayChildren: 0.1,
    },
  },
}

export const staggerItem = {
  hidden: { opacity: 0, y: 20 },
  visible: {
    opacity: 1,
    y: 0,
    transition: { duration: 0.5, ease: [0.16, 1, 0.3, 1] },
  },
}

export const pageTransition = {
  initial: { opacity: 0 },
  animate: { opacity: 1, transition: { duration: 0.4 } },
  exit: { opacity: 0, transition: { duration: 0.15 } },
}
```

使用方式（示例 stagger 容器 + item）：
```tsx
<motion.div variants={staggerContainer} initial="hidden" whileInView="visible" viewport={{ once: true }}>
  <motion.div variants={staggerItem}>
    <Card />
  </motion.div>
</motion.div>
```

### 1.2 缓动函数映射

实际代码中缓动函数以硬编码贝塞尔值形式出现，未抽象为命名 token。所有 Framer Motion 入场/离场统一使用 `[0.16, 1, 0.3, 1]`（自定义 overshoot 曲线），hover 反馈使用 `"easeOut"` 或 `200ms ease-out`。

| 值                          | 使用场景           |
| --------------------------- | ------------------ |
| `[0.16, 1, 0.3, 1]`        | Framer Motion 入场/离场动画 |
| `"easeOut"` 或 `200ms ease-out` | CSS hover 反馈     |
| `"easeInOut"`               | 空闲循环动画       |

### 1.3 时长映射

实际代码中时长以硬编码值形式出现，未抽象为命名 token。以下为实际常用的时长映射：

| 时长    | 使用场景                          |
| ------- | --------------------------------- |
| 200ms   | hover 状态切换、颜色过渡          |
| 350ms   | optionItem 入场                   |
| 400ms   | pageTransition animate、transitionFade |
| 500ms   | staggerItem 入场、chapterReveal、dialogSlideUp |
| 800ms   | LoadingOverlay 幕布收缩           |

---

## 二、Hero 出场序列（叙事式先后入场）

### 2.1 时序图

Hero 出场使用**硬编码延迟链**（非 staggerContainer），每个元素独立控制 delay，实现 Slogan → Name → Title → CTA → Scroll 的渐进式入场节奏。

```
入口触发
  0ms      立绘进入
            opacity: 0→1, y: 30→0, 500ms

  700ms    Slogan（やればできる！）进入
            opacity: 0→1, y: 20→0, 500ms

  1200ms   Name（KONG JUN SIENG）进入
            opacity: 0→1, y: 20→0, 500ms

  1700ms   Title（Software Developer / ...）进入
            opacity: 0→1, y: 20→0, 500ms

  2200ms   CTA 按钮进入
            opacity: 0→1, y: 20→0, 500ms

  2700ms   "Scroll ↓" 提示淡入
            opacity: 0→1, 500ms
```

缓动统一为 `[0.16, 1, 0.3, 1]`（自定义 overshoot）。

### 2.2 实现方案

```tsx
// components/HeroSection.tsx — 硬编码延迟链
"use client"
import { motion, useScroll, useTransform, useSpring } from "framer-motion"
import { useMediaQuery } from "@/hooks/useMediaQuery"
import Image from "next/image"
import profileData from "@/data/profile.json"
import { ScrollToAnchor } from "./ScrollToAnchor"
import { OptionButton } from "./OptionButton"

export function HeroSection() {
  const t = useTranslations("hero")
  const isDesktop = useMediaQuery("(min-width: 1024px)")
  const { scrollYProgress } = useScroll()

  const yOffset = useTransform(scrollYProgress, [0, 0.3], isDesktop ? [0, -60] : [0, 0])
  const glowOpacity = useTransform(scrollYProgress, [0, 0.3], isDesktop ? [0.6, 0] : [0.3, 0.3])
  const smoothY = useSpring(yOffset, { stiffness: 100, damping: 30 })

  return (
    <section id="hero" className="relative overflow-hidden bg-background min-h-[100dvh] md:min-h-screen md:h-[90vh] md:flex md:flex-row">
      <div className="hero-glow" style={{ opacity: glowOpacity }} />

      {/* 左列：立绘（视差驱动） */}
      <motion.div className="md:w-[45%] md:flex md:items-end"
        style={{ y: smoothY }}
        initial={{ opacity: 0, y: 30 }}
        animate={{ opacity: 1, y: 0 }}
        transition={{ duration: 0.5, ease: [0.16, 1, 0.3, 1], delay: 0 }}
      >
        <div className="relative h-full w-full md:scale-[1.4] md:origin-top-left">
          <Image src="/images/hero-character.png" alt="" fill
            className="character-float object-cover"
            style={{ objectPosition: "center 45%" }} priority unoptimized
          />
        </div>
      </motion.div>

      {/* 右列：文字（独立 delay 链） */}
      <div className="md:w-[55%] md:flex md:flex-col md:justify-center md:pr-16">
        <motion.p className="text-xl text-text-secondary md:text-2xl"
          initial={{ opacity: 0, y: 20 }} animate={{ opacity: 1, y: 0 }}
          transition={{ duration: 0.5, ease: [0.16, 1, 0.3, 1], delay: 0.7 }}>
          {profileData.slogan}
        </motion.p>
        <motion.h1 className="mt-4 text-3xl font-bold text-text-primary md:text-4xl"
          initial={{ opacity: 0, y: 20 }} animate={{ opacity: 1, y: 0 }}
          transition={{ duration: 0.5, ease: [0.16, 1, 0.3, 1], delay: 1.2 }}>
          {profileData.name}
        </motion.h1>
        <motion.p className="mt-2 text-lg text-text-secondary"
          initial={{ opacity: 0, y: 20 }} animate={{ opacity: 1, y: 0 }}
          transition={{ duration: 0.5, ease: [0.16, 1, 0.3, 1], delay: 1.7 }}>
          {t("title")}
        </motion.p>
        <motion.div className="mt-6"
          initial={{ opacity: 0, y: 20 }} animate={{ opacity: 1, y: 0 }}
          transition={{ duration: 0.5, ease: [0.16, 1, 0.3, 1], delay: 2.2 }}>
          <ScrollToAnchor href="#case-studies">
            <OptionButton className="py-4 md:py-6">{t("ctaText")}</OptionButton>
          </ScrollToAnchor>
        </motion.div>
        <motion.p className="mt-4 text-sm text-text-secondary"
          initial={{ opacity: 0 }} animate={{ opacity: 1 }}
          transition={{ duration: 0.5, delay: 2.7 }}>
          Scroll ↓
        </motion.p>
      </div>
    </section>
  )
}
```

### 2.3 背景光晕（CSS 循环动画）

```css
/* globals.css — 实际代码 */
@keyframes glow-pulse {
  0%, 100% { opacity: 0.4; }
  50%      { opacity: 0.8; }
}

.hero-glow {
  position: absolute;
  top: 0;
  right: -10%;
  width: 40vh;
  height: 40vh;
  border-radius: 50%;
  background: radial-gradient(circle, rgba(255, 183, 197, 0.2), transparent);
  animation: glow-pulse 4s ease-in-out infinite;
  pointer-events: none;
}
```

光晕 opacity 受 Framer Motion `useTransform` 动态控制（scroll 0→0.3 时 0.6→0），参考 6.2 参数。

### 2.4 立绘浮动（CSS 循环动画）

```css
/* globals.css — 实际代码 */
@keyframes float {
  0%, 100% { transform: translateY(0); }
  50%      { transform: translateY(-8px); }
}

.character-float {
  animation: float 4s ease-in-out infinite;
}
```

### 2.5 响应式行为

| 设备         | 行为                                   |
| ------------ | -------------------------------------- |
| Desktop      | 完整时序，立绘浮动 + 光晕脉冲 + 视差   |
| Tablet       | 完整时序，立绘浮动保留，光晕脉冲保留   |
| Mobile       | 时序保留（无加速），立绘浮动保留，光晕脉冲保留（opacity 降为 0.3），无视差 |

当前实现**未**应用 Mobile 加速逻辑。所有设备共享相同的 delay 时序。

---

## 三、Section 入场动画（whileInView + Stagger）

### 3.1 参数总表

| 区块         | Container Variant | Item Variant | staggerChildren | once  | 说明                         |
| ------------ | ----------------- | ------------ | --------------- | ----- | ---------------------------- |
| Case Studies | `staggerContainer` | `staggerItem` | 0.15            | true  | 仅 1 张卡片，效果为单次入场  |
| Projects     | `staggerContainer` | `staggerItem` | 0.15            | true  | 2 张卡片逐个入场             |
| About        | `staggerContainer` | `staggerItem` | 0.15            | true  | DialogBox → SkillTag 列表    |
| Contact      | `optionStagger`   | `optionItem`  | 0.12            | true  | 4 个 OptionButton 逐个入场   |

### 3.2 通用实现模板

```tsx
// 所有 Section 组件复用此模式
"use client"
import { motion } from "framer-motion"
import { staggerContainer, staggerItem } from "@/lib/animations"

export function ProjectsSection() {
  return (
    <motion.section
      variants={staggerContainer}
      initial="hidden"
      whileInView="visible"
      viewport={{ once: true, amount: 0.2 }}
    >
      {/* Section 标题单独处理，不属于 stagger */}
      <h2 className="text-xl text-left">作品</h2>

      <div className="grid grid-cols-2 gap-6 mt-6">
        {projects.map((project) => (
          <motion.div key={project.slug} variants={staggerItem}>
            <ProjectCard project={project} />
          </motion.div>
        ))}
      </div>
    </motion.section>
  )
}
```

### 3.3 Section 标题入场

Section 标题使用 `<ChapterHeader>` 组件，其入场动画独立于卡片 stagger，通过 `chapterReveal` variant 实现：

```tsx
// chapterReveal: { hidden: { opacity: 0, x: -30 }, visible: { opacity: 1, x: 0, transition: { duration: 0.5, ease: [0.16, 1, 0.3, 1] } } }

<motion.div variants={chapterReveal} initial="hidden" whileInView="visible" viewport={{ once: true }}>
  <span className="text-xs font-semibold tracking-[0.2em] text-dialog-blue">
    CHAPTER {String(chapterNumber).padStart(2, "0")}
  </span>
  <h2 className="mt-1 text-2xl font-bold text-text-primary">{title}</h2>
  <div className="mt-3 h-0.5 w-12 rounded-full bg-dialog-blue" />
</motion.div>
```

- 标题从左侧滑入（x: -30→0）+ 淡入，500ms
- 标题与卡片 stagger 并行触发（均为 `whileInView`），无先后依赖关系

---

## 四、页面过渡（整页淡入淡出）

### 4.1 实现方案

页面过渡由 `LayoutShell` 组件管理，包裹在 `[lang]/layout.tsx` 内：

```tsx
// components/LayoutShell.tsx — 页面过渡核心
"use client"
import { motion, AnimatePresence } from "framer-motion"
import { usePathname } from "@/i18n/routing"
import { pageTransition } from "@/lib/animations"
import { Navbar } from "./Navbar"
import { Footer } from "./Footer"

export function LayoutShell({ children }: { children: React.ReactNode }) {
  const pathname = usePathname()
  const isIntro = pathname === "/intro" || pathname.startsWith("/intro")

  // Intro 页面隐藏 Navbar + Footer
  if (isIntro) return <>{children}</>

  return (
    <>
      <Navbar />
      <AnimatePresence mode="wait"
        onExitComplete={() => window.scrollTo({ top: 0 })}
      >
        <motion.main
          key={pathname}
          variants={pageTransition}
          initial="initial"
          animate="animate"
          exit="exit"
          className="relative pt-16"
        >
          {children}
        </motion.main>
      </AnimatePresence>
      <Footer />
    </>
  )
}
```

### 4.2 参数

| 属性     | 值                                    |
| -------- | ------------------------------------- |
| initial  | `opacity: 0`                          |
| animate  | `opacity: 1`，`transition.duration: 0.4` |
| exit     | `opacity: 0`，`transition.duration: 0.15` |
| mode     | `"wait"`（等待 exit 完成才进入 animate）    |
| key      | `pathname`（变化时触发过渡）               |
| onExitComplete | `window.scrollTo({ top: 0 })` — 退出后 reset 滚动 |

### 4.3 动画时序

```
用户点击 Case Study 卡片
  → 路由变化 → pathname 改变
  → exit: 当前页面 opacity: 1 → 0 (150ms)
  → 等待 exit 完成
  → initial: 新页面 opacity: 0
  → animate: 新页面 opacity: 0 → 1 (200ms)
```

### 4.4 防布局抖动

`AnimatePresence mode="wait"` 确保 exit 动画完成后再渲染新页面，避免两个页面同时存在导致的布局抖动。退出完成后通过 `onExitComplete` 重置滚动位置：

- `AnimatePresence mode="wait"`：等待 exit 完成才进入 animate
- `onExitComplete={() => window.scrollTo({ top: 0 })}`：切换页面后重置滚动位置
- LayoutShell 渲染 `className="relative pt-16"`（64px 导航栏占位），保证导航固定定位不重叠内容

---

## 五、语言切换过渡（B 方案 — 内容区域淡入淡出）

### 5.1 实现方案

```tsx
// components/LocaleContent.tsx — 包裹内容区域
"use client"
import { motion, AnimatePresence } from "framer-motion"
import { useLocale } from "next-intl"

export function LocaleContent({ children }: { children: React.ReactNode }) {
  const locale = useLocale()

  return (
    <AnimatePresence mode="wait">
      <motion.div
        key={locale}
        initial={{ opacity: 0 }}
        animate={{ opacity: 1, transition: { duration: 0.5 } }}
        exit={{ opacity: 0, transition: { duration: 0.3 } }}
      >
        {children}
      </motion.div>
    </AnimatePresence>
  )
}
```

### 5.2 使用方式

```tsx
// app/[lang]/layout.tsx — 包装所有语言敏感内容
<NextIntlClientProvider locale={lang} messages={messages}>
  <JaLocaleToast />
  <LocaleContent>
    <LayoutShell>{children}</LayoutShell>
  </LocaleContent>
</NextIntlClientProvider>
```

注意 `LocaleContent` 包裹的是 `LayoutShell`（内含 Navbar + main + Footer），而非直接包裹布局元素。

### 5.3 关键点

- `key={locale}` 在语言切换时触发 AnimatePresence 替换
- exit 时 opacity 0（300ms）→ 新内容 opacity 1（500ms）
- 导航栏、页脚等公共组件随内容一起过渡，避免视觉闪烁
- 语言切换时 Hero 不重新播放出场序列（仅文字内容替换）

---

## 六、视差效果（A 方案 — Hero 立绘 + 背景视差）

### 6.1 实现方案

```tsx
// components/HeroSection.tsx — 视差部分（内嵌于 HeroSection）
"use client"
import { motion, useScroll, useTransform, useSpring } from "framer-motion"
import { useMediaQuery } from "@/hooks/useMediaQuery"

// HeroSection 内部：
const isDesktop = useMediaQuery("(min-width: 1024px)")
const { scrollYProgress } = useScroll()

// 仅在 Desktop 启用立绘视差
const yOffset = useTransform(
  scrollYProgress, [0, 0.3],
  isDesktop ? [0, -60] : [0, 0]
)
const glowOpacity = useTransform(
  scrollYProgress, [0, 0.3],
  isDesktop ? [0.6, 0] : [0.3, 0.3]
)

const smoothY = useSpring(yOffset, { stiffness: 100, damping: 30 })

// 左列：立绘（受视差驱动）
<motion.div className="md:w-[45%] md:flex md:items-end"
  style={{ y: smoothY }}
>
  <Image src="/images/hero-character.png" alt="" fill
    className="character-float object-cover"
    style={{ objectPosition: "center 45%" }} priority unoptimized
  />
</motion.div>

// 光晕
<div className="hero-glow" style={{ opacity: glowOpacity }} />
```

### 6.2 参数映射

| 元素      | scrollYProgress 范围 | 变换        | 输出范围 | 说明 |
| --------- | -------------------- | ----------- | -------- | ---- |
| Hero 立绘 | `[0, 0.3]`           | `y`         | `[0, -60]` | Desktop 启用，移动端恒为 0 |
| 背景光晕  | `[0, 0.3]`           | `opacity`   | `[0.6, 0]`（Desktop）/ `[0.3, 0.3]`（Mobile） | 移动端保持常亮 |
| Hero 文字 | —                    | 无视差      | —        | — |

### 6.3 响应式禁用

```typescript
// src/hooks/useMediaQuery.ts
import { useState, useEffect } from "react"

export function useMediaQuery(query: string): boolean {
  const [matches, setMatches] = useState(false)
  useEffect(() => {
    const mql = window.matchMedia(query)
    setMatches(mql.matches)
    const handler = (e: MediaQueryListEvent) => setMatches(e.matches)
    mql.addEventListener("change", handler)
    return () => mql.removeEventListener("change", handler)
  }, [query])
  return matches
}
```

Desktop（≥1024px）启用立绘视差，Tablet / Mobile 禁用（`[0, 0]` 恒等映射）。

Desktop（≥1024px）启用视差，Tablet / Mobile 禁用。

### 6.4 性能注意

- 视差元素限制 2 个（立绘 + 光晕），不额外增加
- 使用 `useSpring` 防止突变导致的视觉抖动
- 视差元素添加 `will-change: transform`

---

## 七、VN 式交互动效

### 7.1 章节标题（ChapterHeader）入场

```
触发：Section 进入视口
  → 整体从左侧滑入 (x: -30 → 0) + 淡入     500ms
```

使用 Framer Motion `chapterReveal` variants：

```tsx
// chapterReveal: { hidden: { opacity: 0, x: -30 }, visible: { opacity: 1, x: 0, transition: { duration: 0.5, ease: [0.16, 1, 0.3, 1] } } }

<motion.div variants={chapterReveal} initial="hidden" whileInView="visible" viewport={{ once: true }}>
  <span>CHAPTER 01</span>
  <h2>Case Studies</h2>
  <div className="h-0.5 w-12 rounded-full bg-dialog-blue" />
</motion.div>
```

### 7.2 对话框（DialogBox）入场

```
触发：对话框所在元素进入视口
  → 对话框从底部上浮 (y: 24 → 0) + 缩放 (scale: 0.98 → 1) + 淡入   500ms
```

使用 `dialogSlideUp` variants。结构为双层圆角：外层白底框 `border-dialog-blue rounded-lg`，内层角色名 `rounded-xl bg-dialog-blue` + 正文 `rounded-md bg-dialog-blue`，flex 1:3 比例。角色名与正文均为白色文字。无逐字显示功能。

### 7.3 选项按钮交互

| 交互 | 动画 | 时长 | 实现 |
|------|------|------|------|
| hover (Desktop) | `scale(1.02)` + `x: 4`（放大 + 右移） | 200ms | Framer Motion `whileHover` |
| tap (点击) | `scale(1.0 → 0.98)` | 150ms | Framer Motion `whileTap` |
| 入场（Contact） | `x: -20 → 0` + `opacity: 0→1` (stagger 120ms) | 350ms | Framer Motion `optionStagger` + `optionItem` |

### 7.4 Section 过渡台词

```
触发：过渡台词进入视口
  → 文字从中心淡入放大 (scale: 0.92 → 1) + opacity   400ms
```

结构：水平贯穿线 + 居中 `~ text ~`。使用 `<SectionTransition>` 组件渲染，`transitionFade` variants 控制入场，仅触发一次。

### 7.5 页面暗转过渡（游戏风场景切换）

Case Study 详情页的进入/退出使用现有 `pageTransition` variants（opacity 过渡），未实现全黑帧。

---
### 7.6 游戏风序幕：开场动画（IntroAnimation → TitleScreen）

Intro 全程由状态机（`IntroShell`）控制，分为 4 个 Phase。

**Phase 1: LoadingRipple → 白色闪光**

| 属性 | 值 |
|------|-----|
| 组件 | `<LoadingRipple onComplete={...} />` |
| 波纹 | 3 个同心圆波纹：`border-2 border-primary/40`，`scale 0.3→2.5`，`opacity 0.8→0` |
| 交错 | stagger 0.35s，总时长约 1.65s |
| 完成后 | 闪 500ms 白色画面（`bg-primary-soft`） |

**Phase 2: PixelsCurtain + TitleScreen**

| 属性 | 值 |
|------|-----|
| 组件 | `<PixelsCurtain onComplete={...} />` + `<TitleScreen lang={...} />` |
| PixelsCurtain | 8×8 网格（64 tile），随机顺序从 `scale 1→0` 消失，stagger 12ms，底层渐显 TitleScreen |
| TitleScreen | 渐变背景 `from-primary-soft via-background to-secondary/20` + 径向光晕 `glow-pulse` |
| 核心元素 | `ScrambleText`：默认显示 "KONG JUN SIENG"，2.5s 自动闪烁 1.5s → "START"（或 "开始游戏"/"スタート"），保持 0.5s 后还原 |
| 交互 | hover ScrambleText 立即触发闪烁→变为可点击→点击执行 `onStart`。右下角 Skip 按钮延迟 2.5s 出现 |
| 设定对话框 | ▶ 设定按钮打开 Settings 对话框→语言选择（zh/ja/en，radio 风格）→ Apply 跳转 `/{lang}/intro` |

**Phase 3 → 暗転 Transition**

点击 ScrambleText 后，Phase 变为 `"transition"`，渲染 700ms 全屏黑色遮罩，然后进入 Phase `"scenario"`。

### 7.7 游戏风序幕：Scenario 演绎（VNScenario）

| 属性 | 值 |
|------|-----|
| 推进方式 | 点击对话区（或选项选择后自动推进） |
| 过渡动画 | 场景切换时 `AnimatePresence mode="wait"`，dialog 区 key 变化，`opacity 0→1` 350ms |
| 表情切换 | `characterImage` 变化时 key 变化→图片重新挂载（`new Image()` 预加载）→ `opacity 0→1` 600ms 淡入 |
| 打字机效果 | **无**，台词直接全文显示 |
| 章节标题 | **无**，不显示 ChapterHeader |
| 场景计数 | **无**，不显示场景计数 |
| 角色名 | 显示在底部 dialog 区顶部，`text-lg tracking-wider text-dialog-blue` |
| 对话框 | 底部 `h-[30vh]` 区域，`bg-gradient-to-b from-black/50 via-black/20 to-transparent` 遮罩 |
| 选项分支 | 条件渲染全屏 `bg-black/50` 遮罩+选项列表，每个选项带 ▶ 箭头，stagger 0.1s 入场 |
| Mouse Repell | 可选场景（`repel: true`）：鼠标 200px 内选项区反方向偏移（strength max 1000px），`requestAnimationFrame` 驱动 |
| autoAdvance | 可选场景（`autoAdvanceMs`）：自动选择第一个选项，useRef 锁 2s 防重复 |
| Skip 按钮 | 右下角，全程可见，直接触发 `onFinish` |

### 7.8 游戏风序幕：暗転过渡（Transition Phase）

IntroShell 中的 Phase `"transition"`：

| 属性 | 值 |
|------|-----|
| 结构 | `fixed inset-0 z-50 bg-black` |
| 动画 | `opacity: 0→1` 300ms → 保持 700ms → 切换至 scenario |
| 触发条件 | ScrambleText 点击后 `goToScenario()` |

### 7.9 游戏风序幕：LoadingOverlay（Phase 4）

| 属性 | 值 |
|------|-----|
| 组件 | `<LoadingOverlay lang={...} />` |
| 结构 | 两片 `#1A1A2E` 深色幕布（左右各 50% 宽度） |
| 动画 | `width: 50%→0%`，delay 0.6s，duration 0.8s，缓动 `[0.16, 1, 0.3, 1]` |
| 完成后 | `onAnimationComplete` → `router.push(/${lang})` 跳转首页 |
| 防重复 | `useRef(navigated)` 防多次跳转 |

### 7.10 游戏风动效 variants（animations.ts）

所有游戏风动效 variants 已定义在 `src/lib/animations.ts`：

```typescript
// src/lib/animations.ts — 追加在原文件末尾

export const chapterReveal: Variants = {
  hidden: { opacity: 0, x: -30 },
  visible: {
    opacity: 1,
    x: 0,
    transition: { duration: 0.5, ease: [0.16, 1, 0.3, 1] },
  },
}

export const dialogSlideUp: Variants = {
  hidden: { opacity: 0, y: 24, scale: 0.98 },
  visible: {
    opacity: 1,
    y: 0,
    scale: 1,
    transition: { duration: 0.5, ease: [0.16, 1, 0.3, 1] },
  },
}

export const optionStagger: Variants = {
  hidden: {},
  visible: {
    transition: {
      staggerChildren: 0.12,
      delayChildren: 0.1,
    },
  },
}

export const optionItem: Variants = {
  hidden: { opacity: 0, x: -20 },
  visible: {
    opacity: 1,
    x: 0,
    transition: { duration: 0.35, ease: [0.16, 1, 0.3, 1] },
  },
}

export const transitionFade: Variants = {
  hidden: { opacity: 0, scale: 0.92 },
  visible: {
    opacity: 1,
    scale: 1,
    transition: { duration: 0.4, ease: [0.16, 1, 0.3, 1] },
  },
}
```

---

## 八、Hover 动效与导航栏滚动效果

### 8.1 参数总表

| 元素 | 属性变更 | 时长 | 实现方式 |
| --- | --- | --- | --- |
| ProjectCard hover | `scale(1.03)` + `shadow-md → shadow-lg`，暂停空闲弹跳 | 200ms | Framer Motion `animate` + state |
| CaseStudyCard hover | `rotate: 0` + `scale(1.03)` + `shadow-md → shadow-lg`，暂停空闲微摆 | 200ms | Framer Motion `animate` + state |
| OptionButton hover | `scale(1.02)` + `x: 4` | 200ms | Framer Motion `whileHover` |
| 导航链接 | `color: text-secondary → primary` | 200ms | CSS `.nav-link:hover` |
| 技能标签 | `scale(1.0 → 1.05)` | 200ms | CSS `.tag-hover:hover` |

### 8.2 实现方式

卡片 hover 使用 Framer Motion 驱动（`onMouseEnter/onMouseLeave` + state 切换 `animate` 目标），其余 hover 使用 CSS transition：

```css
/* globals.css — 实际 hover 动效 */
.nav-link {
  transition: color 200ms ease-out;
}
.nav-link:hover {
  color: var(--color-primary);
}

.tag-hover {
  transition: transform 200ms ease-out;
}
.tag-hover:hover {
  transform: scale(1.05);
}
```

### 8.3 响应式

Mobile（<768px）无 hover 概念，所有 hover 动效不生效。
Tablet（≥768px）hover 动效正常工作。

---

### 8.4 导航栏滚动效果

### 8.5 实现方案

```tsx
// components/Navbar.tsx
"use client"
import { useScroll, useTransform, motion } from "framer-motion"

export function Navbar() {
  const { scrollY } = useScroll()
  const boxShadow = useTransform(
    scrollY,
    [0, 50],
    ["0 0 0 transparent", "var(--shadow-sm)"]
  )
  const rawBgAlpha = useTransform(scrollY, [0, 50], [0, 0.85])
  const bgColor = useTransform(rawBgAlpha, (v) => `rgba(255,250,253,${v})`)

  return (
    <motion.nav
      className="fixed top-0 z-50 h-16 w-full
                 backdrop-blur-md
                 flex items-center px-4 lg:px-8"
      style={{ backgroundColor: bgColor, boxShadow }}
    >
      {/* 导航内容 */}
    </motion.nav>
  )
}
```

### 8.6 行为描述

- `scrollY` = 0：背景完全透明（`rgba(255,250,253,0)`），无阴影
- `scrollY` > 50px：背景渐现至 `rgba(255,250,253,0.85)` + `backdrop-blur` + `shadow-sm`
- 过渡平滑：Framer Motion useTransform 连续映射 alpha 通道，非 JS scroll 事件监听

### 8.7 移动端

导航固定顶部行为不变。背景透明度 + 模糊 + 阴影规则与 Desktop 一致。

---

## 九、卡片空闲循环动画

### 9.1 ProjectCard 空闲弹跳

| 属性 | 值 |
|------|-----|
| 效果 | 卡片每 1.5s 向上弹跳 `y: [0, -12, 0]`，0.25s 弹跳 + 1.25s 停顿，`easeInOut` |
| 周期 | 1.5s（duration 0.25s + repeatDelay 1.25s） |
| 驱动方式 | Framer Motion `animate={{ y: [0, -12, 0] }}` + `transition: { repeat: Infinity }` |
| hover 行为 | 弹跳暂停 → `scale(1.03)` + shadow-md → shadow-lg |
| 展开状态 | `expanded=true` 时弹跳暂停 |
| 残障适配 | `prefersReducedMotion` 禁用弹跳 |

### 9.2 CaseStudyCard 空闲微摆

| 属性 | 值 |
|------|-----|
| 效果 | 卡片每 1.5s 左右微摆 `rotate: [0, 1.5, 0, -1.5, 0]`，0.4s 摆动 + 1.1s 停顿，`easeInOut` |
| 周期 | 1.5s（duration 0.4s + repeatDelay 1.1s） |
| 驱动方式 | Framer Motion `animate={{ rotate: [0, 1.5, 0, -1.5, 0] }}` + `transition: { repeat: Infinity }` |
| hover 行为 | 微摆暂停 → `scale(1.03)` + shadow-md → shadow-lg |
| 残障适配 | `prefersReducedMotion` 禁用微摆 |

---

## 十、ProjectCard 点击 clip-path 圆形展开交互

### 10.1 交互概述

| 属性 | 值 |
|------|-----|
| 触发 | 点击 ProjectCard 任意区域 |
| 行为 | 点击切换 `expanded` 状态；展开时初始内容以圆形 clip-path 从中心向外扩散消失，露出详情内容（标题 → 背景标签+内容 → 贡献标签+bullet list → 成果标签+内容）；再次点击圆形从边缘向中心收缩恢复初始内容 |
| 范围 | 整个卡片内部内容全部替换（封面+标题+标签 ↔ 详情文本） |
| 卡片高度 | 由初始内容（封面 4:3 + 标题 + 标签）自然撑起；详情内容 `absolute inset-0` 填满同一空间，内容超出滚动 |

### 10.2 动画参数

| 属性 | Desktop | Mobile (<768px) |
|------|---------|-----------------|
| 展开动画 | `clipPath: circle(100% at 50% 50%)` → `circle(0% at 50% 50%)`，顶部层中心向外扩散消失 | 同上，duration 缩短 |
| 收拢动画 | `clipPath: circle(0% at 50% 50%)` → `circle(100% at 50% 50%)`，顶部层边缘向中心收缩恢复 | 同上，duration 缩短 |
| duration | 0.6s | 0.3s |
| ease | `[0.16, 1, 0.3, 1]` | 同上 |
| 驱动方式 | Framer Motion `animate={{ clipPath }}` | 同上 |

### 10.3 实现结构

```
<article className="relative cursor-pointer overflow-hidden rounded-lg bg-surface shadow-md ...">
  <!-- 底部层：详情内容（absolute 填满父容器，scrollbar 隐藏） -->
  <div className="scrollbar-micro absolute inset-0 overflow-y-auto p-6">
    <h3>标题（{titleKey}）</h3>

    <section>
      <p>背景标签（{backgroundLabel}）</p>
      <p>{background}</p>
    </section>

    <section>
      <p>贡献标签（{contributeLabel}）</p>
      <ul>
        <li>{contribute 直接渲染数组项}</li>
        <li>...</li>
      </ul>
    </section>

    <section>
      <p>成果标签（{outcomeLabel}）</p>
      <p>{outcome}</p>
    </section>
  </div>

  <!-- 顶部层：初始内容（正常文档流撑起卡片高度） -->
  <motion.div
    className="bg-surface p-6"
    initial={{ clipPath: "circle(100% at 50% 50%)" }}
    animate={{ clipPath: expanded ? "circle(0% at 50% 50%)" : "circle(100% at 50% 50%)" }}
    transition={{ duration: desktop ? 0.6 : 0.3, ease: [0.16, 1, 0.3, 1] }}
  >
    <!-- 封面图 4:3 + 标题 + 技能标签 -->
  </motion.div>
</article>
```

### 10.4 移动端策略

- 保留 clip-path 动画，duration 从 0.6s 缩短至 0.3s
- 使用 `src/hooks/useMediaQuery.ts` 中的 `useMediaQuery("(max-width: 767px)")` 检测

### 10.5 响应式禁用策略

| 动效 | Mobile <768px | Tablet 768–1023 | Desktop ≥1024 |
|------|---------------|-----------------|---------------|
| ProjectCard clip-path 圆形展开 | ✅ 保留，duration 0.3s | ✅ 保留，duration 0.6s | ✅ 保留，duration 0.6s |

---

## 十一、响应式禁用策略

| 动效                 | Mobile <768px                          | Tablet 768–1023                    | Desktop ≥1024                     |
| -------------------- | -------------------------------------- | ---------------------------------- | --------------------------------- |
| Hero 出场序列        | ✅ 保留（完整时序，无加速）            | ✅ 保留（完整时序）                | ✅ 保留（完整时序 + 视差）        |
| Section 入场 stagger | ✅ 保留                                | ✅ 保留                            | ✅ 保留                           |
| 视差（Parallax）     | ❌ 禁用（恒等映射 [0,0]）             | ❌ 禁用                            | ✅ 启用                           |
| hover 动效           | ❌ 禁用（无 hover）                    | ✅ 启用                            | ✅ 启用                           |
| 页面过渡             | ✅ 保留                                | ✅ 保留                            | ✅ 保留                           |
| 语言切换过渡         | ✅ 保留                                | ✅ 保留                            | ✅ 保留                           |
| 光晕脉冲 CSS         | ✅ 保留（opacity 0.3 常亮）           | ✅ 启用                            | ✅ 启用（useTransform 动态控制） |
| 立绘浮动 CSS         | ✅ 保留                                | ✅ 启用                            | ✅ 启用                           |
| 章节标题入场         | ✅ 保留（x:-30→0 滑入）                | ✅ 保留                            | ✅ 保留                           |
| 对话框入场           | ✅ 保留（y:24→0）                      | ✅ 保留                            | ✅ 保留                           |
| ProjectCard clip-path 圆形展开 | ✅ 保留，duration 0.3s | ✅ 保留，duration 0.6s | ✅ 保留，duration 0.6s |
| ProjectCard 空闲弹跳 | ✅ 保留 | ✅ 保留 | ✅ 保留 |
| CaseStudyCard 空闲微摆 | ✅ 保留 | ✅ 保留 | ✅ 保留 |
| 过渡台词             | ✅ 保留                                | ✅ 保留                            | ✅ 保留                           |
| 选项按钮 hover       | ❌ 禁用                                | ✅ 启用                            | ✅ 启用                           |
| 页面暗转过渡         | ✅ 保留（pageTransition）              | ✅ 保留                            | ✅ 保留                           |
| 游戏风序幕 LoadingRipple | ✅ 保留 | ✅ 保留 | ✅ 保留 |
| 游戏风序幕 PixelsCurtain | ✅ 保留 | ✅ 保留 | ✅ 保留 |
| 游戏风序幕 TitleScreen | ✅ 保留 | ✅ 保留 | ✅ 保留 |
| 游戏风序幕 Scenario | ✅ 保留（无打字机） | ✅ 保留 | ✅ 保留 |
| 游戏风序幕 LoadingOverlay | ✅ 保留 | ✅ 保留 | ✅ 保留 |

---

## 十二、无障碍与性能

### 12.1 prefers-reduced-motion

```css
/* globals.css */
@media (prefers-reduced-motion: reduce) {
  *, *::before, *::after {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
  }
}
```

用户开启系统"减少动效"后，所有动画直接跳过，显示最终态。

### 12.2 性能注意事项

- **减少 JS 开销**：背景光晕 / 角色浮动使用 CSS animation，不叠加 Framer Motion 逐帧；卡片空闲循环动画使用 Framer Motion `animate` prop 而非 CSS（需要暂停/恢复切换）
- **视差元素上限**：最多 2 个（立绘 + 光晕），不再扩展
- **图片避免 Framer Motion 动画**：立绘使用 CSS 浮动动画，Framer Motion 只控制入场 opacity + y
- **AnimatePresence mode="wait"**：确保退出动画完成后才渲染新内容，避免同时渲染导致的性能峰值
- **viewport once: true**：Section 入场动画只触发一次，不重复

---

## 附录：animations.ts 完整代码

```typescript
// src/lib/animations.ts — 实际代码（供 AI Agent 直接引用）
import type { Variants } from "framer-motion"

export const staggerContainer: Variants = {
  hidden: {},
  visible: {
    transition: {
      staggerChildren: 0.15,
      delayChildren: 0.1,
    },
  },
}

export const staggerItem: Variants = {
  hidden: { opacity: 0, y: 20 },
  visible: {
    opacity: 1,
    y: 0,
    transition: { duration: 0.5, ease: [0.16, 1, 0.3, 1] },
  },
}

export const pageTransition: Variants = {
  initial: { opacity: 0 },
  animate: {
    opacity: 1,
    transition: { duration: 0.4 },
  },
  exit: {
    opacity: 0,
    transition: { duration: 0.15 },
  },
}

/* ── VN browsing experience variants ── */

export const chapterReveal: Variants = {
  hidden: { opacity: 0, x: -30 },
  visible: {
    opacity: 1,
    x: 0,
    transition: { duration: 0.5, ease: [0.16, 1, 0.3, 1] },
  },
}

export const dialogSlideUp: Variants = {
  hidden: { opacity: 0, y: 24, scale: 0.98 },
  visible: {
    opacity: 1,
    y: 0,
    scale: 1,
    transition: { duration: 0.5, ease: [0.16, 1, 0.3, 1] },
  },
}

export const optionStagger: Variants = {
  hidden: {},
  visible: {
    transition: {
      staggerChildren: 0.12,
      delayChildren: 0.1,
    },
  },
}

export const optionItem: Variants = {
  hidden: { opacity: 0, x: -20 },
  visible: {
    opacity: 1,
    x: 0,
    transition: { duration: 0.35, ease: [0.16, 1, 0.3, 1] },
  },
}

export const transitionFade: Variants = {
  hidden: { opacity: 0, scale: 0.92 },
  visible: {
    opacity: 1,
    scale: 1,
    transition: { duration: 0.4, ease: [0.16, 1, 0.3, 1] },
  },
}
```
