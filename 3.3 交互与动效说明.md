# 3.3 交互与动效说明

**项目：** KONG JUN SIENG — Software Developer Portfolio  
**品牌调性：** 日系萌系 × 浅色科技感，Gal 社风格柔和动效  
**动画引擎：** Framer Motion（JS 驱动）+ CSS Animations（装饰性/循环动画）  
**文档目标：** 定义每个交互的动效参数、时序、实现路径与禁用策略，供 AI Agent 直接生成动效代码。

---

## 一、动画系统架构

### 1.1 全局动画 Variants（集中管理）

所有 Framer Motion variants 集中定义在 `src/lib/animations.ts`，组件只引用 variant 名称，不重复写动画参数。

```typescript
// src/lib/animations.ts — 全局动画 variants
export const fadeInUp = {
  hidden: { opacity: 0, y: 20 },
  visible: {
    opacity: 1,
    y: 0,
    transition: { duration: 0.5, ease: [0.16, 1, 0.3, 1] },
  },
}

export const staggerContainer = {
  hidden: {},
  visible: {
    transition: {
      staggerChildren: 0.15,
      delayChildren: 0.1,
    },
  },
}

export const staggerItem = {
  hidden: { opacity: 0, y: 20 },
  visible: {
    opacity: 1,
    y: 0,
    transition: { duration: 0.5, ease: [0.16, 1, 0.3, 1] },
  },
}

export const pageTransition = {
  initial: { opacity: 0 },
  animate: { opacity: 1, transition: { duration: 0.2 } },
  exit: { opacity: 0, transition: { duration: 0.15 } },
}
```

使用方式（示例卡片组件）：
```tsx
<motion.div variants={staggerItem}>
  <Card />
</motion.div>
```

### 1.2 缓动函数映射

| Token           | 值                                   | 使用场景           |
| --------------- | ------------------------------------ | ------------------ |
| `ease-in-soft`  | `cubic-bezier(0.16, 1, 0.3, 1)`     | 入场动画           |
| `ease-out`       | `cubic-bezier(0.25, 0.1, 0.25, 1)`  | hover 反馈         |
| `linear`        | 无缓动                               | 视差驱动           |

### 1.3 时长映射

| Token      | 值     | 使用场景                          |
| ---------- | ------ | --------------------------------- |
| `fast`     | 200ms  | hover 状态切换、颜色过渡          |
| `normal`   | 300ms  | 按钮交互、卡片 hover              |
| `slow`     | 500ms  | Section 入场 fadeInUp             |
| `ambient`  | 800ms  | Hero 入场首帧、大元素渐入         |

---

## 二、Hero 出场序列（B 方案 — 叙事式先后入场）

### 2.1 时序图（当前实现 — staggerContainer 模式）

```
入口触发                        │
0ms        Slogan / Name / Title / CTA 统一容器      │ staggerContainer
           立绘 + 文字整体入场                        │ delayChildren: 0.1
                                                      │
~100ms     Slogan 开始进入                            │ staggerChildren: 0.15
           opacity: 0 → 1, y: 20 → 0               │ duration: 500ms
                                                      │
~250ms     Name 开始进入                              │
           opacity: 0 → 1, y: 20 → 0               │ duration: 500ms
                                                      │
~400ms     Title 开始进入                             │
           opacity: 0 → 1, y: 20 → 0               │ duration: 500ms
                                                      │
~550ms     CTA 按钮开始进入                            │
           opacity: 0 → 1, y: 20 → 0               │ duration: 500ms
```

所有元素使用 `fadeInUp` variant + `staggerContainer` 统一控制，立绘通过 `whileInView` + `fadeInUp` 独立触发出场。

### 2.2 实现方案（当前代码结构）

```tsx
// components/HeroSection.tsx — 使用 staggerContainer 模式
"use client"
import { motion } from "framer-motion"
import { fadeInUp, staggerContainer, staggerItem } from "@/lib/animations"

export function HeroSection() {
  return (
    <section className="relative h-screen flex overflow-hidden">
      {/* 左列：立绘 — whileInView 独立触发 */}
      <motion.div
        className="w-[60%] relative flex items-end"
        variants={fadeInUp}
        initial="hidden"
        animate="visible"
      >
        <Image
          src="/images/hero-character.png"
          alt="KONG JUN SIENG"
          className="h-[70vh] object-contain object-bottom"
          priority
        />
      </motion.div>

      {/* 右列：文字 — staggerContainer */}
      <motion.div
        className="w-[40%] flex flex-col justify-center pr-16"
        variants={staggerContainer}
        initial="hidden"
        animate="visible"
      >
        <motion.p variants={staggerItem}>
          やればできる！
        </motion.p>
        <motion.h1 variants={staggerItem}>
          KONG JUN SIENG
        </motion.h1>
        <motion.p variants={staggerItem}>
          Software Developer / 全栈探索者
        </motion.p>
        <motion.div variants={staggerItem}>
          <button className="bg-primary text-white px-4 py-2 rounded-md">
            查看作品
          </button>
        </motion.div>
      </motion.div>
    </section>
  )
}
```

### 2.3 背景光晕（CSS 循环动画）

```css
/* globals.css */
@keyframes hero-glow-pulse {
  0%, 100% { opacity: 0.3; }
  50%      { opacity: 0.6; }
}

.hero-glow {
  position: absolute;
  top: 0;
  right: -10%;
  width: 40vh;
  height: 40vh;
  border-radius: 50%;
  background: radial-gradient(
    circle,
    rgba(255, 183, 197, 0.2),
    transparent
  );
  animation: hero-glow-pulse 4s ease-in-out infinite;
  pointer-events: none;
}
```

### 2.4 立绘浮动（CSS 循环动画）

```css
/* globals.css */
@keyframes character-float {
  0%, 100% { transform: translateY(0); }
  50%      { transform: translateY(-3px); }
}

.character-float {
  animation: character-float 4s ease-in-out infinite;
}
```

### 2.5 响应式行为

| 设备         | 行为                                   |
| ------------ | -------------------------------------- |
| Desktop      | 完整时序，立绘浮动 + 光晕脉冲          |
| Tablet       | 完整时序，立绘浮动保留，光晕脉冲保留   |
| Mobile       | 时序保留但加速 60%（所有 delay 乘以 0.6）|

Mobile 加速原设计：
```tsx
const isMobile = typeof window !== 'undefined' && window.innerWidth < 768
const delayMultiplier = isMobile ? 0.6 : 1
// 所有 delay 乘以 delayMultiplier
```
当前实现未应用此加速逻辑（待完成）。

---

## 三、Section 入场动画（whileInView + Stagger）

### 3.1 参数总表

| 区块         | Container Variant | Item Variant | staggerChildren | once  | 说明                         |
| ------------ | ----------------- | ------------ | --------------- | ----- | ---------------------------- |
| Case Studies | `staggerContainer` | `staggerItem` | 0.2             | true  | 仅 1 张卡片，效果为单次入场  |
| Projects     | `staggerContainer` | `staggerItem` | 0.15            | true  | 2 张卡片逐个入场             |
| About        | `staggerContainer` | `staggerItem` | 0.1             | true  | 文字 → 标签行                |
| Contact      | `staggerContainer` | `staggerItem` | 0.2             | true  | 邮箱 + 社交 + 简历（无表单）  |

### 3.2 通用实现模板

```tsx
// 所有 Section 组件复用此模式
"use client"
import { motion } from "framer-motion"
import { staggerContainer, staggerItem } from "@/lib/animations"

export function ProjectsSection() {
  return (
    <motion.section
      variants={staggerContainer}
      initial="hidden"
      whileInView="visible"
      viewport={{ once: true, amount: 0.2 }}
    >
      {/* Section 标题单独处理，不属于 stagger */}
      <h2 className="text-xl text-left">作品</h2>

      <div className="grid grid-cols-2 gap-6 mt-6">
        {projects.map((project) => (
          <motion.div key={project.slug} variants={staggerItem}>
            <ProjectCard project={project} />
          </motion.div>
        ))}
      </div>
    </motion.section>
  )
}
```

### 3.3 Section 标题入场

Section 标题`<h2>`的入场独立于卡片 stagger：

```tsx
<motion.h2
  className="text-xl"
  initial={{ opacity: 0, y: 15 }}
  whileInView={{ opacity: 1, y: 0 }}
  transition={{ duration: 0.4, ease: [0.16, 1, 0.3, 1] }}
  viewport={{ once: true }}
>
  案例研究
</motion.h2>
```

标题与卡片的时间关系：
- 标题先入场（y: 15 → 0, 400ms）
- 完成后再触发卡片 stagger

---

## 四、页面过渡（C 方案 — 整页淡入淡出）

### 4.1 实现方案

```tsx
// app/[lang]/layout.tsx — 页面过渡核心
"use client"
import { motion, AnimatePresence } from "framer-motion"
import { usePathname } from "next/navigation"
import { pageTransition } from "@/lib/animations"

export default function LocaleLayout({
  children,
  params,
}: {
  children: React.ReactNode
  params: { lang: string }
}) {
  const pathname = usePathname()

  return (
    <div>
      <Navbar />
      <AnimatePresence mode="wait">
        <motion.main
          key={pathname}
          variants={pageTransition}
          initial="initial"
          animate="animate"
          exit="exit"
        >
          {children}
        </motion.main>
      </AnimatePresence>
      <Footer />
    </div>
  )
}
```

### 4.2 参数

| 属性     | 值                                    |
| -------- | ------------------------------------- |
| initial  | `opacity: 0`                          |
| animate  | `opacity: 1`，`transition.duration: 0.2` |
| exit     | `opacity: 0`，`transition.duration: 0.15` |
| mode     | `"wait"`（等待 exit 完成才进入 animate）    |
| key      | `pathname`（变化时触发过渡）               |

### 4.3 动画时序

```
用户点击 Case Study 卡片
  → 路由变化 → pathname 改变
  → exit: 当前页面 opacity: 1 → 0 (150ms)
  → 等待 exit 完成
  → initial: 新页面 opacity: 0
  → animate: 新页面 opacity: 0 → 1 (200ms)
```

### 4.4 防布局抖动

`<motion.main>` 在 exit 期间设置 `position: absolute`，防止 exit 元素消失瞬间布局塌缩：

```tsx
<motion.main
  key={pathname}
  layout
  className="relative"
  {...pageTransition}
  // 退出时 position: absolute 避免残留占位
  onExitComplete={() => {
    window.scrollTo({ top: 0 })
  }}
>
  {children}
</motion.main>
```

---

## 五、语言切换过渡（B 方案 — 内容区域淡入淡出）

### 5.1 实现方案

```tsx
// components/LocaleContent.tsx — 包裹内容区域
"use client"
import { motion, AnimatePresence } from "framer-motion"
import { useLocale } from "next-intl"

export function LocaleContent({ children }: { children: React.ReactNode }) {
  const locale = useLocale()

  return (
    <AnimatePresence mode="wait">
      <motion.div
        key={locale}
        initial={{ opacity: 0 }}
        animate={{ opacity: 1, transition: { duration: 0.5 } }}
        exit={{ opacity: 0, transition: { duration: 0.3 } }}
      >
        {children}
      </motion.div>
    </AnimatePresence>
  )
}
```

### 5.2 使用方式

```tsx
// app/[lang]/layout.tsx — 包装所有语言敏感内容
<LocaleContent>
  <Navbar />
  <main>{children}</main>
  <Footer />
</LocaleContent>
```

### 5.3 关键点

- `key={locale}` 在语言切换时触发 AnimatePresence 替换
- exit 时 opacity 0（300ms）→ 新内容 opacity 1（500ms）
- 导航栏、页脚等公共组件随内容一起过渡，避免视觉闪烁
- 语言切换时 Hero 不重新播放出场序列（仅文字内容替换）

---

## 六、视差效果（A 方案 — Hero 立绘 + 背景视差）

### 6.1 实现方案

```tsx
// components/HeroSection.tsx — 视差部分
"use client"
import { useScroll, useTransform, useSpring, motion } from "framer-motion"
import { useMediaQuery } from "@/hooks/useMediaQuery" // 自定义 hook

export function HeroParallax() {
  const isDesktop = useMediaQuery("(min-width: 1024px)")
  const { scrollYProgress } = useScroll()

  // 仅在 Desktop 启用视差
  const yOffset = useTransform(
    scrollYProgress,
    [0, 0.3],
    isDesktop ? [0, -60] : [0, 0]
  )
  const glowOpacity = useTransform(
    scrollYProgress,
    [0, 0.3],
    isDesktop ? [0.6, 0] : [0.3, 0.3]
  )

  // useSpring 增加阻尼感
  const smoothY = useSpring(yOffset, { stiffness: 100, damping: 30 })

  return (
    <section className="relative h-screen overflow-hidden">
      {/* 立绘层 - 0.8x 速度 */}
      <motion.div
        className="absolute left-0 bottom-0 w-[60%]"
        style={{ y: smoothY }}
      >
        <Image src="/images/hero-character.png" alt="" priority />
      </motion.div>

      {/* 光晕层 - 0.5x 速度 */}
      <motion.div
        className="hero-glow"
        style={{ opacity: glowOpacity }}
      />

      {/* 文字层 - 1x 速度，无视差 */}
      <div className="relative z-10 w-[40%] ml-auto">
        {/* ... 文字内容 */}
      </div>
    </section>
  )
}
```

### 6.2 参数映射

| 元素      | scrollYProgress 范围 | 变换        | 输出范围 | 速度倍率 |
| --------- | -------------------- | ----------- | -------- | -------- |
| Hero 立绘 | `[0, 0.3]`           | `y`         | `[0, -60]` | 0.8x    |
| 背景光晕  | `[0, 0.3]`           | `opacity`   | `[0.6, 0]` | 0.5x    |
| Hero 文字 | —                    | 无视差      | —        | 1x       |

### 6.3 响应式禁用

```typescript
// hooks/useMediaQuery.ts
export function useMediaQuery(query: string): boolean {
  const [matches, setMatches] = useState(false)
  useEffect(() => {
    const mql = window.matchMedia(query)
    setMatches(mql.matches)
    const handler = (e: MediaQueryListEvent) => setMatches(e.matches)
    mql.addEventListener("change", handler)
    return () => mql.removeEventListener("change", handler)
  }, [query])
  return matches
}
```

Desktop（≥1024px）启用视差，Tablet / Mobile 禁用。

### 6.4 性能注意

- 视差元素限制 2 个（立绘 + 光晕），不额外增加
- 使用 `useSpring` 防止突变导致的视觉抖动
- 视差元素添加 `will-change: transform`

---

## 七、VN 式交互动效

### 7.1 章节标题（ChapterHeader）入场

```
触发：Section 进入视口
  → 整体从左侧滑入 (x: -30 → 0) + 淡入     500ms
```

使用 Framer Motion `chapterReveal` variants：

```tsx
// chapterReveal: { hidden: { opacity: 0, x: -30 }, visible: { opacity: 1, x: 0, transition: { duration: 0.5, ease: [0.16, 1, 0.3, 1] } } }

<motion.div variants={chapterReveal} initial="hidden" whileInView="visible" viewport={{ once: true }}>
  <span>CHAPTER 01</span>
  <h2>Case Studies</h2>
  <div className="h-0.5 w-12 rounded-full bg-dialog-blue" />
</motion.div>
```

### 7.2 对话框（DialogBox）入场

```
触发：对话框所在元素进入视口
  → 对话框从底部上浮 (y: 24 → 0) + 缩放 (scale: 0.98 → 1) + 淡入   500ms
```

使用 `dialogSlideUp` variants。对话框为三层圆角结构：白底框 (`rounded-lg`) → 名字框 (`rounded-xl`) + 文字框 (`rounded-md`)，内层 flex 1:3 固定比例。无逐字显示功能。

### 7.3 选项按钮交互

| 交互 | 动画 | 时长 | 实现 |
|------|------|------|------|
| hover (Desktop) | `scale(1.02)` + `x: 4`（放大 + 右移） | 200ms | Framer Motion `whileHover` |
| tap (点击) | `scale(1.0 → 0.98)` | 150ms | Framer Motion `whileTap` |
| 入场（Contact） | `x: -20 → 0` + `opacity: 0→1` (stagger 120ms) | 350ms | Framer Motion `optionStagger` + `optionItem` |

### 7.4 Section 过渡台词

```
触发：过渡台词进入视口
  → 文字从中心淡入放大 (scale: 0.92 → 1) + opacity   400ms
```

结构：水平贯穿线 + 居中 `~ text ~`。使用 `<SectionTransition>` 组件渲染，`transitionFade` variants 控制入场，仅触发一次。

### 7.5 页面暗转过渡（游戏风场景切换）

Case Study 详情页的进入/退出使用现有 `pageTransition` variants（opacity 过渡），未实现全黑帧。

---
### 7.6 游戏风序幕 TitleScreen 入场

| 元素 | 动画 | 时长 | 实现 |
|------|------|------|------|
| 角色立绘 | `y: 30→0, opacity: 0→1`，缓动 `ease-in-soft` | 800ms | Framer Motion |
| Slogan | `y: 20→0, opacity: 0→1`，`delay: 0.5` | 500ms | Framer Motion |
| 名称 | `y: 20→0, opacity: 0→1`，`delay: 0.9` | 500ms | Framer Motion |
| 开始按钮 | `y: 20→0, opacity: 0→1`，`delay: 1.3` | 500ms | Framer Motion |

### 7.7 游戏风序幕 暗転过渡

| 属性 | 值 |
|------|-----|
| 结构 | 全屏黑色遮罩 + 中心光效粒子向外扩散 |
| 遮罩 | `position: fixed, inset: 0, bg-black`，CSS animation |
| 光效粒子 | `PixelsCurtain` 组件：随机像素/光点向外散射，`@keyframes` 驱动 |
| 持续时间 | ~1500ms（遮罩 opacity: 0→1 持续 400ms → 停留 700ms → 消散 400ms）|
| 触发条件 | TitleScreen「开始」按钮点击 |

### 7.8 游戏风序幕 Scenario 演绎

| 属性 | 值 |
|------|-----|
| 推进方式 | 点击或按任意键进入下一场景 |
| 对话框入场 | 每场景对话框 `y: 20→0, opacity: 0→1`，300ms |
| 表情切换 | 根据 `expression` 字段切换立绘图片，无过渡动画（瞬间切换）|
| 打字机效果 | 台词逐字显示，每字约 30ms，标点停顿加倍 |
| 章节标题 | 仅章节首场景显示，`x: -30→0, opacity: 0→1`，500ms，同 ChapterHeader |
| 场景计数 | 底部显示「场景 N / 43」|

### 7.9 游戏风序幕 LoadingOverlay

| 属性 | 值 |
|------|-----|
| 结构 | 全屏遮罩 + 角色剪影浮现 + 光效粒子 + 波纹扩散 |
| 角色剪影 | 渐变浮现（`opacity: 0→1`），2000ms |
| 光效粒子 | 随机粒子旋转扩散，`@keyframes` 驱动 |
| 波纹 (Ripple) | 圆心向外扩散环，`LoadingRipple` 组件，3 层波纹交错 |
| 完成触发 | 动画完成后自动 `router.push('/[lang]')` 跳转至首页 |

### 7.10 游戏风动效 variants 追加（animations.ts）

```typescript
// 追加游戏风 variants
export const titleReveal: Variants = {
  hidden: { opacity: 0, y: 30 },
  visible: {
    opacity: 1,
    y: 0,
    transition: { duration: 0.8, ease: [0.16, 1, 0.3, 1] },
  },
}

export const sceneEnter: Variants = {
  hidden: { opacity: 0, y: 20 },
  visible: {
    opacity: 1,
    y: 0,
    transition: { duration: 0.3, ease: [0.16, 1, 0.3, 1] },
  },
}
```

---

## 八、Hover 动效

### 8.1 参数总表

| 元素 | CSS 属性变更 | 时长 | 实现方式 |
| --- | --- | --- | --- |
| ProjectCard hover | `scale(1.03)` + `box-shadow: md → lg`，暂停空闲弹跳 | 200ms | Framer Motion `animate` + state |
| CaseStudyCard hover | `scale(1.03)` + `box-shadow: md → lg`，暂停空闲微摆 | 200ms | Framer Motion `animate` + state |
| Primary 按钮 | `filter: brightness(1.05)` | 200ms | CSS transition |
| 导航链接 | `color: text-secondary → primary` | 200ms | CSS transition |
| 技能标签 | `transform: scale(1.0 → 1.05)` | 200ms | CSS transition |
| 社交链接图标 | `color: text-secondary → primary` | 200ms | CSS transition |

### 8.2 实现方式

卡片 hover 使用 Framer Motion 驱动（`onMouseEnter/onMouseLeave` + state 切换 `animate` 目标），其余 hover 使用 CSS transition：

```css
/* globals.css — 非卡片 hover 动效 */
.btn-primary {
  transition: filter 200ms ease-out;
}
.btn-primary:hover {
  filter: brightness(1.05);
}

.nav-link {
  transition: color 200ms ease-out;
}
.nav-link:hover {
  color: var(--color-primary);
}
```

### 8.3 响应式

Mobile（<768px）无 hover 概念，所有 hover 动效不生效。
Tablet（≥768px）hover 动效正常工作。

---

## 八、导航栏滚动效果

### 8.1 实现方案

```tsx
// components/Navbar.tsx
"use client"
import { useScroll, useTransform, motion } from "framer-motion"

export function Navbar() {
  const { scrollY } = useScroll()
  const boxShadow = useTransform(
    scrollY,
    [0, 50],
    ["0 0 0 transparent", "var(--shadow-sm)"]
  )
  const rawBgAlpha = useTransform(scrollY, [0, 50], [0, 0.85])
  const bgColor = useTransform(rawBgAlpha, (v) => `rgba(255,250,253,${v})`)

  return (
    <motion.nav
      className="fixed top-0 z-50 h-16 w-full
                 backdrop-blur-md
                 flex items-center px-4 lg:px-8"
      style={{ backgroundColor: bgColor, boxShadow }}
    >
      {/* 导航内容 */}
    </motion.nav>
  )
}
```

### 8.2 行为描述

- `scrollY` = 0：背景完全透明（`rgba(255,250,253,0)`），无阴影
- `scrollY` > 50px：背景渐现至 `rgba(255,250,253,0.85)` + `backdrop-blur` + `shadow-sm`
- 过渡平滑：Framer Motion useTransform 连续映射 alpha 通道，非 JS scroll 事件监听

### 8.3 移动端

导航固定顶部行为不变。背景透明度 + 模糊 + 阴影规则与 Desktop 一致。

---

### 7.11 游戏风动效 variants（animations.ts 新增）

```typescript
// src/lib/animations.ts — 追加在原文件末尾

export const chapterReveal: Variants = {
  hidden: { opacity: 0, x: -30 },
  visible: {
    opacity: 1,
    x: 0,
    transition: { duration: 0.5, ease: [0.16, 1, 0.3, 1] },
  },
}

export const dialogSlideUp: Variants = {
  hidden: { opacity: 0, y: 24, scale: 0.98 },
  visible: {
    opacity: 1,
    y: 0,
    scale: 1,
    transition: { duration: 0.5, ease: [0.16, 1, 0.3, 1] },
  },
}

export const optionStagger: Variants = {
  hidden: {},
  visible: {
    transition: {
      staggerChildren: 0.12,
      delayChildren: 0.1,
    },
  },
}

export const optionItem: Variants = {
  hidden: { opacity: 0, x: -20 },
  visible: {
    opacity: 1,
    x: 0,
    transition: { duration: 0.35, ease: [0.16, 1, 0.3, 1] },
  },
}

export const transitionFade: Variants = {
  hidden: { opacity: 0, scale: 0.92 },
  visible: {
    opacity: 1,
    scale: 1,
    transition: { duration: 0.4, ease: [0.16, 1, 0.3, 1] },
  },
}
```

---

## 九、卡片空闲循环动画

### 9.1 ProjectCard 空闲弹跳

| 属性 | 值 |
|------|-----|
| 效果 | 卡片每 1.5s 向上弹跳 `y: [0, -12, 0]`，0.25s 弹跳 + 1.25s 停顿，`easeInOut` |
| 周期 | 1.5s（duration 0.25s + repeatDelay 1.25s） |
| 驱动方式 | Framer Motion `animate={{ y: [0, -12, 0] }}` + `transition: { repeat: Infinity }` |
| hover 行为 | 弹跳暂停 → `scale(1.03)` + shadow-md → shadow-lg |
| 展开状态 | `expanded=true` 时弹跳暂停 |
| 残障适配 | `prefersReducedMotion` 禁用弹跳 |

### 9.2 CaseStudyCard 空闲微摆

| 属性 | 值 |
|------|-----|
| 效果 | 卡片每 1.5s 左右微摆 `rotate: [0, 1.5, 0, -1.5, 0]`，0.4s 摆动 + 1.1s 停顿，`easeInOut` |
| 周期 | 1.5s（duration 0.4s + repeatDelay 1.1s） |
| 驱动方式 | Framer Motion `animate={{ rotate: [0, 1.5, 0, -1.5, 0] }}` + `transition: { repeat: Infinity }` |
| hover 行为 | 微摆暂停 → `scale(1.03)` + shadow-md → shadow-lg |
| 残障适配 | `prefersReducedMotion` 禁用微摆 |

---

## 十、ProjectCard 点击 clip-path 圆形展开交互

### 9.1 交互概述

| 属性 | 值 |
|------|-----|
| 触发 | 点击 ProjectCard 任意区域 |
| 行为 | 点击切换 `expanded` 状态；展开时初始内容以圆形 clip-path 从中心向外扩散消失，露出详情内容（标题 → 背景标签+内容 → 贡献标签+bullet list → 成果标签+内容）；再次点击圆形从边缘向中心收缩恢复初始内容 |
| 范围 | 整个卡片内部内容全部替换（封面+标题+标签 ↔ 详情文本） |
| 卡片高度 | 由初始内容（封面 4:3 + 标题 + 标签）自然撑起；详情内容 `absolute inset-0` 填满同一空间，内容超出滚动 |

### 9.2 动画参数

| 属性 | Desktop | Mobile (<768px) |
|------|---------|-----------------|
| 展开动画 | `clipPath: circle(100% at 50% 50%)` → `circle(0% at 50% 50%)`，顶部层中心向外扩散消失 | 同上，duration 缩短 |
| 收拢动画 | `clipPath: circle(0% at 50% 50%)` → `circle(100% at 50% 50%)`，顶部层边缘向中心收缩恢复 | 同上，duration 缩短 |
| duration | 0.6s | 0.3s |
| ease | `[0.16, 1, 0.3, 1]` | 同上 |
| 驱动方式 | Framer Motion `animate={{ clipPath }}` | 同上 |

### 9.3 实现结构

```
<article className="relative cursor-pointer overflow-hidden rounded-lg bg-surface shadow-md ...">
  <!-- 底部层：详情内容（absolute 填满父容器，scrollbar 隐藏） -->
  <div className="scrollbar-micro absolute inset-0 overflow-y-auto p-6">
    <h3>标题（{titleKey}）</h3>

    <section>
      <p>背景标签（{backgroundLabel}）</p>
      <p>{background}</p>
    </section>

    <section>
      <p>贡献标签（{contributeLabel}）</p>
      <ul>
        <li>{contribute 直接渲染数组项}</li>
        <li>...</li>
      </ul>
    </section>

    <section>
      <p>成果标签（{outcomeLabel}）</p>
      <p>{outcome}</p>
    </section>
  </div>

  <!-- 顶部层：初始内容（正常文档流撑起卡片高度） -->
  <motion.div
    className="bg-surface p-6"
    initial={{ clipPath: "circle(100% at 50% 50%)" }}
    animate={{ clipPath: expanded ? "circle(0% at 50% 50%)" : "circle(100% at 50% 50%)" }}
    transition={{ duration: desktop ? 0.6 : 0.3, ease: [0.16, 1, 0.3, 1] }}
  >
    <!-- 封面图 4:3 + 标题 + 技能标签 -->
  </motion.div>
</article>
```

### 9.4 移动端策略

- 保留 clip-path 动画，duration 从 0.6s 缩短至 0.3s
- 使用 `src/hooks/useMediaQuery.ts` 中的 `useMediaQuery("(max-width: 767px)")` 检测

### 9.5 响应式禁用策略

| 动效 | Mobile <768px | Tablet 768–1023 | Desktop ≥1024 |
|------|---------------|-----------------|---------------|
| ProjectCard clip-path 圆形展开 | ✅ 保留，duration 0.3s | ✅ 保留，duration 0.6s | ✅ 保留，duration 0.6s |

---

## 十、响应式禁用策略

| 动效                 | Mobile <768px                          | Tablet 768–1023                    | Desktop ≥1024                     |
| -------------------- | -------------------------------------- | ---------------------------------- | --------------------------------- |
| Hero 出场序列        | ✅ 保留，delay 统一乘 0.6 加速         | ✅ 保留，完整时序                  | ✅ 保留，完整时序                 |
| Section 入场 stagger | ✅ 保留                                | ✅ 保留                            | ✅ 保留                           |
| 视差（Parallax）     | ❌ 禁用                                | ❌ 禁用                            | ✅ 启用                           |
| hover 动效           | ❌ 禁用（无 hover）                    | ✅ 启用                            | ✅ 启用                           |
| 页面过渡             | ✅ 保留                                | ✅ 保留                            | ✅ 保留                           |
| 语言切换过渡         | ✅ 保留                                | ✅ 保留                            | ✅ 保留                           |
| 光晕脉冲 CSS         | ❌ 禁用                                | ✅ 启用                            | ✅ 启用                           |
| 立绘浮动 CSS         | ❌ 禁用                                | ✅ 启用                            | ✅ 启用                           |
| 章节标题入场         | ✅ 保留（x:-30→0 滑入）                | ✅ 保留                            | ✅ 保留                           |
| 对话框入场           | ✅ 保留（y:24→0）                      | ✅ 保留                            | ✅ 保留                           |
| ProjectCard clip-path 圆形展开 | ✅ 保留，duration 0.3s | ✅ 保留，duration 0.6s | ✅ 保留，duration 0.6s |
| ProjectCard 空闲弹跳 | ✅ 保留 | ✅ 保留 | ✅ 保留 |
| CaseStudyCard 空闲微摆 | ✅ 保留 | ✅ 保留 | ✅ 保留 |
| 过渡台词             | ✅ 保留（`~ text ~` 简化版）            | ✅ 保留                            | ✅ 保留                           |
| 选项按钮 hover       | ❌ 禁用                                | ✅ 启用                            | ✅ 启用                           |
| 页面暗转过渡         | ✅ 保留（现有 pageTransition）          | ✅ 保留                            | ✅ 保留                           |
| 游戏风序幕 TitleScreen | ✅ 保留 | ✅ 保留 | ✅ 保留 |
| 游戏风序幕 暗転 | ✅ 保留 | ✅ 保留 | ✅ 保留 |
| 游戏风序幕 Scenario | ✅ 保留（打字机加速） | ✅ 保留 | ✅ 保留 |
| 游戏风序幕 LoadingOverlay | ✅ 保留（简化粒子） | ✅ 保留 | ✅ 保留 |

---

## 十二、无障碍与性能

### 12.1 prefers-reduced-motion

```css
/* globals.css */
@media (prefers-reduced-motion: reduce) {
  *, *::before, *::after {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
  }
}
```

用户开启系统"减少动效"后，所有动画直接跳过，显示最终态。

### 12.2 性能注意事项

- **避免布局抖动（Layout Thrashing）**：stagger 动画添加 `will-change: transform, opacity`
- **减少 JS 开销**：背景光晕 / 角色浮动使用 CSS animation，不叠加 Framer Motion 逐帧；卡片空闲循环动画使用 Framer Motion `animate` prop 而非 CSS（需要暂停/恢复切换）
- **视差元素上限**：最多 2 个（立绘 + 光晕），不再扩展
- **图片避免 Framer Motion 动画**：立绘使用 CSS 浮动动画，Framer Motion 只控制入场 opacity + y
- **AnimatePresence mode="wait"**：确保退出动画完成后才渲染新内容，避免同时渲染导致的性能峰值
- **viewport once: true**：Section 入场动画只触发一次，不重复

---

## 附录：animations.ts 完整代码

```typescript
// src/lib/animations.ts — 供 AI Agent 直接引用
import type { Variants } from "framer-motion"

export const fadeInUp: Variants = {
  hidden: { opacity: 0, y: 20 },
  visible: {
    opacity: 1,
    y: 0,
    transition: { duration: 0.5, ease: [0.16, 1, 0.3, 1] },
  },
}

export const staggerContainer: Variants = {
  hidden: {},
  visible: {
    transition: {
      staggerChildren: 0.15,
      delayChildren: 0.1,
    },
  },
}

export const staggerItem: Variants = {
  hidden: { opacity: 0, y: 20 },
  visible: {
    opacity: 1,
    y: 0,
    transition: { duration: 0.5, ease: [0.16, 1, 0.3, 1] },
  },
}

export const pageTransition: Variants = {
  initial: { opacity: 0 },
  animate: {
    opacity: 1,
    transition: { duration: 0.2 },
  },
  exit: {
    opacity: 0,
    transition: { duration: 0.15 },
  },
}

export const sectionTitle: Variants = {
  hidden: { opacity: 0, y: 15 },
  visible: {
    opacity: 1,
    y: 0,
    transition: { duration: 0.4, ease: [0.16, 1, 0.3, 1] },
  },
}

/* ── game-style browsing experience variants ── */

export const chapterReveal: Variants = {
  hidden: { opacity: 0, x: -30 },
  visible: {
    opacity: 1,
    x: 0,
    transition: { duration: 0.5, ease: [0.16, 1, 0.3, 1] },
  },
}

export const dialogSlideUp: Variants = {
  hidden: { opacity: 0, y: 24, scale: 0.98 },
  visible: {
    opacity: 1,
    y: 0,
    scale: 1,
    transition: { duration: 0.5, ease: [0.16, 1, 0.3, 1] },
  },
}

export const optionStagger: Variants = {
  hidden: {},
  visible: {
    transition: {
      staggerChildren: 0.12,
      delayChildren: 0.1,
    },
  },
}

export const optionItem: Variants = {
  hidden: { opacity: 0, x: -20 },
  visible: {
    opacity: 1,
    x: 0,
    transition: { duration: 0.35, ease: [0.16, 1, 0.3, 1] },
  },
}

export const transitionFade: Variants = {
  hidden: { opacity: 0, scale: 0.92 },
  visible: {
    opacity: 1,
    scale: 1,
    transition: { duration: 0.4, ease: [0.16, 1, 0.3, 1] },
  },
}
```
