# 2D Portfolio — 完整技术规格书与开发计划

> 项目：KONG JUN SIENG — Software Developer Portfolio  
> 品牌调性：日系萌系 × 浅色科技感，Gal 社风格柔和动效  
> VN 浏览体验：单页滚动如推进视觉小说情节——章节标题、对话框叙事、选项式选择  
> 蓝色素材：与立绘配套的蓝色对话框/选项按钮/菜单边框 PNG 整合为全站 UI 系统  
> 目标受众：招聘方（HR / 技术主管）  
> 核心目标：找工作，引导访问者查看 Case Study

---

## 一、技术选型矩阵

| 层面              | 选择                                        | 版本/备注                                                  |
| --------------- | ----------------------------------------- | ------------------------------------------------------ |
| **框架**          | Next.js 16                                | App Router, React 19                                   |
| **语言**          | TypeScript Strict                         | `tsconfig.json` strict: true, 全量 TS                    |
| **包管理**         | pnpm                                      | 最新稳定版                                                  |
| **CSS**         | Tailwind CSS v4                           | 自定义设计令牌                                                |
| **UI 组件**       | shadcn/ui                                 | 基础骨架 + 覆写样式                                            |
| **动画**          | Framer Motion                             | 唯一动画库，无 GSAP                                           |
| **字体**          | 全自托管 + `next/font`                        | JP: M PLUS Rounded 1c, EN: Nunito, ZH: Noto Sans SC |
| **数据校验**        | Zod                                       | 校验 profile.json / projects.json / case-studies.json    |
| **i18n**        | next-intl                                 | path-based 路由策略 (`/[lang]/...`)                        |
| **数据层**         | 本地 JSON + Markdown                        | 无 CMS，无数据库                                             |
| **Markdown 渲染** | gray-matter + react-markdown + remark-gfm | 轻量，无 MDX                                               |
| **CI**          | GitHub Actions                            | lint + typecheck + build                               |
| **部署**          | Vercel                                    | auto deploy from main, PR previews                     |
| **联系表单**        | 纯 UI 展示                                   | 无后端逻辑                                                  |
| **测试**          | 无                                         | v1 不引入测试框架，依赖 TypeScript + ESLint + Build 保障           |
| **分析监控**        | 无                                         | v1 不接入                                                 |
| **原型方式**        | 代码驱动设计                                    | 辅以结构化设计文档（自然语言 + 小标题 MD 文件）                            |
| **依赖更新**        | 手动                                        | 不启用 Dependabot / Renovate                              |

---

## 二、设计系统

### 2.1 设计令牌（Design Tokens）

```css
--color-primary: #FFB7C5;
--color-primary-soft: #FFE4EC;
--color-secondary: #D5C6E0;
--color-accent: #B5EAD7;
--color-background: #FFFAFD;
--color-surface: #FFFFFF;
--color-text-primary: #4A3F42;
--color-text-secondary: #8E8386;
--color-border: #F0E6EB;
--color-dialog-blue: #2FA4D7;
--color-dialog-dark: #1A8DB8;
--color-dialog-soft: #E5F4FC;
```

### 2.2 字体方案

| 语言 | 字体 | 加载方式 | 实际字重 |
|------|------|---------|---------|
| 日文 (ja) | M PLUS Rounded 1c | `next/font/local` 自托管 `src/lib/fonts.ts` | Regular (仅此一字重) |
| 英文 (en) | Nunito | `next/font/local` 自托管 `src/lib/fonts.ts` | Regular (仅此一字重) |
| 中文 (zh) | Noto Sans SC | 手动 `@font-face` `src/styles/fonts.css`（CJK 13 子集 woff2） | Regular (400) |

### 2.3 VN 浏览体验

- **章节式滚动**：每个 Section 顶部显示「CHAPTER N — 标题」装饰性章节标识，模拟 VN 章节切换
- **对话框叙事**：About 自我介绍、Case Study 描述等关键内容以蓝色对话框承载，模拟角色对话
- **选项式交互**：CTA / 查看详情 / 简历下载使用与立绘配套的选项按钮素材，模拟 VN 选择肢
- **过渡台词**：Section 之间插入短暂的过渡台词对话框（淡入→停留 2.5s→淡出），浏览如翻页
- **系统菜单 UI**：Navbar 模拟游戏菜单栏，Footer 模拟系统状态栏

### 2.4 视觉风格

- **Hero 核心**：BOOTH / DLsite 商业授权看板娘立绘（透明背景 PNG），大型角色立绘搭配 Slogan
- **图片格式**：保留原始 PNG，生产通过 Next.js Image 自动优化优先输出 AVIF，WebP 为兼容格式
- **Hero 动画**：纯 CSS 轻量动画（淡入、轻微浮动、渐变光晕），不使用 Lottie / Live2D / 粒子特效
- **整体布局**：参考美少女游戏官网风格，减少边框和卡片元素，强调留白与层次感；融入蓝色系 UI 素材增强游戏感
- **微妙视差**：滚动时前景/背景元素以不同速度移动，营造深度感和沉浸感；通过 Framer Motion scroll progress 驱动
- **配色分工**：粉色系 = 角色/情感区域，蓝色系 = UI/系统/信息区域
- **资产上限**：预期不超过 20 张图片 + 1 张 UI 素材（`option-button.png` 900×60）。`dialog-box.png` 已删除，对话框使用纯 CSS。8 张角色表情 PNG 用于 VN 序幕场景对话

---

## 三、路由与页面结构

```
/[lang]/intro              → VN 序幕（TitleScreen → 暗転 → Scenario → LoadingOverlay → 首页）
/[lang]                    → 首页单页滚动
  ├── #hero                 → Hero Banner（立绘 + Slogan + CTA）
  ├── #case-studies         → Case Studies 卡片列表（跳转详情页）
  ├── #projects             → 项目展示卡片（纯展示，无跳转）
  ├── #about                → 关于我（个人介绍 + 技能标签）
  └── #contact              → 联系区块（邮箱链接 + 社交链接 + 简历下载）

/[lang]/projects/[slug]     → Case Study 详情页（动态路由，Markdown 渲染）
```

> 静态路径合计：3 页面 × 3 语言 = 9 条静态路径（`intro` + `首页` + `projects/[slug]`）

### 重定向策略

| 来源 | 目标 | 方式 |
|------|------|------|
| `/` | `/zh/intro` | proxy.ts 重定向（默认中文，进入 VN 序幕） |
| 未匹配路由 | 404 | `not-found.tsx` |

---

## 四、数据层结构

### 4.1 数据文件

| 文件 | 说明 | 校验 |
|------|------|------|
| `src/data/profile.json` | 个人资料（name, slogan, skills, socialLinks, email）; 含 GitHub（`KONGJUNSIENG`）及 LinkedIn（`jun-sieng-kong-b1b3213b3`） | Zod |
| `src/data/projects.json` | 项目卡片数据（slug, cover, techStack, titleKey, backgroundKey, contributeKey, outcomeKey） | Zod |
| `src/data/case-studies.json` | Case Study 列表数据（slug, cover, techStack, titleKey, descriptionKey） | Zod |
| `src/data/portfolio-{lang}.md` | Case Study 详情 Markdown（中日英各一） | - |
| `src/messages/{lang}.json` | 三语文本 | - |
| `public/resume-{lang}.pdf` | 各语言简历 PDF | - |

### 4.2 数据流

```
messages/{lang}.json      ←  所有文本（导航 / Hero / Projects / Case Studies / About / Contact）
     │
profile.json             ←  name, slogan, skills, socialLinks, email（固定，不翻译）
     │
projects.json            ←  slug, cover, techStack（固定，不翻译）
     │                    ├── titleKey → messages/{lang}.json
     │                    ├── backgroundKey → messages/{lang}.json
     │                    ├── contributeKey → messages/{lang}.json
     │                    └── outcomeKey → messages/{lang}.json
     │
case-studies.json        ←  slug, cover, techStack（固定，不翻译）
     │                    └── titleKey, descriptionKey → messages/{lang}.json
     │
data/portfolio-{lang}.md ←  Case Study 按语言拆分，gray-matter 解析 frontmatter
```

---

## 五、目录结构

```
project-root/
├── .github/
│   └── workflows/
│       └── ci.yml              # GitHub Actions: lint + typecheck + build
├── public/
│   ├── images/                 # 封面图、立绘、项目截图等
│   ├── fonts/                  # 自托管字体文件
│   ├── resume-zh.pdf
│   ├── resume-ja.pdf
│   └── resume-en.pdf
├── src/
│   ├── app/
│   │   ├── [lang]/
│   │   │   ├── intro/
│   │   │   │   └── page.tsx        # VN 序幕
│   │   │   ├── layout.tsx      # 多语布局（Nav + Footer + next-intl provider）
│   │   │   ├── page.tsx        # 首页（组合各 Section）
│   │   │   └── projects/
│   │   │       └── [slug]/
│   │   │           └── page.tsx # Case Study 详情页
│   │   ├── _not-found/         # 404 页面
│   │   ├── layout.tsx          # 根布局（仅 html + body + fonts）
│   │   ├── not-found.tsx       # 404 页面
│   │   ├── sitemap.ts
│   │   └── robots.ts
│   ├── components/
│   │   ├── Navbar.tsx
│   │   ├── Footer.tsx
│   │   ├── LanguageSwitcher.tsx
│   │   ├── HeroSection.tsx
│   │   ├── ChapterHeader.tsx        # ★ VN 章节标题（CHAPTER N + 分割线）
│   │   ├── SectionTransition.tsx    # ★ VN 过渡台词（Section 间短暂出现）
│   │   ├── DialogBox.tsx            # ★ VN 蓝色对话框（自我介绍/描述/台词）
│   │   ├── OptionButton.tsx         # ★ VN 选项按钮（CTA/查看详情/下载）
│   │   ├── CaseStudiesSection.tsx
│   │   ├── ProjectsSection.tsx
│   │   ├── AboutSection.tsx
│   │   ├── ContactSection.tsx
│   │   ├── CaseStudyCard.tsx
│   │   ├── ProjectCard.tsx
│   │   ├── SkillTag.tsx
│   │   ├── SocialLink.tsx
│   │   ├── ScrollToAnchor.tsx
│   │   ├── LayoutShell.tsx      # 客户端 Shell（AnimatePresence + Nav + Footer）
│   │   ├── LocaleContent.tsx    # 客户端内容包装器
│   │   ├── ThemeAwareHtml.tsx   # 根布局辅助
│   │   ├── IntroAnimation.tsx       # ★ VN2 动画协调器
│   │   ├── IntroShell.tsx           # ★ VN2 流程编排器
│   │   ├── LoadingOverlay.tsx        # ★ VN2 加载遮罩
│   │   ├── LoadingRipple.tsx         # ★ VN2 波纹动画
│   │   ├── PixelsCurtain.tsx         # ★ VN2 暗転遮罩
│   │   ├── ScrambleText.tsx          # ★ VN2 文字乱码
│   │   ├── TitleScreen.tsx           # ★ VN2 标题画面
│   │   └── VNScenario.tsx            # ★ VN2 场景演绎
│   ├── hooks/                  # 自定义 React Hooks
│   │   └── useMediaQuery.ts    # 响应式断点检测
│   │
│   ├── i18n/
│   │   └── request.ts          # next-intl 请求配置
│   │
│   ├── lib/
│   │   ├── animations.ts       # Framer Motion variants
│   │   ├── fonts.ts            # next/font 声明（JP/EN）
│   │   ├── profile.schema.ts   # Zod schema: 个人资料
│   │   ├── projects.schema.ts  # Zod schema: 项目数据
│   │   ├── case-studies.schema.ts # Zod schema: Case Study 数据
│   │   ├── read-data.ts        # JSON 读取 + Zod 校验
│   │   ├── read-mdx.ts         # Markdown 读取 + frontmatter 解析
│   │   └── utils.ts            # cn() 工具函数
│   ├── data/
│   │   ├── profile.json
│   │   ├── projects.json
│   │   ├── case-studies.json
│   │   ├── portfolio-zh.md
│   │   ├── portfolio-ja.md
│   │   ├── portfolio-en.md
│   │   ├── vn-scenario.zh.json
│   │   ├── vn-scenario.ja.json
│   │   └── vn-scenario.en.json
│   ├── messages/
│   │   ├── zh.json
│   │   ├── ja.json
│   │   └── en.json
│   ├── styles/
│   │   ├── globals.css         # Tailwind 指令 + 自定义 token
│   │   └── fonts.css           # Noto Sans SC @font-face（13 子集）
│   └── proxy.ts                # i18n 语言检测 + 重定向（Next.js 16 已弃用 middleware.ts）
├── .env.example
├── .gitignore
├── .prettierrc
├── next.config.ts
├── postcss.config.mjs
├── tsconfig.json
├── package.json
└── README.md
```

---

## 六、SDLC 阶段开发计划

### 阶段 1：项目初始化与环境搭建

| # | 操作 | 产出 |
|---|------|------|
| 1.1 | `pnpm create next-app@latest` → App Router + TypeScript + Tailwind + ESLint | 项目骨架 |
| 1.2 | 配置 `tsconfig.json` strict 模式 | 类型安全基础 |
| 1.3 | 安装核心依赖：`next-intl` `framer-motion` `zod` + `shadcn/ui` 初始化 | 依赖就绪 |
| 1.4 | 安装 Markdown 依赖：`gray-matter` `react-markdown` `remark-gfm` | MD 渲染 |
| 1.5 | 配置 Tailwind v4 自定义设计令牌：`globals.css` 中添加 `@import "tailwindcss"` + `@theme` 声明（颜色、字体、间距、圆角、阴影、容器宽度、动效时长） | `src/styles/globals.css` |
| 1.6 | 下载并放置字体文件到 `public/fonts/`，JP/EN 用 `next/font/local`，ZH（CJK 子集）用 `@font-face` | `src/lib/fonts.ts` + `src/styles/fonts.css` |
| 1.7 | 配置 next-intl：`proxy.ts`（Next.js 16 已弃用 middleware.ts） + `i18n/request.ts` + `messages/*.json` | i18n 基础 |
| 1.8 | 搭建目录结构（`src/components/` `src/lib/` `src/data/`） | 目录就绪 |
| 1.9 | 配置 `.env.example` | 环境变量模板 |
| 1.10 | 配置 `.github/workflows/ci.yml`（lint + typecheck + build） | CI 流水线 |

### 阶段 2：数据层与类型系统

| # | 操作 | 产出 |
|---|------|------|
| 2.1 | Zod schema：`profile.schema.ts` | name, slogan, skills, socialLinks |
| 2.2 | Zod schema：`projects.schema.ts` | slug, cover, techStack, titleKey |
| 2.3 | Zod schema：`case-studies.schema.ts` | slug, cover, techStack, titleKey, descriptionKey |
| 2.4 | 创建 `profile.json`（姓名、slogan、技能标签、社交链接） | 数据文件 |
| 2.5 | 创建 `projects.json`（初始 2 条：fyp-iot, portfolio） | 数据文件 |
| 2.6 | 创建 `case-studies.json`（初始 1 条：portfolio） | 数据文件 |
| 2.7 | 创建 `messages/zh.json` `messages/ja.json` `messages/en.json`（完整字段） | 三语文本 |
| 2.8 | 创建 `data/portfolio-zh.md` `data/portfolio-ja.md` `data/portfolio-en.md` | Case Study 内容 |
| 2.9 | 实现 `src/lib/read-data.ts`（通用 JSON 读取 + Zod parse + 错误处理） | 数据访问层 |
| 2.10 | 实现 `src/lib/read-mdx.ts`（Markdown 读取 + frontmatter 解析） | MD 数据层 |

### 阶段 3：全局布局与导航

| # | 操作 | 产出 |
|---|------|------|
| 3.1 | 根 `layout.tsx` — 仅 html + body + next/font 注入 | 根布局 |
| 3.2 | `[lang]/layout.tsx` — NextIntlClientProvider + Navbar + Footer + 响应式容器 | 多语布局 |
| 3.3 | `Navbar.tsx` — 固定顶部导航 + 锚点链接 + 语言切换器 + 滚动背景模糊 | 导航组件 |
| 3.4 | `Footer.tsx` — 版权信息 + 社交链接图标 | 页脚组件 |
| 3.5 | `LanguageSwitcher.tsx` — 中/日/EN 切换按钮，当前语言高亮 | i18n 切换 |
| 3.6 | `ScrollToAnchor.tsx` — 客户端组件，平滑滚动至锚点 | 导航辅助 |

### 阶段 4：首页各区块

| # | 操作 | 产出 |
|---|------|------|
| 4.1 | `HeroSection.tsx` — 角色立绘（Next/Image 优化）+ 姓名 + Slogan + CTA 按钮 + CSS 淡入浮动动画 | Hero 完成 |
| 4.2 | `CaseStudiesSection.tsx` — 卡片列表（封面 + 技术标签 + 标题 + 描述 + CTA），跳转详情页 | Case Studies 完成 |
| 4.3 | `CaseStudyCard.tsx` — 可复用卡片组件，含 hover 动画 | 卡片组件 |
| 4.4 | `ProjectsSection.tsx` — 卡片列表（封面 + 技术标签 + 标题），纯展示无跳转 | Projects 完成 |
| 4.5 | `ProjectCard.tsx` — 可复用卡片组件 | 卡片组件 |
| 4.6 | `AboutSection.tsx` — 个人介绍文本 + 技能标签列表 | About 完成 |
| 4.7 | `SkillTag.tsx` — 技能标签原子组件 | 原子组件 |
| 4.8 | `ContactSection.tsx` — 邮箱链接 + 社交链接图标 + 简历下载按钮（无表单，message 中 formX 键暂未使用） | Contact 完成 |
| 4.9 | `SocialLink.tsx` — 社交链接图标组件 | 原子组件 |
| 4.10 | 首页 `page.tsx` — 组合所有 Section + Framer Motion 入场动画 | 首页完成 |

### 阶段 5：Case Study 详情页

| # | 操作 | 产出 |
|---|------|------|
| 5.1 | `projects/[slug]/page.tsx` — 动态路由，根据 slug 读取对应语言 Markdown | 详情页路由 |
| 5.2 | Markdown 渲染：gray-matter 解析 frontmatter → react-markdown 渲染 body | 渲染管线 |
| 5.3 | 详情页布局：封面图 + 标题 + tech stack 标签 + role + date + Markdown 正文 | 详情页完成 |
| 5.4 | 返回首页导航按钮 + 面包屑或返回链接 | 导航回退 |

### 阶段 6：动效与交互动画系统

| # | 操作 | 产出 |
|---|------|------|
| 6.1 | Hero 背景 CSS 动画：渐变光晕脉冲 `@keyframes hero-glow-pulse` + 立绘轻微浮动 `@keyframes character-float`，Mobile 禁用 | 氛围背景 |
| 6.2 | Section 入场 Framer Motion `whileInView`：T4.3/T4.5/T4.6/T4.7 使用 staggerContainer + staggerItem + sectionTitle | 滚动动画 |
| 6.3 | Hero 视差效果：Desktop 立绘 useTransform(scrollYProgress) + useSpring 阻尼，Tablet/Mobile 禁用 | 视差效果 |
| 6.4 | 卡片 hover + 空闲循环动画：ProjectCard 每 1.5s 弹跳 `y: [0, -12, 0]` + hover 暂停弹跳 `scale(1.03)`；CaseStudyCard 每 1.5s 微摆 `rotate: [0, 1.5, 0, -1.5, 0]` + hover 暂停微摆 `scale(1.03)`；Framer Motion 驱动 | 交互反馈 |
| 6.5 | 导航栏滚动效果：`useScroll` + `useTransform` 驱动 shadow 渐入，背景模糊常驻 | 视觉提升 |
| 6.6 | 创建 `src/hooks/useMediaQuery.ts`：响应式断点检测 hook | hook |
| 6.7 | 语言切换过渡动画：AnimatePresence mode=wait + key=locale，淡出/淡入 | 切换体验 |
| 6.8 | 页面过渡动画：首页 ↔ 详情页 opacity 过渡，exit 150ms → enter 200ms | 页面过渡 |

### 阶段 7：VN 游戏感 UI 系统与首页集成

**Part A — VN 组件构建**

| # | 操作 | 产出 |
|---|------|------|
| 7.1 | 创建 `DialogBox.tsx`：VN 蓝色对话框，三层圆角，白框→蓝底名字框+文字框，flex 1:3 | 对话框组件 |
| 7.2 | 创建 `ChapterHeader.tsx`：VN 章节标题（CHAPTER N 标签 + 标题 + 蓝色下划线） | 章节标题组件 |
| 7.3 | 创建 `OptionButton.tsx`：VN 选项按钮，蓝底白字 + ▶ 指示符，hover 右移放大 | 选项按钮组件 |
| 7.4 | 创建 `SectionTransition.tsx`：过渡台词 `~ text ~` + 水平线，whileInView 淡入 | 过渡组件 |
| 7.5 | `globals.css` 新增 `--color-dialog-blue` / `--color-dialog-dark` / `--color-dialog-soft` | 蓝色色值 |
| 7.6 | `animations.ts` 追加 VN variants（chapterReveal / dialogSlideUp / optionStagger / optionItem / transitionFade） | 动效 variants |
| 7.7 | `messages/{lang}.json` 追加 `transitions` 键（4 条过渡台词） | 台词文本 |

**Part B — Section 集成**

| # | 操作 | 产出 |
|---|------|------|
| 7.8 | Hero CTA 改为 OptionButton | Hero VN 化 |
| 7.9 | CaseStudies Section 顶部加 ChapterHeader(01) | CaseStudy VN 化 |
| 7.10 | Projects Section 顶部加 ChapterHeader(02) | Projects VN 化 |
| 7.11 | About Section 顶部加 ChapterHeader(03)，bio 改为 DialogBox | About VN 化 |
| 7.12 | Contact Section 顶部加 ChapterHeader(04)，联系方式改为 OptionButton 列表 | Contact VN 化 |
| 7.13 | page.tsx Section 之间插入 4 段 SectionTransition | 过渡台词 |

### 阶段 8：SEO 与部署

| # | 操作 | 产出 |
|---|------|------|
| 8.1 | 首页 `generateMetadata`：从 profile.json + messages 动态生成 title/description/OG | SEO meta |
| 8.2 | 详情页 `generateMetadata`：从 frontmatter 动态生成 title/description/OG | SEO meta |
| 8.3 | `sitemap.ts`：动态生成 sitemap.xml（含所有 (lang,slug) 组合） | sitemap |
| 8.4 | `robots.ts`：robots.txt 配置 | robots |
| 8.5 | `not-found.tsx`：404 页面 + 返回首页链接 | 404 |
| 8.6 | 放置公共资源：字体文件 woff2 + 占位图片 + 简历 PDF | public/ |
| 8.7 | 最终 CI 验证：`pnpm lint` + `pnpm typecheck` + `pnpm build` 全部通过 | 质量门禁 |

### 阶段 9：VN 序幕（Intro Scenario）

| # | 操作 | 产出 |
|---|------|------|
| 9.1 | 创建 VN 序幕数据文件 `vn-scenario.{lang}.json`（43 场景） | 三语场景剧本 |
| 9.2 | 创建 `TitleScreen.tsx`：标题画面组件 | TitleScreen |
| 9.3 | 创建 `PixelsCurtain.tsx`：暗転遮罩动画组件 | 暗転效果 |
| 9.4 | 创建 `VNScenario.tsx`：场景演绎组件（表情切换 + 打字机效果） | Scenario |
| 9.5 | 创建 `LoadingOverlay.tsx` + `LoadingRipple.tsx` + `ScrambleText.tsx`：加载过渡组件 | LoadingOverlay |
| 9.6 | 创建 `IntroShell.tsx` + `IntroAnimation.tsx`：流程编排与动效协调 | 编排器 |
| 9.7 | 创建 `intro/page.tsx`：组合所有 VN 序幕组件 | Intro 路由 |
| 9.8 | 更新 `proxy.ts`：`/` → `/zh/intro` 重定向 | 入口重定向 |

### 阶段 10：ProjectCard 点击 clip-path 圆形展开交互

| # | 操作 | 产出 |
|---|------|------|
| 10.1 | Zod schema 扩展：`projects.schema.ts` 追加 `backgroundKey` / `contributeKey` / `outcomeKey` 三个必填 string 字段 | schema 更新 |
| 10.2 | `projects.json` 数据扩展：为 fyp-iot + portfolio 填写新字段值；`messages/{lang}.json` 追加对应的三语文本 | 数据更新 |
| 10.3 | `ProjectCard.tsx` 重写：双层级 DOM 结构（初始内容=正常文档流决定卡片高度，详情内容=`absolute inset-0` 填满同一空间），点击切换 `expanded` 状态，Framer Motion `clipPath: circle()` 驱动圆形扩散/收缩动画，`overflow-hidden` 裁切。Desktop 0.6s / Mobile 0.3s | 交互组件 |

---

## 七、分支策略与 CI/CD

### 分支策略

```
feature/* → Pull Request → main
```

### CI 流水线（`.github/workflows/ci.yml`）

```yaml
name: CI
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm run lint
      - run: pnpm run typecheck
      - run: pnpm run build
```

### 部署

| 环境 | 触发 | 方式 |
|------|------|------|
| Production | push to main | Vercel auto deploy |
| Preview | PR 创建/更新 | Vercel auto deploy for PR |

---

## 八、初始内容清单

### Projects（首页卡片，点击 clip-path 展开详情）

| Slug | 说明 |
|------|------|
| `fyp-iot` | FYP IoT 成品 |
| `portfolio` | 本 2D Portfolio 网站本身 |

### Case Studies（含独立详情页）

| Slug | 对应 Project | 说明 |
|------|-------------|------|
| `portfolio` | portfolio | 本网站的深度开发案例 |

### 简历

| 语言 | 文件路径 |
|------|---------|
| zh | `public/resume-zh.pdf` |
| ja | `public/resume-ja.pdf` |
| en | `public/resume-en.pdf` |

### VN 序幕

| 文件 | 说明 |
|------|------|
| `src/data/vn-scenario.zh.json` | 中文版 43 场景剧本 |
| `src/data/vn-scenario.ja.json` | 日文版 43 场景剧本 |
| `src/data/vn-scenario.en.json` | 英文版 43 场景剧本 |

---

## 九、消息文件结构（messages/{lang}.json）

```jsonc
{
  "nav": {
    "caseStudies": "案例",
    "projects": "作品",
    "about": "关于",
    "contact": "联系"
  },
  "hero": {
    "title": "Software Developer / 全栈探索者",
    "ctaText": "查看作品"
  },
  "caseStudies": {
    "sectionTitle": "案例研究",
    "portfolio": {
      "title": "Portfolio+AI半自动化开发案例",
      "description": "从设计到开发,全程与AI协作完成的Portfolio网站。融合日系萌系视觉风格与VN游戏感浏览体验。"
    },
    "viewButton": "查看详情"
  },
  "projects": {
    "sectionTitle": "作品",
    "fypIot": {
      "title": "FYP IoT 项目",
      "background": "项目背景描述",
      "contribute": "我的贡献描述",
      "outcome": "项目成果描述"
    },
    "portfolio": {
      "title": "Portfolio",
      "background": "项目背景描述",
      "contribute": "我的贡献描述",
      "outcome": "项目成果描述"
    }
  },
  "about": {
    "bio": "毕业设计别人写论文,我写了套让设备自己学会汇报工作的IoT边缘端系统...（完整中文约 350 字个人故事，见 zh.json）"
  },
  "contact": {
    "sectionTitle": "联系我",
    "resumeBtnText": "下载简历",
    "formName": "姓名",
    "formEmail": "邮箱",
    "formMessage": "留言",
    "formSubmit": "发送"
  },
  // 注：formX 键存在于 messages 中，但 ContactSection 当前未渲染表单
  "transitions": {
    "heroToCaseStudies": "来看看我的作品吧",
    "caseStudiesToProjects": "这些是我做过的项目",
    "projectsToAbout": "接下来是关于我的故事",
    "aboutToContact": "来联系我吧"
  },
  "footer": {
    "copyright": "© 2026 KONG JUN SIENG"
  }
}
```

---

## 十、内容更新指南

所有内容通过修改 `src/data/` 和 `src/messages/` 下的文件更新：

1. **修改个人资料** → 编辑 `src/data/profile.json`
2. **增删项目卡片** → 编辑 `src/data/projects.json` + `src/messages/{lang}.json`（添加对应的 titleKey / backgroundKey / contributeKey / outcomeKey）
3. **增删 Case Study** → 编辑 `src/data/case-studies.json` + `src/messages/{lang}.json` + 新建 `data/{slug}-{lang}.md`
4. **修改页面文本** → 编辑 `src/messages/{lang}.json`
5. **更新简历** → 替换 `public/resume-{lang}.pdf`
6. **替换图片** → 放入 `public/images/`，更新 JSON 中的 cover 路径

无需重新部署，推送至 main 后 Vercel 自动生效。

---

## 十二、P1 扩展功能（上线后添加）

| # | 功能 | 说明 |
|---|------|------|
| 1 | **项目分类筛选** | 按 IoT / Web / 游戏等类型筛选，项目 ≥ 4 时启用 |
| 2 | **返回顶部按钮** | 长滚动后出现，平滑回到顶部，Framer Motion 淡入淡出 |
