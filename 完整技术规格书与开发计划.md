# 2D Portfolio — 完整技术规格书与开发计划

> 项目：KONG JUN SIENG — Software Developer Portfolio  
> 品牌调性：日系萌系 × 浅色科技感，Gal 社风格柔和动效  
> 目标受众：招聘方（HR / 技术主管）  
> 核心目标：找工作，引导访问者查看 Case Study

---

## 一、技术选型矩阵

| 层面              | 选择                                        | 版本/备注                                                  |
| --------------- | ----------------------------------------- | ------------------------------------------------------ |
| **框架**          | Next.js 16                                | App Router, React 19                                   |
| **语言**          | TypeScript Strict                         | `tsconfig.json` strict: true, 全量 TS                    |
| **包管理**         | pnpm                                      | 最新稳定版                                                  |
| **CSS**         | Tailwind CSS v4                           | 自定义设计令牌                                                |
| **UI 组件**       | shadcn/ui                                 | 基础骨架 + 覆写样式                                            |
| **动画**          | Framer Motion                             | 唯一动画库，无 GSAP                                           |
| **字体**          | 全自托管 + `next/font`                        | JP: M PLUS Rounded 1c, EN: Nunito, ZH: Noto Sans SC |
| **数据校验**        | Zod                                       | 校验 profile.json / projects.json / case-studies.json    |
| **i18n**        | next-intl                                 | path-based 路由策略 (`/[lang]/...`)                        |
| **数据层**         | 本地 JSON + Markdown                        | 无 CMS，无数据库                                             |
| **Markdown 渲染** | gray-matter + react-markdown + remark-gfm | 轻量，无 MDX                                               |
| **CI**          | GitHub Actions                            | lint + typecheck + build                               |
| **部署**          | Vercel                                    | auto deploy from main, PR previews                     |
| **联系表单**        | 纯 UI 展示                                   | 无后端逻辑                                                  |
| **测试**          | 无                                         | v1 不引入测试框架，依赖 TypeScript + ESLint + Build 保障           |
| **分析监控**        | 无                                         | v1 不接入                                                 |
| **原型方式**        | 代码驱动设计                                    | 辅以结构化设计文档（自然语言 + 小标题 MD 文件）                            |
| **依赖更新**        | 手动                                        | 不启用 Dependabot / Renovate                              |

---

## 二、设计系统

### 2.1 设计令牌（Design Tokens）

```css
--color-primary: #FFB7C5;
--color-primary-soft: #FFE4EC;
--color-secondary: #D5C6E0;
--color-accent: #B5EAD7;
--color-background: #FFFAFD;
--color-surface: #FFFFFF;
--color-text-primary: #4A3F42;
--color-text-secondary: #8E8386;
--color-border: #F0E6EB;
```

### 2.2 字体方案

| 语言 | 字体 | 加载方式 |
|------|------|---------|
| 日文 (ja) | M PLUS Rounded 1c | `next/font/local` 自托管 |
| 英文 (en) | Nunito | `next/font/local` 自托管 |
| 中文 (zh) | Noto Sans SC | `next/font/local` 自托管 |

### 2.3 视觉风格

- **Hero 核心**：BOOTH / DLsite 商业授权看板娘立绘（透明背景 PNG），大型角色立绘搭配 Slogan
- **图片格式**：保留原始 PNG，生产通过 Next.js Image 自动优化优先输出 AVIF，WebP 为兼容格式
- **Hero 动画**：纯 CSS 轻量动画（淡入、轻微浮动、渐变光晕），不使用 Lottie / Live2D / 粒子特效
- **整体布局**：参考美少女游戏官网风格，减少边框和卡片元素，强调留白与层次感
- **微妙视差**：滚动时前景/背景元素以不同速度移动，营造深度感和沉浸感；通过 Framer Motion scroll progress 驱动
- **资产上限**：预期不超过 20 张图片

---

## 三、路由与页面结构

```
/[lang]                     → 首页单页滚动
  ├── #hero                 → Hero Banner（立绘 + Slogan + CTA）
  ├── #case-studies         → Case Studies 卡片列表（跳转详情页）
  ├── #projects             → 项目展示卡片（纯展示，无跳转）
  ├── #about                → 关于我（个人介绍 + 技能标签）
  └── #contact              → 联系区块（UI 表单 + 社交链接 + 简历下载）

/[lang]/projects/[slug]     → Case Study 详情页（动态路由，Markdown 渲染）
```

### 重定向策略

| 来源 | 目标 | 方式 |
|------|------|------|
| `/` | `/zh` | middleware 重定向（默认中文） |
| 未匹配路由 | 404 | `not-found.tsx` |

---

## 四、数据层结构

### 4.1 数据文件

| 文件 | 说明 | 校验 |
|------|------|------|
| `src/data/profile.json` | 个人资料（name, slogan, skills, socialLinks, email）; 含 GitHub（`KONGJUNSIENG`）及 LinkedIn（`jun-sieng-kong-b1b3213b3`） | Zod |
| `src/data/projects.json` | 项目卡片数据（slug, cover, techStack, titleKey） | Zod |
| `src/data/case-studies.json` | Case Study 列表数据（slug, cover, techStack, titleKey, descriptionKey） | Zod |
| `src/data/portfolio-{lang}.md` | Case Study 详情 Markdown（中日英各一） | - |
| `src/messages/{lang}.json` | 三语文本 | - |
| `public/resume-{lang}.pdf` | 各语言简历 PDF | - |

### 4.2 数据流

```
messages/{lang}.json      ←  所有文本（导航 / Hero / Projects / Case Studies / About / Contact）
     │
profile.json             ←  name, slogan, skills, socialLinks, email（固定，不翻译）
     │
projects.json            ←  slug, cover, techStack（固定，不翻译）
     │                    └── titleKey → messages/{lang}.json
     │
case-studies.json        ←  slug, cover, techStack（固定，不翻译）
     │                    └── titleKey, descriptionKey → messages/{lang}.json
     │
data/portfolio-{lang}.md ←  Case Study 按语言拆分，gray-matter 解析 frontmatter
```

---

## 五、目录结构

```
project-root/
├── .github/
│   └── workflows/
│       └── ci.yml              # GitHub Actions: lint + typecheck + build
├── public/
│   ├── images/                 # 封面图、立绘、项目截图等
│   ├── fonts/                  # 自托管字体文件
│   ├── resume-zh.pdf
│   ├── resume-ja.pdf
│   └── resume-en.pdf
├── src/
│   ├── app/
│   │   ├── [lang]/
│   │   │   ├── layout.tsx      # 多语布局（Nav + Footer + next-intl provider）
│   │   │   ├── page.tsx        # 首页（组合各 Section）
│   │   │   └── projects/
│   │   │       └── [slug]/
│   │   │           └── page.tsx # Case Study 详情页
│   │   ├── api/                # 预留，未来联系表单后端
│   │   ├── layout.tsx          # 根布局（仅 html + body + fonts）
│   │   ├── not-found.tsx       # 404 页面
│   │   ├── sitemap.ts
│   │   └── robots.ts
│   ├── components/
│   │   ├── Navbar.tsx
│   │   ├── Footer.tsx
│   │   ├── LanguageSwitcher.tsx
│   │   ├── HeroSection.tsx
│   │   ├── CaseStudiesSection.tsx
│   │   ├── ProjectsSection.tsx
│   │   ├── AboutSection.tsx
│   │   ├── ContactSection.tsx
│   │   ├── CaseStudyCard.tsx
│   │   ├── ProjectCard.tsx
│   │   ├── SkillTag.tsx
│   │   ├── SocialLink.tsx
│   │   └── ScrollToAnchor.tsx
│   ├── hooks/                  # 自定义 React Hooks
│   │   └── useMediaQuery.ts    # 响应式断点检测
│   │
│   ├── i18n/
│   │   └── request.ts          # next-intl 请求配置
│   │
│   ├── lib/
│   │   ├── animations.ts       # Framer Motion variants
│   │   ├── fonts.ts            # next/font 声明（JP/EN）
│   │   ├── profile.schema.ts   # Zod schema: 个人资料
│   │   ├── projects.schema.ts  # Zod schema: 项目数据
│   │   ├── case-studies.schema.ts # Zod schema: Case Study 数据
│   │   ├── read-data.ts        # JSON 读取 + Zod 校验
│   │   ├── read-mdx.ts         # Markdown 读取 + frontmatter 解析
│   │   └── utils.ts            # cn() 工具函数
│   ├── data/
│   │   ├── profile.json
│   │   ├── projects.json
│   │   ├── case-studies.json
│   │   ├── portfolio-zh.md
│   │   ├── portfolio-ja.md
│   │   └── portfolio-en.md
│   ├── messages/
│   │   ├── zh.json
│   │   ├── ja.json
│   │   └── en.json
│   ├── styles/
│   │   ├── globals.css         # Tailwind 指令 + 自定义 token
│   │   └── fonts.css           # Noto Sans SC @font-face（13 子集）
│   └── proxy.ts                # i18n 语言检测 + 重定向（Next.js 16 已弃用 middleware.ts）
├── .env.example
├── .gitignore
├── .prettierrc
├── next.config.ts
├── postcss.config.mjs
├── tsconfig.json
├── package.json
└── README.md
```

---

## 六、SDLC 阶段开发计划

### 阶段 1：项目初始化与环境搭建

| # | 操作 | 产出 |
|---|------|------|
| 1.1 | `pnpm create next-app@latest` → App Router + TypeScript + Tailwind + ESLint | 项目骨架 |
| 1.2 | 配置 `tsconfig.json` strict 模式 | 类型安全基础 |
| 1.3 | 安装核心依赖：`next-intl` `framer-motion` `zod` + `shadcn/ui` 初始化 | 依赖就绪 |
| 1.4 | 安装 Markdown 依赖：`gray-matter` `react-markdown` `remark-gfm` | MD 渲染 |
| 1.5 | 配置 Tailwind v4 自定义设计令牌：`globals.css` 中添加 `@import "tailwindcss"` + `@theme` 声明（颜色、字体、间距、圆角、阴影、容器宽度、动效时长） | `src/styles/globals.css` |
| 1.6 | 下载并放置字体文件到 `public/fonts/`，JP/EN 用 `next/font/local`，ZH（CJK 子集）用 `@font-face` | `src/lib/fonts.ts` + `src/styles/fonts.css` |
| 1.7 | 配置 next-intl：`proxy.ts`（Next.js 16 已弃用 middleware.ts） + `i18n/request.ts` + `messages/*.json` | i18n 基础 |
| 1.8 | 搭建目录结构（`src/components/` `src/lib/` `src/data/`） | 目录就绪 |
| 1.9 | 配置 `.env.example` | 环境变量模板 |
| 1.10 | 配置 `.github/workflows/ci.yml`（lint + typecheck + build） | CI 流水线 |

### 阶段 2：数据层与类型系统

| # | 操作 | 产出 |
|---|------|------|
| 2.1 | Zod schema：`profile.schema.ts` | name, slogan, skills, socialLinks |
| 2.2 | Zod schema：`projects.schema.ts` | slug, cover, techStack, titleKey |
| 2.3 | Zod schema：`case-studies.schema.ts` | slug, cover, techStack, titleKey, descriptionKey |
| 2.4 | 创建 `profile.json`（姓名、slogan、技能标签、社交链接） | 数据文件 |
| 2.5 | 创建 `projects.json`（初始 2 条：fyp-iot, portfolio） | 数据文件 |
| 2.6 | 创建 `case-studies.json`（初始 1 条：portfolio） | 数据文件 |
| 2.7 | 创建 `messages/zh.json` `messages/ja.json` `messages/en.json`（完整字段） | 三语文本 |
| 2.8 | 创建 `data/portfolio-zh.md` `data/portfolio-ja.md` `data/portfolio-en.md` | Case Study 内容 |
| 2.9 | 实现 `src/lib/read-data.ts`（通用 JSON 读取 + Zod parse + 错误处理） | 数据访问层 |
| 2.10 | 实现 `src/lib/read-mdx.ts`（Markdown 读取 + frontmatter 解析） | MD 数据层 |

### 阶段 3：全局布局与导航

| # | 操作 | 产出 |
|---|------|------|
| 3.1 | 根 `layout.tsx` — 仅 html + body + next/font 注入 | 根布局 |
| 3.2 | `[lang]/layout.tsx` — NextIntlClientProvider + Navbar + Footer + 响应式容器 | 多语布局 |
| 3.3 | `Navbar.tsx` — 固定顶部导航 + 锚点链接 + 语言切换器 + 滚动背景模糊 | 导航组件 |
| 3.4 | `Footer.tsx` — 版权信息 + 社交链接图标 | 页脚组件 |
| 3.5 | `LanguageSwitcher.tsx` — 中/日/EN 切换按钮，当前语言高亮 | i18n 切换 |
| 3.6 | `ScrollToAnchor.tsx` — 客户端组件，平滑滚动至锚点 | 导航辅助 |

### 阶段 4：首页各区块

| # | 操作 | 产出 |
|---|------|------|
| 4.1 | `HeroSection.tsx` — 角色立绘（Next/Image 优化）+ 姓名 + Slogan + CTA 按钮 + CSS 淡入浮动动画 | Hero 完成 |
| 4.2 | `CaseStudiesSection.tsx` — 卡片列表（封面 + 技术标签 + 标题 + 描述 + CTA），跳转详情页 | Case Studies 完成 |
| 4.3 | `CaseStudyCard.tsx` — 可复用卡片组件，含 hover 动画 | 卡片组件 |
| 4.4 | `ProjectsSection.tsx` — 卡片列表（封面 + 技术标签 + 标题），纯展示无跳转 | Projects 完成 |
| 4.5 | `ProjectCard.tsx` — 可复用卡片组件 | 卡片组件 |
| 4.6 | `AboutSection.tsx` — 个人介绍文本 + 技能标签列表 | About 完成 |
| 4.7 | `SkillTag.tsx` — 技能标签原子组件 | 原子组件 |
| 4.8 | `ContactSection.tsx` — 纯 UI 表单（姓名/邮箱/留言/发送按钮）+ 邮箱链接 + 社交链接 + 简历下载 | Contact 完成 |
| 4.9 | `SocialLink.tsx` — 社交链接图标组件 | 原子组件 |
| 4.10 | 首页 `page.tsx` — 组合所有 Section + Framer Motion 入场动画 | 首页完成 |

### 阶段 5：Case Study 详情页

| # | 操作 | 产出 |
|---|------|------|
| 5.1 | `projects/[slug]/page.tsx` — 动态路由，根据 slug 读取对应语言 Markdown | 详情页路由 |
| 5.2 | Markdown 渲染：gray-matter 解析 frontmatter → react-markdown 渲染 body | 渲染管线 |
| 5.3 | 详情页布局：封面图 + 标题 + tech stack 标签 + role + date + Markdown 正文 | 详情页完成 |
| 5.4 | 返回首页导航按钮 + 面包屑或返回链接 | 导航回退 |

### 阶段 6：动效注入与润色

| # | 操作 | 产出 |
|---|------|------|
| 6.1 | Hero 背景 CSS 动画 — 渐变光晕脉冲、立绘轻微浮动 | 氛围背景 |
| 6.2 | Section 入场 Framer Motion `whileInView` — 淡入 + 上浮 + 轻微缩放 | 滚动动画 |
| 6.3 | 微妙视差动效 — 滚动时前景/背景元素以不同速度移动，Framer Motion useScroll + useTransform 驱动 | 视差效果 |
| 6.4 | 卡片 hover 动画 — 轻微上浮 + 阴影加深 + 过渡平滑 | 交互反馈 |
| 6.5 | 导航栏滚动效果 — 背景模糊 + 阴影渐入 | 视觉提升 |
| 6.6 | 语言切换过渡动画 — Framer Motion AnimatePresence | 切换体验 |

### 阶段 7：SEO 与部署

| # | 操作 | 产出 |
|---|------|------|
| 7.1 | 各页面 `generateMetadata` — title, description, Open Graph | SEO meta |
| 7.2 | `sitemap.ts` — 动态生成 sitemap.xml（含所有语言页面） | sitemap |
| 7.3 | `robots.ts` — robots.txt 配置 | robots |
| 7.4 | `not-found.tsx` — 404 页面 + 返回首页链接 | 404 |
| 7.5 | 简历 PDF 占位文件放入 `public/`（resume-zh.pdf / resume-ja.pdf / resume-en.pdf） | 下载就绪 |
| 7.6 | 最终 CI 验证（本地运行 lint + typecheck + build 全部通过） | 质量门禁 |
| 7.7 | 推送 main → Vercel 自动部署 | 上线 |

---

## 七、分支策略与 CI/CD

### 分支策略

```
feature/* → Pull Request → main
```

### CI 流水线（`.github/workflows/ci.yml`）

```yaml
name: CI
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm run lint
      - run: pnpm run typecheck
      - run: pnpm run build
```

### 部署

| 环境 | 触发 | 方式 |
|------|------|------|
| Production | push to main | Vercel auto deploy |
| Preview | PR 创建/更新 | Vercel auto deploy for PR |

---

## 八、初始内容清单

### Projects（首页卡片，纯展示）

| Slug | 说明 |
|------|------|
| `fyp-iot` | FYP IoT 成品 |
| `portfolio` | 本 2D Portfolio 网站本身 |

### Case Studies（含独立详情页）

| Slug | 对应 Project | 说明 |
|------|-------------|------|
| `portfolio` | portfolio | 本网站的深度开发案例 |

### 简历

| 语言 | 文件路径 |
|------|---------|
| zh | `public/resume-zh.pdf` |
| ja | `public/resume-ja.pdf` |
| en | `public/resume-en.pdf` |

---

## 九、消息文件结构（messages/{lang}.json）

```jsonc
{
  "nav": {
    "caseStudies": "案例",
    "projects": "作品",
    "about": "关于",
    "contact": "联系"
  },
  "hero": {
    "title": "Software Developer / 全栈探索者",
    "ctaText": "查看作品"
  },
  "caseStudies": {
    "sectionTitle": "案例研究",
    "portfolio": {
      "title": "2D Portfolio 开发案例",
      "description": "一段简短的 Case Study 描述"
    },
    "viewButton": "查看详情"
  },
  "projects": {
    "sectionTitle": "作品",
    "fypIot": {
      "title": "FYP IoT 项目"
    },
    "portfolio": {
      "title": "2D Portfolio"
    }
  },
  "about": {
    "bio": "一段话的个人介绍..."
  },
  "contact": {
    "sectionTitle": "联系我",
    "resumeBtnText": "下载简历",
    "formName": "姓名",
    "formEmail": "邮箱",
    "formMessage": "留言",
    "formSubmit": "发送"
  },
  "footer": {
    "copyright": "© 2026 KONG JUN SIENG"
  }
}
```

---

## 十、内容更新指南

所有内容通过修改 `src/data/` 和 `src/messages/` 下的文件更新：

1. **修改个人资料** → 编辑 `src/data/profile.json`
2. **增删项目卡片** → 编辑 `src/data/projects.json` + `src/messages/{lang}.json`（添加对应的 titleKey）
3. **增删 Case Study** → 编辑 `src/data/case-studies.json` + `src/messages/{lang}.json` + 新建 `data/{slug}-{lang}.md`
4. **修改页面文本** → 编辑 `src/messages/{lang}.json`
5. **更新简历** → 替换 `public/resume-{lang}.pdf`
6. **替换图片** → 放入 `public/images/`，更新 JSON 中的 cover 路径

无需重新部署，推送至 main 后 Vercel 自动生效。

---

## 十一、Phase 2（上线后添加）

| # | 功能 | 说明 |
|---|------|------|
| 1 | **项目分类筛选** | 按 IoT / Web / 游戏等类型筛选，项目 ≥ 4 时启用 |
| 2 | **返回顶部按钮** | 长滚动后出现，平滑回到顶部，Framer Motion 淡入淡出 |
