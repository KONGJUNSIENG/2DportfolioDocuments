# 1.1 个人品牌与网站愿景文档

## 一、个人品牌定位

| 项目 | 内容 |
| ---- | ---- |
| 姓名 | KONG JUN SIENG |
| 职位标签 | Software Developer / 全栈探索者 |
| 网站标题 / Slogan | やればできる！ |
| 品牌调性 | 日系萌系 × 浅色科技感，Gal 社风格柔和动效 |
| 独特卖点 | 日语 N4-N3学习中・游戏 / IoT / Web 均有涉猎的应届全栈探索者 |
| 技能标签 | Python / TypeScript / React / Next.js / Node.js / Git / RESTful APIs / Regular Expressions / i18n / IoT / Framer Motion |

## 二、受众与目标

- **主要受众**：招聘方（HR / 技术主管）
- **核心目标**：找工作
- **关键行动**：引导访问者查看 Case Study
- **浏览体验目标**：**让浏览像玩视觉小说一样推进**——自然滚动触发"章节"切换，蓝色对话框承载关键信息传递，选项按钮风格的 CTA 引导交互，整体呈现 Galgame 系统 UI 感

## 三、内容规划

### 游戏风序幕（`/[lang]/intro`）

序幕由 IntroShell 四阶段状态机驱动：

1. **intro** — `IntroAnimation`（LoadingRipple → 白色闪烁 → PixelsCurtain）→ `TitleScreen`（"Press Start"按钮触发进入）
2. **transition** — 暗転（黑屏 700ms）
3. **scenario** — `VNScenario`（43 场景对话演绎，含角色立绘切换 / 选项分支 / repel 光标排斥 / auto-advance 自动推进 / Skip 跳过，6 种结局汇聚至"Let's go！"）
4. **loading** — `LoadingOverlay`（curtain wipe 过渡动画）→ 自动导航至首页 `/[lang]`

根路径 `/` 在 `proxy.ts` 中硬编码重定向至 `/en/intro`（locale 前缀固定为 en），序幕完成后跳转至对应语言首页。

### 首页 `/[lang]`（自上而下）

各 Section（除 HeroSection 外）顶部配有 CHAPTER 编号 + 蓝色下划线标题（ChapterHeader 组件），Section 之间以 SectionTransition 组件分隔（颜文字 + `~~` 包裹 + 水平横线装饰，带 fadeInUp 动画）：

1. **HeroSection** — 全屏角色插画 + 视差滚动 + 发光脉冲 + 延迟入场序列（Slogan 0.7s / 姓名 1.2s / 职位 1.7s / CTA 按钮 2.2s / Scroll ↓ 2.7s），CTA 按钮滚动至 Case Studies 锚点。无 CHAPTER 编号，作为序幕后的自然起始
2. **CHAPTER 01 — Case Studies** — 深度案例卡片列表（封面 / 标题 / 描述 / 技能标签 / 查看详情链接），含空闲微摆动效，跳转至独立详情页。初始仅 Portfolio 案例
3. **CHAPTER 02 — Projects** — 成品项目卡片（封面 / 标题 / 技能标签），点击 clip-path 圆形展开详情（背景/贡献 bullet list/成果），含空闲弹跳动效。初始 2 个：CCMS、Portfolio
4. **CHAPTER 03 — About** — 个人介绍：DialogBox 承载长文角色独白风格 bio，配合 SkillTag 展示技能标签
5. **CHAPTER 04 — Contact** — 副标题 "SELECT AN OPTION"，OptionButton 列表：邮箱链接、GitHub、LinkedIn、简历下载（↓ 图标）

### 独立子路由

- `/[lang]/projects/[slug]` — Case Study 详情页。动态 slug（当前仅 `casestudy`），10 节结构：
  1. executiveSummary（执行摘要）
  2. background（背景与动机）
  3. methodology（方法论，含 4 子节：documentFirst / agentConfig / architecture / deployment）
  4. developmentProcess（开发过程，含 intro + phase1–7 + metricsNote 共 9 子节）
  5. challenges（关键挑战，含 challenge1–7 共 7 子节）
  6. results（成果展示，含 overview / features / quality / links 共 4 子节）
  7. discussion（讨论，含 advantages / opencodeCliAdvantages / limitations / roleShift / teamScaling / hrPerspective / ossBoundary 共 7 子节）
  8. reflections（反思，含 documentDensity / agentCapability / toolingAdvice 共 3 子节）
  9. conclusion（总结与展望，含 coreFindings / applicableScenarios / futureDirections 共 3 子节）
  10. appendix（附录，含 appendixA–E + references 共 6 子节）
  每节按 `Chapter 1-N` 子编号渲染，含封面图、技术栈标签、侧边导航 CaseStudyNav、PDF 下载按钮（自动检测 `public/{slug}-{lang}.pdf`）、BackToTopButton
- `/[lang]/intro` — 游戏风序幕入口

## 四、设计方向

| 项目 | 方向 |
|------|------|
| 配色 | 浅色系为主（`--color-background: #fffafd`），日系萌系氛围（`--color-primary: #ffb7c5`），引入蓝色系作为 UI 辅助色（`--color-dialog-blue: #2fa4d7`，对话框/选项/系统框架） |
| 页面结构 | 游戏风序幕（`/[lang]/intro`）→ 自动过渡至单页滚动主页，每个 Section 作为游戏「章节」呈现，Case Study 独立详情页 |
| 动效风格 | Galgame 式柔和细腻动画 + 游戏风交互模式：章节标题过渡（`chapterReveal`）、对话框逐字/渐入（`dialogSlideUp`）、选项按钮选择反馈（`optionStagger`/`optionItem`） |
| UI 素材 | 蓝色对话框 CSS 组件（DialogBox 三层圆角结构）+ 选项按钮 PNG 素材（`option-button.png`）整合为全站 UI 系统 |

## 五、游戏风浏览体验设计原则

| 原则 | 说明 |
|------|------|
| **章节感** | 每个 Section（除 HeroSection 外）顶部标识「CHAPTER NN — 标题」，模拟游戏章节切换，使用 `chapterReveal` 动画（从左侧滑入） |
| **对话框传递信息** | About 自我介绍、Case Study 描述等关键内容用蓝色 DialogBox 承载，仿佛角色在讲述，使用 `dialogSlideUp` 动画 |
| **选项式 CTA** | 按钮风格参考游戏选择肢（OptionButton 组件 + 素材），点击有"选中"反馈，Contact 区块使用 `optionStagger`/`optionItem` 错列入场 |
| **场景过渡** | Section 之间以 SectionTransition 组件分隔：水平横线 + 居中颜文字（`~~` 包裹），带 `transitionFade`（淡入 + 缩放）动画 |
| **系统 UI** | Navbar 模拟游戏菜单栏（品牌名 + Case Studies / Projects / About / Contact 四项 + LanguageSwitcher，移动端滑出菜单），Footer 模拟系统状态栏（版权信息） |

## 六、功能清单

- HeroSection：全屏角色插画 + Slogan/姓名/职位延迟入场 + 视差 + 发光脉冲 + "View My Work" CTA 滚动引导
- Case Studies 区块：深度案例卡片 + 跳转至详情页
- Case Study 详情页：10 节结构化内容（执行摘要 → 附录）+ PDF 下载 + 侧边导航 + 回到顶部
- Projects 区块：成品项目卡片（CCMS / Portfolio），点击 clip-path 圆形展开详情（背景/贡献/成果）
- 简历下载（PDF，按中日英三语分离，`public/resume-{locale}.pdf`）
- 邮箱 `js.kong@outlook.com`（链接）
- 社交链接：GitHub、LinkedIn
- 多语言：中日英三语（i18n），path-based 路由 `/[lang]/...`
- 语言切换：序幕阶段 TitleScreen 的 Settings 弹窗切换 + 浏览阶段 Navbar LanguageSwitcher 切换
- 游戏风序幕：4 阶段状态机（IntroAnimation → 暗転 → VNScenario → LoadingOverlay）→ 自动导航至首页
- 章节标题系统（ChapterHeader：CHAPTER 标签 + 蓝色下划线）
- Section 间过渡文字（颜文字 + ~~ 包裹 + 横线分隔）
- 数据文件驱动，无需 CMS

## 七、技术选型

| 项目 | 选择 |
|------|------|
| 框架 | Next.js 16 (App Router) + React 19 |
| 样式 | Tailwind CSS v4（`@theme inline` 自定义设计 Token，无 `tailwind.config`） |
| 动效实现 | Framer Motion |
| 多语言方案 | next-intl（中日英三语，path-based `/[lang]/...`） |
| 数据管理 | 本地 JSON（Zod v4 验证）+ next-intl 消息系统（`t.raw()` 渲染 Case Study 正文） |
| 图标 | Lucide React |
| 工具库 | clsx + tailwind-merge（`cn()` 工具函数） |
| 开发工具 | Obsidian（文档管理）+ OpenCode（AI Agent）+ DeepSeek V4 Flash + Ollama（文档驱动开发） |
| 部署 | Vercel |
