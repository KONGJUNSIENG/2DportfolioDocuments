# 4.3 视觉资产与使用规范

**项目：** KONG JUN SIENG — Software Developer Portfolio  
**文档目标：** 定义所有视觉资产的格式、尺寸、优化策略与使用约束，供 AI 代理按此管理 `public/` 下的资源文件。

---

## 一、资产总清单

### 1.1 UI 素材资产（蓝色 Gal UI 系统）

| # | 文件名 | 路径 | 用途 | 源格式 | 原始分辨率 | 使用方式 |
|---|--------|------|------|--------|-----------|---------|
| 1 | `option-button.png` | `public/images/option-button.png` | 选项按钮背景素材 | PNG（透明背景，蓝色装饰边框 + 内部透明区域供文字） | **900×60** （宽高比 15:1） | ✅ 当前在使用。OptionButton 组件通过 `backgroundImage: url('/images/option-button.png')` 渲染按钮背景 |

> **素材来源**：与 hero-character.png 同一批从 BOOTH/DLsite 购买或获取的商业授权素材。
> **系统菜单**：无对应 PNG 素材，全部由 CSS + Tailwind 代码设计实现。风格参照 `--color-dialog-*` 色系，保持与对话框一致的蓝色调。
> **对话框：纯 CSS 实现**（三层圆角结构）。`dialog-box.png` 已删除，不再存在于 public/images/。
> **角色表情**：8 种表情 PNG（normal/smile/sad/mad/shock/shy/bigsmile/emm），用于游戏风序幕场景对话中的角色表情切换。
> **选项按钮：使用 option-button.png 为背景素材**，组件通过 backgroundImage 引用。

### 1.2 图片资产

| # | 文件名 | 路径 | 用途 | 源格式 | 推荐尺寸（px） | 来源 |
|---|--------|------|------|--------|---------------|------|
| 1 | `hero-character.png` | `public/images/hero-character.png` | Hero 区块大立绘 / 游戏风序幕 TitleScreen 主立绘 | PNG（透明背景） | 宽度 800–1200，高度按比例（~70vh 对应） | 外部购买 BOOTH/DLsite 商业授权素材 |
| 2 | `portfolio-cover.png` | `public/images/portfolio-cover.png` | Portfolio Project 卡片封面 | PNG | 1440×1080（4:3） | 自行制作截图/设计图 |
| 3 | `case-study-cover.png` | `public/images/case-study-cover.png` | Portfolio Case Study 卡片封面 | PNG | 1920×1032（16:9） | 自行制作截图 |
| 4 | `ccms-cover.png` | `public/images/ccms-cover.png` | CCMS Project 卡片封面 | PNG | 800×580（4:3） | 自行制作截图 |
| 5 | `normal.png` | `public/images/normal.png` | 游戏风序幕角色表情 — 通常 | PNG（透明背景） | 与 hero-character.png 同源 | 同一批 BOOTH/DLsite 素材 |
| 6 | `smile.png` | `public/images/smile.png` | 游戏风序幕角色表情 — 微笑 | PNG（透明背景） | 同上 | 同上 |
| 7 | `sad.png` | `public/images/sad.png` | 游戏风序幕角色表情 — 悲伤 | PNG（透明背景） | 同上 | 同上 |
| 8 | `mad.png` | `public/images/mad.png` | 游戏风序幕角色表情 — 生气 | PNG（透明背景） | 同上 | 同上 |
| 9 | `shock.png` | `public/images/shock.png` | 游戏风序幕角色表情 — 震惊 | PNG（透明背景） | 同上 | 同上 |
| 10 | `shy.png` | `public/images/shy.png` | 游戏风序幕角色表情 — 害羞 | PNG（透明背景） | 同上 | 同上 |
| 11 | `bigsmile.png` | `public/images/bigsmile.png` | 游戏风序幕角色表情 — 大笑 | PNG（透明背景） | 同上 | 同上 |
| 12 | `emm.png` | `public/images/emm.png` | 游戏风序幕角色表情 — 无语 | PNG（透明背景） | 同上 | 同上 |
| 13 | `obsidian_graph.png` | `public/images/obsidian_graph.png` | Case Study 内嵌截图 — Obsidian 知识图谱 | PNG | 800×450 | 自行截图 |
| 14 | `opencode_init.jpg` | `public/images/opencode_init.jpg` | Case Study 内嵌截图 — Opencode 初始化 | JPEG | 800×450 | 自行截图 |
| 15 | `homepage.png` | `public/images/homepage.png` | Case Study 内嵌截图 — 首页预览 | PNG | 800×450 | 自行截图 |
| 16 | `vnscene1.png` | `public/images/vnscene1.png` | Case Study 内嵌截图 — VN 场景 1 | PNG | 800×450 | 自行截图 |
| 17 | `vnscene2-option.png` | `public/images/vnscene2-option.png` | Case Study 内嵌截图 — VN 场景 2 选项 | PNG | 800×450 | 自行截图 |
| 18 | `obsidian_repo.png` | `public/images/obsidian_repo.png` | Case Study 内嵌截图 — Obsidian 仓库结构 | PNG | 800×450 | 自行截图 |
| 19 | `opencode_working.jpg` | `public/images/opencode_working.jpg` | Case Study 内嵌截图 — Opencode 工作会话 | JPEG | 800×450 | 自行截图 |
| 20 | `commits.png` | `public/images/commits.png` | Case Study 内嵌截图 — Git 提交记录 | PNG | 800×450 | 自行截图 |
| 21 | `working_record.png` | `public/images/working_record.png` | Case Study 内嵌截图 — 工作记录 | PNG | 800×450 | 自行截图 |

**资产上限**：≤30 张图片。当前 21 张（4 封面 + 1 立绘 + 8 表情 + 9 截图），UI 素材 1 张（option-button.png），共 22 张。DialogBox PNG 已删除。

### 1.3 字体资产

| # | 文件名 | 路径 | 所属字体集 | 字重 | 格式要求 |
|---|--------|------|-----------|------|---------|
| 1 | `RoundedMplus1c-Regular.woff2` | `public/fonts/` | M PLUS Rounded 1c | 400 (仅此一字重) | woff2 |
| 2 | `Nunito-Regular.woff2` | `public/fonts/` | Nunito | 400 (仅此一字重) | woff2 |
| 3 | `noto-sans-sc-*-400-normal.woff2` (13 文件) | `public/fonts/` | Noto Sans SC | 400 (13 CJK 子集拆分) | woff2 |

### 1.4 简历资产

| # | 文件名 | 路径 | 说明 |
|---|--------|------|------|
| 1 | `resume-zh.pdf` | `public/resume-zh.pdf` | 中文简历 PDF |
| 2 | `resume-ja.pdf` | `public/resume-ja.pdf` | 日本語履歴書 PDF |
| 3 | `resume-en.pdf` | `public/resume-en.pdf` | English Resume PDF |
| 4 | `resume-zh.md` | `public/resume-zh.md` | 中文简历 Markdown 源文件 |
| 5 | `resume-ja.md` | `public/resume-ja.md` | 日本語履歴書 Markdown 源文件 |
| 6 | `resume-en.md` | `public/resume-en.md` | English Resume Markdown source |
| 7 | `casestudy-zh.pdf` | `public/casestudy-zh.pdf` | Case Study 中文版 PDF |
| 8 | `casestudy-ja.pdf` | `public/casestudy-ja.pdf` | Case Study 日本語版 PDF |
| 9 | `casestudy-en.pdf` | `public/casestudy-en.pdf` | Case Study English PDF |
| 10 | `casestudy-zh.md` | `public/casestudy-zh.md` | Case Study 中文版 Markdown 源文件 |
| 11 | `casestudy-ja.md` | `public/casestudy-ja.md` | Case Study 日本語版 Markdown 源文件 |
| 12 | `casestudy-en.md` | `public/casestudy-en.md` | Case Study English Markdown source |

> Case Study PDF 通过 `src/app/[lang]/projects/[slug]/page.tsx` 运行时检测文件存在性，并由 `CaseStudyDownloadButton` 组件提供下载（路径 `/{slug}-{lang}.pdf`）。

---

## 二、图片格式规范

### 2.1 源文件格式

| 类型 | 源格式 | 理由 |
|------|--------|------|
| 立绘（透明背景） | **PNG-32**（含 Alpha 通道） | 需要透明背景，JPEG/WebP 不支持 |
| 封面图/截图 | **PNG** 或 **JPEG** | 源文件保留最大质量；生产由 Next.js 自动优化 |

### 2.2 生产输出格式

| 格式 | 优先级 | 说明 |
|------|--------|------|
| **AVIF** | 首选 | 压缩率最高，浏览器支持率已 >92% |
| **WebP** | 降级 | 兼容旧浏览器（Safari 16-/Chrome 32-/Firefox 65-） |

由 `next.config.ts` 配置控制：

```typescript
// next.config.ts
const nextConfig: NextConfig = {
  images: {
    formats: ["image/avif", "image/webp"],
  },
}
```

### 2.3 文件大小约束

| 类型 | 约束 | 说明 |
|------|------|------|
| 源文件（PNG） | ≤2MB | 超出需压缩后再放入 `public/images/` |
| 源文件（JPEG） | ≤500KB | `/images/` 中已有 2 个 JPEG（opencode_init.jpg、opencode_working.jpg） |
| 生产输出（`next/image`） | 自动优化 | 不手动压缩，`next/image` 按需输出合适大小 |
| 生产输出（`unoptimized`） | 跳过优化 | 当前多数图片设 `unoptimized`，直接输出源文件 |

---

## 三、图片尺寸规范

### 3.1 各组件图片使用规格

| 使用位置 | 源文件 | 展示比例 | 展示尺寸（Desktop） | `sizes` 属性 | `priority` | `unoptimized` |
|----------|--------|---------|-------------------|-------------|-----------|-------------|
| Hero 立绘 | `hero-character.png` | 自适应 | 高度 `70vh`，容器 `md:w-[45%]`，内层 `scale-[1.4]` | `"(max-width: 767px) 100vw, 45vw"` | `true`（首屏关键） | `true` |
| Case Study 卡片封面 | `case-study-cover.png` | 16:9 | 满宽 + `max-w-6xl` 约束 | `"(max-width: 768px) 100vw, 80vw"` | `true` | `true` |
| Case Study 详情页封面 | `case-study-cover.png` | 16:9 | 宽度 `min(100%, 768px)`（`max-w-3xl`） | `"(max-width: 768px) 100vw, 768px"` | `true` | — |
| Projects 卡片封面 | `portfolio-cover.png` / `ccms-cover.png` | 4:3 | 2 列网格，每列 ~50% | `"(max-width: 767px) 100vw, 50vw"` | `false` | `true` |

### 3.2 object-fit 策略

| 组件 | 属性 | 值 | 说明 |
|------|------|-----|------|
| Hero 立绘（全断点） | `object-fit` | `cover` | 填充容器，配合 `center 45%` 确保面部区域可见 |
| Hero 立绘（全断点） | `object-position` | `center 45%` | 居中偏下，突出面部区域 |
| 封面图（卡片） | `object-fit` | `cover` | 按比例裁剪填充卡片区域 |
| 封面图（详情页） | `object-fit` | `cover` | 全宽展示 |

### 3.3 响应式图片行为

| 断点 | Hero 立绘 | 卡片封面 |
|------|----------|---------|
| Desktop ≥768px | 容器 `md:w-[45%] md:relative`；内层 `md:scale-[1.4] md:origin-top-left`；`object-fit: cover` + `object-position: center 45%` | 原始比例（2 列网格或 `max-w-6xl` 约束） |
| Mobile <768px | 容器 `absolute inset-0` 全屏覆盖；`object-fit: cover` + `object-position: center 45%` | 自适应宽度，比例不变 |

---

## 四、Next/Image 优化策略

### 4.1 使用规则

| 场景 | 组件 | 属性 | 必须设置 |
|------|------|------|---------|
| 首屏关键图片 | Hero 立绘 | `priority={true}` | ✅ 是（避免 Lazy Load 延迟首屏渲染） |
| 卡片封面 | CaseStudyCard / ProjectCard | `priority` 按需：CaseStudyCard 为 `true`（因在视口中立即显示），ProjectCard 默认 `false` | ⚠️ 视组件需求 |
| 详情页封面 | CaseStudyPage | `priority={true}` | ✅ 是（首屏进入立即显示） |
| 外部图片 | — | 无外部图片需求，全部本地 | — |
| 占位 | 所有图片 | `placeholder="blur"`（可选） | 需要提供 `blurDataURL` 或静态 import |
| **跳过优化** | **Hero / 卡片 / ZoomableImage** | **`unoptimized`** | **当前多数组件使用 `unoptimized` 跳过 `next/image` 优化（因本地 PNG 已压缩且需避免动态 import 路径问题）。仅 Case Study 详情页封面不使用 `unoptimized`。** |

> **关于 `unoptimized`**：当前 `HeroSection`、`CaseStudyCard`、`ProjectCard`、`ZoomableImage` 均设置 `unoptimized`。原因是这些组件使用字符串路径（`src="/images/xxx.png"`）而非静态 import，且 Next.js 对本地 `public/` 图片的优化收益有限。详情页封面未设置 `unoptimized`，由 Next.js 自动优化输出 AVIF/WebP。

### 4.2 Hero 立绘实现模板

```tsx
// HeroSection.tsx — 实际代码
<Image
  src="/images/hero-character.png"
  alt="KONG JUN SIENG"
  fill
  className="character-float object-cover"
  style={{ objectPosition: "center 45%" }}
  priority
  unoptimized
  sizes="(max-width: 767px) 100vw, 45vw"
/>
```

### 4.3 卡片封面实现模板

```tsx
// CaseStudyCard.tsx — 封面图使用方式（fill + unoptimized 模式）
<Image
  src={cover}
  alt={t(titleKey)}
  fill
  className="object-cover"
  unoptimized
  priority
  sizes="(max-width: 768px) 100vw, 80vw"
/>

// ProjectCard.tsx — 类似模式，无 priority
<Image
  src={cover}
  alt={t(titleKey)}
  fill
  className="object-cover"
  unoptimized
  sizes="(max-width: 767px) 100vw, 50vw"
/>
```

### 4.4 `<Image>` 属性速查表

| 属性 | Hero 立绘 | 卡片封面 | 详情页封面 |
|------|----------|---------|-----------|
| `src` | `"/images/hero-character.png"`（字符串路径） | `cover`（字符串路径，来自 data JSON） | `study.cover`（字符串路径，来自 data JSON） |
| `alt` | `"KONG JUN SIENG"` | 对应标题（`t(titleKey)`） | 对应标题 |
| `fill` | `true` | `true` | `true` |
| `priority` | `true` | `true`（CaseStudyCard）/ `false`（ProjectCard） | `true` |
| `unoptimized` | `true` | `true` | — |
| `sizes` | `"(max-width: 767px) 100vw, 45vw"` | `"(max-width: 768px) 100vw, 80vw"`（CaseStudyCard） | `"(max-width: 768px) 100vw, 768px"` |
| `className` | `character-float object-cover` | `object-cover` | `object-cover` |
| `objectPosition` | `center 45%`（`style` prop） | —（默认 `center`） | —（默认 `center`） |

---

## 五、图片目录管理规范

### 5.1 目录结构

```
public/images/
├── hero-character.png          # Hero 立绘 / 游戏风序幕主立绘
├── portfolio-cover.png         # Portfolio Project 卡片封面
├── case-study-cover.png        # Case Study 卡片 & 详情页封面
├── ccms-cover.png              # CCMS Project 卡片封面
├── normal.png                  # 游戏风序幕角色表情
├── smile.png                   # 游戏风序幕角色表情
├── sad.png                     # 游戏风序幕角色表情
├── mad.png                     # 游戏风序幕角色表情
├── shock.png                   # 游戏风序幕角色表情
├── shy.png                     # 游戏风序幕角色表情
├── bigsmile.png                # 游戏风序幕角色表情
├── emm.png                     # 游戏风序幕角色表情
├── option-button.png           # 选项按钮背景素材
├── obsidian_graph.png          # Case Study 内嵌截图
├── opencode_init.jpg           # Case Study 内嵌截图
├── homepage.png                # Case Study 内嵌截图
├── vnscene1.png                # Case Study 内嵌截图
├── vnscene2-option.png         # Case Study 内嵌截图
├── obsidian_repo.png           # Case Study 内嵌截图
├── opencode_working.jpg        # Case Study 内嵌截图
├── commits.png                 # Case Study 内嵌截图
├── working_record.png          # Case Study 内嵌截图
└── ...                         # 后续新增图片请遵循命名规范
```

### 5.2 命名规范

| 规范 | 规则 | 正确示例 | 错误示例 |
|------|------|---------|---------|
| 命名风格 | kebab-case | `portfolio-cover.png` | `portfolioCover.png`、`Portfolio Cover.png` |
| 后缀 | 小写 | `.png`、`.jpg` | `.PNG`、`.JPG` |
| 语义 | `{项目/区块}-{用途}.{ext}` | `ccms-cover.png`、`hero-character.png` | `img1.png`、`未命名.png` |
| 专用性 | 仅含小写字母、数字、连字符 | `case-study-banner.png` | `case_study_banner.png` |

### 5.3 新增图片流程

1. 准备源文件（PNG/JPEG，≤2MB）
2. 按 kebab-case 命名放入 `public/images/`
3. 更新对应 `.json` 数据文件中的 `cover` 路径
4. 若为新项目/案例，同时更新 `messages/{lang}.json` 中的 `titleKey` 文本
5. 若图片用于首页卡片（Hero/Project/CaseStudy），同步更新 `IntroShell.tsx` 中的预载数组（详见 [[7.1 内容更新指南#五.5.3 图片预载机制（Preload）]]）
6. 无需注册/导入，`next/image` 直接通过路径引用

---

## 六、字体资产管理规范

### 6.1 格式要求

| 环境 | 格式 | 说明 |
|------|------|------|
| 源文件 | `.ttf` 或 `.otf` | 下载的原始字体文件 |
| 生产 | **`.woff2`** | 必须转换为 woff2（压缩率最佳） |
| 转换工具 | google-webfonts-helper / `ttf2woff2` / 在线转换 | 首次放入前完成转换 |

### 6.2 字体加载配置

通过 `next/font/local` 声明，字体文件放置于 `public/fonts/`：

```typescript
// src/lib/fonts.ts
export const fontJp = localFont({
  src: "../../public/fonts/RoundedMplus1c-Regular.woff2",
  variable: "--font-jp",
  display: "swap",
})

export const fontEn = localFont({
  src: "../../public/fonts/Nunito-Regular.woff2",
  variable: "--font-en",
  display: "swap",
})
```

### 6.3 字体文件约束

| 约束项 | 规格 |
|--------|------|
| 总字体文件数 | 15 个（JP ×1：RoundedMplus1c + EN ×1：Nunito + ZH ×13：Noto Sans SC CJK 子集拆分） |
| 单文件大小 | ≤200KB（woff2 压缩后；中文子集分拆文件因包含大量字形，体积大于拉丁字体） |
| 加载策略 | JP/EN：`next/font/local` 自动 CSS `font-display: swap`；ZH：手动 `@font-face` 声明于 `src/styles/fonts.css` |
| CSS 变量 | `--font-jp`（`fontJp.variable`）、`--font-en`（`fontEn.variable`）、`--font-zh`（`:root` 中声明 `'Noto Sans SC'`） |
| 请求数量 | 0 额外请求（全部自托管，不请求 Google Fonts CDN） |
| 字重 | 所有字体仅 400（Regular）一字重，无 Bold/Medium 变体 |

---

## 七、文档资产（简历 & Case Study PDF）管理规范

### 7.1 简历 PDF

| 规范 | 规则 |
|------|------|
| 文件格式 | PDF（附带 Markdown 源文件） |
| 命名规则 | `resume-{lang}.pdf`、`resume-{lang}.md`（lang = zh / ja / en） |
| 放置路径 | `public/resume-{lang}.pdf`、`public/resume-{lang}.md` |
| 文件大小 | ≤5MB（PDF） |
| 更新方式 | 直接替换对应文件，无需修改代码 |
| 调用方式 | Contact 区块：`<a href={\`/resume-${locale}.pdf\`} target="_blank" rel="noopener noreferrer">`（新标签打开） |

### 7.2 Case Study PDF

| 规范 | 规则 |
|------|------|
| 文件格式 | PDF（附带 Markdown 源文件） |
| 命名规则 | `{slug}-{lang}.pdf`、`{slug}-{lang}.md`（slug 来自 `src/data/case-studies.json`） |
| 放置路径 | `public/{slug}-{lang}.pdf`、`public/{slug}-{lang}.md` |
| 文件大小 | ≤10MB（PDF） |
| 检测机制 | 详情页 `CaseStudyPage` 通过 `fs.existsSync` 运行时检测对应语言版本的 PDF 是否存在 |
| 调用方式 | `CaseStudyDownloadButton` 组件添加 `download` 属性触发下载 |

---

## 八、图标使用规范

| 规范 | 规则 |
|------|------|
| 图标库 | **Lucide React**（全站统一） |
| 风格 | 线性（Line），不允许使用填充（Fill）变体 |
| `strokeWidth` | 按组件需求：导航图标默认 `2`，下载按钮 `2`，反馈确认 `2.5` |
| 色值 | 继承父级 `color` 或显式指定 `text-text-primary` / `text-text-secondary` |
| 尺寸（Tailwind） | `w-5 h-5`（导航栏）、`size` 12px（下载按钮） |
| 非图标装饰 | VN 风格的 `▶`、`↓`、`→`、`✦` 等 Unicode 字符直接以文本渲染，不使用图标组件 |

### 当前实际使用的 Lucide 图标

| 图标名 | 使用位置 | 尺寸 | `strokeWidth` | 说明 |
|--------|---------|------|------|------|
| `Menu` | `Navbar.tsx` — 移动端汉堡菜单按钮 | 24px（`size={24}`） | 默认 2 | 仅 mobile `<768px` 显示 |
| `X` | `Navbar.tsx` — 移动端菜单关闭按钮 | 24px（`size={24}`） | 默认 2 | 与 `Menu` 互斥显示 |
| `Download` | `CaseStudyDownloadButton.tsx` — 下载 PDF | 12px（`size={12}`） | 2 | 下载状态切换动画 |
| `Check` | `CaseStudyDownloadButton.tsx` — 下载完成反馈 | 12px（`size={12}`） | 2.5 | 临时 1.5s 后恢复为 Download 图标 |

### 非图标交互元素

以下位置使用 Unicode/文本装饰而非 Lucide 图标：

| 元素 | 字符 | 使用位置 |
|------|------|---------|
| 选项按钮指示器 | `▶` | `OptionButton` / `TitleScreen` / `VNScenario` |
| VN 选项 hover 指示 | `▶` | `VNScenario` choice buttons |
| 查看详情箭头 | `→` | `CaseStudyCard` 查看详情链接 |
| 简历下载箭头 | `↓` | `ContactSection` 简历按钮 |
| 章节装饰 | `✦` | `TitleScreen` 标题装饰线 |
| 滚动指示 | `Scroll ↓` | `HeroSection` 底部 |
| 版权声明 | 文本翻译（`t("footer.copyright")`） | `Footer` |

---

## 九、资产约束汇总

| 类别 | 总上限 | 当前数量 | 单文件大小上限 | 格式 |
|------|--------|---------|---------------|------|
| 图片（PNG/JPEG） | ≤30 张 | 21 张（4 封面 + 1 立绘 + 8 表情 + 9 截图）+ 1 张 UI 素材 | ≤2MB（PNG）/ ≤500KB（JPEG） | PNG（透明/源）/ JPEG（截图源）/ AVIF+WebP（next/image 优化输出） |
| UI 素材（PNG） | — | 1 张（option-button.png） | ≤1MB | PNG（按原始分辨率使用，不建议自动压缩避免失真） |
| 字体（woff2） | ≤20 个 | 15 个（JP×1 + EN×1 + ZH×13） | ≤200KB | woff2（生产，全部自托管） |
| 简历（PDF） | ≤3 个 | 3 个（zh/ja/en） | ≤5MB | PDF |
| Case Study PDF | ≤3 个 | 3 个（zh/ja/en） | ≤10MB | PDF |
| 其他文档（MD） | ≤6 个 | 6 个（resume + casestudy × 3 语言） | — | Markdown（源文件，与 PDF 并列分发） |
| 图标 | 按需 | 4 个（Menu / X / Download / Check） | — | Lucide React 组件 |

---

## 十、维护规范

1. **替换图片**：直接覆盖 `public/images/` 同名文件 → 推送部署 → 自动生效
2. **新增图片**：放入 `public/images/` → 更新对应 `.json` 的 `cover` 路径
3. **替换字体**：覆盖 `public/fonts/` 同名 woff2 文件 → 推送部署 → 自动生效（需确保字重/名称不变）
4. **替换简历**：覆盖 `public/resume-{lang}.pdf` → 推送部署 → 自动生效
5. **新增图标**：从 `lucide-react` 导入即可，无额外配置
6. **新增 Case Study PDF/MD**：放入 `public/{slug}-{lang}.pdf` 及 `public/{slug}-{lang}.md`；详情页自动检测文件存在性（`fs.existsSync`），存在则显示下载按钮
7. **新增 JPEG 截图**：放入 `public/images/`（无需转换，`next/image` 支持 JPEG），命名遵循 kebab-case
8. **不要**：在 `public/images/` 以外的目录放置图片资源；使用 base64 内联图片
