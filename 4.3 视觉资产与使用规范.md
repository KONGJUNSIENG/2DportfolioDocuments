# 4.3 视觉资产与使用规范

**项目：** KONG JUN SIENG — Software Developer Portfolio  
**文档目标：** 定义所有视觉资产的格式、尺寸、优化策略与使用约束，供 AI 代理按此管理 `public/` 下的资源文件。

---

## 一、资产总清单

### 1.1 UI 素材资产（蓝色 Gal UI 系统）

| # | 文件名 | 路径 | 用途 | 源格式 | 原始分辨率 | 使用方式 |
|---|--------|------|------|--------|-----------|---------|
| 1 | `option-button.png` | `public/images/option-button.png` | 选项按钮背景素材 | PNG（透明背景，蓝色装饰边框 + 内部透明区域供文字） | **900×60** （宽高比 15:1） | ✅ 当前在使用。OptionButton 组件通过 `backgroundImage: url('/images/option-button.png')` 渲染按钮背景 |

> **素材来源**：与 hero-character.png 同一批从 BOOTH/DLsite 购买或获取的商业授权素材。
> **系统菜单**：无对应 PNG 素材，全部由 CSS + Tailwind 代码设计实现。风格参照 `--color-dialog-*` 色系，保持与对话框一致的蓝色调。
> **对话框：纯 CSS 实现**（三层圆角结构），不使用 dialog-box.png。
> **选项按钮：使用 option-button.png 为背景素材**，组件通过 backgroundImage 引用。

### 1.2 图片资产

| # | 文件名 | 路径 | 用途 | 源格式 | 推荐尺寸（px） | 来源 |
|---|--------|------|------|--------|---------------|------|
| 1 | `hero-character.png` | `public/images/hero-character.png` | Hero 区块大立绘 | PNG（透明背景） | 宽度 800–1200，高度按比例（~70vh 对应） | 外部购买 BOOTH/DLsite 商业授权素材 |
| 2 | `portfolio-cover.png` | `public/images/portfolio-cover.png` | Portfolio 项目封面（Case Study 卡片 16:9 + Projects 卡片 4:3 共用） | PNG | 1200×675（16:9） | 自行制作截图/设计图。Projects 卡片以 `object-fit: cover` 裁剪适配 4:3 容器，请确保图片主体位于安全区域内 |
| 3 | `fyp-iot-cover.png` | `public/images/fyp-iot-cover.png` | FYP IoT 项目封面 | PNG | 800×600（4:3） | 自行制作截图 |

**资产上限**：≤20 张图片。当前初始 3 张，预留后续扩展空间。

### 1.3 字体资产

| # | 文件名 | 路径 | 所属字体集 | 字重 | 格式要求 |
|---|--------|------|-----------|------|---------|
| 1 | `RoundedMplus1c-Regular.woff2` | `public/fonts/` | M PLUS Rounded 1c | 400 (仅此一字重) | woff2 |
| 2 | `Nunito-Regular.woff2` | `public/fonts/` | Nunito | 400 (仅此一字重) | woff2 |
| 3 | `noto-sans-sc-*-400-normal.woff2` (13 文件) | `public/fonts/` | Noto Sans SC | 400 (13 CJK 子集拆分) | woff2 |

### 1.4 简历资产

| # | 文件名 | 路径 | 说明 |
|---|--------|------|------|
| 1 | `resume-zh.pdf` | `public/resume-zh.pdf` | 中文简历 |
| 2 | `resume-ja.pdf` | `public/resume-ja.pdf` | 日本語履歴書 |
| 3 | `resume-en.pdf` | `public/resume-en.pdf` | English Resume |

---

## 二、图片格式规范

### 2.1 源文件格式

| 类型 | 源格式 | 理由 |
|------|--------|------|
| 立绘（透明背景） | **PNG-32**（含 Alpha 通道） | 需要透明背景，JPEG/WebP 不支持 |
| 封面图/截图 | **PNG** 或 **JPEG** | 源文件保留最大质量；生产由 Next.js 自动优化 |

### 2.2 生产输出格式

| 格式 | 优先级 | 说明 |
|------|--------|------|
| **AVIF** | 首选 | 压缩率最高，浏览器支持率已 >92% |
| **WebP** | 降级 | 兼容旧浏览器（Safari 16-/Chrome 32-/Firefox 65-） |

由 `next.config.ts` 配置控制：

```typescript
// next.config.ts
const nextConfig: NextConfig = {
  images: {
    formats: ["image/avif", "image/webp"],
  },
}
```

### 2.3 文件大小约束

| 类型 | 约束 | 说明 |
|------|------|------|
| 源文件（PNG） | ≤2MB | 超出需压缩后再放入 `public/images/` |
| 生产输出 | Next.js 自动优化 | 不手动压缩，`next/image` 按需输出合适大小 |

---

## 三、图片尺寸规范

### 3.1 各组件图片使用规格

| 使用位置 | 源文件 | 展示比例 | 展示尺寸（Desktop） | `sizes` 属性 | `priority` |
|----------|--------|---------|-------------------|-------------|-----------|
| Hero 立绘 | `hero-character.png` | 自适应 | 高度 `70vh`，宽度 ≤60vw | `"(max-width: 768px) 70vw, 60vw"` | `true`（首屏关键） |
| Case Study 卡片封面 | `portfolio-cover.png` | 16:9 | 宽度 `min(100%, 1152px)` | `"(max-width: 768px) 100vw, 1152px"` | `false` |
| Case Study 详情页封面 | `portfolio-cover.png` | 16:9 | 宽度 `min(100%, 768px)` | `"(max-width: 768px) 100vw, 768px"` | `false` |
| Projects 卡片封面 | `fyp-iot-cover.png` / `portfolio-cover.png` | 4:3 | 每列 ~48vw，最大 576px | `"(max-width: 768px) 100vw, (max-width: 1024px) 48vw, 576px"` | `false` |

### 3.2 object-fit 策略

| 组件 | 属性 | 值 | 说明 |
|------|------|-----|------|
| Hero 立绘（Desktop） | `object-fit` | `contain` | 保持完整立绘可见 |
| Hero 立绘（Desktop） | `object-position` | `center 45%` | 居中偏下对齐，突出面部区域 |
| Hero 立绘（Mobile） | `object-fit` | `cover` | 填充容器避免极窄变形 |
| Hero 立绘（Mobile） | `object-position` | `center 45%` | 与桌面端一致，居中偏下对齐 |
| 封面图（卡片） | `object-fit` | `cover` | 按比例裁剪填充卡片区域 |
| 封面图（详情页） | `object-fit` | `cover` | 全宽展示 |

### 3.3 响应式图片行为

| 断点 | Hero 立绘 | 卡片封面 |
|------|----------|---------|
| Desktop ≥1024px | 宽 60%，高 `70vh`，`object-position: bottom` | 原始比例 |
| Tablet 768–1023px | 宽 45%，高 `50vh`，`object-position: bottom` | 原始比例 |
| Mobile <768px | 宽 70% 居中，高 `50vh`，`object-position: top` | 自适应宽度，比例不变 |

---

## 四、Next/Image 优化策略

### 4.1 使用规则

| 场景 | 组件 | 属性 | 必须设置 |
|------|------|------|---------|
| 首屏关键图片 | Hero 立绘 | `priority={true}` | ✅ 是（避免 Lazy Load 延迟首屏渲染） |
| 一般图片 | 卡片封面/详情页封面 | `priority={false}`（默认懒加载） | — |
| 外部图片 | — | 无外部图片需求，全部本地 | — |
| 占位 | 所有图片 | `placeholder="blur"`（可选） | 需要提供 `blurDataURL` 或静态 import |

### 4.2 Hero 立绘实现模板

```tsx
// HeroSection.tsx — 正确使用方式
import Image from "next/image"
import heroImage from "@/../public/images/hero-character.png" // 静态 import 获取宽高

<Image
  src={heroImage}
  alt="KONG JUN SIENG"
  priority
  className="h-[70vh] w-full object-contain object-bottom"
  sizes="(max-width: 768px) 70vw, 60vw"
/>
```

### 4.3 卡片封面实现模板

```tsx
// CaseStudyCard.tsx — 封面图使用方式
<Image
  src={item.cover}
  alt={title}
  width={1200}
  height={675}
  className="w-full rounded-lg object-cover"
  sizes="(max-width: 768px) 100vw, 1152px"
/>
```

### 4.4 `<Image>` 属性速查表

| 属性 | Hero 立绘 | 卡片封面 | 详情页封面 |
|------|----------|---------|-----------|
| `src` | 静态 import（自动宽高） | `item.cover`（字符串路径） | `frontmatter.cover` |
| `alt` | "hero character" | 对应标题 | 对应标题 |
| `priority` | `true` | — | — |
| `sizes` | `"(max-width: 768px) 70vw, 60vw"` | `"(max-width: 768px) 100vw, 1152px"` | `"(max-width: 768px) 100vw, 768px"` |
| `className` | `h-[70vh] w-full object-contain object-bottom` | `w-full rounded-lg object-cover` | `w-full rounded-lg object-cover` |
| 静态宽高 | 自动（静态 import） | 手动 `width/height` | 手动 `width/height` |

---

## 五、图片目录管理规范

### 5.1 目录结构

```
public/images/
├── hero-character.png          # Hero 立绘
├── portfolio-cover.png         # Portfolio 封面（Case Study + Projects 共用）
├── fyp-iot-cover.png           # FYP IoT 封面
└── ...                         # 后续新增图片请遵循命名规范
```

### 5.2 命名规范

| 规范 | 规则 | 正确示例 | 错误示例 |
|------|------|---------|---------|
| 命名风格 | kebab-case | `portfolio-cover.png` | `portfolioCover.png`、`Portfolio Cover.png` |
| 后缀 | 小写 | `.png`、`.jpg` | `.PNG`、`.JPG` |
| 语义 | `{项目/区块}-{用途}.{ext}` | `fyp-iot-cover.png`、`hero-character.png` | `img1.png`、`未命名.png` |
| 专用性 | 仅含小写字母、数字、连字符 | `case-study-banner.png` | `case_study_banner.png` |

### 5.3 新增图片流程

1. 准备源文件（PNG/JPEG，≤2MB）
2. 按 kebab-case 命名放入 `public/images/`
3. 更新对应 `.json` 数据文件中的 `cover` 路径
4. 若为新项目/案例，同时更新 `messages/{lang}.json` 中的 `titleKey` 文本
5. 无需注册/导入，`next/image` 直接通过路径引用

---

## 六、字体资产管理规范

### 6.1 格式要求

| 环境 | 格式 | 说明 |
|------|------|------|
| 源文件 | `.ttf` 或 `.otf` | 下载的原始字体文件 |
| 生产 | **`.woff2`** | 必须转换为 woff2（压缩率最佳） |
| 转换工具 | google-webfonts-helper / `ttf2woff2` / 在线转换 | 首次放入前完成转换 |

### 6.2 字体加载配置

通过 `next/font/local` 声明，字体文件放置于 `public/fonts/`：

```typescript
// src/lib/fonts.ts
export const jaFont = localFont({
  src: [
    { path: "../../public/fonts/MPlusRounded1c-Regular.woff2", weight: "400" },
    { path: "../../public/fonts/MPlusRounded1c-Medium.woff2", weight: "500" },
    { path: "../../public/fonts/MPlusRounded1c-Bold.woff2", weight: "700" },
  ],
  variable: "--font-ja",
})
```

### 6.3 字体文件约束

| 约束项 | 规格 |
|--------|------|
| 总字体文件数 | ≤10 个（ja ×3 + en ×4 + zh ×3） |
| 单文件大小 | ≤50KB（woff2 压缩后） |
| 加载策略 | `next/font/local` 自动 CSS `font-display: swap` |
| 请求数量 | 0 额外请求（自托管，不请求 Google Fonts CDN） |

---

## 七、简历资产管理规范

| 规范 | 规则 |
|------|------|
| 文件格式 | PDF |
| 命名规则 | `resume-{lang}.pdf`（lang = zh / ja / en） |
| 放置路径 | `public/resume-{lang}.pdf` |
| 文件大小 | ≤5MB |
| 更新方式 | 直接替换对应文件，无需修改代码 |
| 下载方式 | Contact 区块 `<a href="/resume-{lang}.pdf" download>` |

---

## 八、图标使用规范

| 规范 | 规则 |
|------|------|
| 图标库 | **Lucide React**（全站统一） |
| 风格 | 线性（Line），不允许使用填充（Fill）变体 |
| `strokeWidth` | `1.5`（统一） |
| 色值 | 继承父级 `color` 或显式指定 `--color-primary` |
| 尺寸 | 16px（内联/标签内）、20px（导航栏）、24px（CTA/大按钮） |
| 使用示例 | `<Github className="w-5 h-5" strokeWidth={1.5} />` |

### 所需图标清单

| 图标名 | 使用位置 | 数量 | 尺寸 |
|--------|---------|------|------|
| `Github`（或 `ExternalLink` 组合） | SocialLink（GitHub） | 2 处 | 20px |
| `Linkedin` | SocialLink（LinkedIn） | 2 处 | 20px |
| `Menu` | 移动端导航汉堡按钮 | 1 处 | 24px |
| `X` / `Close` | 移动端菜单关闭按钮 | 1 处 | 24px |
| `ArrowRight` | Case Study 卡片"查看详情"按钮 | 1 处 | 16px |
| `ArrowLeft` | Case Study 详情页"返回"按钮 | ≤2 处 | 16px |
| `ChevronDown` | 滚动引导指示（Hero 底部） | 1 处 | 24px |
| `Download` | 简历下载按钮 | 1 处 | 16px |
| `Send` | 联系表单提交按钮 | 1 处 | 16px |
| `Mail` | 邮箱链接 | 1 处 | 20px |
| `Copyright`（或文本 `©`） | Footer 版权 | 1 处 | —（直接文本） |

---

## 九、资产约束汇总

| 类别 | 总上限 | 初始数量 | 单文件大小上限 | 格式 |
|------|--------|---------|---------------|------|
| 图片（PNG） | ≤20 张 | 3 张（封面/立绘） + 2 张（UI 素材） | ≤2MB | PNG（源）/ AVIF+WebP（输出） |
| UI 素材（PNG） | — | 2 张 | ≤1MB | PNG（按原始分辨率使用，不建议自动压缩避免失真） |
| 字体（woff2） | ≤10 个 | 10 个 | ≤50KB | woff2（生产） |
| 简历（PDF） | ≤3 个 | 3 个 | ≤5MB | PDF |
| 图标 | 按需（≤15 个） | ~11 个 | — | Lucide React 组件 |

---

## 十、维护规范

1. **替换图片**：直接覆盖 `public/images/` 同名文件 → 推送部署 → 自动生效
2. **新增图片**：放入 `public/images/` → 更新对应 `.json` 的 `cover` 路径
3. **替换字体**：覆盖 `public/fonts/` 同名 woff2 文件 → 推送部署 → 自动生效（需确保字重/名称不变）
4. **替换简历**：覆盖 `public/resume-{lang}.pdf` → 推送部署 → 自动生效
5. **新增图标**：从 `lucide-react` 导入即可，无额外配置
6. **不要**：在 `public/images/` 以外的目录放置图片资源；使用 base64 内联图片
