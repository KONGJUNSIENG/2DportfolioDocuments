# 4.2 项目文件结构文档

**项目：** KONG JUN SIENG — Software Developer Portfolio  
**文档目标：** 定义完整文件树结构、每个文件的职责与产出规格、组件接口类型，供 AI 代理按此逐个创建目录和组件文件。

---

## 一、完整文件树

见 `完整技术规格书与开发计划.md §五`（树结构定义在该文档中）。

本文件重点描述该树中各节点的职责与产出规格，不再重复粘贴目录树。
[[完整技术规格书与开发计划#五、目录结构]]

**新增游戏风序幕子系统：**

```
src/app/[lang]/intro/page.tsx        ← 游戏风序幕路由（proxy.ts 将 / 重定向至此）
src/components/IntroShell.tsx        ← 游戏风序幕流程编排器
src/components/TitleScreen.tsx        ← 游戏风标题画面
src/components/VNScenario.tsx         ← 游戏风场景演绎
src/components/LoadingOverlay.tsx     ← 游戏风加载动画
src/components/LoadingRipple.tsx      ← 波纹扩散
src/components/PixelsCurtain.tsx      ← 暗転遮罩
src/components/ScrambleText.tsx       ← 乱码闪烁
src/components/IntroAnimation.tsx     ← 全局动画协调
src/data/vn-scenario.en.json          ← 游戏风剧本（英）
src/data/vn-scenario.ja.json          ← 游戏风剧本（日）
src/data/vn-scenario.zh.json          ← 游戏风剧本（中）
public/images/{emotion}.png           ← 角色立绘表情 PNG（normal / smile / bigsmile / emm / shock / mad / sad / shy，共 8 文件，位于 `public/images/` 根目录）
```


---

## 二、页面文件职责与接口

### 2.1 `src/app/layout.tsx` — 根布局

| 属性 | 值 |
|------|-----|
| 类型 | Server Component |
| 职责 | 提供 `<html>` + `<body>` + 字体加载（`next/font` 日英文 + `fonts.css` 中文字体），不含任何 UI 逻辑 |
| 产出 | 注入 `fontJp.variable`（`--font-jp`）与 `fontEn.variable`（`--font-en`）到 `<html className>`；`fonts.css` 设置 `--font-zh` 于 `:root` |
| 不包含 | 导航栏、页脚、Provider——这些在 `[lang]/layout.tsx` 中 |
| Props | `{ children: React.ReactNode }` |

### 2.2 `src/app/[lang]/layout.tsx` — 多语布局

| 属性 | 值 |
|------|-----|
| 类型 | Server Component（内部使用 LocaleContent[Client] + LayoutShell[Client]） |
| 职责 | 包裹 `NextIntlClientProvider` + `JaLocaleToast` + 组合 LocaleContent（语言切换过渡）+ LayoutShell（页面过渡 + Nav + Footer） |
| 产出 | 所有语言敏感内容的统一包装器（含 ja 语言首次提示） |
| Props | `{ children: React.ReactNode, params: Promise<{ lang: string }> }`（Next.js 16 路由参数为 Promise，需 `await`） |
| 数据依赖 | `messages/{lang}.json`（通过 next-intl 自动注入） |

### 2.3 `src/app/[lang]/page.tsx` — 首页

| 属性 | 值 |
|------|-----|
| 类型 | Server Component |
| 职责 | 组合首页所有 Section 与 SectionTransition 过渡台词：Hero → SectionTransition → CaseStudies → SectionTransition → Projects → SectionTransition → About → SectionTransition → Contact |
| 产出 | `generateMetadata` 导出，SEO title/description/OG |
| Props | `{ params: Promise<{ lang: string }> }`（需 `await`） |
| 数据依赖 | `profile.json`、`projects.json`、`case-studies.json`、`messages/{lang}.json`（均通过子组件派生） |

### 2.4 `src/app/[lang]/projects/[slug]/page.tsx` — Case Study 详情页

| 属性 | 值 |
|------|-----|
| 类型 | Server Component（内含多个 Client 组件） |
| 职责 | 从 `case-studies.json` + `messages/{lang}.json` 读取结构化数据，渲染详情页含 9 个章节；侧栏导航（CaseStudyNav）、PDF 下载按钮（CaseStudyDownloadButton）、回顶按钮（BackToTopButton） |
| Props | `{ params: Promise<{ lang: string, slug: string }> }` |
| `generateMetadata` | 从 `messages/{lang}.json` 动态生成 title / description / OG |
| 数据依赖 | `case-studies.json`（slug/cover/techStack）、`messages/{lang}.json`（title/description + 10 节结构化正文：executiveSummary / background / methodology（4 sub）/ developmentProcess（9 sub）/ challenges（7 sub）/ results（4 sub）/ discussion（7 sub）/ reflections（3 sub）/ conclusion（3 sub）/ appendix（6 sub）） |
| 路由 | `/en/projects/casestudy`、`/zh/projects/casestudy`、`/ja/projects/casestudy` |
| 客户端组件 | `CaseStudySections`（whileInView 入场 + ChapterHeader + CaseStudyContent 渲染器）+ `CaseStudyNav`（右侧固定侧栏导航，滚动高亮）+ `CaseStudyDownloadButton`（VN 系统菜单风格，点击 blur→绿色完成态 1.5s 重置）+ `BackToTopButton`（纯文字 ▲，scrollY>250 浮现） |

### 2.X `src/app/[lang]/intro/page.tsx` — 游戏风序幕

| 属性 | 值 |
|------|-----|
| 类型 | Server Component（内部传入 scenes 给 IntroShell [Client] 驱动） |
| 职责 | `import "server-only"`；读取 VN 剧本后传入 `IntroShell`（客户端状态机），组织四阶段：TitleScreen → 暗転 → Scenario → LoadingOverlay → 首页跳转 |
| Props | `{ params: Promise<{ lang: string }> }` |
| 路由 | `/en/intro`（默认）、`/zh/intro`、`/ja/intro` |
| 数据依赖 | `src/data/vn-scenario.{lang}.json`（通过 `getVNScenario(lang)` 读取 + Zod 校验） |
| 特殊行为 | `proxy.ts` 将 `/` 重定向至 `/en/intro`；无 locale 校验通过 `hasLocale()` 后自动 `notFound()` |

### 2.5 `src/app/not-found.tsx` — 404 页面

| 属性 | 值 |
|------|-----|
| 类型 | Server Component |
| 职责 | 显示 VN 游戏风 404 页面 |
| 样式 | ERROR 404 标签（`text-dialog-blue` 小字）+ 粉色大字 "404" + 蓝线分隔 + "Page not found" + ▶ Go home 按钮（hover 右移+变蓝） |
| 注意 | 不感知语言，固定英文文案 |

### 2.6 `src/app/sitemap.ts` — 动态站点地图

| 属性 | 值 |
|------|-----|
| 类型 | Server-only（非 React 组件） |
| 职责 | 生成 sitemap.xml，包含所有语言组合的首页和详情页 URL |
| 产出 | 6 个 URL（3 languages ×（home + case study detail）），baseUrl `https://junsieng-portfolio.vercel.app` |

### 2.7 `src/app/robots.ts` — robots.txt

| 属性 | 值 |
|------|-----|
| 类型 | Server-only |
| 职责 | 允许所有爬虫，指向 sitemap |

---

## 三、组件文件职责与接口

### 3.1 `Navbar.tsx`

| 属性 | 值 |
|------|-----|
| 类型 | Client Component（滚动监听 + 汉堡菜单交互） |
| 职责 | 固定顶部导航栏：Logo + 锚点链接（案例/作品/关于/联系）+ 语言切换器 |
| Props | 无（数据从 next-intl `useTranslations` 获取） |
| 状态 | `scrollY > 50` → 渐入阴影 + 背景透明度渐现；移动端 `isMenuOpen` → 汉堡菜单展开 |
| 产出 | `<nav>` 固定定位，`z-50`，背景 `backdrop-blur-md`，使用 `useTransform` 动态驱动 bg alpha：scrollY[0,50] → [0,0.85] |
| 子组件 | `LanguageSwitcher`、`ScrollToAnchor` |
| 动画 | Framer Motion `useScroll` → `useTransform` 驱动 boxShadow + 背景 alpha；`AnimatePresence` 汉堡菜单展开/收起 |

### 3.2 `Footer.tsx`

| 属性 | 值 |
|------|-----|
| 类型 | Client Component（使用 `useTranslations`） |
| 职责 | 版权信息（居中布局） |
| Props | 无 |
| 子组件 | 无 |
| 数据依赖 | `messages/{lang}.json`（footer.copyright） |

### 3.3 `LanguageSwitcher.tsx`

| 属性 | 值 |
|------|-----|
| 类型 | Client Component（路由切换交互） |
| 职责 | 渲染三个语言按钮（中/日/EN），当前语言高亮，点击切换 `/[lang]` |
| Props | 无（从 next-intl `useLocale` / `usePathname` / `useRouter` 获取状态） |
| 产出 | `<div>` flex 布局，三个 `<button>` 或 `<Link>` |
| 样式 | 当前语言 `--color-primary` 高亮，其余 `--color-text-secondary` |

### 3.4 `HeroSection.tsx`

| 属性 | 值 |
|------|-----|
| 类型 | Client Component（Framer Motion 入场序列 + 视差） |
| 职责 | Hero 全屏区块：立绘（左 60%）+ 文字（右 40%），出场动画 0 → 2.2s 时序 |
| Props | 无（数据从 next-intl + profile.json 获取） |
| 产出 | `<section className="h-screen">` |
| 子组件 | 无（立绘 `<Image>` + 文字 + CTA 按钮内联） |
| 动画 | 立绘：`y: 30→0, opacity: 0→1` (500ms)；Slogan/Name/Title/CTA 依次延迟 0.7/1.2/1.7/2.2s |
| 装饰 | 背景光晕脉冲（CSS `@keyframes`）+ 立绘浮动（CSS `@keyframes`） |
| 视差 | Desktop 启用：立绘 `y: [0, -60]`、光晕 `opacity: [0.6, 0]`（scrollY 0→0.3） |

### 3.5 `CaseStudiesSection.tsx`

| 属性 | 值 |
|------|-----|
| 类型 | Client Component（whileInView stagger 入场） |
| 职责 | Section 标题 + Case Study 卡片列表（初始 1 张） |
| Props | 无 |
| 子组件 | `CaseStudyCard`（×1） |
| 动画 | `staggerContainer` + `staggerItem`，`whileInView` once |
| 数据依赖 | `case-studies.json`（slug/cover/techStack/titleKey/descriptionKey）+ `messages/{lang}.json`（翻译文本） |

### 3.6 `ProjectsSection.tsx`

| 属性 | 值 |
|------|-----|
| 类型 | Client Component（whileInView stagger 入场） |
| 职责 | Section 标题 + 项目卡片网格（双列，纯展示无跳转） |
| Props | 无 |
| 子组件 | `ProjectCard`（×2） |
| 动画 | `staggerContainer` + `staggerItem`，卡片逐个入场延迟 0.15s |
| 数据依赖 | `projects.json`（slug/cover/techStack/titleKey）+ `messages/{lang}.json`（翻译文本） |

### 3.7 `AboutSection.tsx`

| 属性 | 值 |
|------|-----|
| 类型 | Client Component（whileInView 入场） |
| 职责 | 个人介绍段落（65ch 行宽）+ 技能标签网格 |
| Props | 无 |
| 子组件 | `SkillTag`（×N） |
| 动画 | 标题独立入场 → 文字 → 标签 stagger |
| 数据依赖 | `profile.json`（skills）+ `messages/{lang}.json`（bio） |

### 3.8 `ContactSection.tsx`

| 属性 | 值 |
|------|-----|
| 类型 | Client Component（whileInView 入场） |
| 职责 | 邮箱链接 + 社交链接图标 + 简历下载按钮（无表单） |
| Props | 无 |
| 子组件 | 无（链接通过 a 标签直接包裹 OptionButton，社交图标内联渲染） |
| 简历下载 | 按钮链接 `public/resume-{lang}.pdf`，根据当前 `lang` 拼接路径 |
| 数据依赖 | `profile.json`（email, socialLinks）+ `messages/{lang}.json`（简历按钮文本） |

### 3.9 `CaseStudyCard.tsx`

| 属性 | 值 |
|------|-----|
| 类型 | Client Component（Framer Motion 空闲微摆 + hover 动画） |
| 职责 | 渲染单张 Case Study 卡片：封面 + 标签 + 标题 + 描述 + 查看详情按钮 |
| Props | `{ slug: string, cover: string, techStack: string[], titleKey: string, descriptionKey: string }` |
| 动画 | idle: `rotate: [0, 1.5, 0, -1.5, 0]` 每 1.5s 循环（Framer Motion `repeat: Infinity` + `repeatDelay: 1.1`）；hover: 微摆暂停 → `scale(1.03)` + shadow-md → shadow-lg；`useReducedMotion` 禁用 |
| 状态 | `isHovered: boolean` — mouse enter/leave 控制 |
| 跳转 | "查看详情"按钮 → `Link href="/[lang]/projects/${slug}"` |

### 3.10 `ProjectCard.tsx`

| 属性 | 值 |
|------|-----|
| 类型 | Client Component（Framer Motion 空闲弹跳 + clip-path 切换 + hover 动画） |
| 职责 | 渲染单张项目卡片：初始态=封面+标题+标签；点击后 clip-path 圆形扩散展示详情（标题 → 背景标签+内容 → 贡献标签+bullet list → 成果标签+内容）；再点击收拢 |
| Props | `{ cover: string, techStack: string[], titleKey: string, backgroundKey: string, contributeKey: string, outcomeKey: string }`（无 slug 字段） |
| 状态 | `expanded: boolean` — 点击切换；初始 `false`；`isHovered: boolean` — mouse enter/leave；`useMediaQuery` 检测 mobile 缩短 clip 时长 |
| 结构 | 双层级 DOM：底部层（详情内容，始终渲染，`scrollbar-micro` 隐藏 scrollbar）+ 顶部层（初始内容，absolute 定位 `inset-0` + `bg-surface`，`clipPath` 动画控制显隐） |
| 动画 | idle: `y: [0, -12, 0]` 每 1.25s 弹跳（Framer Motion `repeat: Infinity`），`easeInOut`；hover: 弹跳暂停 → `scale(1.03)` + shadow-md → shadow-lg；expanded: 弹跳/悬停全部暂停；clip-path: Desktop 0.6s / Mobile 0.3s，`ease: [0.16, 1, 0.3, 1]`；`useReducedMotion` 禁用 idle |
| 固定高度 | 由初始内容（封面 4:3 + 标题 + 标签）自然撑起；详情内容 `absolute inset-0` 填满同一空间，内容超出滚动（scrollbar 隐藏） |

### 3.11 `SkillTag.tsx`

| 属性 | 值 |
|------|-----|
| 类型 | Server Component |
| 职责 | 渲染单个技能标签：pill 形状，`rounded-full`，`bg-primary-soft`，`text-xs` |
| Props | `{ name: string }` |
| 动画 | CSS hover: `scale(1.0 → 1.05)` 200ms |

### 3.12 ~~`SocialLink.tsx`~~（已删除）

| 属性 | 值 |
|------|-----|
| 状态 | **已删除** — 社交链接改为 ContactSection 中直接内联渲染，不再使用独立组件 |
| 替代方式 | Footer 中社交链接直接通过 `<a>` 标签 + `lucide-react` 图标内联渲染 |

### 3.13 `ScrollToAnchor.tsx`

| 属性 | 值 |
|------|-----|
| 类型 | Client Component |
| 职责 | 平滑滚动到指定锚点，配合导航栏高度偏移（`scroll-mt-16`） |
| Props | `{ href: string, children: React.ReactNode }` |
| 实现 | `<Link>` + `scroll` 行为由 Navbar 高度动态补偿 |

### 3.14 `LocaleContent.tsx`

| 属性 | 值 |
|------|-----|
| 类型 | Client Component（AnimatePresence 语言切换过渡） |
| 职责 | 包裹全局内容，语言切换时触发 AnimatePresence mode="wait" + key={locale}，exit opacity 0(300ms) → enter opacity 1(500ms) |
| Props | `{ children: React.ReactNode }` |
| 位置 | 在 `[lang]/layout.tsx` 中包裹 `<LayoutShell>` |
| 包含 | Navbar + main(Foreground content) + Footer 全部语言敏感内容 |
| 动画 | Framer Motion AnimatePresence，key={locale} 变化时触发淡入淡出 |

### 3.16 `DialogBox.tsx`

| 属性 | 值 |
|------|-----|
| 类型 | Client Component（Framer Motion `dialogSlideUp` 入场） |
| 职责 | 游戏风蓝色对话框，用于 About 自我介绍 |
| Props | `{ characterName?: string, children: React.ReactNode, className?: string }` |
| 样式 | 三层圆角结构：外层白框 `rounded-lg` → 名字框 `rounded-xl` + 文字框 `rounded-md`。底色 `#2FA4D7`。`md:min-h-[100px]`（桌面自适应高度），手机 `min-h-[120px]`。内层 flex 1:3（名字 25% / 文字 75%）。白框随内容自动撑高。纯 CSS，不使用 `dialog-box.png` |
| 动画 | `dialogSlideUp`：`y:24→0, scale:0.98→1, opacity:0→1, 500ms` |
| 使用位置 | AboutSection |

### 3.17 `CaseStudyDownloadButton.tsx`

| 属性 | 值 |
|------|-----|
| 类型 | Client Component（Motion 交互按钮） |
| 职责 | VN 系统菜单风格下载按钮：点击后 blur 过渡 + 绿/黑白色切换，文字变为 "Downloaded!"，1.5s 后重置 |
| Props | `{ href: string, label: string }`（`href` 为 PDF 相对路径，如 `/${slug}-${lang}.pdf`） |
| 动画 | 初始态黑白灰（`rgba(0,0,0,0.35)` border + transparent bg + `rgba(0,0,0,0.7)` text），点击变深绿（`rgb(30,168,132)`）；AnimatePresence 文字切换；hover/tap 缩放 |

### 3.18 `BackToTopButton.tsx`

| 属性 | 值 |
|------|-----|
| 类型 | Client Component（滚动监听） |
| 职责 | VN Scenario Skip 按钮风格纯文字回顶按钮：▲ 默认隐藏/hover 上滑，scrollY>250 浮现，click 平滑回顶 |
| Props | 无 |
| 动画 | Framer Motion animate 控制 opacity/y；hover 时 translateY(-4px) |

### 3.19 `CaseStudySections.tsx`

| 属性 | 值 |
|------|-----|
| 类型 | Client Component（Framer Motion whileInView） |
| 职责 | Case Study 详情页的章节容器：组合 `ChapterHeader` + `CaseStudyContent` 渲染器，每节 `dialogSlideUp` 入场动画 |
| Props | `{ sections: { label: string; content?: string; subsections?: string[] }[] }` |
| 子组件 | `ChapterHeader`（复用）、`CaseStudyContent` |
| 使用位置 | `projects/[slug]/page.tsx` |

### 3.20 `CaseStudyContent.tsx`

| 属性 | 值 |
|------|-----|
| 类型 | 无 `"use client"`（纯渲染组件，由客户端父组件调用） |
| 职责 | Markdown 正文解析渲染器：将 messages JSON 中的 Markdown 字符串解析为 heading / list / code / table / paragraph / imageGrid 等 block，支持 `**bold**` 行内渲染、`→` 流程步骤检测（FlowStepBar）、`![]()` 图片网格（ZoomableImage） |
| Props | `{ text: string }` |
| 子组件 | `FlowStepBar`、`ZoomableImage` |
| 使用位置 | `CaseStudySections` 内部 |

### 3.21 `CaseStudyNav.tsx`

| 属性 | 值 |
|------|-----|
| 类型 | Client Component（scroll 事件监听） |
| 职责 | Case Study 详情页右侧固定侧栏导航：圆点指示器 + hover 显示 chapter label，滚动时高亮当前章节 |
| Props | `{ sections: { label: string }[] }` |
| 动画 | Framer Motion 动态 `width`（16↔32px）和 `backgroundColor`（secondary↔dialog-blue）切换 |
| 使用位置 | `projects/[slug]/page.tsx`，`md:` 断点以上显示 |

### 3.22 `FlowStepBar.tsx`

| 属性 | 值 |
|------|-----|
| 类型 | 无 `"use client"`（纯展示组件） |
| 职责 | 流程步骤可视化：圆点 + 连接线 + 步骤文字，可选的文档编号标注 |
| Props | `{ steps: string[] }` |
| 使用位置 | `CaseStudyContent` 内，检测到 `→` 流程句式时自动渲染 |

### 3.23 `ZoomableImage.tsx`

| 属性 | 值 |
|------|-----|
| 类型 | Client Component（点击放大 modal） |
| 职责 | 图片点击放大查看：modal 背景半透明黑，spring 弹出动画，Escape 关闭 |
| Props | `{ src: string, alt: string }` |
| 动画 | Framer Motion spring（damping: 25, stiffness: 300），AnimatePresence 进出 |
| 使用位置 | `CaseStudyContent` 内，检测到 Markdown `![]()` 图片语法时渲染 |

### 3.24 `JaLocaleToast.tsx`

| 属性 | 值 |
|------|-----|
| 类型 | Client Component |
| 职责 | ja 语言首次加载时显示 "日本語は勉強中です" 提示 Toast，3s 后自动消失 |
| Props | 无 |
| 全局锁 | `hasShownGlobally` 静态变量确保只显示一次 |

### 3.25 `ChapterHeader.tsx`

| 属性 | 值 |
|------|-----|
| 类型 | Client Component（whileInView `chapterReveal`） |
| 职责 | 渲染游戏风章节标题 = CHAPTER N 标签 + 章节名称 + 蓝色下划线 |
| Props | `{ chapterNumber?: number, title: string, subtitle?: string, label?: string, className?: string }`（`label` 为自定义标签文本，如 `"CHAPTER 1-1"`；`chapterNumber` 与 `label` 二选一） |
| 动画 | `chapterReveal`：`x:-30→0, opacity:0→1, 500ms` |
| 使用位置 | 首页各 Section 顶部（CH01 CaseStudies / CH02 Projects / CH03 About / CH04 Contact）+ Case Study 详情页各章节（`Chapter 1-{N}`） |

### 3.26 `SectionTransition.tsx`

| 属性 | 值 |
|------|-----|
| 类型 | Client Component（Framer Motion `transitionFade`） |
| 职责 | 在 Section 之间显示过渡台词（`~ text ~` + 水平线） |
| Props | `{ text: string, className?: string }`（文本从 messages 传入） |
| 动画 | `transitionFade`：`scale:0.92→1, opacity:0→1, 400ms`，仅在视口触发一次 |
| 使用位置 | page.tsx 中 Section 之间 |

### 3.27 `OptionButton.tsx`

| 属性 | 值 |
|------|-----|
| 类型 | Client Component（hover/tap 动效） |
| 职责 | 游戏风选择肢风格按钮，蓝底白字 + ▶ 指示符 |
| Props | `{ children: React.ReactNode, onClick?: () => void, className?: string }` |
| 动画 | `whileHover: { scale:1.02, x:4 }` 放大右移；`whileTap: { scale:0.98 }` 点击缩小 |
| 使用位置 | Hero CTA、ContactSection 系统菜单 |

### 3.28 `LayoutShell.tsx`

| 属性 | 值 |
|------|-----|
| 类型 | Client Component |
| 职责 | Navbar + AnimatePresence 页面过渡 + Footer 的客户端布局壳；intro 页面跳过 Navbar/Footer |
| Props | `{ children: React.ReactNode }` |
| 动画 | AnimatePresence mode="wait" + key={pathname}，opacity 过渡 200ms，onExitComplete scrollToTop；`isIntro` 判断：若 pathname 含 `/intro` 则直接渲染 children |

### 3.29 `useMediaQuery.ts`

| 属性 | 值 |
|------|-----|
| 类型 | Client Hook |
| 职责 | 响应式断点检测 hook，接收 CSS media query 字符串，返回 `boolean` 匹配状态 |
| 签名 | `(query: string): boolean` |
| 实现 | `window.matchMedia` + `useState` + `useEffect` |
| 使用位置 | HeroSection（Desktop 视差条件判断） |

### 3.30 `IntroShell.tsx`

| 属性 | 值 |
|------|-----|
| 类型 | Client Component |
| 职责 | 游戏风序幕流程编排器：管理 TitleScreen → 暗転 → Scenario → LoadingOverlay 四阶段状态切换 |
| Props | `{ scenes: VNScene[], lang: string }` |
| 子组件 | TitleScreen、PixelsCurtain、VNScenario、LoadingOverlay |

### 3.31 `TitleScreen.tsx`

| 属性 | 值 |
|------|-----|
| 类型 | Client Component |
| 职责 | 游戏风标题画面：角色立绘居中 + Slogan + 名称 +「开始」按钮。按钮点击触发暗転 |
| Props | `{ onStart: () => void }` |
| 动画 | 立绘/Slogan/名称/按钮依次淡入上移（opacity:0→1, y:20→0），stagger 0.4s |

### 3.32 `VNScenario.tsx`

| 属性 | 值 |
|------|-----|
| 类型 | Client Component |
| 职责 | 游戏风场景演绎：接收 `VNScene[]` 数组，逐场景推进。角色立绘表情切换 + 蓝色对话框 + 打字机效果 + 选项分支 + 鼠标排斥 + 自动推进 |
| Props | `{ scenes: VNScene[], onComplete: () => void }` |
| 动画 | 每场景对话框淡入上浮（`dialogSlideUp`）；打字机逐字显示；章节标题滑入（`chapterReveal`） |

### 3.33 `LoadingOverlay.tsx`

| 属性 | 值 |
|------|-----|
| 类型 | Client Component |
| 职责 | Scenario 完成后的加载动画：粒子光效 + 角色剪影 + 波纹扩散。动画结束后回调跳转首页 |
| Props | `{ onLoaded: () => void }` |
| 子组件 | LoadingRipple、ScrambleText |

### 3.34 `LoadingRipple.tsx`

| 属性 | 值 |
|------|-----|
| 类型 | Client Component |
| 职责 | 波纹扩散动画：3 层同心圆环向外扩散，CSS @keyframes 驱动 |
| Props | 无 |

### 3.35 `PixelsCurtain.tsx`

| 属性 | 值 |
|------|-----|
| 类型 | Client Component |
| 职责 | 暗転遮罩动画：全屏黑色 + 随机光效粒子向外扩散消散，CSS animation 驱动 |
| Props | `{ onComplete: () => void }` |

### 3.36 `ScrambleText.tsx`

| 属性 | 值 |
|------|-----|
| 类型 | Client Component |
| 职责 | 文字乱码闪烁效果，LoadingOverlay 中用于显示加载提示文字 |
| Props | `{ text: string }` |

### 3.37 `IntroAnimation.tsx`

| 属性 | 值 |
|------|-----|
| 类型 | Client Component |
| 职责 | 游戏风序幕全局动画协调器：管理入场时序、场景过渡、暗転、加载动画等共享动画状态 |
| Props | 无（通过 IntroShell 间接使用） |

---

## 四、库文件职责与接口

### 4.1 `src/lib/fonts.ts`

| 属性 | 值 |
|------|-----|
| 类型 | Server-only（next/font 声明） |
| 职责 | 声明日英文 `next/font/local` 实例并导出 CSS 变量 |
| 产出 | `fontJp` / `fontEn` 两个实例，供 `root layout.tsx` 调用 |
| 注意 | 中文 Noto Sans SC 因 CJK 子集拆分（13 个 woff2 文件），改用 `src/styles/fonts.css` 手动 @font-face |

```typescript
// 导出接口
import localFont from "next/font/local"

export const fontJp = localFont({
  src: "../../public/fonts/RoundedMplus1c-Regular.woff2",
  variable: "--font-jp",
  display: "swap",
})

export const fontEn = localFont({
  src: "../../public/fonts/Nunito-Regular.woff2",
  variable: "--font-en",
  display: "swap",
})
```

### 4.2 `src/i18n/routing.ts`

| 属性 | 值 |
|------|-----|
| 类型 | Shared |
| 职责 | 定义 locales 列表、defaultLocale、localePrefix；导出 Link / usePathname / useRouter 等导航工具 |
| 导出接口 | `routing`（供 proxy.ts 和 request.ts 使用）、`Link`、`usePathname`、`useRouter` |

### 4.3 `src/lib/read-data.ts`

| 属性 | 值 |
|------|-----|
| 类型 | Server-only（直接文件 I/O） |
| 职责 | 通用 JSON 文件读取 + Zod 校验，导出类型安全的读取函数 |

```typescript
// 导出接口
export function getCaseStudies(): CaseStudy[]
export function getVNScenario(lang: string): VNScenario
```

错误处理：Zod `parse` 失败时抛出 `ZodError`，build 阶段立即暴露数据问题（`server-only` 确保不会在客户端执行）。

### 4.4 `src/lib/animations.ts`

| 属性 | 值 |
|------|-----|
| 类型 | Shared（Client + Server 均可导入，但仅 Client 执行） |
| 职责 | 集中管理所有 Framer Motion variants，避免组件内重复定义动画参数 |

```typescript
// 导出接口
export const staggerContainer: Variants  // 父容器 stagger（子项间隔 0.15s）
export const staggerItem: Variants       // 子项 stagger 条目（y:20→0, opacity:0→1）
export const pageTransition: Variants    // 页面进入/退出过渡（opacity 0→1 / 1→0）
export const chapterReveal: Variants     // 章节标题滑入（x:-30→0, opacity:0→1）
export const dialogSlideUp: Variants     // 对话框上浮（y:24→0, scale:0.98→1, opacity:0→1）
export const optionStagger: Variants     // 选项按钮 stagger 父容器
export const optionItem: Variants        // 选项按钮条目（x:-20→0, opacity:0→1）
export const transitionFade: Variants    // 过渡台词缩放淡入（scale:0.92→1, opacity:0→1）
```

详见 `3.3 交互与动效说明.md` 附录具体实现。[[3.3 交互与动效说明#附录：animations.ts 完整代码]]

### 4.5 `src/lib/utils.ts`

| 属性 | 值 |
|------|-----|
| 类型 | Shared |
| 职责 | Tailwind class 合并工具函数（clsx + tailwind-merge） |

```typescript
export function cn(...inputs: ClassValue[]): string
```

---

## 五、数据文件结构定义

### 5.1 `src/data/profile.json`

```json
{
  "name": "KONG JUN SIENG",
  "slogan": "やればできる！",
  "skills": ["Python", "TypeScript", "React", "Next.js", "Node.js", "Git", "RESTful APIs", "Regular Expressions", "Internationalization (i18n)", "IoT", "Framer Motion"],
  "socialLinks": {
    "github": "https://github.com/KONGJUNSIENG",
    "linkedin": "https://www.linkedin.com/in/jun-sieng-kong-b1b3213b3"
  },
  "email": "js.kong@outlook.com"
}
```

### 5.2 `src/data/projects.json`

```json
[
  {
    "slug": "Chicken Coop Monitoring System (CCMS)",
    "cover": "/images/ccms-cover.png",
    "techStack": ["IoT", "Python", "Raspberry Pi", "Fast API", "RESTful APIs", "Next.js", "Node.js"],
    "titleKey": "projects.ccms.title",
    "backgroundKey": "projects.ccms.background",
    "contributeKey": "projects.ccms.contribute",
    "outcomeKey": "projects.ccms.outcome"
  },
  {
    "slug": "portfolio",
    "cover": "/images/portfolio-cover.png",
    "techStack": ["Next.js 16", "Node.js", "TypeScript", "Tailwind CSS", "Framer Motion"],
    "titleKey": "projects.portfolio.title",
    "backgroundKey": "projects.portfolio.background",
    "contributeKey": "projects.portfolio.contribute",
    "outcomeKey": "projects.portfolio.outcome"
  }
]
```

### 5.3 `src/data/case-studies.json`

```json
[
  {
    "slug": "casestudy",
    "cover": "/images/case-study-cover.png",
    "techStack": ["Obsidian", "Opencode", "DeepSeek V4 Flash", "Ollama", "Qwen3:14B"],
    "titleKey": "caseStudies.casestudy.title",
    "descriptionKey": "caseStudies.casestudy.description"
  }
]
```

正文内容通过 `messages/{lang}.json` 中 `caseStudies.{slug}.*` 结构化 key 引用，由 `getTranslations().raw()` 读取 Markdown 格式正文并通过 `CaseStudyContent` 组件渲染，参见 [[2.2 内容规划表.md#四 i18n 字段索引表]]。

### 5.4 `src/messages/{lang}.json`

JSON 结构（以 zh.json 为例）见 [[2.2 内容规划表.md#四 i18n 字段索引表]]。
包含以下顶级键：`nav`、`hero`、`caseStudies`（含详情正文字段：executiveSummary / background / methodology（含 4 子节 documentFirst / agentConfig / architecture / deployment）/ developmentProcess（含 9 子节 intro / phase1-7 / metricsNote）/ challenges（含 7 子节 challenge1-7）/ results（含 4 子节 overview / features / quality / links）/ discussion（含 7 子节 advantages / opencodeCliAdvantages / limitations / roleShift / teamScaling / hrPerspective / ossBoundary）/ reflections（含 3 子节 documentDensity / agentCapability / toolingAdvice）/ conclusion（含 3 子节 coreFindings / applicableScenarios / futureDirections）/ appendix（含 6 子节 appendixA-E / references））、`projects`、`about`、`contact`、`footer`、`transitions`（VN 过渡台词）。

### 5.6 `src/data/vn-scenario.{lang}.json`

43 场景游戏风序幕剧本（各语言独立文件：`vn-scenario.zh.json` / `vn-scenario.ja.json` / `vn-scenario.en.json`）：

| 字段 | 类型 | 必需 | 说明 |
|------|------|------|------|
| `scenes[]` | 数组 | 是 | 场景对象列表 |
| `scenes[].id` | string | 是 | 场景唯一标识，如 `"1"` |
| `scenes[].characterName` | string / null | 是 | 说话角色名，`null` 表示 narrator 旁白 |
| `scenes[].characterImage` | string (可选) | 否 | 角色立绘路径，如 `"/images/normal.png"` |
| `scenes[].dialogue` | string | 是 | 对话/旁白文本 |
| `scenes[].choices` | 数组 / null | 是 | 选项分支：`[{ text: string, nextScene: string }]`，`null` 表示无分支 |
| `scenes[].nextScene` | string (可选) | 否 | 下一场景 id（choices 为 null 时使用） |
| `scenes[].repel` | boolean (可选) | 否 | 鼠标排斥效果，光标靠近时对话框躲开 |
| `scenes[].autoAdvanceMs` | number (可选) | 否 | 自动推进毫秒数 |
| `scenes[].isEnding` | boolean (可选) | 否 | 是否为终场场景 |

---

## 六、文件创建顺序（依赖拓扑）

```
Phase 1: 项目初始化与环境搭建
    1. package.json           ← pnpm create next-app 生成
    2. pnpm-workspace.yaml    ← 手动配置 allowBuilds
    3. tsconfig.json          ← 同上，启用 strict 模式
    4. next.config.ts         ← 手动配置 i18n 插件
    5. postcss.config.mjs     ← pnpm create 生成
    6. .gitignore             ← pnpm create 生成
    7. .env.example           ← 手动创建
    8. src/styles/globals.css          ← Tailwind v4 `@import "tailwindcss"` + `@theme inline` 色彩令牌 + `@keyframes`
    9. src/lib/fonts.ts                ← next/font/local 声明（JP/EN）
    10. src/styles/fonts.css           ← 中文 Noto Sans SC 手动 @font-face（13 子集 woff2）
    11. src/app/layout.tsx             ← 根布局（html + body + fonts + globals.css + fonts.css）
    12. src/proxy.ts                  ← i18n 重定向（Next.js 16 约定替代 middleware.ts）
    13. src/i18n/request.ts            ← next-intl 请求配置
    14. src/i18n/routing.ts            ← next-intl 路由配置（locales / defaultLocale / 导航工具）
    15. .github/workflows/ci.yml       ← CI 流水线

Phase 2: 数据层与类型系统
     16. src/data/profile.json            ← 个人资料数据
     17. src/data/projects.json           ← 项目数据
     18. src/data/case-studies.json       ← Case Study 列表数据
     19. src/lib/case-studies.schema.ts   ← Zod schema（case studies 校验）
     20. src/messages/zh.json             ← 中文消息（含 caseStudies 9 节结构化正文）
     21. src/messages/ja.json             ← 日文消息
     22. src/messages/en.json             ← 英文消息
     23. src/lib/read-data.ts             ← JSON 读取 + Zod 校验（getCaseStudies / getVNScenario）
     24. src/lib/utils.ts                 ← cn() = clsx + tailwind-merge
     25. src/lib/animations.ts            ← Framer Motion variants 集中管理

Phase 3: 全局布局与导航
    26. src/components/SkillTag.tsx
    27. src/components/LanguageSwitcher.tsx
    28. src/components/Navbar.tsx
    29. src/components/Footer.tsx
    30. src/components/ScrollToAnchor.tsx
    31. src/components/LocaleContent.tsx        ← 语言切换 AnimatePresence 包装器
    32. src/components/LayoutShell.tsx          ← Nav + 页面过渡 + Footer 布局壳
    33. src/app/[lang]/layout.tsx               ← 多语布局（NextIntlClientProvider + LocaleContent + LayoutShell）

Phase 4: 首页各区块
    34. src/components/HeroSection.tsx
    35. src/components/CaseStudyCard.tsx
    36. src/components/CaseStudiesSection.tsx
    37. src/components/ProjectCard.tsx
    38. src/components/ProjectsSection.tsx
    39. src/components/AboutSection.tsx
    40. src/components/ContactSection.tsx
    41. src/app/[lang]/page.tsx

Phase 5: Case Study 详情页
     42. src/components/CaseStudyContent.tsx        ← Markdown 正文解析渲染器
     43. src/components/CaseStudySections.tsx        ← 章节容器（ChapterHeader + CaseStudyContent）
     44. src/components/ZoomableImage.tsx            ← 图片点击放大
     45. src/components/FlowStepBar.tsx              ← 流程步骤可视化
     46. src/components/CaseStudyNav.tsx             ← 右侧固定侧栏导航
     47. src/app/[lang]/projects/[slug]/page.tsx

Phase 6: 动效与交互动画系统
   （均为修改已有文件，无新增文件；useMediaQuery.ts 在 Phase 2 工具函数中已创建）

Phase 7: 游戏风 UI 系统与首页集成
   Part A — 游戏风组件（新增）
    48. src/components/DialogBox.tsx             ← 游戏风对话框组件
    49. src/components/ChapterHeader.tsx          ← 游戏风章节标题组件
    50. src/components/OptionButton.tsx           ← 游戏风选项按钮组件
    51. src/components/SectionTransition.tsx      ← 游戏风过渡台词组件
   Part B — Section 游戏风化（修改 Phase 4 已创建的文件，无新增文件）
   HeroSection(CTA→OptionButton) / CaseStudyCard / CaseStudiesSection(+ChapterHeader) /
   ProjectCard / ProjectsSection(+ChapterHeader) / AboutSection(+ChapterHeader+DialogBox) /
   ContactSection(+ChapterHeader+OptionButton) / page.tsx(+SectionTransition)

Phase 8: SEO 与部署
    52. src/app/not-found.tsx
    53. src/app/sitemap.ts
    54. src/app/robots.ts
    55. public/images/*                 ← 放置封面图、角色立绘、Case Study 正文图
    56. public/fonts/*                  ← 放置字体文件
    57. public/resume-{lang}.pdf        ← 简历 PDF
    58. public/resume-{lang}.md         ← 简历 Markdown 源文件

Phase 10: ProjectCard 点击 clip-path 圆形展开交互
    — src/data/projects.json               ← 修改（追加 3 字段值）
    — src/messages/{lang}.json             ← 修改（追加 background/contribute/outcome 文本）
    — src/components/ProjectCard.tsx        ← 修改（重写实现 clip-path 动画）

Phase 11: Case Study 详情页增强
    59. src/components/CaseStudyDownloadButton.tsx  ← 新增（Motion 交互下载按钮）
    60. src/components/BackToTopButton.tsx           ← 新增（Skip 风格回顶按钮）

Phase 12: 收尾修复
    61. src/app/not-found.tsx                       ← VN 游戏风风格改造
    62. src/app/layout.tsx                          ← metadata title 改为从 profile.json 读取
    63. src/app/[lang]/intro/page.tsx               ← 加入 generateMetadata
    64. src/app/[lang]/projects/[slug]/page.tsx     ← 加入 openGraph + CaseStudyNav + CaseStudySections
    65. src/app/[lang]/layout.tsx                   ← 加入 JaLocaleToast
    66. src/components/JaLocaleToast.tsx              ← 新增
```

---

## 七、文件命名约定

| 类别 | 约定 | 示例 |
|------|------|------|
| React 组件 | PascalCase | `HeroSection.tsx`、`SkillTag.tsx` |
| 工具/库文件 | kebab-case | `read-data.ts` |
| JSON 数据 | kebab-case | `profile.json`、`case-studies.json` |
| Case Study 正文 | 存储在 `messages/{lang}.json` 中，通过结构化 key 引用 | `caseStudies.portfolio.executiveSummary` |
| 消息文件 | `{lang}.json` | `zh.json`、`ja.json` |
| 图片文件 | kebab-case | `hero-character.png`、`ccms-cover.png` |
| 字体文件 | 原始名称（全小写 + 数字编号） | `RoundedMplus1c-Regular.woff2`、`noto-sans-sc-{4-30}-400-normal.woff2` |
| 简历文件 | `resume-{lang}.pdf` | `resume-zh.pdf` |

---

## 八、文件引用关系图

```
src/app/layout.tsx
  ├── src/lib/fonts.ts (fontJp.variable → --font-jp, fontEn.variable → --font-en)
  ├── src/styles/globals.css (@import "tailwindcss" + @theme inline + @keyframes)
  └── src/styles/fonts.css (Noto Sans SC @font-face ×13 + --font-zh)

src/app/[lang]/layout.tsx
  ├── src/components/JaLocaleToast.tsx (ja 首次加载提示)
  ├── src/components/LocaleContent.tsx (AnimatePresence key=locale)
  │   └── src/components/LayoutShell.tsx
  │       ├── src/lib/animations.ts (pageTransition)
  │       ├── src/components/Navbar.tsx
  │       │   ├── src/components/LanguageSwitcher.tsx
  │       │   ├── src/components/ScrollToAnchor.tsx
  │       │   └── src/messages/{lang}.json (nav.*)
  │       ├── src/components/Footer.tsx
  │       │   └── src/messages/{lang}.json (footer.copyright)
  │       └── src/lib/utils.ts (cn)
  └── src/i18n/routing.ts

src/app/[lang]/page.tsx
  ├── src/data/profile.json (name, slogan, skills)
  ├── src/data/projects.json
  ├── src/data/case-studies.json
  ├── src/messages/{lang}.json (hero.*, caseStudies.*, projects.*, about.*, contact.*, transitions.*)
  ├── src/components/HeroSection.tsx
  │   └── src/lib/animations.ts (staggerContainer, staggerItem, chapterReveal)
  ├── src/components/SectionTransition.tsx (×4)
  │   └── src/lib/animations.ts (transitionFade)
  ├── src/components/CaseStudiesSection.tsx
  │   ├── src/lib/animations.ts (staggerContainer, staggerItem)
  │   └── src/components/CaseStudyCard.tsx
  ├── src/components/ProjectsSection.tsx
  │   ├── src/lib/animations.ts (staggerContainer, staggerItem)
  │   └── src/components/ProjectCard.tsx
  │       └── src/hooks/useMediaQuery.ts
  ├── src/components/AboutSection.tsx
  │   ├── src/lib/animations.ts (staggerContainer, staggerItem, dialogSlideUp)
  │   ├── src/components/DialogBox.tsx
  │   └── src/components/SkillTag.tsx
  └── src/components/ContactSection.tsx
      ├── src/lib/animations.ts (staggerContainer, staggerItem, optionStagger, optionItem)
      └── src/components/OptionButton.tsx

src/app/[lang]/projects/[slug]/page.tsx
  ├── src/data/case-studies.json
  ├── src/messages/{lang}.json (caseStudies.{slug}.* — 9 节 × 子字段)
  ├── src/components/SkillTag.tsx
  ├── src/components/CaseStudySections.tsx (Client)
  │   ├── src/lib/animations.ts (dialogSlideUp)
  │   ├── src/components/ChapterHeader.tsx
  │   │   └── src/lib/animations.ts (chapterReveal)
  │   └── src/components/CaseStudyContent.tsx
  │       ├── src/components/FlowStepBar.tsx
  │       └── src/components/ZoomableImage.tsx
  ├── src/components/CaseStudyNav.tsx (Client)
  ├── src/components/CaseStudyDownloadButton.tsx (Client)
  └── src/components/BackToTopButton.tsx (Client)

src/app/[lang]/intro/page.tsx
  ├── src/lib/read-data.ts (getVNScenario)
  ├── src/components/IntroShell.tsx (Client — 四阶段状态机)
  │   ├── src/components/TitleScreen.tsx
  │   ├── src/components/PixelsCurtain.tsx
  │   ├── src/components/VNScenario.tsx
  │   │   ├── src/components/ChapterHeader.tsx
  │   │   └── src/components/DialogBox.tsx
  │   ├── src/components/LoadingOverlay.tsx
  │   │   ├── src/components/LoadingRipple.tsx
  │   │   └── src/components/ScrambleText.tsx
  │   └── src/components/IntroAnimation.tsx
  └── src/lib/animations.ts (staggerContainer, staggerItem, transitionFade, chapterReveal, dialogSlideUp)

src/lib/read-data.ts (server-only)
  ├── "server-only"
  ├── src/data/case-studies.json (getCaseStudies → CaseStudiesSchema.parse)
  └── src/data/vn-scenario.{lang}.json (getVNScenario → fs.readFileSync + VNScenarioSchema.parse)
```
