# 4.2 项目文件结构文档

**项目：** KONG JUN SIENG — Software Developer Portfolio  
**文档目标：** 定义完整文件树结构、每个文件的职责与产出规格、组件接口类型，供 AI 代理按此逐个创建目录和组件文件。

---

## 一、完整文件树

见 `完整技术规格书与开发计划.md §五`（树结构定义在该文档中）。

本文件重点描述该树中各节点的职责与产出规格，不再重复粘贴目录树。
[[完整技术规格书与开发计划#五、目录结构]]

**新增游戏风序幕子系统：**

```
src/app/[lang]/intro/page.tsx        ← 游戏风序幕路由（proxy.ts 将 / 重定向至此）
src/components/IntroShell.tsx        ← 游戏风序幕流程编排器
src/components/TitleScreen.tsx        ← 游戏风标题画面
src/components/VNScenario.tsx         ← 游戏风场景演绎
src/components/LoadingOverlay.tsx     ← 游戏风加载动画
src/components/LoadingRipple.tsx      ← 波纹扩散
src/components/PixelsCurtain.tsx      ← 暗転遮罩
src/components/ScrambleText.tsx       ← 乱码闪烁
src/components/IntroAnimation.tsx     ← 全局动画协调
src/data/vn-scenario.en.json          ← 游戏风剧本（英）
src/data/vn-scenario.ja.json          ← 游戏风剧本（日）
src/data/vn-scenario.zh.json          ← 游戏风剧本（中）
public/images/intro/character-*.png   ← 角色立绘表情 PNG（8 文件）
```


---

## 二、页面文件职责与接口

### 2.1 `src/app/layout.tsx` — 根布局

| 属性 | 值 |
|------|-----|
| 类型 | Server Component |
| 职责 | 提供 `<html>` + `<body>` + 字体加载（`next/font`），不含任何 UI 逻辑 |
| 产出 | 注入中日英三套字体的 CSS 变量到 `:root` |
| 不包含 | 导航栏、页脚、Provider——这些在 `[lang]/layout.tsx` 中 |
| Props | `{ children: React.ReactNode }` |

### 2.2 `src/app/[lang]/layout.tsx` — 多语布局

| 属性 | 值 |
|------|-----|
| 类型 | Server Component（内部使用 LocaleContent[Client] + LayoutShell[Client]） |
| 职责 | 包裹 `NextIntlClientProvider` + 组合 LocaleContent（语言切换过渡）+ LayoutShell（页面过渡 + Nav + Footer） |
| 产出 | 所有语言敏感内容的统一包装器 |
| Props | `{ children: React.ReactNode, params: { lang: string } }` |
| 数据依赖 | `messages/{lang}.json`（通过 next-intl 自动注入） |

### 2.3 `src/app/[lang]/page.tsx` — 首页

| 属性 | 值 |
|------|-----|
| 类型 | Server Component |
| 职责 | 组合首页所有 Section：Hero → CaseStudies → Projects → About → Contact |
| 产出 | `metadata` 导出（generateMetadata），SEO title/description/OG |
| Props | `{ params: { lang: string } }` |
| 数据依赖 | `profile.json`、`projects.json`、`case-studies.json`、`messages/{lang}.json` |

### 2.4 `src/app/[lang]/projects/[slug]/page.tsx` — Case Study 详情页

| 属性 | 值 |
|------|-----|
| 类型 | Server Component |
| 职责 | 读取对应语言 Markdown → 解析 frontmatter + body → 渲染详情页 |
| Props | `{ params: { lang: string, slug: string } }` |
| `generateStaticParams` | 返回所有 `(lang, slug)` 组合以 SSG 预渲染 |
| `generateMetadata` | 根据 frontmatter 动态生成 title / description / OG |
| 数据依赖 | `src/data/{slug}-{lang}.md` |
| 路由 | `/zh/projects/portfolio`、`/ja/projects/portfolio`、`/en/projects/portfolio` |

### 2.X `src/app/[lang]/intro/page.tsx` — 游戏风序幕

| 属性 | 值 |
|------|-----|
| 类型 | Client Component（全客户端动效） |
| 职责 | 组织游戏风序幕四阶段：TitleScreen → 暗転 → Scenario → LoadingOverlay → 首页跳转 |
| Props | `{ params: { lang: string } }` |
| 路由 | `/zh/intro`、`/ja/intro`、`/en/intro` |
| 数据依赖 | `src/data/vn-scenario.{lang}.json` |
| 特殊行为 | `proxy.ts` 将 `/` 重定向至此路由 |

### 2.5 `src/app/not-found.tsx` — 404 页面

| 属性 | 值 |
|------|-----|
| 类型 | Server Component |
| 职责 | 显示"页面未找到"信息 + 返回首页链接 |
| 样式 | 与全站风格一致，居中布局 |
| 注意 | 不感知语言，使用默认 locale（zh）显示 |

### 2.6 `src/app/sitemap.ts` — 动态站点地图

| 属性 | 值 |
|------|-----|
| 类型 | Server-only（非 React 组件） |
| 职责 | 生成 sitemap.xml，包含所有语言组合的首页和详情页 URL |
| 产出 | 6 个页面 × 语言组合 |

### 2.7 `src/app/robots.ts` — robots.txt

| 属性 | 值 |
|------|-----|
| 类型 | Server-only |
| 职责 | 允许所有爬虫，指向 sitemap |

---

## 三、组件文件职责与接口

### 3.1 `Navbar.tsx`

| 属性 | 值 |
|------|-----|
| 类型 | Client Component（滚动监听 + 汉堡菜单交互） |
| 职责 | 固定顶部导航栏：Logo + 锚点链接（案例/作品/关于/联系）+ 语言切换器 |
| Props | 无（数据从 next-intl `useTranslations` 获取） |
| 状态 | `scrollY > 50` → 渐入阴影 + 背景透明度渐现；移动端 `isMenuOpen` → 汉堡菜单展开 |
| 产出 | `<nav>` 固定定位，`z-50`，背景 `backdrop-blur-md`，使用 `useTransform` 动态驱动 bg alpha：scrollY[0,50] → [0,0.85] |
| 子组件 | `LanguageSwitcher` |
| 动画 | Framer Motion `useScroll` → `useTransform` 驱动 boxShadow + 背景 alpha |

### 3.2 `Footer.tsx`

| 属性 | 值 |
|------|-----|
| 类型 | Client Component（framer-motion hover 动画） |
| 职责 | 版权信息 (居中布局) |
| Props | 无 |
| 子组件 | 无 |
| 数据依赖 | `profile.json`（socialLinks — 对象格式）、`messages/{lang}.json`（copyright） |

### 3.3 `LanguageSwitcher.tsx`

| 属性 | 值 |
|------|-----|
| 类型 | Client Component（路由切换交互） |
| 职责 | 渲染三个语言按钮（中/日/EN），当前语言高亮，点击切换 `/[lang]` |
| Props | 无（从 next-intl `useLocale` / `usePathname` / `useRouter` 获取状态） |
| 产出 | `<div>` flex 布局，三个 `<button>` 或 `<Link>` |
| 样式 | 当前语言 `--color-primary` 高亮，其余 `--color-text-secondary` |

### 3.4 `HeroSection.tsx`

| 属性 | 值 |
|------|-----|
| 类型 | Client Component（Framer Motion 入场序列 + 视差） |
| 职责 | Hero 全屏区块：立绘（左 60%）+ 文字（右 40%），出场动画 0 → 2.2s 时序 |
| Props | 无（数据从 next-intl + profile.json 获取） |
| 产出 | `<section className="h-screen">` |
| 子组件 | 无（立绘 `<Image>` + 文字 + CTA 按钮内联） |
| 动画 | 立绘：`y: 30→0, opacity: 0→1` (500ms)；Slogan/Name/Title/CTA 依次延迟 0.7/1.2/1.7/2.2s |
| 装饰 | 背景光晕脉冲（CSS `@keyframes`）+ 立绘浮动（CSS `@keyframes`） |
| 视差 | Desktop 启用：立绘 `y: [0, -60]`、光晕 `opacity: [0.6, 0]`（scrollY 0→0.3） |

### 3.5 `CaseStudiesSection.tsx`

| 属性 | 值 |
|------|-----|
| 类型 | Client Component（whileInView stagger 入场） |
| 职责 | Section 标题 + Case Study 卡片列表（初始 1 张） |
| Props | 无 |
| 子组件 | `CaseStudyCard`（×1） |
| 动画 | `staggerContainer` + `staggerItem`，`whileInView` once |
| 数据依赖 | `case-studies.json`（slug/cover/techStack/titleKey/descriptionKey）+ `messages/{lang}.json`（翻译文本） |

### 3.6 `ProjectsSection.tsx`

| 属性 | 值 |
|------|-----|
| 类型 | Client Component（whileInView stagger 入场） |
| 职责 | Section 标题 + 项目卡片网格（双列，纯展示无跳转） |
| Props | 无 |
| 子组件 | `ProjectCard`（×2） |
| 动画 | `staggerContainer` + `staggerItem`，卡片逐个入场延迟 0.15s |
| 数据依赖 | `projects.json`（slug/cover/techStack/titleKey）+ `messages/{lang}.json`（翻译文本） |

### 3.7 `AboutSection.tsx`

| 属性 | 值 |
|------|-----|
| 类型 | Client Component（whileInView 入场） |
| 职责 | 个人介绍段落（65ch 行宽）+ 技能标签网格 |
| Props | 无 |
| 子组件 | `SkillTag`（×N） |
| 动画 | 标题独立入场 → 文字 → 标签 stagger |
| 数据依赖 | `profile.json`（skills）+ `messages/{lang}.json`（bio） |

### 3.8 `ContactSection.tsx`

| 属性 | 值 |
|------|-----|
| 类型 | Client Component（whileInView 入场） |
| 职责 | 邮箱链接 + 社交链接图标 + 简历下载按钮（无表单） |
| Props | 无 |
| 子组件 | 无（链接通过 a 标签直接包裹 OptionButton） |
| 简历下载 | 按钮链接 `public/resume-{lang}.pdf`，根据当前 `lang` 拼接路径 |
| 数据依赖 | `profile.json`（email, socialLinks）+ `messages/{lang}.json`（简历按钮文本） |

### 3.9 `CaseStudyCard.tsx`

| 属性 | 值 |
|------|-----|
| 类型 | Client Component（Framer Motion 循环微摆 + hover 动画） |
| 职责 | 渲染单张 Case Study 卡片：封面 + 标签 + 标题 + 描述 + 查看详情按钮 |
| Props | `{ slug: string, cover: string, techStack: string[], titleKey: string, descriptionKey: string }` |
| 动画 | idle: `rotate: [0, 1.5, 0, -1.5, 0]` 每 1.5s 循环（Framer Motion `repeat: Infinity`）；hover: 微摆暂停 → `scale(1.03)` + shadow-md → shadow-lg；`prefersReducedMotion` 禁用 |
| 跳转 | "查看详情"按钮 → `Link href="/[lang]/projects/${slug}"` |

### 3.10 `ProjectCard.tsx`

| 属性 | 值 |
|------|-----|
| 类型 | Client Component（Framer Motion 空闲弹跳 + clip-path 切换 + hover 动画） |
| 职责 | 渲染单张项目卡片：初始态=封面+标题+标签；点击后 clip-path 圆形扩散展示详情（标题 → 背景标签+内容 → 贡献标签+bullet list → 成果标签+内容）；再点击收拢 |
| Props | `{ slug: string, cover: string, techStack: string[], titleKey: string, backgroundKey: string, contributeKey: string, outcomeKey: string }` |
| 状态 | `expanded: boolean` — 点击切换；初始 `false`；`isHovered: boolean` — mouse enter/leave |
| 结构 | 双层级 DOM：底部层（详情内容，始终渲染，`scrollbar-micro` 隐藏 scrollbar）+ 顶部层（初始内容，绝对定位 `inset-0` + `bg-surface`，`clipPath` 动画控制显隐） |
| 动画 | idle: `y: [0, -12, 0]` 每 1.5s 弹跳（Framer Motion `repeat: Infinity`），`easeInOut`；hover: 弹跳暂停 → `scale(1.03)` + shadow-md → shadow-lg；expanded: 弹跳暂停；clip-path: Desktop 0.6s / Mobile 0.3s，`ease: [0.16, 1, 0.3, 1]`；`prefersReducedMotion` 禁用 idle |
| 固定高度 | 由初始内容（封面 4:3 + 标题 + 标签）自然撑起；详情内容 `absolute inset-0` 填满同一空间，内容超出滚动（scrollbar 隐藏） |

### 3.11 `SkillTag.tsx`

| 属性 | 值 |
|------|-----|
| 类型 | Server Component |
| 职责 | 渲染单个技能标签：pill 形状，`rounded-full`，`bg-primary-soft`，`text-xs` |
| Props | `{ name: string }` |
| 动画 | CSS hover: `scale(1.0 → 1.05)` 200ms |

### 3.12 `SocialLink.tsx`

| 属性 | 值 |
|------|-----|
| 类型 | Server Component |
| 职责 | 渲染社交链接图标 + 平台名，点击跳转到对应 URL |
| Props | `{ platform: string, url: string }` |
| 图标 | 来自 `lucide-react`，`strokeWidth={1.5}`，尺寸 20px（导航）/ 16px（footer） |

### 3.13 `ScrollToAnchor.tsx`

| 属性 | 值 |
|------|-----|
| 类型 | Client Component |
| 职责 | 平滑滚动到指定锚点，配合导航栏高度偏移（`scroll-mt-16`） |
| Props | `{ href: string, children: React.ReactNode }` |
| 实现 | `<Link>` + `scroll` 行为由 Navbar 高度动态补偿 |

### 3.14 `LocaleContent.tsx`

| 属性 | 值 |
|------|-----|
| 类型 | Client Component（AnimatePresence 语言切换过渡） |
| 职责 | 包裹全局内容，语言切换时触发 AnimatePresence mode="wait" + key={locale}，exit opacity 0(300ms) → enter opacity 1(500ms) |
| Props | `{ children: React.ReactNode }` |
| 位置 | 在 `[lang]/layout.tsx` 中包裹 `<LayoutShell>` |
| 包含 | Navbar + main(Foreground content) + Footer 全部语言敏感内容 |
| 动画 | Framer Motion AnimatePresence，key={locale} 变化时触发淡入淡出 |

### 3.16 `DialogBox.tsx`

| 属性 | 值 |
|------|-----|
| 类型 | Client Component（Framer Motion `dialogSlideUp` 入场） |
| 职责 | 游戏风蓝色对话框，用于 About 自我介绍 |
| Props | `{ characterName?: string, children: React.ReactNode, className?: string }` |
| 样式 | 三层圆角结构：外层白框 `rounded-lg` → 名字框 `rounded-xl` + 文字框 `rounded-md`。底色 `#2FA4D7`。`md:min-h-[100px]`（桌面自适应高度），手机 `min-h-[120px]`。内层 flex 1:3（名字 25% / 文字 75%）。白框随内容自动撑高。纯 CSS，不使用 `dialog-box.png` |
| 动画 | `dialogSlideUp`：`y:24→0, scale:0.98→1, opacity:0→1, 500ms` |
| 使用位置 | AboutSection |

### 3.17 `ChapterHeader.tsx`

| 属性 | 值 |
|------|-----|
| 类型 | Client Component（whileInView `chapterReveal`） |
| 职责 | 渲染游戏风章节标题 = CHAPTER N 标签 + 章节名称 + 蓝色下划线 |
| Props | `{ chapterNumber?: number, title: string, subtitle?: string, className?: string }` |
| 动画 | `chapterReveal`：`x:-30→0, opacity:0→1, 500ms` |
| 使用位置 | 每个 Section 顶部（CH01 CaseStudies / CH02 Projects / CH03 About / CH04 Contact） |

### 3.18 `SectionTransition.tsx`

| 属性 | 值 |
|------|-----|
| 类型 | Client Component（Framer Motion `transitionFade`） |
| 职责 | 在 Section 之间显示过渡台词（`~ text ~` + 水平线） |
| Props | `{ text: string, className?: string }`（文本从 messages 传入） |
| 动画 | `transitionFade`：`scale:0.92→1, opacity:0→1, 400ms`，仅在视口触发一次 |
| 使用位置 | page.tsx 中 Section 之间 |

### 3.19 `OptionButton.tsx`

| 属性 | 值 |
|------|-----|
| 类型 | Client Component（hover/tap 动效） |
| 职责 | 游戏风选择肢风格按钮，蓝底白字 + ▶ 指示符 |
| Props | `{ children: React.ReactNode, onClick?: () => void, className?: string }` |
| 动画 | `whileHover: { scale:1.02, x:4 }` 放大右移；`whileTap: { scale:0.98 }` 点击缩小 |
| 使用位置 | Hero CTA、ContactSection 系统菜单 |

### 3.20 `LayoutShell.tsx`

| 属性 | 值 |
|------|-----|
| 类型 | Client Component |
| 职责 | Navbar + AnimatePresence 页面过渡 + Footer 的客户端布局壳 |
| Props | `{ children: React.ReactNode }` |
| 动画 | AnimatePresence mode="wait" + key={pathname}，opacity 过渡 200ms，onExitComplete scrollToTop |

### 3.21 `useMediaQuery.ts`

| 属性 | 值 |
|------|-----|
| 类型 | Client Hook |
| 职责 | 响应式断点检测 hook，接收 CSS media query 字符串，返回 `boolean` 匹配状态 |
| Props | `(query: string): boolean` |
| 实现 | `window.matchMedia` + `useState` + `useEffect` |
| 使用位置 | HeroSection（Desktop 视差条件判断） |

### 3.22 `IntroShell.tsx`

| 属性 | 值 |
|------|-----|
| 类型 | Client Component |
| 职责 | 游戏风序幕流程编排器：管理 TitleScreen → 暗転 → Scenario → LoadingOverlay 四阶段状态切换 |
| Props | `{ lang: string }` |
| 子组件 | TitleScreen、PixelsCurtain、VNScenario、LoadingOverlay |

### 3.23 `TitleScreen.tsx`

| 属性 | 值 |
|------|-----|
| 类型 | Client Component |
| 职责 | 游戏风标题画面：角色立绘居中 + Slogan + 名称 +「开始」按钮。按钮点击触发暗転 |
| Props | `{ onStart: () => void }` |
| 动画 | 立绘/Slogan/名称/按钮依次 fadeInUp，stagger 0.4s |

### 3.24 `VNScenario.tsx`

| 属性 | 值 |
|------|-----|
| 类型 | Client Component |
| 职责 | 游戏风场景演绎：读取 `vn-scenario.{lang}.json`，逐场景推进。角色立绘表情切换 + 蓝色对话框 + 打字机效果 |
| Props | `{ lang: string, onComplete: () => void }` |
| 动画 | 每场景对话框淡入上浮；打字机逐字显示；章节标题滑入 |

### 3.25 `LoadingOverlay.tsx`

| 属性 | 值 |
|------|-----|
| 类型 | Client Component |
| 职责 | Scenario 完成后的加载动画：粒子光效 + 角色剪影 + 波纹扩散。动画结束后回调跳转首页 |
| Props | `{ onLoaded: () => void }` |
| 子组件 | LoadingRipple、ScrambleText |

### 3.26 `LoadingRipple.tsx`

| 属性 | 值 |
|------|-----|
| 类型 | Client Component |
| 职责 | 波纹扩散动画：3 层同心圆环向外扩散，CSS @keyframes 驱动 |
| Props | 无 |

### 3.27 `PixelsCurtain.tsx`

| 属性 | 值 |
|------|-----|
| 类型 | Client Component |
| 职责 | 暗転遮罩动画：全屏黑色 + 随机光效粒子向外扩散消散，CSS animation 驱动 |
| Props | `{ onComplete: () => void }` |

### 3.28 `ScrambleText.tsx`

| 属性 | 值 |
|------|-----|
| 类型 | Client Component |
| 职责 | 文字乱码闪烁效果，LoadingOverlay 中用于显示加载提示文字 |
| Props | `{ text: string }` |

### 3.29 `IntroAnimation.tsx`

| 属性 | 值 |
|------|-----|
| 类型 | Client Component |
| 职责 | 游戏风序幕全局动画协调器：管理入场时序、场景过渡、暗転、加载动画等共享动画状态 |
| Props | 无（通过 IntroShell 间接使用） |

---

## 四、库文件职责与接口

### 4.1 `src/lib/fonts.ts`

| 属性 | 值 |
|------|-----|
| 类型 | Server-only（next/font 声明） |
| 职责 | 声明日英文 `next/font/local` 实例并导出 CSS 变量 |
| 产出 | `fontJp` / `fontEn` 两个实例，供 `root layout.tsx` 调用 |
| 注意 | 中文 Noto Sans SC 因 CJK 子集拆分（13 个 woff2 文件），改用 `src/styles/fonts.css` 手动 @font-face |

```typescript
// 导出接口
import localFont from "next/font/local"

export const fontJp = localFont({
  src: "../../public/fonts/RoundedMplus1c-Regular.woff2",
  variable: "--font-jp",
  display: "swap",
})

export const fontEn = localFont({
  src: "../../public/fonts/Nunito-Regular.woff2",
  variable: "--font-en",
  display: "swap",
})
```

### 4.2 `src/i18n/routing.ts`

| 属性 | 值 |
|------|-----|
| 类型 | Shared |
| 职责 | 定义 locales 列表、defaultLocale、localePrefix；导出 Link / redirect / usePathname / useRouter 等导航工具 |
| 导出接口 | `routing`（供 proxy.ts 和 request.ts 使用）、`Link`、`redirect`、`usePathname`、`useRouter`、`getPathname` |

### 4.3 `src/lib/read-data.ts`

| 属性 | 值 |
|------|-----|
| 类型 | Server-only（直接文件 I/O） |
| 职责 | 通用 JSON 文件读取 + Zod 校验，导出类型安全的读取函数 |

```typescript
// 导出接口
export function getProfile(): Profile
export function getProjects(): Project[]
export function getCaseStudies(): CaseStudy[]
```

错误处理：Zod `parse` 失败时 `console.error` + `throw`，build 阶段暴露数据问题。

### 4.4 `src/lib/read-mdx.ts`

| 属性 | 值 |
|------|-----|
| 类型 | Server-only |
| 职责 | 读取 `src/data/{slug}-{lang}.md`，用 gray-matter 解析 frontmatter 和 body |

```typescript
// 导出接口
export function readCaseStudyDetail(
  slug: string,
  lang: string
): { frontmatter: Frontmatter; content: string }

// Frontmatter 类型
interface Frontmatter {
  title: string
  slug: string
  cover: string
  techStack: string[]
  role: string
  date: string
}
```

### 4.5 `src/lib/animations.ts`

| 属性 | 值 |
|------|-----|
| 类型 | Shared（Client + Server 均可导入，但仅 Client 执行） |
| 职责 | 集中管理所有 Framer Motion variants，避免组件内重复定义动画参数 |

```typescript
// 导出接口
export const fadeInUp: Variants       // 通用淡入上浮
export const staggerContainer: Variants  // 父容器 stagger
export const staggerItem: Variants       // 子项 stagger
export const pageTransition: Variants    // 页面过渡
export const sectionTitle: Variants      // Section 标题入场
```

详见 `3.3 交互与动效说明.md` 附录具体实现。[[3.3 交互与动效说明#附录：animations.ts 完整代码]]

### 4.6 `src/lib/utils.ts`

| 属性 | 值 |
|------|-----|
| 类型 | Shared |
| 职责 | Tailwind class 合并工具函数（clsx + tailwind-merge） |

```typescript
export function cn(...inputs: ClassValue[]): string
```

---

## 五、数据文件结构定义

### 5.1 `src/data/profile.json`

```json
{
  "name": "KONG JUN SIENG",
  "slogan": "やればできる！",
  "skills": ["Python", "TypeScript", "React", "Next.js", "Node.js", "Git", "RESTful APIs", "Regular Expressions", "Internationalization (i18n)", "IoT", "Framer Motion"],
  "socialLinks": {
    "github": "https://github.com/KONGJUNSIENG",
    "linkedin": "https://www.linkedin.com/in/jun-sieng-kong-b1b3213b3"
  },
  "email": "js.kong@outlook.com"
}
```

### 5.2 `src/data/projects.json`

```json
[
  {
    "slug": "ccms",
    "cover": "/images/ccms-cover.png",
    "techStack": ["ESP32", "MQTT", "React"],
    "titleKey": "projects.ccms.title",
    "backgroundKey": "projects.ccms.background",
    "contributeKey": "projects.ccms.contribute",
    "outcomeKey": "projects.ccms.outcome"
  },
  {
    "slug": "portfolio",
    "cover": "/images/portfolio-cover.png",
    "techStack": ["Next.js", "TypeScript", "Tailwind CSS", "Framer Motion"],
    "titleKey": "projects.portfolio.title",
    "backgroundKey": "projects.portfolio.background",
    "contributeKey": "projects.portfolio.contribute",
    "outcomeKey": "projects.portfolio.outcome"
  }
]
```

### 5.3 `src/data/case-studies.json`

```json
[
  {
    "slug": "portfolio",
    "cover": "/images/case-study-cover.png",
    "techStack": ["Next.js", "TypeScript", "Tailwind CSS", "Framer Motion"],
    "titleKey": "caseStudies.portfolio.title",
    "descriptionKey": "caseStudies.portfolio.description"
  }
]
```

### 5.4 `src/data/portfolio-{lang}.md`

Frontmatter 字段：

| 字段 | 多语 | 示例值 |
|------|------|--------|
| `title` | 是 | "Portfolio+AI半自动化开发案例" |
| `slug` | 否 | "portfolio" |
| `cover` | 否 | "/images/portfolio-cover.png" |
| `techStack` | 否 | ["Next.js", "TypeScript", "Tailwind CSS", "Framer Motion"] |
| `role` | 是 | "全栈开发者 / 设计师" |
| `date` | 否 | "2026" |

Body 章节：项目概述 / 技术要点（随内容扩展）

### 5.5 `src/messages/{lang}.json`

JSON 结构（以 zh.json 为例）见 [[2.2 内容规划表.md#四 i18n 字段索引表]]。
包含以下顶级键：`nav`、`hero`、`caseStudies`、`projects`、`about`、`contact`、`footer`、`transitions`（VN 过渡台词）。

### 5.6 `src/data/vn-scenario.{lang}.json`

43 场景游戏风序幕剧本：

| 字段 | 类型 | 示例 |
|------|------|------|
| `scenes[]` | 数组 | 43 个场景对象 |
| `scenes[].id` | number | 1 |
| `scenes[].chapterTitle` | string (可选) | "プロローグ" |
| `scenes[].speaker` | string | "KONG JUN SIENG" |
| `scenes[].expression` | string | "normal" |
| `scenes[].text` | string | "这是我的故事…" |
| `scenes[].nextScene` | number (null) | 2 / null |

---

## 六、文件创建顺序（依赖拓扑）

```
Phase 1: 项目初始化与环境搭建
   1. package.json           ← pnpm create next-app 生成
   2. tsconfig.json          ← 同上，启用 strict
   3. next.config.ts         ← 手动配置 i18n 插件
   4. postcss.config.mjs     ← pnpm create 生成
   5. .gitignore             ← pnpm create 生成
   6. .env.example           ← 手动创建
   7. src/styles/globals.css          ← Tailwind v4 `@import "tailwindcss"` + `@theme` 设计令牌
   8. src/lib/fonts.ts                ← next/font 声明
   9. src/app/layout.tsx             ← 根布局（html + body + fonts）
   10. src/proxy.ts                  ← i18n 重定向（Next.js 16 已弃用 middleware.ts）
   11. src/i18n/request.ts            ← next-intl 请求配置
   12. .github/workflows/ci.yml       ← CI 流水线

Phase 2: 数据层与类型系统
   13. src/data/profile.json          ← 个人数据
   14. src/data/projects.json         ← 项目数据
   15. src/data/case-studies.json     ← Case Study 列表数据
   16. src/data/portfolio-zh.md       ← Case Study 内容（中文）
   17. src/data/portfolio-ja.md       ← Case Study 内容（日文）
   18. src/data/portfolio-en.md       ← Case Study 内容（英文）
   19. src/messages/zh.json           ← 中文消息
   20. src/messages/ja.json           ← 日文消息
   21. src/messages/en.json           ← 英文消息
   22. src/lib/read-data.ts           ← JSON 读取 + Zod 校验
   23. src/lib/read-mdx.ts            ← Markdown 读取 + frontmatter 解析
   24. src/lib/utils.ts               ← cn() 工具
   25. src/lib/animations.ts          ← Framer Motion variants
   26. src/styles/fonts.css           ← 中文 Noto Sans SC 手动 @font-face（13 个子集）

Phase 3: 全局布局与导航
   27. src/components/SocialLink.tsx
   28. src/components/SkillTag.tsx
   29. src/components/LanguageSwitcher.tsx
   30. src/components/Navbar.tsx
   31. src/components/Footer.tsx
   32. src/components/ScrollToAnchor.tsx
   33. src/components/LocaleContent.tsx      ← 语言切换 AnimatePresence 包装器
   34. src/components/LayoutShell.tsx        ← Nav + 页面过渡 + Footer 布局壳
   35. src/app/[lang]/layout.tsx             ← 多语布局（集成 LocaleContent + LayoutShell）

Phase 4: 首页各区块
   36. src/components/HeroSection.tsx
   37. src/components/CaseStudyCard.tsx
   38. src/components/CaseStudiesSection.tsx
   39. src/components/ProjectCard.tsx
   40. src/components/ProjectsSection.tsx
   41. src/components/AboutSection.tsx
   42. src/components/ContactSection.tsx
   43. src/app/[lang]/page.tsx

Phase 5: Case Study 详情页
   44. src/app/[lang]/projects/[slug]/page.tsx

Phase 6: 动效与交互动画系统
   （均为修改已有文件，无新增文件；useMediaQuery.ts 在 Phase 2 工具函数中已创建）

Phase 7: 游戏风 UI 系统与首页集成
   Part A — 游戏风组件（新增）
   45. src/components/DialogBox.tsx             ← 游戏风对话框组件
   46. src/components/ChapterHeader.tsx          ← 游戏风章节标题组件
   47. src/components/OptionButton.tsx           ← 游戏风选项按钮组件
   48. src/components/SectionTransition.tsx      ← 游戏风过渡台词组件
   Part B — Section 游戏风化（修改 Phase 4 已创建的文件，无新增文件）
   HeroSection(CTA→OptionButton) / CaseStudyCard / CaseStudiesSection(+ChapterHeader) /
   ProjectCard / ProjectsSection(+ChapterHeader) / AboutSection(+ChapterHeader+DialogBox) /
   ContactSection(+ChapterHeader+OptionButton) / page.tsx(+SectionTransition)

Phase 8: SEO 与部署
    49. src/app/not-found.tsx
    50. src/app/sitemap.ts
    51. src/app/robots.ts
    52. public/images/*                 ← 放置封面图、立绘
    53. public/fonts/*                  ← 放置字体文件
    54. public/resume-{lang}.pdf       ← 简历文件

Phase 10: ProjectCard 点击 clip-path 圆形展开交互
    55. src/lib/projects.schema.ts        ← 修改（追加 3 字段）
    56. src/data/projects.json            ← 修改（追加 3 字段值）
    57. src/messages/{lang}.json          ← 修改（追加 background/contribute/outcome 文本）
    58. src/components/ProjectCard.tsx     ← 修改（重写实现 clip-path 动画）
```

---

## 七、文件命名约定

| 类别 | 约定 | 示例 |
|------|------|------|
| React 组件 | PascalCase | `HeroSection.tsx`、`SkillTag.tsx` |
| 工具/库文件 | kebab-case | `read-data.ts`、`read-mdx.ts` |
| JSON 数据 | kebab-case | `profile.json`、`case-studies.json` |
| Markdown 数据 | `{slug}-{lang}.md` | `portfolio-zh.md` |
| 消息文件 | `{lang}.json` | `zh.json`、`ja.json` |
| 图片文件 | kebab-case | `hero-character.png`、`ccms-cover.png` |
| 字体文件 | PascalCase (原始名称) | `MPlusRounded1c-Regular.woff2` |
| 简历文件 | `resume-{lang}.pdf` | `resume-zh.pdf` |

---

## 八、文件引用关系图

```
src/app/layout.tsx
  ├── src/lib/fonts.ts (fontJp, fontEn)
  └── src/styles/fonts.css (Noto Sans SC @font-face + --font-zh)

src/app/[lang]/layout.tsx
  ├── src/components/LocaleContent.tsx (AnimatePresence key=locale)
  │   └── src/components/LayoutShell.tsx
  │       ├── src/lib/animations.ts (pageTransition)
  │       ├── src/components/Navbar.tsx
  │       │   ├── src/components/LanguageSwitcher.tsx
  │       │   └── src/messages/{lang}.json (nav.*)
  │       ├── src/components/Footer.tsx
  │       │   ├── src/components/SocialLink.tsx
   │   │   ├── src/data/profile.json (socialLinks — 对象格式)
  │       │   └── src/messages/{lang}.json (footer.copyright)
  │       └── src/lib/utils.ts (cn)
  └── src/i18n/routing.ts

src/app/[lang]/page.tsx
  ├── src/data/profile.json (name, slogan, skills)
  ├── src/data/projects.json
  ├── src/data/case-studies.json
  ├── src/messages/{lang}.json (hero.*, caseStudies.*, projects.*, about.*, contact.*)
  ├── src/components/HeroSection.tsx
  │   └── src/lib/animations.ts (fadeInUp)
  ├── src/components/CaseStudiesSection.tsx
  │   ├── src/lib/animations.ts (staggerContainer, staggerItem, sectionTitle)
  │   └── src/components/CaseStudyCard.tsx
  ├── src/components/ProjectsSection.tsx
  │   ├── src/lib/animations.ts (staggerContainer, staggerItem, sectionTitle)
  │   └── src/components/ProjectCard.tsx
  ├── src/components/AboutSection.tsx
  │   ├── src/lib/animations.ts (staggerContainer, staggerItem, sectionTitle)
  │   └── src/components/SkillTag.tsx
  └── src/components/ContactSection.tsx
      ├── src/lib/animations.ts (staggerContainer, staggerItem, sectionTitle)
      └── src/components/SocialLink.tsx

src/app/[lang]/projects/[slug]/page.tsx
  ├── src/lib/read-mdx.ts
  └── src/data/{slug}-{lang}.md

src/lib/read-data.ts
  ├── src/data/profile.json
  ├── src/data/projects.json
  └── src/data/case-studies.json
```
