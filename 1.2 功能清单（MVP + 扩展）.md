# 1.2 功能清单（MVP + 扩展）

**项目：** KONG JUN SIENG — Software Developer Portfolio  
**文档目标：** 定义所有功能模块、优先级及路由映射，供 AI 决定开发顺序和路由结构。

---

## 一、MVP（P0 — 核心上线必须）

| # | 功能 | 归属页面 | 路由 | 说明 | 数据源 |
|---|------|----------|------|------|--------|
| 1 | **Hero Banner** | 首页 Hero 区块 | `/[lang]` (锚点) | 姓名 + 职位标签 + Slogan + CTA | `profile.json`, `messages/{lang}.json` |
| 2 | **Case Studies 列表** | 首页 Case Studies 区块 | `/[lang]#case-studies` | 卡片式布局：封面图 + 标题 + 技术标签 +「查看详情」按钮 | `case-studies.json`, `messages/{lang}.json` |
| 3 | **Case Study 详情页** | 独立详情页 | `/[lang]/projects/[slug]` | 背景/目标/角色/过程/关键决策/结果 | `data/*-{lang}.md` |
| 4 | **项目展示卡片** | 首页 Projects 区块 | `/[lang]#projects` | 封面图 + 标题 + 技术标签，纯展示无跳转 | `projects.json`, `messages/{lang}.json` |
| 5 | **关于我** | 首页 About 区块 | `/[lang]#about` | 个人介绍（一段话）、技能标签 | `profile.json`, `messages/{lang}.json` |
| 6 | **联系区块** | 首页 Contact 区块 | `/[lang]#contact` | 联系表单（UI 展示，无后端）+ 邮箱 + 社交链接 + 简历下载按钮 | 硬编码, `profile.json`, `messages/{lang}.json` |
| 7 | **简历下载** | Contact 区块内 | — | 根据当前语言下载对应 PDF | `public/resume-{lang}.pdf` |
| 8 | **三语切换 (i18n)** | 全局导航 | `/[lang]/...` | 中文 / 日本語 / English | `messages/{lang}.json` |
| 9 | **固定导航栏** | 全局 | — | 顶部固定，锚点跳转至首页各区块 + 语言切换 | `messages/{lang}.json` |
| 10 | **页脚** | 全局 | — | 版权、社交链接 | `messages/{lang}.json`, `profile.json` |
| 11 | **响应式布局** | 全局 | — | 桌面 / 平板 / 手机全适配 | — |
| 12 | **Galgame 式动效** | 全局 | — | 淡入浮游、微妙视差、氛围背景动效（Framer Motion） | — |
| 13 | **SEO 基础** | 全局 | — | 各页面 meta / Open Graph / sitemap.xml | — |

---

## 二、初始内容清单

### Projects（首页卡片，纯展示）

| Slug | 说明 |
|------|------|
| `fyp-iot` | FYP IoT 成品 |
| `portfolio` | 本 2D Portfolio 网站本身 |

### Case Studies（含独立详情页）

| Slug | 对应 Project | 说明 |
|------|-------------|------|
| `portfolio` | portfolio | 本网站的深度开发案例，后续可扩充 |

---

## 三、数据源文件汇总

| 文件 | 说明 |
|------|------|
| `profile.json` | 个人资料（name, slogan, skills, socialLinks） |
| `projects.json` | 项目卡片数据（slug, cover, techStack, titleKey） |
| `case-studies.json` | Case Study 列表数据（slug, cover, techStack, titleKey, descriptionKey） |
| `messages/{lang}.json` | 多语文本（导航 / 所有区块文本） |
| `data/*-{lang}.md` | Case Study 详情页 Markdown |
| `public/resume-{lang}.pdf` | 各语言简历 PDF |

---

## 四、Phase 2（P1 — 上线后添加）

| # | 功能 | 说明 |
|---|------|------|
| 14 | **项目分类筛选** | 按 IoT / Web / 游戏等类型筛选，项目 ≥ 4 时启用 |
| 15 | **返回顶部按钮** | 长滚动后出现，平滑回到顶部，Framer Motion 淡入淡出 |

---

## 五、未来扩展（P2 — 按需）

| # | 功能 | 说明 |
|---|------|------|
| 16 | **深色模式** | CSS 变量 + 切换按钮 |
| 17 | **页面过渡动画** | Framer Motion AnimatePresence 路由过渡 |
| 18 | **访问统计** | Vercel Analytics 或 Plausible |
| 19 | **联系表单后端** | 接入 Formspree / Resend 实现真实发送 |
| 20 | **博客/笔记（独立路由）** | `/[lang]/blog`，初期完全不包含 |

---

## 六、路由结构（AI 直接映射）

```
/[lang]                     → 单页滚动（Hero + Case Studies + Projects + About + Contact）
/[lang]/projects/[slug]     → Case Study 详情页
```

简历通过 Contact 区块按钮直接下载 `public/resume-{lang}.pdf`，无独立路由。

---

## 七、AI 开发顺序建议

| 阶段 | 内容 |
|------|------|
| 1 | Next.js 初始化 + App Router + 目录结构 |
| 2 | 数据层：`profile.json` + `projects.json` + `case-studies.json` + `messages/*.json` |
| 3 | 全局布局：导航栏 + 页脚 + 响应式框架 |
| 4 | Hero Banner + i18n 联动 |
| 5 | Case Studies 区块 + 详情页路由和数据绑定 |
| 6 | Projects 卡片区块 |
| 7 | About + Contact（含简历下载）|
| 8 | Framer Motion 动效注入（含视差效果） |
| 9 | SEO meta + 部署到 Vercel |
