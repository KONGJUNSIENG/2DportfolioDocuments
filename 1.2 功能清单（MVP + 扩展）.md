# 1.2 功能清单（MVP + 扩展）

**项目：** KONG JUN SIENG — Software Developer Portfolio  
**文档目标：** 定义所有功能模块、优先级及路由映射，供 AI 决定开发顺序和路由结构。

---

## 一、MVP（P0 — 核心上线必须）

| # | 功能 | 归属页面 | 路由 | 说明 | 数据源 |
|---|------|----------|------|------|--------|
| 1 | **Hero Banner** | 首页 Hero 区块 | `/[lang]` (锚点) | 全屏角色插图（浮动动画）+ 视差滚动 + 光晕脉冲，显示姓名 + 职位标签 + Slogan + CTA | `profile.json`, `messages/{lang}.json`, `public/images/hero-character.png` |
| 2 | **Case Studies 列表** | 首页 Case Studies 区块 | `/[lang]#case-studies` | 卡片式布局：封面图 + 标题 + 技术标签 +「查看详情」按钮，卡片悬停摇摆动画 | `case-studies.json`, `messages/{lang}.json` |
| 3 | **Case Study 详情页** | 独立详情页 | `/[lang]/projects/[slug]` | 章节目录导航（右侧固定侧栏）+ 9 个结构化章节 + Markdown 风格渲染（heading/list/code/table/imageGrid/流程图行）+ PDF 下载 | `case-studies.json`, `messages/{lang}.json`（数据源，非 `.md`） |
| 4 | **项目展示卡片** | 首页 Projects 区块 | `/[lang]#projects` | 封面图 + 标题 + 技术标签。点击触发 clip-path 圆形扩散动画，展开详情（背景→贡献 bullet list→成果）；再次点击收拢 | `projects.json`, `messages/{lang}.json` |
| 5 | **关于我** | 首页 About 区块 | `/[lang]#about` | 个人介绍（一段话）、技能标签（圆角药丸徽章） | `profile.json`, `messages/{lang}.json` |
| 6 | **联系区块（选项按钮式快捷入口）** | 首页 Contact 区块 | `/[lang]#contact` | 4 个可点击选项按钮：邮箱（mailto）、GitHub、LinkedIn、简历下载。无表单 | `messages/{lang}.json`, `profile.json`, `public/option-button.png` |
| 7 | **简历下载** | Contact 区块内 | — | 根据当前语言下载对应 PDF，点击后显示勾选动画（1.5s 恢复） | `public/resume-{lang}.pdf` |
| 8 | **三语切换 (i18n)** | 全局导航 | `/[lang]/...` | 中文 / 日本語 / English，切换时淡入淡出过渡 | `messages/{lang}.json` |
| 9 | **固定导航栏** | 全局 | — | 顶部固定，滚动渐显毛玻璃背景（backdrop-blur）。4 个锚点链接 + 语言切换器 + 移动端折叠菜单 | `messages/{lang}.json` |
| 10 | **页脚** | 全局 | — | 版权信息 + 语言切换器 | `messages/{lang}.json` |
| 11 | **响应式布局** | 全局 | — | 桌面 / 平板 / 手机全适配 | — |
| 12 | **Galgame 式动效** | 全局 | — | 淡入浮游、微妙视差、氛围背景动效（Framer Motion），交错入场 / `staggerContainer` | `src/lib/animations.ts` |
| 13 | **章节标题系统** | 全局 | — | 每个 Section 顶部显示「CHAPTER N — 标题」装饰性章节标识，蓝色下划线 | — |
| 14 | **蓝色对话框系统** | 全局 | — | About 等关键内容用蓝色对话框承载（角色名标签 + 蓝色区域文本），模拟游戏角色对话 | `public/images/*.png` |
| 15 | **选项按钮风格 CTA** | 全局 | — | CTA / 查看详情 / 简历下载按钮使用平铺 `option-button.png` 素材 + ▶ 箭头，模拟游戏选择肢 | `public/option-button.png` |
| 16 | **Section 过渡台词** | 首页 | — | 章节之间出现一句带 ~ 装饰的过渡台词（如 `(∠・ω< )⌒★`），淡入展示 | `messages/{lang}.json`（transitions 命名空间） |
| 17 | **页面过渡动画** | 全局 | — | Framer Motion AnimatePresence 路由过渡（淡入/淡出），由 `LayoutShell` 管理 | — |
| 18 | **返回顶部按钮** | 首页 / Case Study 详情页 | — | 滚动 >250px 后出现固定按钮，▲ 图标，Framer Motion 淡入淡出 | — |
| 19 | **SEO 基础** | 全局 | — | 各页面 meta / Open Graph / sitemap.xml / robots.txt | — |
| 20 | **游戏风序幕 TitleScreen** | Intro | `/[lang]/intro` | 游戏标题画面：角色立绘 + `ScrambleText` 混淆动画标题 +「START」按钮（启动剧情）+「Settings」按钮（语言切换弹窗）+「Skip」按钮（2.5s 后出现，跳过序幕） | `vn-scenario.{lang}.json` |
| 21 | **游戏风序幕 开幕动画** | Intro | `/[lang]/intro` | 3 阶段动画序列：LoadingRipple（波纹 1.3s）→ 白屏（0.5s）→ PixelsCurtain（8×8 帷幕 0.6s），完成后自动显示 TitleScreen | — |
| 22 | **游戏风序幕 Scenario 演绎** | Intro | `/[lang]/intro` | 43 场景 VN 引擎：角色立绘 8 种表情切换 + 蓝色对话框逐句推进 + 选择分支（6 种结局）+ 鼠标排斥交互 + 自动推进 + 打字机效果。底部半透明对话覆盖层，选择时暗色背景 | `vn-scenario.{lang}.json` |
| 23 | **游戏风序幕 LoadingOverlay** | Intro | `/[lang]/intro` | 深色帷幕从中间劈开向两侧滑开（1.4s 动画），完成后自动导航至首页 | — |
| 24 | **场景/角色资源预加载** | Intro | `/[lang]/intro` | `IntroShell` 在开幕动画期间预加载所有 VN 角色图片+主页图片，确保无缝衔接 | `public/images/*.png` |
| 25 | **Case Study 流程图渲染** | Case Study 详情页 | `/[lang]/projects/[slug]` | 含 3+ 箭头（→）的段落自动渲染为 `FlowStepBar` 垂直流程图组件（蓝点 + 连接线 + 文档编号标签） | `messages/{lang}.json` |
| 26 | **日语语言提示** | 全局 | — | 切换日语时弹出一次性 toast 提示「日本語は勉強中です…」 | `messages/ja.json` |
| 27 | **ZoomableImage 图片放大** | Case Study 详情页 | `/[lang]/projects/[slug]` | 详情页内含图片可点击全屏放大（黑色遮罩 + 弹簧缩放动画） | — |
| 28 | **ScrambleText 混淆文字** | Intro / 全局 | — | 文字混淆动画：`/intro` 标题每 2.5s 自动触发吸引模式，悬停触发，可点击 | — |

---

## 二、初始内容清单

### Projects（首页卡片，纯展示）

| Slug | 说明 |
|------|------|
| `ccms` | CCMS 成品 |
| `portfolio` | 本 2D Portfolio 网站本身 |

### Case Studies（含独立详情页）

| Slug | 对应 Project | 说明 |
|------|-------------|------|
| `casestudy` | portfolio | 本网站的深度开发案例（文档驱动 + AI Agent 开发方法论实证） |

> 注意：代码库中当前仅部署 1 个案例研究，slug 为 `casestudy`。未来可扩充更多条目到 `case-studies.json` 及对应 `messages/{lang}.json`。

---

## 三、数据源文件汇总

| 文件 | 说明 |
|------|------|
| `profile.json` | 个人资料（name, slogan, skills, socialLinks, email） |
| `projects.json` | 项目卡片数据（slug, cover, techStack, titleKey, backgroundKey, contributeKey, outcomeKey） |
| `case-studies.json` | Case Study 列表数据（slug, cover, techStack, titleKey, descriptionKey） |
| `messages/{lang}.json` | **主内容源**：导航 / 首页各区块文本 / Case Study 详情页全部章节内容（通过 `t.raw()` 渲染 Markdown 样式文本） |
| `vn-scenario.{lang}.json` | 游戏风序幕场景数据（43 场景剧本，含台词/表情/分支/结局） |
| `public/resume-{lang}.pdf` | 各语言简历 PDF |
| `public/casestudy-{lang}.pdf` | 各语言 Case Study PDF 下载版 |
| `public/images/` | 角色表情（8 种）、首页角色立绘、封面图、VN 场景图、UI 素材、案例研究截图 |
| `src/lib/animations.ts` | Framer Motion 动效变量（staggerContainer, pageTransition 等） |

---

## 四、未来扩展（P2 — 按需）

| # | 功能 | 说明 |
|---|------|------|
| 29 | **项目分类筛选** | 按 IoT / Web / 游戏等类型筛选，项目 ≥ 4 时启用 |
| 30 | **联系表单后端** | 接入 Formspree / Resend 实现真实发送 |
| 31 | **深色模式** | CSS 变量 + 切换按钮 |
| 32 | **访问统计** | Vercel Analytics 或 Plausible |
| 33 | **博客/笔记（独立路由）** | `/[lang]/blog`，初期完全不包含 |
| 34 | **更多案例研究** | 扩充 `case-studies.json` 和 messages，添加新的独立详情页 |

---

## 五、路由结构（AI 直接映射）

```
/[lang]/intro              → 游戏风序幕（开幕动画 → TitleScreen → Scenario 演绎 → LoadingOverlay → 首页）
/[lang]                    → 单页滚动（Hero + Case Studies + Projects + About + Contact）
/[lang]/projects/[slug]    → Case Study 详情页（完整结构化章节）
```

- `/` 根路由 → proxy.ts 自动重定向到 `/en/intro`
- 简历 / Case Study PDF 通过按钮直接下载，无独立路由

```