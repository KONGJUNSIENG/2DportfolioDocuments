# 7.1 内容更新指南

**项目：** KONG JUN SIENG — Software Developer Portfolio  
**文档目标：** 定义数据文件修改流程与对应关系，指导用户自主更新网站所有内容，无需修改代码。

---

## 一、内容架构概览

### 1.1 数据流关系

```
你编辑这些文件 → 推送 Git → Vercel 自动部署 → 网站更新

src/data/
├── profile.json          ← 姓名 / Slogan / 技能 / 社交链接 / 邮箱
├── projects.json         ← 项目卡片列表（封面/技术栈/标题 key）
└── case-studies.json     ← Case Study 卡片列表（封面/技术栈/标题 key/描述 key）

src/data/*-{lang}.md      ← Case Study 详情正文（三语各自一份）

src/messages/
├── zh.json               ← 所有中文文本（导航/Hero/区块标题/按钮……）
├── ja.json               ← 日文文本
└── en.json               ← 英文文本

public/
├── images/*              ← 封面图、立绘（PNG）
├── fonts/*               ← 字体文件（woff2，通常不需修改）
└── resume-{lang}.pdf     ← 简历 PDF
```

### 1.2 一句话原则

| 你要改什么 | 改哪个文件 |
|-----------|-----------|
| 姓名 / Slogan / 技能标签 / 社交链接 / 邮箱 | `src/data/profile.json` |
| 页面上的所有文字（导航/Hero/按钮/介绍……） | `src/messages/{lang}.json` |
| 项目卡片的新增/删除/修改 | `projects.json` + `messages/{lang}.json` |
| Case Study 卡片 + 详情页内容 | `case-studies.json` + `messages/{lang}.json` + `data/{slug}-{lang}.md` |
| 封面图/立绘 | 替换 `public/images/` + 更新对应 JSON |
| 简历 PDF | 替换 `public/resume-{lang}.pdf` |

---

## 二、更新场景速查表

| # | 场景 | 涉及文件 | 步骤数 | 难度 |
|---|------|---------|--------|------|
| 1 | 修改个人资料（姓名/技能/链接） | 1 个 JSON | 2 | ⭐ |
| 2 | 修改页面文本（导航/标题/bio） | 3 个 JSON | 4 | ⭐ |
| 3 | 新增项目卡片 | 2 类文件（JSON + messages) | 5 | ⭐⭐ |
| 4 | 删除项目卡片 | 2 类文件（JSON + messages) | 4 | ⭐⭐ |
| 5 | 新增 Case Study（卡片 + 详情页） | 3 类文件 + 3 个 Markdown | 6 | ⭐⭐⭐ |
| 6 | 修改 Case Study 详情正文 | 3 个 Markdown | 4 | ⭐⭐ |
| 7 | 替换封面图/立绘 | 1 个图片文件 + 1 个 JSON | 3 | ⭐ |
| 8 | 更新简历 PDF | 替换 1 个文件 | 2 | ⭐ |

---

## 三、详细操作步骤

### 场景 1：修改个人资料

**修改内容**：姓名、Slogan、技能标签、社交链接、邮箱

**步骤：**

| # | 操作 | 说明 |
|---|------|------|
| 1 | 编辑 `src/data/profile.json` | 直接修改对应字段的值 |
| 2 | 推送至 GitHub `main` 分支 | Vercel 自动部署生效 |

**示例 — 修改技能标签：**

```jsonc
// src/data/profile.json — 修改前
{
  "skills": ["Figma", "React", "IoT", "TypeScript", "Python", "日语 N4"]
}

// 修改后（新增 Vue、移除日语 N4）
{
  "skills": ["Figma", "React", "Vue", "IoT", "TypeScript", "Python"]
}
```

**示例 — 修改社交链接：**

```jsonc
// src/data/profile.json
{
  "socialLinks": [
    { "platform": "GitHub", "url": "https://github.com/KONGJUNSIENG" },
    { "platform": "LinkedIn", "url": "https://linkedin.com/in/你的ID" }
  ]
}
```

> 技能标签会自动渲染为 About 区块中的 pill 形标签。增删后无需修改任何代码。

---

### 场景 2：修改页面文本

**修改内容**：导航栏文字、Hero 标题/CTA、Section 标题、bio、联系表单标签、页脚版权

**步骤：**

| # | 操作 | 说明 |
|---|------|------|
| 1 | 编辑 `src/messages/zh.json` | 修改中文文本 |
| 2 | 编辑 `src/messages/ja.json` | 同步修改日文文本 |
| 3 | 编辑 `src/messages/en.json` | 同步修改英文文本 |
| 4 | 推送至 GitHub `main` | 自动部署生效 |

**示例 — 修改 Hero CTA 文本（三语同步）：**

```jsonc
// src/messages/zh.json
{
  "hero": {
    "title": "Software Developer / 全栈探索者",
    "ctaText": "查看作品"
  }
}
// 改为：
{
  "hero": {
    "title": "Software Developer / 全栈探索者",
    "ctaText": "浏览我的项目"
  }
}
```

```jsonc
// src/messages/ja.json — 对应修改
{
  "hero": {
    "title": "Software Developer / 全栈エクスプローラー",
    "ctaText": "作品を見る"
  }
}
```

```jsonc
// src/messages/en.json — 对应修改
{
  "hero": {
    "title": "Software Developer / Full-stack Explorer",
    "ctaText": "View My Work"
  }
}
```

**JSON 结构速查：**

```
nav
├── caseStudies    ← 导航"案例"
├── projects       ← 导航"作品"
├── about          ← 导航"关于"
└── contact        ← 导航"联系"

hero
├── title          ← Hero 职位标签
└── ctaText        ← CTA 按钮文字

caseStudies
├── sectionTitle   ← Section 标题
├── portfolio
│   ├── title      ← 案例卡片标题
│   └── description ← 案例卡片描述
└── viewButton     ← "查看详情"按钮

projects
├── sectionTitle   ← Section 标题
├── fypIot
│   └── title      ← FYP IoT 项目标题
└── portfolio
    └── title      ← Portfolio 项目标题

about
└── bio            ← 个人介绍正文

contact
├── sectionTitle   ← Section 标题
├── resumeBtnText  ← 简历下载按钮
├── formName       ← 表单"姓名"
├── formEmail      ← 表单"邮箱"
├── formMessage    ← 表单"留言"
└── formSubmit     ← 表单"发送"

footer
└── copyright      ← 版权信息
```

---

### 场景 3：新增项目卡片

**修改内容**：在首页 Projects 区块新增一个项目卡片（纯展示，无详情页）

**步骤：**

| # | 操作 | 说明 |
|---|------|------|
| 1 | 准备封面图 → 放入 `public/images/{slug}-cover.png` | 4:3 比例，推荐 800×600px |
| 2 | 编辑 `src/data/projects.json` | 在 `projects` 数组中追加新条目 |
| 3 | 编辑 `src/messages/zh.json` | 在 `projects` 下添加对应的 `titleKey` |
| 4 | 编辑 `src/messages/ja.json` | 同步骤 3 |
| 5 | 编辑 `src/messages/en.json` | 同步骤 3 |
| 6 | 推送至 GitHub `main` | 自动部署生效 |

**示例 — 新增 "Smart Home Dashboard" 项目：**

```jsonc
// src/data/projects.json — 在 projects 数组中追加
{
  "projects": [
    // ... 现有条目保持不变
    {
      "slug": "smart-home",
      "cover": "/images/smart-home-cover.png",
      "techStack": ["React", "Node.js", "WebSocket"],
      "titleKey": "projects.smartHome.title"
    }
  ]
}
```

```jsonc
// src/messages/zh.json — 在 projects 下追加
{
  "projects": {
    "sectionTitle": "作品",
    "fypIot":   { "title": "FYP IoT 项目" },
    "portfolio": { "title": "2D Portfolio" },
    "smartHome": { "title": "Smart Home 面板" }
  }
}
```

---

### 场景 4：删除项目卡片

**步骤：**

| # | 操作 | 说明 |
|---|------|------|
| 1 | 编辑 `src/data/projects.json` | 从 `projects` 数组中移除对应条目 |
| 2 | 编辑 `src/messages/zh.json` | 移除对应的 `titleKey` 字段 |
| 3 | 编辑 `src/messages/ja.json` | 同步骤 2 |
| 4 | 编辑 `src/messages/en.json` | 同步骤 2 |

> 封面图文件保留在 `public/images/` 中无需删除（不影响运行），但建议清理以避免混淆。

---

### 场景 5：新增 Case Study（卡片 + 详情页）

**修改内容**：在首页 Case Studies 区块新增一张案例卡片，同时在 `/[lang]/projects/[slug]` 新增独立详情页

**步骤：**

| # | 操作 | 说明 |
|---|------|------|
| 1 | 准备封面图 → 放入 `public/images/{slug}-cover.png` | 16:9 比例，推荐 1200×675px |
| 2 | 编辑 `src/data/case-studies.json` | 在 `caseStudies` 数组中追加新条目 |
| 3 | 编辑 `src/messages/zh.json` | 在 `caseStudies` 下添加 `titleKey` 和 `descriptionKey` |
| 4 | 编辑 `src/messages/ja.json` | 同步骤 3 |
| 5 | 编辑 `src/messages/en.json` | 同步骤 3 |
| 6 | 创建三语 Markdown 正文 | 在 `src/data/` 下创建 `{slug}-zh.md` / `{slug}-ja.md` / `{slug}-en.md` |
| 7 | 推送至 GitHub `main` | 自动部署生效 |

**示例 — 新增 "FYP IoT" Case Study：**

```jsonc
// src/data/case-studies.json
{
  "caseStudies": [
    { "slug": "portfolio", /* 现有条目 */ },
    {
      "slug": "fyp-iot",
      "cover": "/images/fyp-iot-cover.png",
      "techStack": ["ESP32", "MQTT", "React"],
      "titleKey": "caseStudies.fypIot.title",
      "descriptionKey": "caseStudies.fypIot.description"
    }
  ]
}
```

```jsonc
// src/messages/zh.json — 在 caseStudies 下追加
{
  "caseStudies": {
    "sectionTitle": "案例研究",
    "portfolio": {
      "title": "2D Portfolio 开发案例",
      "description": "一段简短的描述……"
    },
    "fypIot": {
      "title": "FYP IoT 智能监控系统",
      "description": "基于 ESP32 + MQTT 的物联网监控项目"
    },
    "viewButton": "查看详情"
  }
}
```

**Markdown 正文模板（`src/data/fyp-iot-zh.md`）：**

```markdown
---
title: "FYP IoT 智能监控系统"
slug: "fyp-iot"
cover: "/images/fyp-iot-cover.png"
techStack: ["ESP32", "MQTT", "React"]
role: "全栈开发者"
date: "2026"
---

## 背景

一段话描述项目背景。

## 目标

一段话描述项目目标。

## 我的角色

一段话描述承担的角色与职责。

## 过程

一段话描述开发/设计过程。

## 关键决策

一段话描述关键决策与原因。

## 结果

一段话描述项目成果、数据与反馈。
```

> 三语各一份：`fyp-iot-zh.md` / `fyp-iot-ja.md` / `fyp-iot-en.md`。slug 字段保持一致。

---

### 场景 6：修改 Case Study 详情正文

**步骤：**

| # | 操作 | 说明 |
|---|------|------|
| 1 | 编辑 `src/data/{slug}-zh.md` | 修改中文版正文 |
| 2 | 编辑 `src/data/{slug}-ja.md` | 同步修改日文版 |
| 3 | 编辑 `src/data/{slug}-en.md` | 同步修改英文版 |
| 4 | 推送至 GitHub `main` | 自动部署生效 |

> **不修改 frontmatter**：如需修改标题/封面/技术栈/角色/日期，需同时更新 `case-studies.json` 和 `messages/{lang}.json` 中的对应字段。

---

### 场景 7：替换封面图/立绘

**步骤：**

| # | 操作 | 说明 |
|---|------|------|
| 1 | 准备新图片 → PNG 格式，≤2MB，kebab-case 命名 | 覆盖 `public/images/` 下同名文件 |
| 2 | 如文件名变化，更新对应 `.json` 中的 `cover` 路径 | `projects.json` / `case-studies.json` |
| 3 | 推送至 GitHub `main` | 自动部署生效 |

**图片替换说明：**

| 图片文件 | 使用位置 | 建议尺寸 |
|---------|---------|---------|
| `hero-character.png` | Hero 立绘（透明背景 PNG） | 宽度 800-1200px |
| `portfolio-cover.png` | Portfolio Case Study 卡片 + Project 卡片共用 | 1200×675px (16:9) |
| `fyp-iot-cover.png` | FYP IoT Project 卡片 | 800×600px (4:3) |

> 无需修改代码，`next/image` 自动从 `public/` 读取并优化输出 AVIF/WebP。

---

### 场景 8：更新简历 PDF

**步骤：**

| # | 操作 | 说明 |
|---|------|------|
| 1 | 准备新 PDF，按语言命名 | `resume-zh.pdf` / `resume-ja.pdf` / `resume-en.pdf` |
| 2 | 覆盖 `public/` 下同名文件 | 推送后自动生效 |

> 网站 Contact 区块的简历按钮根据当前语言自动拼接路径 `<a href="/resume-{lang}.pdf" download>`，文件名变化需同步修改代码中的拼接逻辑。

---

## 四、新增语言指南

如需新增第四种语言（如韩语 `ko`）：

| # | 操作 | 涉及文件 |
|---|------|---------|
| 1 | 更新 `src/middleware.ts` | 在 `locales` 数组追加 `"ko"` |
| 2 | 创建 `src/messages/ko.json` | 复制 `zh.json` 结构，翻译所有值 |
| 3 | 更新 `src/i18n/request.ts` | 添加 `ko: () => import("./messages/ko.json")` |
| 4 | 为每个 Case Study 创建 `data/{slug}-ko.md` | 复制 `{slug}-zh.md`，翻译正文 |
| 5 | 创建 `public/resume-ko.pdf` | 韩语简历 PDF |
| 6 | 推送至 GitHub `main` | 自动部署生效，新语言路由 `/ko/...` 立即可访问 |

---

## 五、图片管理规范

### 5.1 命名规则

```
{block}-{用途}.png
```

| 正确 | 错误 |
|------|------|
| `hero-character.png` | `heroCharacter.png` |
| `fyp-iot-cover.png` | `fyp_iot_cover.png` |
| `smart-home-cover.png` | `Smart Home Cover.png` |

### 5.2 文件约束

| 约束项 | 规格 |
|--------|------|
| 格式 | PNG（立绘需透明背景）/ JPEG（封面图） |
| 单文件上限 | ≤2MB |
| 总图片上限 | ≤20 张 |
| 生产输出 | Next.js 自动转为 AVIF + WebP |

### 5.3 多项目共用图片

`portfolio-cover.png` 同时被 Case Study 卡片和 Project 卡片引用。替换该文件会影响两处展示。

---

## 六、内容修改验证流程

每次内容修改后，建议按以下步骤验证：

```
编辑文件 → 本地预览 → 确认无误 → 推送 → 确认部署成功 → 线上验证
```

### 6.1 本地预览

```bash
pnpm run dev
# 访问 http://localhost:3000 查看修改效果
```

### 6.2 构建验证

```bash
pnpm run build
# 确保零报错，Zod 数据校验会在 build 时检查 JSON 完整性
```

### 6.3 内容修改后检查清单

- [ ] `pnpm run build` 通过（Zod 校验数据完整性）
- [ ] 新页面/新路由可访问（新增 Case Study 时）
- [ ] 三语言同步更新（messages JSON + Markdown）
- [ ] 图片路径正确，封面正常加载
- [ ] 链接/按钮点击正常
- [ ] 简历文件可下载

---

## 七、常见错误与避免

| 错误 | 后果 | 避免方法 |
|------|------|---------|
| 只改中文 messages，忘记改日/英文 | 切换语言后文字缺失或显示旧内容 | 三语同步修改 |
| JSON 末尾多了一个逗号 | Build 报错 | 使用 VSCode 等编辑器自动校验 JSON 语法 |
| 新增 Case Study 忘记创建 Markdown | 详情页 404 | 步骤 6 不要跳过 |
| 封面图文件名与 JSON 中 cover 路径不一致 | 图片 404 不显示 | 复制路径时检查拼写 |
| `slug` 字段在 JSON 和 Markdown frontmatter 中不一致 | 详情页 404 或匹配错误 | 保持 slug 完全一致 |
| 修改 `profile.json` 时删除 `email` 字段 | Zod 校验失败，build 报错 | 使用 Zod schema 结构修改，不删必要字段 |
| images 文件 ≥2MB | 增加加载时间 | 先用工具压缩再放入 |

---

## 八、数据文件字段速查

### profile.json

```jsonc
{
  "name": "KONG JUN SIENG",         // 文本
  "slogan": "やればできる！",         // 文本
  "skills": ["Figma", "React"],      // 字符串数组
  "socialLinks": [                   // 对象数组
    { "platform": "GitHub", "url": "..." },
    { "platform": "LinkedIn", "url": "..." }
  ],
  "email": "js.kong@outlook.com"    // 邮箱格式
}
```

### projects.json

```jsonc
{
  "projects": [
    {
      "slug": "fyp-iot",                    // 文本，URL 用
      "cover": "/images/fyp-iot-cover.png", // 图片路径
      "techStack": ["ESP32", "MQTT"],       // 字符串数组
      "titleKey": "projects.fypIot.title"   // → messages/{lang}.json
    }
  ]
}
```

### case-studies.json

```jsonc
{
  "caseStudies": [
    {
      "slug": "portfolio",                          // 文本，URL 用
      "cover": "/images/portfolio-cover.png",       // 图片路径
      "techStack": ["Next.js", "Framer Motion"],    // 字符串数组
      "titleKey": "caseStudies.portfolio.title",    // → messages/{lang}.json
      "descriptionKey": "caseStudies.portfolio.description" // → messages/{lang}.json
    }
  ]
}
```

### Markdown frontmatter

```markdown
---
title: "2D Portfolio 开发案例"   // 多语
slug: "portfolio"                // 固定，与 URL 匹配
cover: "/images/portfolio-cover.png"
techStack: ["Next.js", "Framer Motion"]
role: "全栈开发者 / 设计师"       // 多语
date: "2026"                     // 固定
---
```

---

## 九、更新流程总结

```
┌──────────────────────────────────────────────────┐
│              你准备要更新内容                       │
└────────────────┬─────────────────────────────────┘
                 ▼
┌──────────────────────────────────────────────────┐
│  文字类？  ──→ 修改 src/messages/{lang}.json       │
│  个人资料？──→ 修改 src/data/profile.json           │
│  项目卡片？──→ 修改 src/data/projects.json          │
│  Case Study？→ 修改 case-studies.json + Markdown    │
│  图片类？  ──→ 替换 public/images/ + 更新 JSON     │
│  简历？    ──→ 替换 public/resume-{lang}.pdf       │
└────────────────┬─────────────────────────────────┘
                 ▼
┌──────────────────────────────────────────────────┐
│  三语同步：zh.json / ja.json / en.json 都要更新    │
└────────────────┬─────────────────────────────────┘
                 ▼
┌──────────────────────────────────────────────────┐
│  本地验证：pnpm run build                         │
└────────────────┬─────────────────────────────────┘
                 ▼
┌──────────────────────────────────────────────────┐
│  推送至 GitHub main → Vercel 自动部署 → 上线      │
└──────────────────────────────────────────────────┘
```

---

## 十、与现有文档引用关系

| 节 | 来源文档 |
|----|---------|
| 数据流关系 | 4.1 §4.1 |
| 基本更新条目 | 完整技术规格书 §十 |
| 图片规范 | 4.3 §二、§五、§十 |
| 字体规范 | 4.3 §六 |
| JSON 结构 | 2.2 内容规划表 |
| Markdown 模板 | 2.2 §2.6 |
| i18n 配置 | 4.1 §7.4 |
