# 7.1 内容更新指南

**项目：** KONG JUN SIENG — Software Developer Portfolio  
**文档目标：** 定义数据文件修改流程与对应关系，指导用户自主更新网站所有内容，无需修改代码。

---

## 一、内容架构概览

### 1.1 数据流关系

```
你编辑这些文件 → 推送 Git → Vercel 自动部署 → 网站更新

src/data/
├── profile.json          ← 姓名 / Slogan / 技能 / 社交链接 / 邮箱
├── projects.json         ← 项目卡片列表（封面/技术栈/标题 key）
├── vn-scenario.{lang}.json   ← 游戏风序幕场景数据（zh/ja/en 三语各自一份）
└── case-studies.json     ← Case Study 卡片列表（封面/技术栈/标题 key/描述 key）

(Case Study 详情正文已迁移至 `src/messages/{lang}.json` 结构化字段，不再使用独立 MD 文件)

src/messages/
├── zh.json               ← 所有中文文本（导航/Hero/区块标题/按钮……）
├── ja.json               ← 日文文本
└── en.json               ← 英文文本

public/
├── images/*              ← 封面图、立绘、截图（PNG / JPG）
├── fonts/*               ← 字体文件（woff2，通常不需修改）
└── resume-{lang}.pdf     ← 简历 PDF
```

### 1.2 一句话原则

| 你要改什么 | 改哪个文件 |
|-----------|-----------|
| 姓名 / Slogan / 技能标签 / 社交链接 / 邮箱 | `src/data/profile.json` |
| 页面上的所有文字（导航/Hero/按钮/介绍……） | `src/messages/{lang}.json` |
| 项目卡片的新增/删除/修改 | `projects.json` + `messages/{lang}.json` |
| Case Study 卡片 + 详情页内容 | `case-studies.json` + `messages/{lang}.json`（正文 10 节结构化字段含嵌套子节，直存于 messages JSON） |
| 封面图/立绘 | 替换 `public/images/` + 更新对应 JSON |
| 简历 PDF | 替换 `public/resume-{lang}.pdf` |
| 游戏风序幕场景剧本（台词/表情/章节） | `src/data/vn-scenario.{lang}.json` |

---

## 二、更新场景速查表

| # | 场景 | 涉及文件 | 步骤数 | 难度 |
|---|------|---------|--------|------|
| 1 | 修改个人资料（姓名/技能/链接） | 1 个 JSON | 2 | ⭐ |
| 2 | 修改页面文本（导航/标题/bio） | 3 个 JSON | 4 | ⭐ |
| 3 | 新增项目卡片 | 2 类文件（JSON + messages) | 5 | ⭐⭐ |
| 4 | 删除项目卡片 | 2 类文件（JSON + messages) | 4 | ⭐⭐ |
| 5 | 新增 Case Study（卡片 + 详情页） | 2 类文件（case-studies.json + messages JSON） | 6 | ⭐⭐⭐ |
| 6 | 修改 Case Study 详情正文 | 3 个 messages JSON | 4 | ⭐⭐⭐ |
| 7 | 替换封面图/立绘 | 1 个图片文件 + 1 个 JSON | 3 | ⭐ |
| 8 | 更新简历 PDF | 替换 1 个文件 | 2 | ⭐ |
| 9 | 修改游戏风序幕场景剧本 | 3 个 JSON | 4 | ⭐⭐ |

---

## 三、详细操作步骤

### 场景 1：修改个人资料

**修改内容**：姓名、Slogan、技能标签、社交链接、邮箱

**步骤：**

| # | 操作 | 说明 |
|---|------|------|
| 1 | 编辑 `src/data/profile.json` | 直接修改对应字段的值 |
| 2 | 推送至 GitHub `main` 分支 | Vercel 自动部署生效 |

**示例 — 修改技能标签：**

```jsonc
// src/data/profile.json — 修改前
{
  "skills": ["Figma", "React", "IoT", "TypeScript", "Python", "日语 N4"]
}

// 修改后（新增 Git、RESTful APIs、i18n 等游戏行业关键词，移除 Figma、日语 N4）
{
  "skills": ["Python", "TypeScript", "React", "Next.js", "Node.js", "Git", "RESTful APIs", "Regular Expressions", "Internationalization (i18n)", "IoT", "Framer Motion"]
}
```

**示例 — 修改社交链接：**

```jsonc
// src/data/profile.json
{
  "socialLinks": {
    "github": "https://github.com/KONGJUNSIENG",
    "linkedin": "https://www.linkedin.com/in/jun-sieng-kong-b1b3213b3"
  }
}
```

> 技能标签会自动渲染为 About 区块中的 pill 形标签。增删后无需修改任何代码。

---

### 场景 2：修改页面文本

**修改内容**：导航栏文字、Hero 标题/CTA、Section 标题、bio、联系区块、页脚版权

**步骤：**

| # | 操作 | 说明 |
|---|------|------|
| 1 | 编辑 `src/messages/zh.json` | 修改中文文本 |
| 2 | 编辑 `src/messages/ja.json` | 同步修改日文文本 |
| 3 | 编辑 `src/messages/en.json` | 同步修改英文文本 |
| 4 | 推送至 GitHub `main` | 自动部署生效 |

**示例 — 修改 Hero CTA 文本（三语同步）：**

```jsonc
// src/messages/zh.json
{
  "hero": {
    "title": "Software Developer / 全栈探索者",
    "ctaText": "查看作品"
  }
}
// 改为：
{
  "hero": {
    "title": "Software Developer / 全栈探索者",
    "ctaText": "浏览我的项目"
  }
}
```

```jsonc
// src/messages/ja.json — 对应修改
{
  "hero": {
    "title": "Software Developer / 全栈エクスプローラー",
    "ctaText": "作品を見る"
  }
}
```

```jsonc
// src/messages/en.json — 对应修改
{
  "hero": {
    "title": "Software Developer / Full-stack Explorer",
    "ctaText": "View My Work"
  }
}
```

**JSON 结构速查：**

```
nav
├── caseStudies    ← 导航"案例"
├── projects       ← 导航"作品"
├── about          ← 导航"关于"
└── contact        ← 导航"联系"

hero
├── title          ← Hero 职位标签
└── ctaText        ← CTA 按钮文字

caseStudies
├── sectionTitle              ← Section 标题
├── executiveSummaryLabel     ← "项目概述"标签
├── backgroundLabel           ← "背景与动机"标签
├── methodologyLabel          ← "方法论与架构"标签
├── developmentProcessLabel   ← "实施过程"标签
├── challengesLabel           ← "关键挑战与解决方案"标签
├── resultsLabel              ← "成果与展示"标签
├── discussionLabel           ← "讨论"标签
├── reflectionsLabel          ← "反思与最佳实践"标签
├── conclusionLabel           ← "结论与展望"标签
├── appendixLabel             ← "附录"标签
├── downloadPdf               ← "下载 PDF"按钮文字
├── backToHome                ← "返回顶部"按钮文字
├── casestudy
│   ├── title                 ← 案例卡片标题
│   ├── description           ← 案例卡片描述
│   ├── executiveSummary      ← 项目概述正文（含 Markdown 图片引用）
│   ├── background            ← 背景与动机正文
│   ├── methodology           ← 方法论（4 子节）
│   │   ├── documentFirst
│   │   ├── agentConfig
│   │   ├── architecture
│   │   └── deployment
│   ├── developmentProcess    ← 实施过程（intro + 7 阶段 + metricsNote）
│   ├── challenges            ← 关键挑战（challenge1–7）
│   ├── results               ← 成果（overview / features / quality / links）
│   ├── discussion            ← 讨论（7 子节）
│   ├── reflections           ← 反思（3 子节）
│   ├── conclusion            ← 结论（3 子节）
│   └── appendix              ← 附录（6 子节）
└── viewButton                ← "查看详情"按钮

projects
├── sectionTitle   ← Section 标题
├── backgroundLabel  ← "背景"标签
├── contributeLabel ← "贡献"标签
├── outcomeLabel    ← "成果"标签
├── ccms
│   ├── title      ← CCMS 项目标题
│   ├── background ← 项目背景描述
│   ├── contribute ← 我的贡献（bullet point 式）
│   └── outcome    ← 项目成果描述
└── portfolio
    ├── title      ← Portfolio 项目标题
    ├── background ← 项目背景描述
    ├── contribute ← 我的贡献（bullet point 式）
    └── outcome    ← 项目成果描述

about
└── bio            ← 个人介绍正文

contact
├── sectionTitle   ← Section 标题
└── resumeBtnText  ← 简历下载按钮

footer
└── copyright      ← 版权信息
```

---

### 场景 3：新增项目卡片

**修改内容**：在首页 Projects 区块新增一个项目卡片（纯展示，无详情页）

**步骤：**

| # | 操作 | 说明                             |
|---|------|------|
| 1 | 准备封面图 → 放入 `public/images/{slug}-cover.png` | 4:3 比例，推荐 800×600px            |
| 2 | 编辑 `src/data/projects.json` | 在 `projects` 数组中追加新条目          |
| 3 | 编辑 `src/messages/zh.json` | 在 `projects` 下添加对应的 `titleKey` |
| 4 | 编辑 `src/messages/ja.json` | 同步骤 3                          |
| 5 | 编辑 `src/messages/en.json` | 同步骤 3                          |
| 6 | 更新预载数组（如新增封面图需在游戏风序幕提前加载） | [[#5.3 图片预载机制（Preload）]]       |
| 7 | 推送至 GitHub `main` | 自动部署生效                         |

**示例 — 新增 "Smart Home Dashboard" 项目：**

```jsonc
// src/data/projects.json — 在数组末尾追加
[
  // ... 现有条目保持不变
  {
    "slug": "smart-home",
    "cover": "/images/smart-home-cover.png",
    "techStack": ["React", "Node.js", "WebSocket"],
    "titleKey": "projects.smartHome.title",
    "backgroundKey": "projects.smartHome.background",
    "contributeKey": "projects.smartHome.contribute",
    "outcomeKey": "projects.smartHome.outcome"
  }
]
```

```jsonc
// src/messages/zh.json — 在 projects 下追加
{
  "projects": {
    "sectionTitle": "作品",
    "backgroundLabel": "背景",
    "contributeLabel": "贡献",
    "outcomeLabel": "成果",
    "ccms":   { "title": "Chicken Coop Monitoring System (CCMS)" },
    "portfolio": { "title": "Portfolio" },
    "smartHome": { "title": "Smart Home 面板" }
  }
}
```

---

### 场景 4：删除项目卡片

**步骤：**

| # | 操作 | 说明 |
|---|------|------|
| 1 | 编辑 `src/data/projects.json` | 从 `projects` 数组中移除对应条目 |
| 2 | 编辑 `src/messages/zh.json` | 移除对应的 `titleKey` 字段 |
| 3 | 编辑 `src/messages/ja.json` | 同步骤 2 |
| 4 | 编辑 `src/messages/en.json` | 同步骤 2 |

> 封面图文件保留在 `public/images/` 中无需删除（不影响运行），但建议清理以避免混淆。

---

### 场景 5：新增 Case Study（卡片 + 详情页）

**修改内容**：在首页 Case Studies 区块新增一张案例卡片，同时在 `/[lang]/projects/[slug]` 新增独立详情页

**步骤：**

| # | 操作 | 说明 |
|---|------|------|
| 1 | 准备封面图 → 放入 `public/images/{slug}-cover.png` | 16:9 比例，推荐 1200×675px |
| 2 | 编辑 `src/data/case-studies.json` | 在 `caseStudies` 数组中追加新条目 |
| 3 | 编辑 `src/messages/zh.json` | 在 `caseStudies` 下添加 title/description + 全部章节（executiveSummary / background / methodology / developmentProcess / challenges / results / discussion / reflections / conclusion / appendix） |
| 4 | 编辑 `src/messages/ja.json` | 同步骤 3 |
| 5 | 编辑 `src/messages/en.json` | 同步骤 3 |
| 6 | 更新预载数组（如新增封面图需在 VN 序幕提前加载） | [[#5.3 图片预载机制（Preload）]] |
| 7 | 推送至 GitHub `main` | 自动部署生效 |

**示例 — 新增 "CCMS" Case Study：**

```jsonc
// src/data/case-studies.json
[
  { "slug": "casestudy", /* 现有条目 */ },
  {
    "slug": "ccms",
    "cover": "/images/ccms-cover.png",
    "techStack": ["IoT", "Python", "ESP32"],
    "titleKey": "caseStudies.ccms.title",
    "descriptionKey": "caseStudies.ccms.description"
  }
]
```

```jsonc
// src/messages/zh.json — 在 caseStudies 下追加
{
  "caseStudies": {
    "sectionTitle": "案例研究",
    "executiveSummaryLabel": "项目概述",
    "backgroundLabel": "背景与动机",
    "methodologyLabel": "方法论与架构",
    "developmentProcessLabel": "实施过程",
    "challengesLabel": "关键挑战与解决方案",
    "resultsLabel": "成果与展示",
    "discussionLabel": "讨论",
    "reflectionsLabel": "反思与最佳实践",
    "conclusionLabel": "结论与展望",
    "appendixLabel": "附录",
    "casestudy": {
      "title": "文档驱动·人机协作：AI Agent 开发方法论实证",
      "description": "一份以文档驱动与人机协作为核心的个人项目开发实证记录",
      "executiveSummary": "…",
      "background": "…",
      "methodology": {
        "documentFirst": "…",
        "agentConfig": "…",
        "architecture": "…",
        "deployment": "…"
      },
      "developmentProcess": {
        "intro": "…",
        "phase1": "…",
        "phase2": "…",
        "phase3": "…",
        "phase4": "…",
        "phase5": "…",
        "phase6": "…",
        "phase7": "…",
        "metricsNote": "…"
      },
      "challenges": {
        "challenge1": "…",
        "challenge2": "…",
        "challenge3": "…",
        "challenge4": "…",
        "challenge5": "…",
        "challenge6": "…",
        "challenge7": "…"
      },
      "results": {
        "overview": "…",
        "features": "…",
        "quality": "…",
        "links": "…"
      },
      "discussion": {
        "advantages": "…",
        "opencodeCliAdvantages": "…",
        "limitations": "…",
        "roleShift": "…",
        "teamScaling": "…",
        "hrPerspective": "…",
        "ossBoundary": "…"
      },
      "reflections": {
        "documentDensity": "…",
        "agentCapability": "…",
        "toolingAdvice": "…"
      },
      "conclusion": {
        "coreFindings": "…",
        "applicableScenarios": "…",
        "futureDirections": "…"
      },
      "appendix": {
        "appendixA": "…",
        "appendixB": "…",
        "appendixC": "…",
        "appendixD": "…",
        "appendixE": "…",
        "references": "…"
      }
    },
    "ccms": {
      "title": "Chicken Coop Monitoring System (CCMS)",
      "description": "基于 ESP32 + MQTT 的物联网监控项目",
      "executiveSummary": "…",
      "background": "…",
      // … 其余字段同 casestudy 结构
    },
    "viewButton": "查看详情"
  }
}
```

> 正文共 10 个顶层节：executiveSummary / background / methodology（4 子节）/ developmentProcess（9 子节）/ challenges（7 子节）/ results（4 子节）/ discussion（7 子节）/ reflections（3 子节）/ conclusion（3 子节）/ appendix（6 子节）。三语 JSON 各维护一份，字段 key 保持一致。

---

### 场景 6：修改 Case Study 详情正文

**步骤：**

| # | 操作 | 说明 |
|---|------|------|
| 1 | 编辑 `src/messages/zh.json` | 修改中文版正文（`caseStudies.{slug}.{section}` 字段） |
| 2 | 编辑 `src/messages/ja.json` | 同步修改日文版 |
| 3 | 编辑 `src/messages/en.json` | 同步修改英文版 |
| 4 | 推送至 GitHub `main` | 自动部署生效 |

> 正文共 10 个顶层节（executiveSummary / background / methodology / developmentProcess / challenges / results / discussion / reflections / conclusion / appendix），其中 methodology 等节嵌套多层子节。各字段为纯文本 string，支持 `\n` 换行（页面中通过 `whitespace-pre-wrap` 保留）。如需修改标题/封面/技术栈等元数据，需更新 `case-studies.json`。

---

### 场景 7：替换封面图/立绘

**步骤：**

| # | 操作 | 说明                                    |
|---|------|------|
| 1 | 准备新图片 → PNG 格式，≤2MB，kebab-case 命名 | 覆盖 `public/images/` 下同名文件             |
| 2 | 如文件名变化，更新对应 `.json` 中的 `cover` 路径 | `projects.json` / `case-studies.json` |
| 3 | 若图片文件名变化，同步更新预载数组 | [[#5.3 图片预载机制（Preload）]]              |
| 4 | 推送至 GitHub `main` | 自动部署生效                                |

**图片替换说明：**

| 图片文件 | 使用位置 | 建议尺寸 |
|---------|---------|---------|
| `hero-character.png` | Hero 立绘（透明背景 PNG） | 宽度 800-1200px |
| `portfolio-cover.png` | Portfolio Project 卡片 | 1440×1080px (4:3) |
| `ccms-cover.png` | CCMS Project 卡片 | 800×580px (4:3) |
| `case-study-cover.png` | Portfolio Case Study 卡片 | 1920×1032px (16:9) |

> 无需修改代码，`next/image` 自动从 `public/` 读取并优化输出 AVIF/WebP。

---

### 场景 8：更新简历 PDF

**步骤：**

| # | 操作 | 说明 |
|---|------|------|
| 1 | 准备新 PDF，按语言命名 | `resume-zh.pdf` / `resume-ja.pdf` / `resume-en.pdf` |
| 2 | 覆盖 `public/` 下同名文件 | 推送后自动生效 |

> 网站 Contact 区块的简历按钮根据当前语言自动拼接路径 `<a href="/resume-{lang}.pdf" download>`，文件名变化需同步修改代码中的拼接逻辑。

---

### 场景 9：修改游戏风序幕场景剧本

**修改内容**：VN 序幕 43 场景的台词、角色表情、分支选项

**步骤：**

| # | 操作 | 说明 |
|---|------|------|
| 1 | 编辑 `src/data/vn-scenario.zh.json` | 修改中文版场景剧本 |
| 2 | 编辑 `src/data/vn-scenario.ja.json` | 同步修改日文版 |
| 3 | 编辑 `src/data/vn-scenario.en.json` | 同步修改英文版 |
| 4 | 推送至 GitHub `main` | 自动部署生效 |

**JSON 结构示例：**

```json
{
  "scenes": [
    {
      "id": "1",
      "characterName": "露卡酱",
      "characterImage": "/images/normal.png",
      "dialogue": "……",
      "choices": null
    },
    {
      "id": "3",
      "characterName": null,
      "characterImage": "/images/emm.png",
      "dialogue": "你该不会以为进错网站了吧",
      "choices": [
        { "text": "果然进错网站了吗……怎么那么怪", "nextScene": "4" },
        { "text": "好像没有错……", "nextScene": "6" }
      ]
    }
  ]
}
```

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | string | 场景 ID（如 "1", "2"） |
| `characterName` | string 或 null | 说话者名称，null 表示内心独白（无角色名显示） |
| `characterImage` | string（可选） | 角色立绘路径（`/images/{表情}.png`） |
| `dialogue` | string | 台词正文 |
| `choices` | array 或 null | 选项分支，null 表示自动推进；每个选项含 `text` 和 `nextScene` |
| `nextScene` | string（可选） | 下一场景 ID，仅无选项且非结局时使用 |
| `repel` | boolean（可选） | 是否启用鼠标排斥效果 |
| `autoAdvanceMs` | number（可选） | 自动推进延迟毫秒数 |
| `isEnding` | boolean（可选） | 是否结局场景，是则触发 Loading 过渡进入首页 |

> 角色表情通过 `characterImage` 路径切换，图片文件位于 `public/images/`，可选表情：normal / smile / sad / mad / shock / shy / bigsmile / emm。

> 修改完成后运行 `pnpm run build` 确认 JSON 格式正确，无解析错误。

---

## 四、新增语言指南

如需新增第四种语言（如韩语 `ko`）：

| # | 操作 | 涉及文件 |
|---|------|---------|
| 1 | 更新 `src/i18n/routing.ts` | 在 `locales` 数组追加 `"ko"` |
| 2 | 创建 `src/messages/ko.json` | 复制 `zh.json` 结构，翻译所有值（含 caseStudies 全部字段 + projects contribute/outcome 等） |
| 3 | 创建 `src/data/vn-scenario.ko.json` | 复制并翻译 VN 序幕剧本 |
| 4 | 创建 `public/resume-ko.pdf` | 韩语简历 PDF |
| 5 | 推送至 GitHub `main` | 自动部署生效，新语言路由 `/ko/...` 立即可访问 |

> `src/i18n/request.ts` 使用动态 `import(../messages/${locale}.json)`，无需手动添加导入。`src/proxy.ts` 通过 `src/i18n/routing.ts` 导入配置，修改 routing.ts 即可同步生效。

---

## 五、图片管理规范

### 5.1 命名规则

```
{block}-{用途}.png
```

| 正确 | 错误 |
|------|------|
| `hero-character.png` | `heroCharacter.png` |
| `ccms-cover.png` | `ccms_cover.png` |
| `smart-home-cover.png` | `Smart Home Cover.png` |

### 5.2 文件约束

| 约束项 | 规格 |
|--------|------|
| 格式 | PNG（立绘需透明背景）/ JPEG（封面图） |
| 单文件上限 | ≤2MB |
| 总图片上限 | ≤20 张 |
| 生产输出 | Next.js 自动转为 AVIF + WebP |

### 5.3 图片预载机制（Preload）

首页的 Hero 立绘、项目卡片、Case Study 封面和 VN 序幕截图在游戏风序幕阶段会提前加载，确保进入首页时图片立即显示。预载队列定义在 `src/components/IntroShell.tsx` 的两处 `useEffect` 中：一处位于 scenario 阶段自动预载逻辑中（VN 场景播放时异步加载首页图片），另一处位于 Skip 按钮点击时的预载逻辑中。

新增/替换封面图时，若图片文件名发生变化，需要同步更新两处预载数组：

```ts
preload([
  "/images/hero-character.png",
  "/images/portfolio-cover.png",
  "/images/ccms-cover.png",
  "/images/case-study-cover.png",
  "/images/option-button.png",
  "/images/obsidian_graph.png",
  "/images/opencode_init.jpg",
  "/images/homepage.png",
  "/images/vnscene1.png",
  "/images/vnscene2-option.png",
  "/images/obsidian_repo.png",
  "/images/opencode_working.jpg",
  "/images/commits.png",
  "/images/working_record.png",
]);
```

两处数组内容必须保持一致。新增或移除图片时记得同步修改两处。

---

## 六、内容修改验证流程

每次内容修改后，建议按以下步骤验证：

```
编辑文件 → 本地预览 → 确认无误 → 推送 → 确认部署成功 → 线上验证
```

### 6.1 本地预览

```bash
pnpm run dev
# 访问 http://localhost:3000 查看修改效果
```

### 6.2 构建验证

```bash
pnpm run build
# 确保零报错，Zod 数据校验会在 build 时检查 JSON 完整性
```

### 6.3 内容修改后检查清单

- [ ] `pnpm run build` 通过（Zod 校验数据完整性）
- [ ] 新页面/新路由可访问（新增 Case Study 时）
- [ ] 三语言同步更新（messages JSON）
- [ ] 图片路径正确，封面正常加载
- [ ] 链接/按钮点击正常
- [ ] 简历文件可下载

---

## 七、常见错误与避免

| 错误 | 后果 | 避免方法 |
|------|------|---------|
| 只改中文 messages，忘记改日/英文 | 切换语言后文字缺失或显示旧内容 | 三语同步修改 |
| JSON 末尾多了一个逗号 | Build 报错 | 使用 VSCode 等编辑器自动校验 JSON 语法 |
| 封面图文件名与 JSON 中 cover 路径不一致 | 图片 404 不显示 | 复制路径时检查拼写 |
| 修改 `profile.json` 时删除 `email` 字段 | Zod 校验失败，build 报错 | 使用 Zod schema 结构修改，不删必要字段 |
| images 文件 ≥2MB | 增加加载时间 | 先用工具压缩再放入 |
| Case Study 正文漏填某节（如漏了 methodology 或 challenges 等） | 详情页对应 Section 显示空白 | 三语 JSON 各节字段都要完整 |

---

## 八、数据文件字段定义

JSON 结构与 Zod schema 定义见 [[4.2 项目文件结构文档.md#五]]（含 JSON 注释格式说明）。

> 数据字段以 Zod schema（`src/lib/*.schema.ts`）为唯一事实来源。本节仅提供引用路径，不再重复定义。

---

## 九、更新流程总结

```
┌──────────────────────────────────────────────────┐
│              你准备要更新内容                       │
└────────────────┬─────────────────────────────────┘
                 ▼
┌──────────────────────────────────────────────────┐
│  文字类？  ──→ 修改 src/messages/{lang}.json       │
│  个人资料？──→ 修改 src/data/profile.json           │
│  项目卡片？──→ 修改 src/data/projects.json          │
│  Case Study？→ 修改 case-studies.json + messages JSON│
│  图片类？  ──→ 替换 public/images/ + 更新 JSON     │
│  简历？    ──→ 替换 public/resume-{lang}.pdf       │
└────────────────┬─────────────────────────────────┘
                 ▼
┌──────────────────────────────────────────────────┐
│  三语同步：zh.json / ja.json / en.json 都要更新    │
└────────────────┬─────────────────────────────────┘
                 ▼
┌──────────────────────────────────────────────────┐
│  本地验证：pnpm run build                         │
└────────────────┬─────────────────────────────────┘
                 ▼
┌──────────────────────────────────────────────────┐
│  推送至 GitHub main → Vercel 自动部署 → 上线      │
└──────────────────────────────────────────────────┘
```


| 图片规范 | [[4.3 视觉资产与使用规范.md#二]]、[[4.3 视觉资产与使用规范.md#五]]、[[4.3 视觉资产与使用规范.md#十]] |
| 字体规范 | [[4.3 视觉资产与使用规范.md#六]] |
| JSON 结构 | [[2.2 内容规划表]] |
| Markdown 模板 | [[2.2 内容规划表#2.6]] |
| i18n 配置 | [[4.1 技术栈与架构文档.md#7.4]] |
