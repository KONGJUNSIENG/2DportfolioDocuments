# 3.1 风格指南（Style Guide）

**项目：** KONG JUN SIENG — Software Developer Portfolio  
**品牌调性：** 日系萌系 × 浅色科技感，Gal 社风格柔和动效  
**文档目标：** 统一定义视觉与交互规范，约束 AI 生成样式代码，确保全站一致性。

---

## 一、品牌色彩系统

### 1.1 色板

| 角色 | 色值 | CSS 变量 | 用途 |
|------|------|---------|------|
| **主色** | `#FFB7C5` | `--color-primary` | CTA 按钮、强调元素、链接悬停 |
| **主色柔和** | `#FFE4EC` | `--color-primary-soft` | 标签背景、装饰区块、hover 状态背景 |
| **辅色** | `#D5C6E0` | `--color-secondary` | 次要装饰元素、分割线 |
| **强调色** | `#B5EAD7` | `--color-accent` | 成功状态、点缀元素、标签点缀 |
| **页面背景** | `#FFFAFD` | `--color-background` | 站点主背景色 |
| **表面色** | `#FFFFFF` | `--color-surface` | 卡片、表单输入框、导航栏 |
| **主文字** | `#4A3F42` | `--color-text-primary` | 正文、标题 |
| **次要文字** | `#8E8386` | `--color-text-secondary` | 辅助说明、占位符、footer 文字 |
| **边框** | `#F0E6EB` | `--color-border` | 卡片边框、分割线、表单边框 |

### 1.2 使用规则

- **主色**：仅用于 CTA 按钮和关键强调元素，不可用于大面积背景。
- **主色柔和**：标签背景、卡片 hover 背景、装饰性区域。
- **页面背景**：全站主背景，配合 `--color-surface` 卡片产生层次。
- **强调色**：点缀使用，单页面最多出现 2 次。
- **不要**：在主色背景上放置深色文字 —— 主色仅限于按钮/装饰。

---

## 二、排版系统

### 2.1 字体族

| 语言 | 字体 | 加载方式 | 字重 |
|------|------|---------|------|
| 日文 (ja) | M PLUS Rounded 1c | `next/font/local` 自托管 | 400, 500, 700 |
| 英文 (en) | Nunito | `next/font/local` 自托管 | 400, 500, 600, 700 |
| 中文 (zh) | Noto Sans SC | `next/font/local` 自托管 | 400, 500, 700 |

### 2.2 字号比例尺

| 层级 | 大小 | 行高 | 字重 | 用途 |
|------|------|------|------|------|
| `text-xs` | 12px | 1.5 | 400 | 辅助文字、版权、标签 |
| `text-sm` | 14px | 1.5 | 400 | 次要正文、导航链接 |
| `text-base` | 16px | 1.6 | 400 | **正文基准** |
| `text-lg` | 18px | 1.6 | 500 | 卡片标题、区块副标题 |
| `text-xl` | 20px | 1.4 | 600 | Section 标题（如"案例研究"） |
| `text-2xl` | 24px | 1.3 | 700 | Hero Slogan、详情页标题 |
| `text-3xl` | 30px | 1.2 | 700 | Hero 姓名 / 大标题 |
| `text-4xl` | 36px | 1.1 | 800 | 首页大 Hero 文字（可选） |
| `text-5xl` | 48px | 1.1 | 800 | 仅极端情况使用 |

### 2.3 排版规则

- **正文**：`text-base (16px)`，行高 `1.6`，保证阅读舒适。
- **标题与正文的对比**：Section 标题至少比正文大 2 级（`text-xl` 以上）。
- **行宽限制**：正文段落最大宽度 `65ch`，避免长行疲劳。
- **中日英混排**：统一使用对应字体族，行高保持一致。

---

## 三、间距与网格系统

### 3.1 间距基准

采用 **4px 基准**（Tailwind 标准），关键间距值：

| Token | 值 | 用途 |
|-------|-----|------|
| `space-1` | 4px | 标签间距、图标间距 |
| `space-2` | 8px | 元素内间距（按钮内边距） |
| `space-3` | 12px | 紧凑元素间距 |
| `space-4` | 16px | **基准间距**、卡片内边距 |
| `space-6` | 24px | Section 内区块间距 |
| `space-8` | 32px | Section 间间距 |
| `space-12` | 48px | 大区块间距 |
| `space-16` | 64px | 页面级间距 |
| `space-20` | 80px | Hero 区块上下间距 |

### 3.2 容器与宽度

| 层级 | 最大宽度 | 适用 |
|------|---------|------|
| 内容容器 | 1152px (`max-w-6xl`) | 首页各区块内容 |
| 窄容器 | 768px (`max-w-3xl`) | Case Study 详情页正文 |
| 全宽 | 100% | Hero 背景区域 |

### 3.3 响应式断点

| 断点 | 宽度 | 行为 |
|------|------|------|
| `sm` | ≥640px | 平板竖屏调整 |
| `md` | ≥768px | 平板横屏 / 小桌面 |
| `lg` | ≥1024px | 桌面端，导航展开、双列布局 |
| `xl` | ≥1280px | 宽屏，保留内容容器宽度不变 |

**移动优先**：基础样式面向手机（<640px），逐级向上覆写。

---

## 四、圆角与阴影

### 4.1 圆角层级

| Token | 值 | 适用 |
|-------|-----|------|
| `rounded-sm` | 4px | 标签、小元素 |
| `rounded-md` | 8px | **基准**、按钮、输入框 |
| `rounded-lg` | 12px | 卡片 |
| `rounded-xl` | 16px | 大卡片、模态框 |
| `rounded-full` | 9999px | 头像、Pill 标签 |

### 4.2 阴影层级

暖调偏移阴影，保持柔和感：

| Token | 值 | 适用 |
|-------|-----|------|
| `shadow-xs` | `0 1px 2px rgba(74,63,66,0.05)` | 轻微分割 |
| `shadow-sm` | `0 1px 3px rgba(74,63,66,0.08)` | 按钮默认 |
| `shadow-md` | `0 4px 6px rgba(74,63,66,0.08)` | **卡片默认** |
| `shadow-lg` | `0 10px 15px rgba(74,63,66,0.1)` | 卡片 hover、导航栏 |
| `shadow-xl` | `0 20px 25px rgba(74,63,66,0.12)` | 模态、弹出层 |

---

## 五、组件样式规范

### 5.1 按钮

| 变体 | 背景 | 文字 | 边框 | hover | 圆角 |
|------|------|------|------|-------|------|
| **Primary（填充）** | `--color-primary` | `#FFFFFF` | 无 | 亮度 +5%，阴影加深 | `rounded-md` |
| **Outline（轮廓）** | 透明 | `--color-primary` | `1px solid --color-primary` | 背景填 `--color-primary-soft` | `rounded-md` |
| **Ghost（文本）** | 透明 | `--color-text-secondary` | 无 | 文字变 `--color-primary` | `rounded-md` |

- 内边距：垂直 `space-2` (8px)，水平 `space-4` (16px)
- 禁用状态：`opacity-50` + `cursor-not-allowed`
- 文字大小：`text-sm`（导航内按钮）或 `text-base`（CTA）

### 5.2 卡片

| 属性 | 值 |
|------|-----|
| 背景 | `--color-surface` |
| 圆角 | `rounded-lg` (12px) |
| 阴影 | `shadow-md` 默认→`shadow-lg` on hover |
| 边框 | 可选 `1px solid --color-border` |
| 内边距 | `space-6` (24px) |
| hover 动效 | `translateY(-2px)` + 阴影加深，`transition: all 300ms ease-out` |

### 5.3 标签 / 技能标签

| 属性 | 值 |
|------|-----|
| 背景 | `--color-primary-soft`（或其他柔色调） |
| 文字 | `--color-text-secondary`，`text-xs` |
| 圆角 | `rounded-full` (pill 形) |
| 内边距 | 垂直 2px，水平 8px |
| 间距 | `gap-1` (4px) 排列 |

### 5.4 表单

| 元素 | 样式 |
|------|------|
| 输入框/文本域 | 背景 `--color-surface`，边框 `1px solid --color-border`，圆角 `rounded-md`，内边距 `space-2` + `space-3`，聚焦时边框变为 `--color-primary` |
| 标签 | `text-sm`，`--color-text-secondary`，`mb-1` |
| 提交按钮 | 使用 Primary 按钮变体 |
| 占位符 | `--color-text-secondary`，`opacity-60` |

### 5.5 导航栏

| 属性 | 值 |
|------|-----|
| 背景 | `rgba(255,250,253,0.85)` 毛玻璃效果（`backdrop-blur-md`） |
| 高度 | 64px（`h-16`） |
| 内边距 | 水平 `space-4`→`space-8`（响应式） |
| 链接 | `text-sm`，`--color-text-secondary`，active/hover 变为 `--color-primary` |
| 阴影 | 滚动后 `shadow-sm` 渐入 |
| z-index | `z-50` |

---

## 六、图标风格

| 属性 | 规范 |
|------|------|
| 图标库 | Lucide React（统一风格） |
| 风格 | 线性（Line），`strokeWidth={1.5}` |
| 色值 | 继承文字颜色或 `--color-primary` |
| 尺寸 | 16px（内联/标签），20px（导航），24px（CTA/大按钮） |

---

## 七、动效系统

### 7.1 缓动函数

| 场景 | 缓动函数 | 说明 |
|------|---------|------|
| **入场动画** | `cubic-bezier(0.16, 1, 0.3, 1)` | 柔和弹入，不过度，Galgame 感 |
| **hover 反馈** | `cubic-bezier(0.25, 0.1, 0.25, 1)` | 标准缓出 |
| **视差/滚动** | 线性（无缓动） | useScroll + useTransform 驱动 |

### 7.2 持续时长

| 层级 | 时长 | 适用 |
|------|------|------|
| 快速 | 200ms | hover 状态切换、颜色过渡 |
| **基准** | **300ms** | 按钮交互、卡片 hover、入场动画 |
| 慢速 | 500ms | Section 入场 `whileInView` |
| 氛围 | 800ms | Hero 背景光晕脉冲、大元素渐入 |

### 7.3 动画类型速查

| 动画 | 实现方式 | 参数 |
|------|---------|------|
| 淡入上浮 | Framer Motion `whileInView` | `y: 20 → 0`, `opacity: 0 → 1`, 300ms |
| 卡片 hover | CSS `transform` | `translateY(-2px)` + `shadow-md→shadow-lg`, 300ms |
| 立绘浮动 | CSS `@keyframes float` | `translateY(-3px) → 3px`, 4s 循环 |
| 背景光晕脉冲 | CSS `@keyframes pulse` | `opacity: 0.3 → 0.6`, 4s 循环 |
| 导航栏滚入 | Framer Motion `useScroll` | 滚动 >50px 时 `shadow-sm` 渐入 |
| 视差 | Framer Motion `useScroll + useTransform` | 前景/背景不同速度 |

---

## 八、写作语调

| 语言 | 语调 | 风格说明 |
|------|------|---------|
| **英文 (en)** | Professional yet Friendly | 第一人称，简洁专业，避免过于口语化 |
| **日文 (ja)** | 敬語＋丁寧語 | 使用「です・ます」调，保持礼貌但不过于生硬 |
| **中文 (zh)** | 简洁正式 | 避免网络用语，保持技术博客式的清晰感 |

- **全局规则**：不使用 emoji，标点统一全角（中日）／半角（英）。
- **CTA 用语**：行动导向，如"查看作品""View Projects""作品を見る"。

---

## 九、设计令牌映射表（Tailwind 兼容）

以下为 `tailwind.config.ts` 所需的完整自定义令牌：

```typescript
// tailwind.config.ts — 自定义设计令牌
export default {
  theme: {
    extend: {
      colors: {
        primary: {
          DEFAULT: '#FFB7C5',
          soft: '#FFE4EC',
        },
        secondary: '#D5C6E0',
        accent: '#B5EAD7',
        background: '#FFFAFD',
        surface: '#FFFFFF',
        'text-primary': '#4A3F42',
        'text-secondary': '#8E8386',
        border: '#F0E6EB',
      },
      fontFamily: {
        ja: ['"M PLUS Rounded 1c"', 'sans-serif'],
        en: ['Nunito', 'sans-serif'],
        zh: ['"Noto Sans SC"', 'sans-serif'],
      },
      fontSize: {
        xs: ['12px', '1.5'],
        sm: ['14px', '1.5'],
        base: ['16px', '1.6'],
        lg: ['18px', '1.6'],
        xl: ['20px', '1.4'],
        '2xl': ['24px', '1.3'],
        '3xl': ['30px', '1.2'],
        '4xl': ['36px', '1.1'],
        '5xl': ['48px', '1.1'],
      },
      spacing: {
        '1': '4px',
        '2': '8px',
        '3': '12px',
        '4': '16px',
        '6': '24px',
        '8': '32px',
        '12': '48px',
        '16': '64px',
        '20': '80px',
      },
      borderRadius: {
        sm: '4px',
        md: '8px',
        lg: '12px',
        xl: '16px',
      },
      boxShadow: {
        xs: '0 1px 2px rgba(74,63,66,0.05)',
        sm: '0 1px 3px rgba(74,63,66,0.08)',
        md: '0 4px 6px rgba(74,63,66,0.08)',
        lg: '0 10px 15px rgba(74,63,66,0.1)',
        xl: '0 20px 25px rgba(74,63,66,0.12)',
      },
      maxWidth: {
        content: '1152px',
        narrow: '768px',
      },
      screens: {
        sm: '640px',
        md: '768px',
        lg: '1024px',
        xl: '1280px',
      },
      transitionDuration: {
        fast: '200ms',
        normal: '300ms',
        slow: '500ms',
        ambient: '800ms',
      },
      transitionTimingFunction: {
        'in-soft': 'cubic-bezier(0.16, 1, 0.3, 1)',
      },
    },
  },
}
```

### CSS 自定义属性等效声明

```css
/* src/styles/globals.css */
:root {
  --color-primary: #FFB7C5;
  --color-primary-soft: #FFE4EC;
  --color-secondary: #D5C6E0;
  --color-accent: #B5EAD7;
  --color-background: #FFFAFD;
  --color-surface: #FFFFFF;
  --color-text-primary: #4A3F42;
  --color-text-secondary: #8E8386;
  --color-border: #F0E6EB;

  --font-ja: 'M PLUS Rounded 1c', sans-serif;
  --font-en: 'Nunito', sans-serif;
  --font-zh: 'Noto Sans SC', sans-serif;

  --space-1: 4px;
  --space-2: 8px;
  --space-3: 12px;
  --space-4: 16px;
  --space-6: 24px;
  --space-8: 32px;
  --space-12: 48px;
  --space-16: 64px;
  --space-20: 80px;

  --radius-sm: 4px;
  --radius-md: 8px;
  --radius-lg: 12px;
  --radius-xl: 16px;

  --ease-in-soft: cubic-bezier(0.16, 1, 0.3, 1);
  --duration-fast: 200ms;
  --duration-normal: 300ms;
  --duration-slow: 500ms;
  --duration-ambient: 800ms;
}
```

---

## 附录：维护说明

- 本风格指南与 `tailwind.config.ts` 应保持同步。
- 新增组件前，先确认其样式是否能由现有令牌组合实现。
- 如需新增令牌，同步更新本节和 `tailwind.config.ts`。
- 修改颜色/字体等品牌核心元素时，更新 `1.1 个人品牌与网站愿景文档.md` 中的对应章节。
