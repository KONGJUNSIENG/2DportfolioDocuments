# 3.1 风格指南（Style Guide）

**项目：** KONG JUN SIENG — Software Developer Portfolio  
**品牌调性：** 日系萌系 × 浅色科技感，Gal 社风格柔和动效  
**文档目标：** 统一定义视觉与交互规范，约束 AI 生成样式代码，确保全站一致性。

---

## 一、品牌色彩系统

### 1.1 色板

| 角色 | 色值 | CSS 变量 | 用途 |
|------|------|---------|------|
| **主色** | `#FFB7C5` | `--color-primary` | CTA 按钮、强调元素、链接悬停 |
| **主色柔和** | `#FFE4EC` | `--color-primary-soft` | 标签背景、装饰区块、hover 状态背景 |
| **辅色** | `#D5C6E0` | `--color-secondary` | 次要装饰元素、分割线 |
| **强调色** | `#B5EAD7` | `--color-accent` | 成功状态、点缀元素、标签点缀 |
| **页面背景** | `#FFFAFD` | `--color-background` | 站点主背景色 |
| **表面色** | `#FFFFFF` | `--color-surface` | 卡片、表单输入框、导航栏 |
| **主文字** | `#4A3F42` | `--color-text-primary` | 正文、标题 |
| **次要文字** | `#8E8386` | `--color-text-secondary` | 辅助说明、占位符、footer 文字 |
| **边框** | `#F0E6EB` | `--color-border` | 卡片边框、分割线、表单边框 |
| **对话框蓝** | `#2FA4D7` | `--color-dialog-blue` | 对话框内框背景、选项按钮主色（匹配蓝色素材） |
| **对话框蓝深** | `#1A8DB8` | `--color-dialog-dark` | 对话框内框装饰线、选项按钮悬停态边框 |
| **对话框蓝柔** | `#E5F4FC` | `--color-dialog-soft` | 对话框/选项按钮 hover 态背景、轻量蓝色装饰 |

### 1.2 使用规则

- **主色**：仅用于 CTA 按钮和关键强调元素，不可用于大面积背景。
- **主色柔和**：标签背景、卡片 hover 背景、装饰性区域。
- **页面背景**：全站主背景，配合 `--color-surface` 卡片产生层次。
- **强调色**：点缀使用，单页面最多出现 2 次。
- **对话框蓝**：对话框内层背景、选项按钮主色。白色文字配合使用。不用于大面积背景或正文。
- **对话框蓝深**：对话框内层装饰线、选项按钮悬停态边框。
- **对话框蓝柔**：对话框/选项按钮 hover 态背景、轻量蓝色装饰。
- **不要**：将蓝色系与粉色系在同一个组件中并列为同等面积（蓝为"系统/UI"，粉为"角色/情感"）。
- **不要**：在主色背景上放置深色文字 —— 主色仅限于按钮/装饰。

---

## 二、排版系统

### 2.1 字体族

| 语言 | 字体 | 加载方式 | 字重 |
|------|------|---------|------|
| 日文 (ja) | M PLUS Rounded 1c | `next/font/local` 自托管 `src/lib/fonts.ts` | 400 (仅此一字重) |
| 英文 (en) | Nunito | `next/font/local` 自托管 `src/lib/fonts.ts` | 400 (仅此一字重) |
| 中文 (zh) | Noto Sans SC | 手动 `@font-face` `src/styles/fonts.css`（CJK 13 子集 woff2） | 400 (Regular) |

### 2.2 字号比例尺

| 层级 | 大小 | 行高 | 字重 | 用途 |
|------|------|------|------|------|
| `text-xs` | 12px | 1.5 | 400 | 辅助文字、版权、标签 |
| `text-sm` | 14px | 1.5 | 400 | 次要正文、导航链接 |
| `text-base` | 16px | 1.6 | 400 | **正文基准** |
| `text-lg` | 18px | 1.6 | 500 | 卡片标题、区块副标题 |
| `text-xl` | 20px | 1.4 | 600 | Section 标题（如"案例研究"） |
| `text-2xl` | 24px | 1.3 | 700 | Hero Slogan、详情页标题 |
| `text-3xl` | 30px | 1.2 | 700 | Hero 姓名 / 大标题 |
| `text-4xl` | 36px | 1.1 | 800 | 首页大 Hero 文字（可选） |
| `text-5xl` | 48px | 1.1 | 800 | 仅极端情况使用 |

### 2.3 排版规则

- **正文**：`text-base (16px)`，行高 `1.6`，保证阅读舒适。
- **标题与正文的对比**：Section 标题至少比正文大 2 级（`text-xl` 以上）。
- **行宽限制**：正文段落最大宽度 `65ch`，避免长行疲劳。
- **中日英混排**：统一使用对应字体族，行高保持一致。

---

## 三、间距与网格系统

### 3.1 间距基准

采用 **4px 基准**（Tailwind 标准），关键间距值：

| Token | 值 | 用途 |
|-------|-----|------|
| `space-1` | 4px | 标签间距、图标间距 |
| `space-2` | 8px | 元素内间距（按钮内边距） |
| `space-3` | 12px | 紧凑元素间距 |
| `space-4` | 16px | **基准间距**、卡片内边距 |
| `space-6` | 24px | Section 内区块间距 |
| `space-8` | 32px | Section 间间距 |
| `space-12` | 48px | 大区块间距 |
| `space-16` | 64px | 页面级间距 |
| `space-20` | 80px | Hero 区块上下间距 |

### 3.2 容器与宽度

| 层级 | 最大宽度 | 适用 |
|------|---------|------|
| 内容容器 | 1152px (`max-w-6xl`) | 首页各区块内容 |
| 窄容器 | 768px (`max-w-3xl`) | Case Study 详情页正文 |
| 全宽 | 100% | Hero 背景区域 |

### 3.3 响应式断点

| 断点 | 宽度 | 行为 |
|------|------|------|
| `sm` | ≥640px | 平板竖屏调整 |
| `md` | ≥768px | 平板横屏 / 小桌面 |
| `lg` | ≥1024px | 桌面端，导航展开、双列布局 |
| `xl` | ≥1280px | 宽屏，保留内容容器宽度不变 |

**移动优先**：基础样式面向手机（<640px），逐级向上覆写。

---

## 四、圆角与阴影

### 4.1 圆角层级

Tailwind v4 默认值（未自定义 `@theme` 令牌，使用框架内置值）：

| Token | 值 | 适用 |
|-------|-----|------|
| `rounded-sm` | 2px (0.125rem) | 标签、小元素 |
| `rounded` | 4px (0.25rem) | 轻微圆角 |
| `rounded-md` | 6px (0.375rem) | **基准**、按钮、输入框 |
| `rounded-lg` | 8px (0.5rem) | 卡片、对话框外层 |
| `rounded-xl` | 12px (0.75rem) | 对话框名字框 |
| `rounded-2xl` | 16px (1rem) | 大卡片（当前未使用） |
| `rounded-full` | 9999px | 头像、Pill 标签 |

### 4.2 阴影层级

使用 Tailwind v4 默认阴影（未自定义 `@theme` 令牌）。若需暖调偏移阴影，需在 `@theme inline` 中新增 `--shadow-*` 令牌。

| Token | 值（Tailwind v4 默认） | 适用 |
|-------|------------------------|------|
| `shadow-xs` | `0 1px 2px 0 rgb(0 0 0 / 0.05)` | 对话框底部阴影 |
| `shadow-sm` | `0 1px 3px 0 rgb(0 0 0 / 0.1), 0 1px 2px -1px rgb(0 0 0 / 0.1)` | 按钮默认（当前未使用独立按钮） |
| `shadow-md` | `0 4px 6px -1px rgb(0 0 0 / 0.1), 0 2px 4px -2px rgb(0 0 0 / 0.1)` | **卡片默认** |
| `shadow-lg` | `0 10px 15px -3px rgb(0 0 0 / 0.1), 0 4px 6px -4px rgb(0 0 0 / 0.1)` | 卡片 hover、导航栏菜单 |
| `shadow-xl` | `0 20px 25px -5px rgb(0 0 0 / 0.1), 0 8px 10px -6px rgb(0 0 0 / 0.1)` | 模态、弹出层（当前未使用） |

---

## 五、组件样式规范

### 5.1 按钮

代码库中无通用 Button 组件。交互按钮统一使用 `OptionButton` 组件（见 5.8），CTA 跳转使用 `ScrollToAnchor` + `OptionButton`。若后续引入通用按钮，建议规范如下：

| 变体 | 背景 | 文字 | 边框 | hover | 圆角 |
|------|------|------|------|-------|------|
| **Primary（填充）** | `--color-primary` | `#FFFFFF` | 无 | 亮度 +5%，阴影加深 | `rounded-md` |
| **Outline（轮廓）** | 透明 | `--color-primary` | `1px solid --color-primary` | 背景填 `--color-primary-soft` | `rounded-md` |
| **Ghost（文本）** | 透明 | `--color-text-secondary` | 无 | 文字变 `--color-primary` | `rounded-md` |

- 内边距建议：垂直 `space-2` (8px)，水平 `space-4` (16px)
- 禁用状态：`opacity-50` + `cursor-not-allowed`
- 文字大小：`text-sm`（导航内按钮）或 `text-base`（CTA）

### 5.2 卡片

| 属性 | 值 |
|------|-----|
| 背景 | `--color-surface` |
| 圆角 | `rounded-lg` (8px) |
| 阴影 | `shadow-md` 默认→`shadow-lg` on hover |
| 边框 | 可选 `1px solid --color-border` |
| 内边距 | `space-6` (24px) |
| hover 动效 | Framer Motion `whileHover`：`scale(1.03)` + CSS `transition-shadow duration-300 hover:shadow-lg` |

### 5.3 标签 / 技能标签

| 属性 | 值 |
|------|-----|
| 背景 | `--color-primary-soft`（或其他柔色调） |
| 文字 | `--color-text-secondary`，`text-xs` |
| 圆角 | `rounded-full` (pill 形) |
| 内边距 | 垂直 2px，水平 8px |
| 间距 | `gap-1` (4px) 排列 |

### 5.4 表单（当前未使用）

Contact Section 采用纯展示（邮箱/社交/简历下载），无表单组件。以下留作后续扩展参考：

| 元素 | 样式 |
|------|------|
| 输入框/文本域 | 背景 `--color-surface`，边框 `1px solid --color-border`，圆角 `rounded-md`，内边距 `space-2` + `space-3`，聚焦时边框变为 `--color-primary` |
| 标签 | `text-sm`，`--color-text-secondary`，`mb-1` |
| 提交按钮 | 使用 Primary 按钮变体 |
| 占位符 | `--color-text-secondary`，`opacity-60` |

### 5.5 导航栏

| 属性 | 值 |
|------|-----|
| 背景 | 动态透明度：scrollY=0 时透明（`rgba(255,250,253,0)`），scrollY>50 渐现至 `rgba(255,250,253,0.85)` + `backdrop-blur-md` |
| 高度 | 64px（`h-16`） |
| 内边距 | 水平 `space-4`→`space-8`（响应式） |
| 链接 | `text-sm`，`--color-text-secondary`，active/hover 变为 `--color-primary` |
| 阴影 | 滚动后 `shadow-sm` 渐入 |
| z-index | `z-50` |

---

### 5.6 蓝色对话框（DialogBox）

对话框结构为**三层圆角**：白色底框 → 蓝色名字框 + 蓝色文字框，内部 flex 比例固定 1:3。

```
┌──────────────────────────────────┐  ← 白色底框, border-dialog-blue, rounded-lg
│  ┌──────────────┐               │  ← 名字框 bg-dialog-blue, rounded-xl (最圆)
│  │ KONG JUN     │               │
│  │ SIENG        │               │
│  └──────────────┘               │      占 25% 高度 (flex-1)
│  ┌────────────────────────────┐  │  ← 文字框 bg-dialog-blue, rounded-md (最方)
│  │ bio text...                │  │      占 75% 高度 (flex-[3])
│  └────────────────────────────┘  │
└──────────────────────────────────┘
```

| 属性 | 值 |
|------|-----|
| **外层白框** | 背景 `--color-surface`（白），边框 `1px solid --color-dialog-blue`，圆角 `rounded-lg` (8px)，内边距 `p-1 md:p-2.5`，`md:min-h-[100px]`（桌面自适应高度）+ `min-h-[120px]`（手机保底） |
| **名字框** | 背景 `--color-dialog-blue` (`#2FA4D7`)，圆角 `rounded-xl` (12px)，`px-5 py-1`，白色文字 `text-sm`（无 `font-bold`） |
| **文字框** | 背景 `--color-dialog-blue` (`#2FA4D7`)，圆角 `rounded-md` (6px)，`px-6 py-3`，文字顶部对齐（`flex items-start`），白色文字 |
| 文字颜色 | `#FFFFFF`（白色文字在蓝色背景上） |
| 布局 | 外层白框内 `flex h-full flex-col gap-1`，名字框所在容器 `flex-1`，文字框 `flex-[3]` |
| 动画 | Framer Motion `dialogSlideUp` variants：`y: 24→0, scale: 0.98→1, 500ms` |
| 使用场景 | About 自我介绍（`characterName="KONG JUN SIENG"`） |

> **纯 CSS 实现**：不使用 `dialog-box.png`。PNG 文件保留在 `public/images/` 但当前不参与渲染。

### 5.7 游戏风章节标题（ChapterHeader）

| 属性 | 值 |
|------|-----|
| 结构 | `CHAPTER N` 标签 → 章节名称 → 蓝色下划线装饰条 |
| Props | `{ chapterNumber?: number, title: string, subtitle?: string, label?: string, className? }` |
| 字号 | `CHAPTER N` 标签（或 `label` 自定义）：`text-xs`，`font-semibold`，`tracking-[0.2em]`，`--color-dialog-blue` |
| 章节名称 | `text-2xl font-bold`，`--color-text-primary` |
| 副标题 | `text-sm`，`--color-text-secondary` |
| 下划线 | `h-0.5 w-12 rounded-full bg-dialog-blue` |
| 入场动画 | Framer Motion `chapterReveal` variants：`x: -30→0, opacity: 0→1, 500ms`，`viewport: { once: true }` |

### 5.8 选项按钮（OptionButton）

使用 option-button.png 素材作为背景，单变体。

| 属性 | 值 |
|------|-----|
| 结构 | `▶` 指示符 + 文字，蓝底白字 |
| 背景 | `url('/images/option-button.png')` 背景素材，`background-size: 100% 100%`，覆盖 `--color-dialog-blue` 实色 |
| 文字 | `#FFFFFF` 白色，`text-base` |
| 内边距 | `px-6 py-3`（默认），可通过 `className` 覆盖（Hero CTA 使用 `py-6`） |
| 圆角 | 无（按钮本身无圆角，通过父容器控制） |
| 宽度 | `w-full`（填充父容器宽度） |
| 入场 | Framer Motion `optionStagger` + `optionItem`（ContactSection 使用） |
| hover | `whileHover: { scale: 1.02, x: 4 }` — 放大 + 右移 4px |
| tap | `whileTap: { scale: 0.98 }` — 点击缩小 |


## 六、图标风格

| 属性 | 规范 |
|------|------|
| 图标库 | Lucide React（统一风格） |
| 风格 | 线性（Line），建议 `strokeWidth={1.5}`（未显式指定时默认为 2） |
| 色值 | 继承文字颜色或 `--color-primary` |
| 尺寸 | 16px（内联/标签），20px（导航），24px（CTA/大按钮） |

---

## 七、动效系统

### 7.1 缓动函数

| 场景 | 缓动函数 | 说明 |
|------|---------|------|
| **入场动画** | `cubic-bezier(0.16, 1, 0.3, 1)` | 柔和弹入，不过度，Galgame 感 |
| **hover 反馈** | `cubic-bezier(0.25, 0.1, 0.25, 1)` | 标准缓出 |
| **视差/滚动** | 线性（无缓动） | useScroll + useTransform 驱动 |

### 7.2 持续时长

| 层级 | 时长 | 适用 |
|------|------|------|
| 快速 | 200ms | hover 状态切换、颜色过渡 |
| **基准** | **300ms** | 按钮交互、卡片 hover、入场动画 |
| 慢速 | 500ms | Section 入场 `whileInView` |
| 氛围 | 800ms | Hero 背景光晕脉冲、大元素渐入 |

### 7.3 游戏风专属动效

| 动画 | 实现方式 | variants / 参数 |
|------|---------|----------------|
| **对话框入场** | Framer Motion `whileInView` | `dialogSlideUp`：`y: 24→0, scale: 0.98→1, opacity: 0→1`，500ms |
| **章节标题入场** | Framer Motion `whileInView` | `chapterReveal`：`x: -30→0, opacity: 0→1`，500ms |
| **选项按钮入场** | Framer Motion `whileInView` | `optionStagger`（stagger 120ms）+ `optionItem`（x: -20→0, 350ms）|
| **选项按钮悬停** | Framer Motion `whileHover` | `scale: 1.02, x: 4`（放大 + 右移）|
| **选项按钮点击** | Framer Motion `whileTap` | `scale: 0.98`（缩小）|
| **过渡台词弹出** | Framer Motion `whileInView` | `transitionFade`：`scale: 0.92→1, opacity: 0→1`，400ms |

### 7.4 无障碍动效

- 使用 `prefers-reduced-motion` media query 检测用户偏好：`Framer Motion` 的 `useReducedMotion()` hook 控制卡片空闲弹跳/摆动动画降级
- CSS 兜底：`@media (prefers-reduced-motion: reduce)` 将全局动画/过渡持续时间压缩至最小值（`0.01ms`）

### 7.5 动画类型速查

| 动画 | 实现方式 | 参数 |
|------|---------|------|
| 淡入上浮 | Framer Motion `whileInView` | `y: 20 → 0`, `opacity: 0 → 1`, 500ms（`staggerItem` variants） |
| 卡片 hover | Framer Motion `whileHover` | `scale(1.03)`，CSS `transition-shadow duration-300 hover:shadow-lg` |
| 立绘浮动 | CSS `@keyframes float` | `translateY(0) → -8px → 0`, 4s ease-in-out 循环 |
| 背景光晕脉冲 | CSS `@keyframes glow-pulse` | `opacity: 0.4 → 0.8`, 4s ease-in-out 循环 |
| 导航栏滚入 | Framer Motion `useScroll` + `useTransform` | scrollY `[0, 50]` 映射背景 alpha `0→0.85`、阴影 `transparent→var(--shadow-sm)` |
| 视差 | Framer Motion `useScroll + useTransform` + `useSpring` | 前景/背景不同速度，scrollY `[0, 0.3]` 映射 Y 偏移 `[0, -60]`，spring stiffness 100 damping 30 |

---

## 八、写作语调

| 语言 | 语调 | 风格说明 |
|------|------|---------|
| **英文 (en)** | Professional yet Friendly | 第一人称，简洁专业，避免过于口语化 |
| **日文 (ja)** | 敬語＋丁寧語 | 使用「です・ます」调，保持礼貌但不过于生硬 |
| **中文 (zh)** | 简洁正式 | 避免网络用语，保持技术博客式的清晰感 |

- **全局规则**：不使用 emoji，标点统一全角（中日）／半角（英）。
- **CTA 用语**：行动导向，如"查看作品""View Projects""作品を見る"。

---

## 九、设计令牌映射表（Tailwind v4 CSS-first）

所有自定义令牌通过 `@theme inline` 指令在 `src/styles/globals.css` 中声明（当前仅颜色令牌，其余使用 Tailwind 默认值）

### 9.1 `src/styles/globals.css` — 主样式入口

```css
@import "tailwindcss";

@theme inline {
  --color-primary: #ffb7c5;
  --color-primary-soft: #ffe4ec;
  --color-secondary: #d5c6e0;
  --color-accent: #b5ead7;
  --color-background: #fffafd;
  --color-surface: #ffffff;
  --color-text-primary: #4a3f42;
  --color-text-secondary: #8e8386;
  --color-border: #f0e6eb;
  --color-dialog-blue: #2fa4d7;
  --color-dialog-dark: #1a8db8;
  --color-dialog-soft: #e5f4fc;
}
```

### 9.2 `postcss.config.mjs` — PostCSS 配置

```mjs
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
```

---

### 9.3 CSS 工具类（`src/styles/globals.css`）

| 类名 | 用途 |
|------|------|
| `.hero-glow` | Hero 右上方粉色径向渐变光晕，`glow-pulse` 动画 4s 循环 |
| `.character-float` | Hero 立绘上下浮动，`float` 动画 4s ease-in-out 循环 |
| `.nav-link` | 导航链接颜色过渡 200ms，hover 切换至 `--color-primary` |
| `.tag-hover` | 技能标签 hover 放大 `scale(1.05)`，200ms 过渡 |
| `.scrollbar-micro` | 隐藏滚动条（保留可滚动性），通过 `-ms-overflow-style: none` / `scrollbar-width: none` / `::-webkit-scrollbar { display: none }` 实现 |
| `body` | 背景 `--color-background`，文字色 `--color-text-primary` |

---

## 附录：维护说明

- 本风格指南与 `src/styles/globals.css` 中的 `@theme` 块应保持同步。
- 新增组件前，先确认其样式是否能由现有令牌组合实现。
- 如需新增令牌，在 `globals.css` 的 `@theme {}` 中添加，并同步更新本节的对应令牌表。
- 修改颜色/字体等品牌核心元素时，更新 [[1.1 个人品牌与网站愿景文档.md]] 中的对应章节。
